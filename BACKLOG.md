# LWBridge implementation backlog

- [x] **MAP DATA OWNER CORRECTIONS (`LWB-R7-130`):** current-v20 live proof closes the reported Player City HP defect at `(67,858)` (`rawCurHp=1222` retained diagnostically, effective published HP `10000`), reduces Truck/Monster full-world scan wall from ~135–137 s to ~75–78 s by skipping unselected City/Resource serialization without weakening exact 10,000-cell coverage, and proves all-eight 2,500/2,500 at 77.89 s. A two-server 2212/2213 run retains/reopens both datasets and returns to origin while short same-session retries recover transient `overview_session_unavailable` gaps. Auto Scan now isolates per-server failures; Stop disables scheduling before backend cancellation; all saved server IDs are browseable; Manual/Auto/result/server choices persist per profile; Clear invalidates stale search/treasure generations. Release 0/0 and all deterministic groups pass. Normal-window click/reopen confirmation for the UI-only persistence/server-selector/Clear timing remains owner-visible follow-up. Evidence: `2026-09-22-r7-map-correctness-multiserver-speed.json`.

## Current owner prerequisite - Home current-client regression, 2026-09-16

- [x] `LWB-OVR-015` TECHNICALLY LIVE-PROVES **Launch Game -> connected -> Close Game** on current Last War Lua content v17.
- [x] `LWB-OVR-015` TECHNICALLY LIVE-PROVES **Open games at startup -> connected -> Close Game** through production startup reconcile on v17.
- [x] Replace the obsolete exact Lua-package pin with fail-closed automatic compatibility: content-only package identity may advance only while recovered executable/xLua/managed/Lua anchors remain exact; critical changes return `GAME_UPDATE_UNSUPPORTED`.
- [x] Replace the generic update-mismatch Home error with a useful localized `GAME_UPDATE_UNSUPPORTED` message.
- [x] `LWB-OVR-017` fixes the v18 auto-update regression: a cached v17 settle marker could skip untouched official settlement and let the launcher patch LWBridge-modified candidate bytes. Fresh Lua CRC failure detection now restores, invalidates the marker, forces one untouched official settle/update, revalidates compatibility and retries exactly once. The same 18<-17 delta succeeds on untouched v17; current v18 passes the critical-anchor compatibility gate.
- [x] `LWB-OVR-018` fixes the v19 compatibility regression: the untouched official launcher applied `19 <- 18` and later reported local/remote Lua content 19 current, while LocalLow `version.txt` legitimately remained 18. Compatibility policy v2 keeps LWLF header + exact package size/CRC + recovered executable/xLua/managed/critical-Lua anchors authoritative; the marker remains required/positive and may lag but may not exceed the header. Manual and auto-start v19 lifecycle cycles both reached Connected and closed/restored cleanly.
- [x] `LWB-R7-062` hardens the owner-visible Home launch path after a real v19 UI failure where the official launcher finished pack verification but never spawned `LastWar.exe` before timeout. The helper cleanup was complete and the next two launches succeeded, so production retries once only for that exact launcher-handoff timeout within the existing overall start budget; deterministic retry, patched manual+auto live proof and the full suite pass.
- [x] `LWB-R7-063` fixes owner-reported cross-server Fast Truck/Secret Task scans stopping near 98% with zero rows. Both server-2204 runs were 2499/2500 with one `non-rectangular v18 AOI footprint` failure. Footprint validation now participates in the existing three-attempt recoverable probe loop instead of bypassing retry. Live server-2204 Fast acceptance passes Truck 2500/2500 with 278 rows and Secret Task 2500/2500 with 319 rows, both reopening exactly.
- [x] `LWB-R7-064` closes the server-2207 Secret Task stability follow-up without weakening exact AOI admission. Fourteen full 2207 Fast scans completed 2500/2500; final exact-code same-session acceptance published 67/80/126 rows. A live early probe failure is now decoded truthfully as `overview_session_unavailable` instead of a misleading success-parameter mismatch and recovered on the existing bounded retry. The historical three-attempt non-rectangular sample did not recur, so no unsupported reset/re-anchor behavior was introduced.
- [x] **OWNER OVERRIDE / REMOVE EXCEL EXPORT (`LWB-R7-066`):** City Excel export is completely removed from the shipped product. The visible control, frontend/API invocation and nine export locale strings are gone; `map_city_export` is absent from production routing; WinForms save-dialog/workbook writer/export-only SQLite paths/tests are deleted; a negative backend regression requires `COMMAND_NOT_IMPLEMENTED`; and a candidate-only browser check proves Map Data never exposes/invokes export. Historical R6/R7 export evidence remains provenance only.
- [x] **OWNER OVERRIDE / AUTO-SELECT SCAN STRATEGY (`LWB-R7-067`):** Manual/Auto Normal/Fast selectors and persisted preferences are removed. Both frontend Start paths omit `scanMode`; legacy caller mode is ignored for compatibility. Backend planning now chooses the fastest proven-safe strategy after live context: standard current-v19 1000x1000 scans use the proven Fast path at internal concurrency 20 (dedicated Monster/Zombie Boss uses `current_fast_monster_lod2_v1`), while only proven nonstandard single City/Resource uses ordinary LOD0 concurrency 8 and other unsupported geometry/category combinations fail closed. Browser, deterministic and live no-mode Zombie Boss acceptance pass.
- [x] **TREASURE QUERY/STATE-CACHE SLICE (`LWB-R7-068`):** original SQL now pins foreign-radar visibility, explicit/row-embedded viewer identity, lucky-first ordering by cached `claimPriority`, the `treasure_claim_states` join, expiry cleanup and upsert. Production accepts the four Treasure query fields, overlays cached state over persisted rows and keeps them fail-closed on other kinds. Release build/full deterministic acceptance pass. The owner-facing Treasure item remained open here pending live read-only state refresh; that follow-up is now closed by `LWB-R7-069`.
- [x] **TREASURE READ-ONLY STATE REFRESH (`LWB-R7-069`):** current-v19 source recovery and production now implement `map_treasure_state_refresh`, `_refresh_all`, and `map_treasure_claim_status` on the existing owned game session. The live inspector uses only authoritative Treasure/Supplies read paths, persists/overlays recovered state, keeps ambiguous labels `unknown`, and never synthesizes `claimPriority`. Fast live proof on server 2212 completed 2,500/2,500, refreshed/cached/overlaid 8/8 Treasure rows, and resolved all eight player states to `unclaimed` from viewer-relative scan-time membership. No claim/scout/march action was invoked; `map_treasure_claim` remains blocked. Evidence: `2026-09-20-r7-treasure-state-refresh.json`.
- [x] **TREASURE CLAIM OFFLINE CONTRACT (`LWB-R7-086`):** verified 0.3.1 recovery pins `boxes|season|single`, nonempty `single.targetUuid`, default lucky priority, the exact persisted claim-candidate SQL, separate Supplies-only SQL, and 5 s `claimTreasures`/status boundaries. Current-v19 pins ordinary direct claim to `detect.event.claim.treasure(uuid:PutLong,targetServer:PutInt)`, the local duplicate gate, rejection/success mutations and UUID-correlated push signal; Supplies is a separate scout/march path. Rebuild code/tests reproduce only this proven validator/candidate/direct-transport slice. Protected scope/lucky/scout batch orchestration remains `UNKNOWN/BLOCKED`, so production still does not route `map_treasure_claim`; no state-changing action was run. Evidence: `2026-09-20-r7-treasure-claim-offline-contract.json`.

- [x] **TREASURE CLAIM FRONTEND/STATUS CONTRACT (`LWB-R7-087`):** the hash-locked original Map Data/API frontend plus 0.3.1 host boundary now pin the owner-visible claim state machine without crossing the protected-loader SB-79 boundary. Claim calls use `boxes|season|single`; the frontend starts status polling only when the immediate result has `queued>0`, polls every 1,000 ms for at most 1,800 attempts (30 minutes), overlays returned states by Treasure UUID, and stops only when a `batch` exists whose state is not exact `running`. Lucky priority persists as `lwbridge.mapLuckyTreasurePriority` and defaults on unless local storage is exact `false`. The exact single-row button gate and action-phase state/block-reason vocabulary are also recovered/offline-tested. This closes frontend/status orchestration only: protected scope filtering/order, lucky-slot scheduling, scout-slot reservation and exact batch terminal enumeration remain `UNKNOWN/BLOCKED`, so `map_treasure_claim` remains unrouted. No game or state-changing action ran. Evidence: `2026-09-20-r7-treasure-claim-frontend-status.json`.

- [x] **OFFLINE RELEASE INTEGRITY AUDIT (`LWB-R7-088`):** the canonical frontend generator, current Release build, deterministic/transport/host boundaries, fixture captures and browser/visual checks are current again. The audit caught two maintenance regressions before release: `build_lwbridge_frontend.py --check` was stale because R7-066/R7-067 plus six maintained Map Data panel checkpoints were not fully represented in generation, and original-parity browser/pixel checks still expected the retired Export Excel/Normal-Fast UI and no Zombie Boss. Generation now explicitly encodes the owner overrides and applies a base/result-SHA-locked exact Map Data delta; regeneration reproduces all tracked WebUi bytes. Current Release builds 0/0, all six deterministic groups pass, host/transport/security checks pass with no live game commands, 36 browser checks pass, 28 unchanged screenshot pairs are pixel-identical, and the four documented Map Data override pairs remain within bounded visual deltas. The 47-row acceptance snapshot is conservative: C06 is retired, population/action/interruption gaps stay open, and this checkpoint does **not** claim final integrated live acceptance. Evidence: `2026-09-20-r7-release-integrity-audit.json`.

- [x] **HOME 20-CYCLE LIFECYCLE STRESS (`LWB-R7-089` / A09):** current-v19 production lifecycle completed 20/20 consecutive owned cycles with no external retry: 10 manual Start/Close and 10 startup-reconcile Start/Close, 20 unique sessions and PIDs. Every Start reached authoritative `running/connected`; every Close correlated to the exact owned PID, reported `processExited=true`, restored the original `LWScripts.data/.txt/version.txt`, left `installedFilesChanged=false` and `gameRunning=false`, and the final files exactly matched the pre-stress baseline. No residual Last War/launcher process remained. The historical R7-088 snapshot's A09 `not_run` status is superseded by this live proof only; other partial/blocked rows remain unchanged. Evidence: `2026-09-20-r7-overview-twenty-cycle-stress.json`.

- [x] **HOME STARTUP FAILURE MATRIX (`LWB-R7-090` / A10):** the admitted startup pipeline is now closed OFFLINE with 18 forced-failure checks: six host-pipeline branches plus 12 production-helper transaction stages. The helper matrix runs the real backup/recovery-journal/install/rollback/clear code on temporary client files while mocking only external launcher/game/readiness edges. It covers interrupted recovery, current verification, backup, recovery-arm, candidate build, all three install steps, launcher spawn, game spawn and bridge readiness, plus unmanaged-process preservation. Every cleanly rollbackable failure returns exact SHA-256 triplets to baseline, the interrupted-recovery case preserves its journal until the next retry repairs it, helper-owned launcher/game cleanup is asserted, and every stage then succeeds on the next normal start + clean stop. Host checks additionally cover official recovery/settle failures, Lua-update retry, launcher-handoff retry, generic helper failure retry and post-helper readiness cleanup/retry. Missing root/helper deployment, duplicate/concurrent Start and unmanaged-instance request admission remain under A04/A05/A08 rather than being hidden inside A10. Evidence: `2026-09-20-r7-overview-startup-failure-matrix.json`.

- [x] **HOME LAUNCH-SPAM / REFRESH STRESS (`LWB-R7-091` / A04):** the production `OverviewLifecycleService` now has a focused concurrency regression for an intentionally blocked Start. Across 10 stress runs, all 480 secondary Launch attempts returned `GAME_OPERATION_IN_PROGRESS`, all 1,200 `profile_instance_status` refreshes stayed responsive and reported the same `starting` session with no fabricated PID, helper Start ran exactly once per iteration, only the original session transitioned to `running/connected`, and every run ended with one clean Stop and no synthetic owned process. The slowest 120-refresh batch completed in 821 ms. This closes A04 at deterministic/offline scope only; `get_status.pending` remains S02/A11 UNKNOWN/BLOCKED. Evidence: `2026-09-20-r7-overview-launch-spam-refresh.json`.

- [x] **HOME PROCESS OWNERSHIP MATRIX (`LWB-R7-092` / A05):** managed/unmanaged ownership is now closed OFFLINE with real Windows process discovery rather than a pure mock. A harmless Windows console executable is copied into temporary selected/foreign `Game\LastWar.exe` paths so production `Process.GetProcessesByName("LastWar")`, `MainModule.FileName`, PID and creation-time identity are exercised without launching the official game. Across 10 runs, each selected-root managed process started/stopped with exact identity while a simultaneous foreign-path `LastWar` remained alive before/during/after the managed lifecycle; duplicate managed Start returned `GAME_RUNNING` without a second helper. A separately started selected-root unmanaged process was reported by status with exact PID + `UNMANAGED_GAME_RUNNING`, blocked Start with the same error, and was deliberately preserved; the unrelated foreign process was also preserved. All fixture processes were cleaned afterward with no residual `LastWar`. Evidence: `2026-09-20-r7-overview-process-ownership-matrix.json`.

- [x] **HOME RECONNECT POLICY MATRIX (`LWB-R7-093` / A07):** recovered reconnect ON/OFF behavior is now closed OFFLINE with a focused ten-run matrix. With reconnect enabled and the owned process still alive but its bridge heartbeat unavailable, no action occurs before the recovered 60 s disconnect threshold; at exactly 60 s the exact owned identity is terminated once, the exited session is restored once, one normal lifecycle relaunch occurs, and repairing -> launching -> verifying -> idle completes through the recovered 15 s stable gate. With reconnect disabled before the same observed disconnect, there are zero terminations, recovery cleanups or relaunches. A third branch forces a recovery-launch failure into the recovered first 15 s retry, disables `autoForceUpdateReload` through the public `set_automation` command during that delay, proves the active recovery token cancels to idle, preserves `GameDesiredRunning=true`, and proves repeated later observations launch nothing. Intentional Stop after successful recovery still clears desired-running and cannot be resurrected. A06 remains partial: R7-012 proves `map_scan_stop` itself reaches idle, but current evidence does not source-back Home `profile_instance_stop` automatically owning/cancelling an active Manual scan. Evidence: `2026-09-20-r7-overview-reconnect-policy-matrix.json`.

- [x] **HOME FAULT ADMISSION MATRIX (`LWB-R7-094` / A08):** Hook/handshake/ABI/path/permission/timeout failure admission is now closed OFFLINE. Ten focused repetitions prove exact concrete errors for blank root, missing required files, denied read, unreadable I/O, malformed PE and non-AMD64 game image; missing selected root, missing deployed helper and closed lifecycle reject before helper execution and never fabricate PID/connected state. Corrupt helper challenge hash, wrong helper-returned game path and changed critical current-client anchor each fail closed as `LAUNCH_FAILED`, with the same lifecycle succeeding after corrected proofs. A stale post-start heartbeat is externally `error/error/BRIDGE_DISCONNECTED` while retaining the exact PID only for safe cleanup. The focused command also runs the existing bounded official recovery/settle, launcher-spawn, readiness-timeout, cleanup and retry regressions; R7-090 separately retains the 12-stage production-helper transaction proof. Evidence: `2026-09-20-r7-overview-fault-admission-matrix.json`.

- [x] **HOME CLOSE TIMING MATRIX (`LWB-R7-095` / A06):** recovered Home Close reachability and all four acceptance timing states are now closed OFFLINE. Original 0.3.1 static/frontend recovery proves `profile_instance_stop` and `map_scan_stop` are separate host commands; Map Stop alone invokes game-side `stopMapScan` with a 5 s boundary, while the Home Close handler refreshes the owned instance then invokes only `profile_instance_stop`. The recovered Quick Actions gate disables Close while idle or launching, so direct backend probes correctly fail closed as `INSTANCE_NOT_OWNED` / `GAME_OPERATION_IN_PROGRESS`. Across 10 focused runs, launching preserved the one admitted Start until exact Stop; scanning Home Close restored/stopped the owned game, cleared desired-running, caused the dependent scan to terminate truthfully as incomplete/failed, preserved its completed checkpoint + bounded failed checkpoint, and left the previous published dataset unchanged; recovering Close cancelled waiting recovery to idle, restored/stopped exactly once and produced zero future starts. No real game/process ran. Evidence: `2026-09-20-r7-overview-close-timing-matrix.json`.

- [x] **HOME STATUS/PENDING CONTRACT RECOVERY (`LWB-R7-096` / A11 partial):** original 0.3.1 static recovery now resolves S02 exactly. `get_status.pending` is the numeric length of the mutex-protected bridge-store collection of outstanding bridge-to-Lua calls awaiting correlated results. `get_status` locks store `+0x100`, copies inner `+0x38` to status `+0x50`, and serializers emit that field as `pending`; the shutdown drain takes/zeros inner `+0x20..+0x38`, iterates exactly that count of 0x50-byte entries and completes them with `APP_SHUTTING_DOWN`, while timeout handling emits `LUA_CALL_TIMEOUT`. The original frontend displays host `status.pending` (`?? 0` is presentation fallback only) and Refresh Status runs `get_status -> proxy_status -> call_lua("getStatus", {})`; the Lua result is logged, not used as the pending source. A11 remains BLOCKED because the rebuild control-pipe implementation intentionally stops before source-backed `hello.ack` / outbound command/result serialization, so generic `call_lua("getStatus")` is still `COMMAND_NOT_IMPLEMENTED` and `pending` correctly remains `null` rather than a fabricated zero or UI-request count. Evidence: `2026-09-20-r7-overview-status-pending-contract.json`.

- [x] **HOME BRIDGE RPC WIRE CONTRACT (`LWB-R7-097` / A11 partial):** original 0.3.1 static recovery now closes the next protocol layer without fabricating a transport host. The shared envelope is `version/type/profileId/instanceId/requestId/timestamp/payload`; `hello.ack` is version 1, echoes profile/instance/request identity, uses a fresh numeric timestamp and an empty payload. Generic Lua RPC sends `type="command"` with payload `{id,kind:"call",fn,args,createdAt}`; the generated monotonic `cmd_<n>` is used for both outer `requestId` and payload `id`. Proxy result messages use `type="result"`; the host consumes payload `id`, `ok`, success `result` or failure `error` and correlates `id` against the pending bridge-store collection. Protocol-only encode/parse support and deterministic guards are implemented. A11 remains BLOCKED because the rebuild still has no persistent named-pipe server/instance-registration/routing/backpressure/disconnect host, so production `call_lua` stays `COMMAND_NOT_IMPLEMENTED` and `pending` stays `null`. Evidence: `2026-09-20-r7-overview-bridge-rpc-wire-contract.json`.

- [x] **HOME BRIDGE INSTANCE REGISTRY (`LWB-R7-098` / A11 partial):** original 0.3.1 store/connection recovery now pins the persistent identity state machine. Registrations are keyed by `instanceId`, retain `profileId + SHA-256(token) + expiry + claimed`, require non-empty profile/instance, token >=7 bytes and positive deadline, and reject duplicate pending/connected keys. Startup registers for exactly 90,000 ms; only unclaimed pending entries can refresh. First handshake requires exact profile/token plus `deadline > now`, then retains the claimed registration and installs a host-global generation-tagged connected route. Reconnect keeps profile/token checks but skips the expired first-claim deadline, advances generation and replaces the route. Ordinary disconnect removes only the matching instance+generation route, so stale old pipes cannot erase a newer reconnect; explicit unregister removes both retained registration and connected route. Literal `default` routing is accepted only with exactly one connected route. This is implemented/tested offline only; the named-pipe listener, queue/timeouts, proxy env binding, pending-call ownership and production `call_lua` remain blocked. R7-098 also corrects the R7-097 harness placement so status/RPC/registry guards now run in the true default deterministic sequence. Evidence: `2026-09-20-r7-overview-bridge-registry-lifecycle.json`.

- [x] **HOME A11 PRODUCTION TRANSPORT + ACTIVE ACCOUNT RAIL (`LWB-R7-127`):** the remaining read-only transport blocker is closed at live scope. A managed Last War launch authenticated the production per-user control-pipe route from inside `LastWar.exe`; `get_status` reported `xluaOnline=true`, exact allowlisted `call_lua("getStatus", {})` returned an object, pending remained 0 before/after and generic Lua calls stayed blocked. Exact owned Stop unregistered the route, restored the original package and left no Last War/launcher process. Home now renders only profiles whose `profile_instance_status` carries both PID and non-empty `instanceId`, auto-selects a running owned profile when the configured selection is stopped, and binds Stop to that exact instance. A generated two-profile browser fixture (one stopped, one owned/running) rendered exactly the running profile as active and populated the Overview detail panel without page errors. Simultaneous real multi-account UI population remains an evidence limit, not a transport blocker. Evidence: `2026-09-21-r7-home-a11-live-transport-ui.json`.

- [x] **NORMAL EXECUTABLE STARTUP/CLOSE ROBUSTNESS (`LWB-R7-128`):** pending control-pipe accept polling now detaches from the pre-`Application.Run` WinForms synchronization context, and real Overview/Map Data windows pass Responding/IsHungAppWindow/WM_NULL probes. Closing normal LWBridge during an in-flight production Start now emits an exact session/challenge cancel marker that the Python helper observes through install/launch/readiness/deferred-restore transitions, reusing the existing rollback path; the host additionally performs an exact compensating Stop if cancellation lands after helper success but before ownership publication. Live early-transaction and launcher-window acceptance both end with zero helper/launcher/game, no recovery/cancel marker and exact original package hashes. The launcher-window reproduction also closed a real handoff-child leak: cleanup tracks only exact selected-root launcher lineage absent from the pre-launch baseline, revalidates path/parent/start identity, tries normal close first and permits bounded force termination only for that correlated launcher process. LastWar.exe retains exact-PID normal-close-only handling. A11 read-only getStatus/Stop was rerun successfully afterward; Release 0/0 and the full deterministic suite pass. Evidence: `2026-09-21-r7-normal-window-close-cancellation.json`.

- [x] **REPORTED SCAN FIXES FRESH SMOKE (`LWB-R7-129`):** the pushed post-R7-128 build was re-tested from a clean detached worktree on server 2212. Player City completed 2,500/2,500 with 0 failed/unread and published/reopened 6,684 rows. Truck completed 2,500/2,500 with 0 failed/unread and published/reopened 215 rows, including 14 Special UR rows and 1,499 currentGoods items. Generic Monster completed 2,500/2,500 with 0 failed/unread and published/reopened 6,848 rows; direct inspection of that exact result database found 25 live Doom Walkers with the recovered type-8/special-11 identity and observed levels through 220. Final cleanup leaves no helper/game/launcher and the original Lua-package hashes match exactly. These three owner-reported scan defects are ready for owner UI testing. Evidence: `2026-09-21-r7-reported-scan-smoke.json`.

- [x] **MAP DATA BROWSER ACCEPTANCE + CLEAR RACE (`LWB-R7-131`):** real headless Edge now drives the generated shipped Map Data bundle with a backend-contract-correct two-server fixture. Manual categories, Manual/Auto tab, result tab and saved browse server persist through full reload; both saved servers are selectable; Auto Stop persists disabled, sends one backend Stop and prevents the next target. Failure-first timing found R7-130's Clear invalidated the old search but its own state resets immediately launched a replacement query; the canonical generator now suppresses exactly that one Clear-induced automatic search. The permanent CI regression requires no post-Clear `map_search` and no false saved-search alert. Release 0/0, all six deterministic groups, 36 browser checks and 28/28 strict pixel-identical pairs pass. Final normal-user built-executable walkthrough remains open. Evidence: `2026-09-22-r7-map-browser-acceptance-clear-race.json`.

- [x] **AUTO SCAN NAVIGATION/REFRESH/RECONNECT OWNERSHIP (`LWB-R7-132`):** failure-first generated-frontend timing reproduced one due cycle starting twice after disconnect/reconnect because the scheduler effect depended on connection state and was cancelled before its deadline was persisted. The scheduler is now profile-owned, reads connectivity from `autoOnlineRef`, and checks enabled/online state before each target plus immediately after confirmed travel. Browser acceptance proves navigation away/back with repeated Refresh Status does not duplicate the two-target 2212/2213 cycle, no scan starts before a held jump confirms, disconnect at a target boundary advances the schedule without starting later targets, reconnect cannot re-admit the interrupted cycle, and disconnect while a jump is pending prevents that target scan after the jump returns. CI, Release 0/0, all deterministic groups, 36 browser checks and final 28/28 strict pixel identity pass. Evidence: `2026-09-22-r7-auto-scheduler-navigation-reconnect.json`.

- [x] **AUTO NATIVE TRAVEL FAILURE CONTINUATION (`LWB-R7-133`):** one owned live session on 2212 produced exact `SERVER_JUMP_FAILED / server_jump_precheck_failed` for target 2148, remained authoritatively on 2212, and then completed the next valid Fast Zombie Boss path 2,500/2,500 with zero failed/unread in 5.53 s without restarting the bridge/game. This closes the previously synthetic-only native travel failure evidence gap; R7-132 remains the browser authority for the shipped scheduler's per-target catch/continue and single-owner behavior. Cleanup restored the exact three original package hashes with zero game/launcher/helper. Evidence: `2026-09-22-r7-native-auto-failure-continuation.json`.

- [x] **47-CASE ACCEPTANCE REFRESH + BROWSER PORT HARDENING (`LWB-R7-134`):** the full task.md acceptance ledger is current against R7-089–R7-133. A fresh clean run exposed one test-only flake: `listen(0)` assigned Chromium-blocked port 10080 to R7-131. R7-131/R7-132 now use safe loopback defaults 18081/18082 with overrides. Fresh generator/browser/visual/Python/Release/deterministic gates pass: 36 browser checks, 28/28 strict pixel identity, 0 warnings/0 errors, six deterministic groups true. Stale A04–A11/C05 statuses are corrected only where durable later evidence closes them; population/action/active-restart/F04 gaps remain explicit. Evidence: `2026-09-22-r7-acceptance-matrix-refresh.json`.

- [x] **NORMAL PRODUCTION VS FIXTURE SEPARATION + LEDGER REFRESH (`LWB-R7-135`):** maintained `tools/check_normal_release_windows.ps1` passively launches real Release Overview and Map Data under production service composition while suppressing auto-launch, blocking state-changing owner commands, isolating/restoring WebView Presentation state and requiring config/config-backup hash identity. Both windows pass Responding/IsHungAppWindow/WM_NULL, no game/launcher/helper appears, Map Data executes a real saved City search/render, and final processes are zero. R7-134 remains the separate fixture/browser/pixel authority. S01/S02/S03/S06/S07 and M01–M07/M11 were refreshed from current evidence. Evidence: `2026-09-22-r7-normal-production-window-smoke.json`.
- [x] **MANUAL/AUTO APP-RESTART SAFETY (`LWB-R7-136`):** B08/D04 are closed at current offline/browser scope. Manual scans hold an exclusive per-profile scan-owner lease; orphaned durable `running` rows become `failed` with the app-restart reason, checkpoints remain, interrupted staging cannot publish, prior published data survives and a concurrent second app cannot steal/reconcile the active owner. Auto Scan persists an in-flight cycle marker before travel; after a full context/app restart the target list is never resumed, return-to-origin is restored/retried before future scheduling when enabled, failed restoration admits zero scans, and return-disabled restart preserves the current server. Existing R7-131/R7-132 + new R7-136 browser regressions, 36 browser checks, final 28/28 strict visual identity, Release 0/0, six deterministic groups and normal-production smoke pass. No intentional live-game OS process kill is claimed. Evidence: `2026-09-22-r7-scan-restart-safety.json` and `2026-09-22-r7-acceptance-matrix-r7136.json`.
- [x] **THREE FULL MULTI-SERVER AUTO CYCLES (`LWB-R7-137`):** D02 is closed as `PASS_CURRENT_PLUS_HISTORICAL`. One owned live session completed three consecutive real `2212 -> 2213` cycles with six unique Fast Zombie Boss scan runs; every leg completed 2,500/2,500 with 0 failed/unread, authoritative server context was confirmed after every travel, all three cycles returned to 2212 and final context was 2212. R7-039 remains the live authority for persisted `nextRunAt` scheduling/advancement, while R7-132/R7-136 own scheduler/restart semantics; do not rewrite R7-137 as if the React timer initiated all three cycles. Exact package restoration and zero game/launcher/helper cleanup pass. Evidence: `2026-09-22-r7-three-multiserver-auto-cycles.json` and `2026-09-22-r7-acceptance-matrix-r7137.json`.
- [x] **DUPLICATE START + STOP TIMING ACCEPTANCE (`LWB-R7-138`):** B05/B06 are closed without a production code change. A changed-type City Start attempted during an active Resource scan is rejected by `SCAN_RUNNING` without changing the owned run ID, selected types, backend-selected mode/strategy/concurrency or reacquiring live context. A five-block Stop matrix covers early/mid/near-completion at exact 0/2/4 checkpoints with one in-flight capture canceled, terminal idle counters, no canceled checkpoint, no post-Stop scheduling and no partial publication. R7-130 remains the live public Stop authority. Release 0/0 and all six deterministic groups pass. Evidence: `2026-09-22-r7-scan-ownership-stop-timing.json` and `2026-09-22-r7-acceptance-matrix-r7138.json`.
- [x] **DEFINITIVE BRIDGE LOSS DURING ACTIVE SCAN (`LWB-R7-139`):** B07 is live-closed with a production failure-boundary repair. Failure-first live proof stopped the exact owned game/bridge during an active Fast Monster scan and eventually surfaced generic `direct map scan contains failed batches` after ~65.9 s. `MapScanEngine` now treats exact recovered `GAME_CONNECTION_UNAVAILABLE` as run-terminal while preserving all ordinary/transient block retries. Deterministic coverage requires one source call and `begin -> fail` with no synthetic failed checkpoint/publication. Repaired live proof on 2212 ended in 18.324 s scan wall with exact `game connection unavailable`, durable `failed`, `failedBlocks=0`, prior Monster publication preserved, resume false, exact package restoration and zero owned processes. Evidence: `2026-09-22-r7-bridge-loss-scan-failfast.json` and `2026-09-22-r7-acceptance-matrix-r7139.json`.

### Current unfinished Map Data roadmap - owner-facing

- [x] **Secret Task cross-server stability (`LWB-R7-064`):** fourteen full Fast Secret Task scans on server 2207 completed 2,500/2,500 during the investigation, including the final exact-code three-scan same-owned-session pass with 67/80/126 rows. The historical all-three-attempt non-rectangular footprint did not recur, so no speculative reset/re-anchor was added. Live stress instead exposed an early failed probe whose success-only target/view fields were absent; host validation had masked the native error as a parameter mismatch. Failed correlated results now surface their native error before success-only validation, while proven results retain the strict request/geometry/10,000-AOI gates. Final exact-code iteration 1 decoded `overview_session_unavailable`, recovered on the existing bounded retry, and completed normally; iterations 2-3 were clean.
- [x] **Truck/reward game artwork (`LWB-R7-065`):** public `game_asset_image({assetPath})` is now LIVE-PROVEN on current-v19. The game-side bridge uses the native xLua extension form `SpriteRenderer:LoadSpriteAuto`, extracts the authoritative Sprite `textureRect` with GPU `Graphics.Blit`, encodes PNG/base64, and the host validates/caches the exact source. Four representative Truck reward paths passed in one owned session, including extensionless item paths, `.png` paths and a packed atlas UI sprite; focused pixel checks proved populated artwork rather than transparent PNGs. `spriteName` remains fail-closed and is not required by Truck `currentGoods`. Evidence: `2026-09-20-r7-truck-game-asset-image.json`.
- [x] **Remove Excel export (`LWB-R7-066`):** owner override is fully delivered; no production City export UI/command/dialog/writer/storage path remains. Historical recovery evidence is retained only as superseded provenance.
- [x] **Automatic scan strategy planner (`LWB-R7-067`):** user-owned Normal/Fast state is gone from Manual/Auto; effective strategy/concurrency are backend-selected diagnostics, and the current-v19 no-mode live proof completed 2,500/2,500 with the selected Fast Monster strategy.
- [x] **Treasure result/status filters (`LWB-R7-068/069`):** foreign-radar visibility, lucky-first ordering, viewer identity, cached-state overlay, `map_treasure_state_refresh`, `_refresh_all`, and `map_treasure_claim_status` are recovered/implemented. Current-v19 live proof refreshed/cached/overlaid 8/8 rows and resolved all eight player states to `unclaimed`; ambiguous future cases remain `unknown`. `claimPriority` is not fabricated and state-changing `map_treasure_claim` remains blocked.
- [x] **Auto Scan scheduler (`LWB-R7-038/039/070/132/133/136/137`):** the core owner-facing scheduler is complete. R7-038/039 live-prove repeated Run Now, ordered multi-target scanning, disable-during-cycle skip, return-to-origin/status truth, persisted future scheduling across full desktop/game restart and automatic `nextRunAt` advancement. R7-132 browser-proves navigation away/back, repeated Refresh Status and disconnect/reconnect ownership without duplicate cycles or scans before confirmed travel. R7-133 live-proves a real native `server_jump_precheck_failed` followed by same-session successful next-target scan continuation. R7-136 closes app/process restart semantics at browser/offline scope. R7-137 closes D02 by live-proving three consecutive 2212/2213 scan/return cycles with six unique exact scan runs, composed with R7-039 for the durable next-deadline portion. No remaining scheduler implementation gap is identified.
- [ ] **Ghost positive-row live proof:** implementation/query/sorts are ready; positive population proof is owner-deferred until Thursday 2026-09-24.
- [x] **Railway positive-row acceptance (`LWB-R7-071`):** current-v19 Fast full-world proof on server 2204 completed 2,500/2,500 with zero failed/unread blocks, published/reopened 3 authentic Railway rows, passed 12 sort scenarios before and after reopen, proved relative ordering across 3 rows, exercised `itemCount` with nonempty `currentGoods`, and live-proved public moving Follow on an exact published `marchUuid`. The session returned to original server 2212; temporary Train-list discovery code was removed after it served only to nominate the authoritative full-scan target. Optional raw `trainData` was absent in this population and remains unclaimed.
- [ ] **Supplies positive-row acceptance (`LWB-R7-072`, population-pending):** the strict current-v19 read-only acceptance harness is ready and now supports bounded target-server proof plus an explicit positive `WorldSuppliesPoint` gate. Server 2204 completed a full Fast 2,500/2,500 scan with zero failed/unread blocks but published 3 ordinary Treasure and 0 Supplies; a second strict full-world run on reachable server 2213 also completed 2,500/2,500 and published 2 ordinary Treasure, 0 Supplies, then returned to 2212. A temporary read-only season/activity diagnostic exhaustively checked every server 2149-2211 and found zero active Supplies activity objects; 2148/2147/2146 and earlier sampled lower servers were rejected by the game's own `server_jump_precheck`. The temporary diagnostic lane was removed. Keep this item open and rerun the unchanged strict harness when the event/population appears.
- [ ] **Remaining result/action audit:** Dispatch/Resource/City/Monster result filters are closed by `LWB-R7-073`–`076`; public completed/persisted `map_data_options` by `LWB-R7-077`; public active/completed/reopened `map_summary` by `LWB-R7-078`; Scheduled Plunder read/list/status persistence by `LWB-R7-079`; Dispatch schedule/cancel validation, persistence, current-v19 eligibility/executor, durable worker and public schedule/cancel wrapper by `LWB-R7-080`–`084`; Dispatch Alliance Share original/current-v19 payload/validation by `LWB-R7-085`; Treasure host/direct-current-v19 claim-contract recovery by `LWB-R7-086`; and Treasure frontend/status polling, UUID overlay, lucky preference and row-gate recovery by `LWB-R7-087`. Treasure remains intentionally unavailable because protected scope filtering/order, lucky-slot scheduling, scout-slot reservation and exact batch terminal enumeration are still unrecovered. Dispatch plunder implementation and Alliance Share offline payload work are complete; live plunder/share/Treasure acceptance remains NOT_RUN. Railway positive acceptance is already closed by R7-071. Continue population-dependent Ghost/Supplies gaps plus explicitly authorized live Truck/Dispatch/Alliance/Treasure action acceptance. Live messaging/state-changing actions require explicit authorization and suitable targets.
- [ ] **Final integrated release acceptance:** R7-088 closes the offline Release-integrity gate; R7-089–095 close A04–A10 at their documented live/offline scopes; R7-127 supplies A11 live transport; R7-131 closes Map persistence/Stop/Clear-race browser follow-up; R7-132 closes Auto navigation/refresh/reconnect ownership; R7-133 closes real native travel-failure continuation; R7-134 refreshes all 47 acceptance rows; R7-135 closes separate normal-production versus fixture verification; R7-136 closes B08/D04 app/process restart safety; R7-137 closes D02 through three live multi-server scan/return cycles composed with R7-039 scheduling; R7-138 closes B05/B06 changed-type ownership plus early/mid/near-completion Stop timing; and R7-139 closes B07 deliberate bridge loss with truthful fail-fast terminal semantics. Final acceptance stays open for B11/C04/C07 technical follow-up, Ghost positive proof on 2026-09-24, Supplies positive population, explicitly authorized live Truck/Dispatch/Alliance/Treasure actions, simultaneous real multi-account UI population, and the final human built-executable restart/walkthrough.
- [x] **OWNER MAP DATA RETURN:** the owner resumed Map Data and reported real Monster scan progress/Stop defects after the Home prerequisite.
- [x] **MAP DATA PERFORMANCE + STOP checkpoint:** `LWB-R7-012` measures the current-v17 native footprint as 5x10 AOIs, reduces full-world acquisition from 250 to 200 sequential requests without weakening the exact 10,000-AOI publication gate, live-proves Monster full runs at 43.620 s Normal and 70.746 s Fast, and repairs live status/summary plus one-click Stop-to-idle integration.

## Current owner priority - shared Map Data Manual Scan engine, 2026-09-13

- [x] **Mixed/all-eight shared-engine acceptance (`LWB-R7-036`):** production now accepts arbitrary mixtures of the recovered original eight Map Data kinds in one run while dedicated Zombie Boss remains intentionally standalone. Same-owned-session mixed Normal -> all-eight Normal completed 2,500/2,500 twice with zero failed/unread blocks in 129.335 s / 127.211 s and exact reopen equality. A second same-owned-session all-eight Normal -> Fast proof completed 2,500/2,500 twice with zero failed/unread in 133.891 s / 127.058 s at concurrency 8 / 20; Fast reopen reproduced every per-kind count exactly. Deterministic coverage drives all eight normalizers through one 10,000-AOI run and proves transactional all-eight publication. Manual multi-select and Normal/Fast controls are restored with Zombie Boss mutually exclusive. Shared Manual selection/mode acceptance is closed; Auto Scan was subsequently delivered by R7-037/038/039/070 and browser-hardened by R7-130/131. Ghost positive-row acceptance remains population-pending.
- [x] **Treasure ordinary Manual Start acceptance (`LWB-R7-035`):** current-v19 Treasure-only Normal scan completed 2,500/2,500 with zero failed/unread blocks in 123.150 s, published one authentic native type-21 `TreasurePointInfo` row and reopened exactly one. That row preserved the recovered ordinary Treasure identity/type/source contract plus rewarded/digging/reward-max/remaining-box counts. Point type 27 `WorldSuppliesPoint` + `TableName.LWIceSupplies` is source-recovered and deterministically covered, but this live population had zero Supplies rows, so positive Supplies live proof remains observationally open. Exact v19 compatibility/restoration and cleanup pass. Mixed/all-eight selection acceptance is next; Ghost positive-row acceptance remains pending.
- [x] **Ghost Ops implementation / positive-row acceptance pending (`LWB-R7-033`):** current-v19 source recovery pins `WorldPointType.GHOSTRECON_POINT = 29`, native `GhostreconPointInfo`, `WorldPointManager.GetGhostreconPointInfoByIndex`, and `TableName.LwGhostreconTask`. Release build and all six deterministic groups pass, including a 2,500-block Ghost regression. A real Ghost-only Normal run reached the post-scan positive-row gate with 2,500/2,500, zero failed/unread blocks, exact LOD0/10x100 restoration, but the current map exposed zero authentic Ghost rows, so LIVE-PROVEN acceptance remains open and the gate is not weakened. Treasure is now LIVE-PROVEN under `LWB-R7-035`; mixed/all-eight acceptance is next while Ghost awaits a positive population.
- [x] **Dispatch/Secret Task ordinary Manual Start acceptance (`LWB-R7-032`):** current-v19 Dispatch-only Normal scan completed 2,500/2,500 with zero failed/unread blocks in 105.782 s, published 74 authentic live Secret Task rows and reopened exactly 74. All 74 preserved point type 17, `HeroDispatchMissionPointInfo`, config/level/quality/special flag, mission timing, owner UID, list counts, name key and exact source identity; 70/74 carried alliance ID. Exact v19 restoration/compatibility and cleanup pass. Ghost Ops is subsequently implemented/live-pending under `LWB-R7-033`; Treasure is the active task.
- [x] **Railway/Train ordinary Manual Start acceptance (`LWB-R7-031`):** current-v19 Railway-only Normal scan completed 2,500/2,500 with zero failed/unread blocks in 112.327 s, published 1 authentic live Railway row and reopened exactly 1. The row carried source-backed train type/quality/power/config/raw train data, arrival time, robbery count, march identity and protection time. This population did not expose `maxLootCount` or nonempty `currentGoods`; those remain optional/source-backed and are not fabricated. Exact v19 restoration/compatibility and cleanup pass. Dispatch/Secret Task is next on the same scanner.
- [x] **Monster/Zombie Boss fast-only recovery (`LWB-R7-030`):** owner-reported repeated scans could suddenly crawl or become unusable. Failure-first inspection found a real `zoom_restore_confirmation_timeout` where camera/svLod returned to normal but AOI geometry remained stuck at 1000/1. Production no longer silently falls back to the slow LOD0 full-world path; Monster/Zombie Boss fail truthfully if LOD2 cannot continue. Zoom restoration now gets one bounded native `WorldPointManager.UpdateLWAoi_Normal(true)` recovery attempt. Two same-owned-session Zombie Boss scans completed 2,500/2,500 in 38.414 s / 30.715 s with exact 10x100 restoration, and a final clean run naturally exercised recovery and completed in 41.053 s with 10x100 restored. Exact v18 compatibility/cleanup pass.
- [x] **Zombie Boss live countdown (`LWB-R7-029`):** owner acceptance found that authoritative shield deadlines displayed correctly but Remaining froze until another scan. The existing one-second Map Data clock omitted `zombie_boss`; production now includes it. Real desktop proof required no rescan and observed three saved timers decrease by exactly four seconds over a 4.2-second window. Syntax/build/deterministic checks and exact v18 restoration/compatibility pass.
- [x] **Dedicated Zombie Boss scan/filter checkpoint (`LWB-R7-028`):** current-v18 production now separates `zombie_boss` from generic Monster, removes protection-detail network work from ordinary Monster scans, and gives Zombie Boss its own persisted count/data plus localized Name, <= Level, Remaining, Distance and Jump results. Dedicated live transport processed 54/54 bosses with 48 authoritative states and 7 active deadlines; fresh desktop acceptance restored `Zombie` + Level 65 without pressing Search, completed in 18.510 s, and an immediate repeated scan completed in 14.594 s with the same filters/tab and six visible countdown rows. Exact official v18 restoration/compatibility and cleanup pass; Railway remains separate and unfinished.
- [x] **Monster transient readiness resume (`LWB-R7-027`):** failure-first 7-second same-owned-session readiness loss reproduced 94 `game connection unavailable` failures after 2,406 completed blocks. Production now creates the exact full-world run/session resume anchor before LOD2 and keeps it across fallback; repeating the same pause completed 2,500/2,500 with zero failures in 18.294 s, published/reopened 6,912 rows, preserved Distance on every row, and received 12/12 required protection replies with 12 active deadlines. Different session identity still fails closed; exact v18 restoration/compatibility and cleanup pass.
- [x] **Monster fast-scan target (`LWB-R7-026`):** current-v18 Monster-only discovery uses one native LOD2 whole-world snapshot with exact LOD0/camera restoration. R7-026 historically retained automatic LOD0 fallback; `LWB-R7-030` subsequently removes that slow fallback and adds bounded native AOI restoration recovery. Two consecutive ordinary Normal runs completed 2,500/2,500 with zero failures in 11.418 s / 10.903 s, preserved Distance on all 7,271 / 7,194 rows, and retained automatic first-scan Remaining with 10/10 and 9/9 required protection replies producing 10 and 9 active future deadlines. Same-owned-session brief readiness resume is subsequently LIVE-PROVEN by `LWB-R7-027`.
- [x] **Zombie Boss first-scan Remaining accepted (`LWB-R7-024/025`):** exact v18 shared-UUID recovery is implemented with one in-flight detail request, and a fresh one-scan Normal proof (no Jump/click/priming scan) completed 2,500/2,500 with zero failures in 108.144 s, found 21 eligible bosses, sent 2 detail requests, received 2/2 replies and persisted 2 active future shield deadlines. The same proof updates the current-v18 native footprint from v17 5x10 to v18 3x10 and uses a conservative 340-request exact-10,000-AOI planner. Railway is now the active category.
- [x] **Monster backend positive Remaining source (`LWB-R7-023`):** fresh current-v18 ordinary scan completed 2,500/2,500 with zero failures in 73.206 s, published 13,462 Monster rows, found 97 exact Invasion Zombie Bosses, and persisted 40 authoritative positive protection deadlines; six were still future at publication. Final retry correctly targeted only the 57 unanswered bosses. Frontend generator and local-only ticking Remaining browser regression pass.
- [x] **Black-screen bootstrap regression (`LWB-R7-022`):** removed the stale `M.Pump()` call to deleted `pump_monster_protection_batch_retry`; Player.log root cause is proven, deterministic guard added, and current-v18 manual + auto-start lifecycle launches both reach Connected and close cleanly.
- [x] **Historical owner Zombie Boss Remaining diagnosis (`LWB-R7-021`, superseded by R7-024):** owner normal Monster scan completes 2,500/2,500 in 74.791 s with 10,297 rows. Owner screenshot proves active protection timers, while one authoritative protected reply still persisted deadline `0`. The scanner now preserves the recovered `createTime + monster_invasion.k12 * 1000` deadline while loaded, removes the premature 40 ms all-target retry, and retries only unanswered UUIDs once at final collection. First retest goal: active protected rows show ticking Remaining. Railway remains gated.

**PM17 corrections remain delivered.** Player City `LWB-PC-001/002/003` remains credited for authentic current-client city acquisition, persistence/reopen and owner-visible saved-row rendering. This does not prove the ordinary Manual Start button performs a complete world scan.

**Current sequence:** build the shared scanner before treating Resource/Monster/etc. as separate delivery items. The core work is M01-M05/R7: authoritative map geometry/traversal, one owned scan run, block scheduling, Normal/Fast concurrency, native point/march capture, acknowledgements/removals, Stop/cancel/retry/failure, truthful progress/completion, durable staging/publication and Clear with late-result protection. Then route the eight `selectedTypes` through that same engine.

- [x] PM17 delivery and owner-authorized direct Map Data continuation.
- [x] Player City source/storage/reopen/owner-visible foundation under `LWB-PC-001/002/003`.
- [x] **Player City ordinary Manual Start complete-run acceptance:** `LWB-R7-008` LIVE-PROVES the common production service scanning all 10,000 LOD0 AOIs / 2,500 logical blocks through ordinary Manual Start, publishing/reopening 2,592 Cities with zero failed blocks in 51.524 s scan wall time. A same-session-family dedicated comparator separately covered 10,000/10,000 AOIs in 250 requests and published/reopened 2,254 Cities. Exact live City count is not a fixed invariant; full AOI coverage plus publish/reopen equality is. The later direct-snapshot completion contract is closed by `LWB-R7-055`; original native queue/ack counters are intentionally not reproduced by this transport.
- [x] **Normal/Fast / owner progress fidelity:** `LWB-R7-009` keeps the proven sequential full-City acquisition/publication boundary but exposes truthful native AOI-union progress during the 250-request acquisition, capped below 100% until logical checkpoint/publication completes. It also fixes Manual Scan selection polling so stopped-state backend defaults no longer overwrite the owner's local checkbox choices. `LWB-R7-012` supersedes that 250-request performance baseline with the current-v17 200-request planner and live status/summary event integration.
- [x] **Progress/completion integrity (`LWB-R7-055`):** the production direct-snapshot scanner now closes the recovered completion contract for its actual architecture. Real scheduler counters drive unread/inflight/rate and 98%-until-`completed`; exhausted capture retries fail the run; cancellation stops without publication; foreign/duplicate captures fail closed; exact logical coverage and zero failures are required before publish; the SQLite completion transaction conditionally owns `running -> completed` and verifies the exact run after commit; status now explicitly reports `resumeAvailable=false`; and bounded WorldMarch enumeration must prove exhaustion instead of silently accepting a 50,000-row prefix. The rebuild does **not** instantiate the original bot's native pending-points/marches/removals/acks queue, so `nativePendingRecords`/`nativeDroppedRecords` remain unknown/null rather than fabricated. Direct snapshots represent removals by authoritative selected-kind replacement at successful publication. Resume serialization remains unsupported and separately open; no 100%-only success shortcut exists.
- [x] **Clear ownership / server scope (`LWB-R7-054`):** production `map_scan_clear` is now owned by the shared Manual Scan service instead of bypassing it. It preserves the recovered `SCAN_RUNNING` and exact-current-live-server gates, serializes Clear against Start on the same service lock, atomically removes only the current server's scan/index scope, preserves player marks and other servers, publishes an idle/zero-progress reset, supports safe empty repeat Clear, and fails closed when only saved/replay context exists. Because active Clear is rejected and Stop waits for terminal scan ownership before returning, successful Clear has no accepted old active run that can later republish rows. Hash-locked `LWB-R6-057` recovery and in-memory destructive acceptance pass; no real user map database was cleared.
- [x] **Monster ordinary Manual Start complete-run acceptance:** `LWB-R7-010` LIVE-PROVES Monster-only ordinary Manual Start over all 10,000 LOD0 AOIs / 2,500 logical blocks, zero failed blocks, and transactional publication/reopen of 6,934 game-classified monsters/bosses in 71.161 s. The route is generic (`IsMonsterOrBoss` + game-owned MonsterTemplate lookup), not Zombie-Invasion-only.
- [x] **Monster readable localized names:** `LWB-R7-011` restores production `lastwar_localize` from the verified original offline cache contract, exact 200-key cap and requested-language -> English -> key fallback. Real-cache backend proof and isolated Map Data language-switch browser proof pass; this is IMPLEMENTED/OFFLINE-TESTED, not current-client live localization proof.
- [x] **Full-world performance / progress / single Stop:** `LWB-R7-012` LIVE-PROVES the current-v17 5x10 native AOI footprint and 200-request full-world planner, preserving exact 10,000/10,000 AOI admission. Monster full-run acceptance is 43.620 s Normal and 70.746 s Fast with dynamic row counts; a dedicated current-v17 proof returns idle after one Stop, while production now emits `bridge://map-scan-status` and `map_summary` follows the active Manual run instead of stale saved state.
- [x] **Monster result usability:** `LWB-R7-013` LIVE-PROVES current-v17 Monster timer-field acquisition (11/3,853 live rows carried a positive `endTime` in the sanitized sample) and IMPLEMENTS/OFFLINE-TESTS localized visible-name search, exact-level options/filtering, and a locally ticking countdown that shows `-` when no supported positive deadline exists. Exact-level and visible-keyword behavior are explicit rebuild usability policy; no raw JSON property-name false matches are accepted.
- [x] **Owner Monster/Home correction pass:** `LWB-R7-014` supersedes the R7-013 presentation policy: Remaining is Zombie/Boss `zMBossInfo.shieldEndTime` only; Monster level means `<= selected`; Level/Distance sorts no longer clear rows; query errors preserve displayed data; current-v17 home tile `(230,257)` and game-owned `TileDistance` populate Distance for all 12,731 rows in the latest full-world proof; same-tick camera restore retains complete acquisition. `LWB-OVR-016` reuses an exact validated root/package official-settle marker so unchanged starts no longer perform the launcher/game warm-up every time. Active positive shield time and owner-visible anti-flicker confirmation remain observational follow-ups, not blockers to Resource work.
- [x] **Owner Monster result follow-up:** `LWB-R7-017` rechecks the immutable 0.3.1 Monster UI and confirms it has no original Remaining column or level selector. Rebuild Remaining stays shield-only (`ZMBossInfo.shieldEndTime`) with current-v17 typed metadata recovery and a same-field reflection fallback; generic `endTime`/`nextStageTime` are not substituted. Level choices now advance by five (`5,10,15,...`) while preserving the `<= selectedMaximum` predicate. Distance is explicitly base/home-relative through `PlayerWorldPointId -> IndexToTilePos -> WorldScene.TileDistance(homeTile, monsterTile)`, with a deterministic request regression. Fresh current-v17 ordinary Monster Start completed 2,500/2,500 with zero failed/unread, published/reopened 12,017 rows, and populated Distance on all 12,017. That population had zero `ZMBossInfo` / shield-deadline rows, so `-` remains the truthful Remaining display until a shielded Zombie Boss is actually present. Release builds, deterministic suite and 36 browser checks pass. Owner retest is the next gate before Railway/Train.

- [x] **Monster v18 scan-resilience / automatic detail transport:** `LWB-R7-018/019` diagnose the ~4.5% -> 0% loop, keep optional detail failure from discarding base AOI capture, and reproduce the exact click-triggered `MonsterInvasionBossDetail` request automatically for each loaded eligible boss. Final normal proof completed 2,500/2,500 with zero failed/unread blocks in 82.962 s, published/reopened 10,350 rows, requested 81 bosses and captured 50 authoritative replies after one bounded retry. No active positive deadline existed in that dynamic population; owner-visible positive Remaining remains an observation check, not a technical full-scan blocker. Railway/Train is next after the owner sees the delivered Monster page.
- [x] **Owner UI Monster scan validator correction (`LWB-R7-020`):** the discarded 31-failure run was traced primarily to the UI validator rejecting valid cross-AOI protection carry-over (`target=1,newRequest=0,ready=1`) after R7-019 UUID deduplication. The validator now accepts request/ready independently up to the current target count. Subsequent owner/fresh v18 scans reach 2,500/2,500 with zero failures; the remaining gate is positive Remaining under R7-021/023, not scan completion.
- [x] **Resource ordinary Manual Start complete-run acceptance:** `LWB-R7-015` LIVE-PROVES current-v17 Resource-only ordinary Start over all 2,500 logical blocks with zero failures in 70.958 s Normal, publishing/reopening exactly 442 live Resource rows. All 442 carried source-backed `resourceTypeId`, level and known gather occupancy; 71 were occupied in that live population. The row counts are dynamic observations, not invariants. The unresolved numeric `resourceTypeId` -> public `resourceNameKey` mapping stays downstream and is not guessed.
- [x] **Resource authoritative full-state/filter acceptance (`LWB-R7-034`):** current-v19 Resource-only Normal Start completes 2,500/2,500 with zero failed/unread in 131.208 s and publishes/reopens 838 rows. Type/level/name/max/occupancy/black-tile classification cover all 838; all 782 final idle rows have authoritative `WorldGetDetail` amounts (696 full, 86 partial, zero invalid pairs). Default Idle+Full+Exclude-black returns 696. Live LOD2 returns zero Resource points, so complete Resource discovery remains LOD0; detail requests overlap asynchronously. Lua probe local count is reduced 201 -> 192 with a deterministic `<200` bootstrap guard.
- [x] **Truck ordinary Manual Start complete-run acceptance:** `LWB-R7-016` LIVE-PROVES current-v17 Truck-only ordinary Start over all 2,500 logical blocks with zero failures/unread blocks in 71.321 s Normal, publishing/reopening exactly 175 live Truck rows. Every row in that population carried authoritative Truck type, quality, power, train config/raw train data and normalized `arriveTs`/`robTimes`. The count is dynamic; `maxLootCount`, `protectTime`, `isSpecialURQuality`, and UI-ready `currentGoods` remain unproven and are not guessed.
- [x] **CATEGORY ACCEPTANCE on the common engine:** Player City, Monster, Resource, Truck, Railway/Train, Dispatch/Secret Task and Treasure ordinary Manual Start are live-proven; `LWB-R7-036` additionally LIVE-PROVES mixed selections, all original eight together, same-owned-session repeated runs, and all-eight Normal/Fast. Ghost Ops remains positive-row LIVE-PENDING only because current combined populations still expose zero type-29 rows; rerun that unchanged gate when population exists. Reuse this same acquisition/publication architecture for Auto Scan; do not create a second scanner.
- [ ] **DOWNSTREAM after acquisition:** finish per-kind normalization plus Search/filter/sort/options/export/navigation. Scan Content selects acquisition; result filters query persisted data.
- [x] **AUTO SCAN after Manual:** delivered on the shared scan engine by `LWB-R7-037/038/039/070`; R7-130 adds per-target failure isolation, saved-server browsing and synchronous Stop ownership, while R7-131 browser-proves persisted controls/server choice and Stop preventing the next target. No second acquisition implementation was created.
- [x] **S03 runtime Refresh Status transport (`LWB-R7-127`):** production now has an authenticated live pipe route, truthful pending ownership and exact allowlisted `call_lua("getStatus", {})`; a real managed session returned an Object and Stop unregistered/restored cleanly. Generic Lua remains blocked. The reported Home active-account/right-pane selection is also generated-frontend browser-proven. S06 cross-server travel is separately LIVE-PROVEN by `LWB-R7-037`.

The present `LiveResourceProbeCommandService` city/resource route is support/evidence code only: it accepts one city/resource kind and leaves full block/progress metrics unknown. SB-97 remains an operation-specific restriction and must not be recreated or rerouted. Continue permitted recovery/implementation/offline work instead of skipping the shared-engine priority.

### Preserved delivered OVL milestone

- [x] OVL-00 PM15 handoff cleanup delivered at `cc4bc77`; CI passed.
- [x] OVL-01 exact dependency chain bounded. Original proof/ticket/final pipe grammar stays open; independent current-v14 route is explicitly policy.
- [x] OVL-02/03/04 technical lifecycle is live-proven in `LWB-OVL-002`: two fresh current-client cycles reached exact same-session game-side readiness/message evidence, then exact-PID normal Close and byte-exact deferred restoration. Normal Overview button/visual placement is owner-verified in `LWB-OVL-003`.
- [x] Normal Overview owner run completed: Launch opened the real game, **LWbridge is running** was visible top-centre, and Close completed with automatic cleanup evidence (`LWB-OVL-003`).
- [x] OVL-05 automatic per-session evidence + tested `Start Overview Verification.cmd` + beginner owner guide delivered.
- [x] OVL-06 owner-visible normal Overview verification and explicit acceptance recorded as `LWB-OVL-003`; later O01/O06 success findings `LWB-OVR-013/014` remain credited within scope, but review 16 reopens O01/O06 edge-case closure.

### Retained earlier resource/PM15 checkpoint — not the active assignment


Last updated: 2026-09-11

Project-manager review 14 audited `7ca6d5c`. Saved browsing, feedback, lifecycle and proof-correlation repairs are accepted at their documented scopes. The resource implementation is a test candidate; fresh normal-window completion remains unverified and subject to SB-97. [Current audit](docs/lwbridge-project-status.md) and [test checklist](docs/user-test-checklist.md) define the next deliverable.

## Review 15 — current queue at `154ce35`

- [x] PM audited LWB-PM13-009 source and original owner files; no-context interpretation accepted, no owner repeat.
- [x] **WEB / PM15-01 / P1:** implementation `1e9af6e` isolates each app launch in a collector-generated session directory, binds evidence to the exact launched PID/session start/end, and requires session-two Search/result/render proof; prior-session evidence cannot satisfy reopen.
- [x] **WEB / PM15-02 / P2:** implementation `1e9af6e` preserves process observation `ok/error/diagnostic`, blocks unknown preflight state, and makes failed app/UI/process/runtime/store evidence INCOMPLETE instead of clean/success.
- [ ] **PM:** audit `LWB-PM15-001`, the seven isolated regressions, exact package identity and final CI; no owner task is requested.
- **OWNER:** no task now. **SOL/DAYBREAK:** no assignment. Fresh acquisition remains SB-97 blocked.

[Detailed review and exit criteria](docs/reviews/2026-09-11-review-15-owner-evidence.md). Historical checked repairs below retain their stated scope; recorder future-use readiness is withheld by PM15-01/02.

## Previous owner-package checkpoints — 2026-09-11

- [x] **WEB:** prepared build/test packet at `9c20896`; preparation only.
- [x] **HISTORICAL ATTEMPT/DIAGNOSIS:** `ccb74b0` / `537a5b9` record native navigation, upstream rejection before dispatch, no Start sent and completed saved-log diagnosis. Reviewer-specific cause remains unknown; live gate stays open.
- [x] **WEB:** `LWB-PM13-008` implements and verifies passive automatic technical capture for the permitted saved Resource Search/reopen scope, including the actual normal `map_search` request/result, passive render correlation, profile/store/process/recovery/current-client evidence and cleanup checks. Repo-root `Start Owner Resource Check.cmd` is the tested entry point.
- [x] **WEB:** completed the beginner-friendly [owner guide](docs/user-test-checklist.md) with one UI action per step, English/Chinese labels, screenshot checkpoints, empty-result/reopen branching and first-failure stop guidance. No command/log/database work is assigned to the owner.
- [x] **PM review:** `LWB-PM13-008` was independently re-audited on 2026-09-11. The owner-evidence gate blocks state-changing commands before backend dispatch, the collector preserves the SB-97 boundary, and a stale duplicate source-hash section in the durable evidence was corrected. **Approved only for the passive saved Search/reopen owner check; fresh acquisition remains blocked.**
- [x] **OWNER:** completed the first permitted passive check in attempt `20260911T085114Z-eb1cd35e-7f21ad24`. The normal page reported no saved map server; no game/scan action ran. The original collector returned a false `INCOMPLETE` because the frontend intentionally suppresses `map_search` when no saved server exists.
- [x] **WEB:** `LWB-PM13-009` diagnoses that no-context branch, preserves the original failed recorder bundle, and repairs passive collection to record normal `map_summary` `MAP_SAVED_CONTEXT_UNAVAILABLE` plus empty published-server state as `COMPLETE_NO_SAVED_CONTEXT`. The owner guide now branches before Search. No owner repeat is required for the current empty profile.
- [x] **PM / review 15:** audited LWB-PM13-009 and original owner evidence. Accepted no-context interpretation; returned the wider collector for PM15-01/02 above. No fresh context was created.
- **SOL:** no separate active role. **DAYBREAK:** no assignment.

[Current workflow/prompts](docs/team-workflow.md) now govern the shared Manual Scan engine. The resource notes below are retained as historical review-14 evidence and do not override the current sequence. Do not send blocked actions to the owner or another executor.

## Historical review-14 resource function - superseded by shared-engine priority

The review-14 instruction below previously treated Resource as the one active function. It is retained for provenance only. The 2026-09-13 owner direction above supersedes its sequencing: shared Manual Scan lifecycle first, with Player City as the first common-engine acceptance category. SB-97 still applies to the denied Resource operation.

- [x] **PM13-01:** existing source-backed saved resource now reopens through ordinary Resource Search with saved/live distinction and profile/server scope preserved. `LWB-PM13-001` implements the unambiguous saved-profile context; `LWB-PM13-001B` rediscovered the exact review-13 store in the Codex package-local cache, verified byte-identical normal-window search across an app restart, and restored the direct-desktop test root afterward. No new acquisition was claimed.
- [x] **PM13-01b:** `LWB-PM13-002` preserves structured Start errors, adds nine-language Resource-only guidance, distinguishes empty/missing-context/query failures, and keeps unsupported categories fail-closed without silently discarding them. Connected normal-window revalidation remains part of PM13-04.
- [x] **PM13-03:** `LWB-PM13-003` separates validation from store mutation, adds an atomic `reading -> committing` completion/cancel decision, and reserves helper cleanup ownership before process start. Deterministic Stop/Close boundary tests pass; final PM12-B lifecycle closure is restored for this bounded route.
- [x] **PM13-02:** `LWB-PM13-004` rejects empty/loading/unrelated-stale and same-point-stale rows, explicitly clicks normal Resource Search after each acquisition, and binds immutable result -> native request/query -> exact returned row -> rendered five-cell row including the acquisition timestamp. `LWB-PM13-006` corrects the real coordinate-button cell (`481,32` plus visible `Jump`) so valid rows are not falsely rejected, and adds a repeatable ordinary-window saved-reopen verifier. Screenshot directories are prepared before either PNG write.
- [ ] **PM13-04 / PM12-D — ACTIVE/BLOCKED:** `LWB-PM13-005` revalidates the normal rebuilt window for unsupported all-category Start and missing saved context with clear current messages, no category fallback, no game/helper launch, and exact direct-root cleanup. The required fresh resource-only two-read/Search/render operation remains unexecuted: `LWB-PM13-006` supplies the follow-on saved-reopen verifier, and `LWB-PM13-007` preserves the Computer Use/session-gap diagnosis. `LWB-PM13-008` supplied the passive owner package; `LWB-PM13-009` records the real owner no-saved-context outcome and fixes its false-INCOMPLETE collector branch. SB-97 is still not cleared; fresh acquisition remains unexecuted and must not be rerouted through Remote Desktop Commander, scripts, the owner, ordinary Node, WebView automation or a helper protocol.
- [ ] **NEXT FUNCTION / not started:** monster acquisition -> persisted index -> ordinary Monster Search with real positive/negative query proof. Regular AI owns research and implementation; no Daybreak assignment.

Reference SHA-256: `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`

## Historical delivery priority - review 9, superseded

The following sequence produced the bounded Resource/Player City foundations but is no longer the active delivery order. The 2026-09-13 owner direction at the top of this file now requires the shared Manual Scan engine first. Preserve the historical evidence and SB-97 restriction; do not use this old "Resource then Monster" order to override the current plan.

1. Historical R5 connection work established the bounded current-game route.
2. Historical R6/R7 work acquired/displayed real Resource and Player City rows with source-correlated proof.
3. The old one-category-at-a-time continuation is superseded by common-engine implementation and then per-kind enablement on that engine.

`docs/first-live-result.md` remains historical evidence for the bounded demonstration, not the current sequencing authority.

## Project rules

**Mandatory on every task/checkpoint, not one-time checkboxes:** follow [AGENTS.md](AGENTS.md). Prioritize reverse-engineering verified LWBridge and current official Last War artifacts; never invent production facts or numbers. Immediately document every successful recovery with source/hash/locator/reproduction/limits. Update evidence and progress, run applicable checks, then commit and push the completed task/checkpoint to the verified GitHub branch and confirm the remote revision. Report actual blockers rather than silently skipping these requirements.

**Tool permission is already granted:** discover/install/configure useful reverse-engineering tools and runtimes as needed. Missing integration is a setup problem to solve, not a reason to guess or stop. Record tool versions/locations/invocations. Attribute real environment denials to the rejecting system and its stated reason, not to the user; continue permitted independent work.

- [x] LWBridge is the only feature authority for this repository.
- [x] Active login/account/license UI is excluded from the independent rebuild.
- [x] Original recovered frontend assets are persisted with an integrity manifest.
- [x] Fixture/capture mode is isolated from live game actions.
- Keep RECOVERED, IMPLEMENTED/OFFLINE-TESTED, LIVE-PROVEN, UNKNOWN/BLOCKED and explicitly labelled IMPLEMENTATION POLICY distinct in every feature record.
- Document each newly confirmed finding immediately; no recovery/task checkpoint is complete without its durable evidence and GitHub delivery or an explicitly recorded delivery blocker.

## Ownership and escalation — review 9 outcomes

**Regular AI first, including permitted binary analysis.** DB-01–06 are topic labels, not model assignments. Only request Daybreak when relevant permitted methods have been exhausted and recorded with outputs/alternatives/reasons under an ESC ID; the PM reviews the bounded scope. See AGENTS.md section 6. A denial or missing integration alone does not establish a specialist assignment.

- [x] Integrate Daybreak R6-006/007 and regular-AI R6-008–013 in the shared branch; no cherry-pick required.
- [x] PM5-01: correct old boolean inspector's railway/reindeer metadata and identify R6-010 as the saved frontend authority.
- [x] Regular AI: persisted quality regressions now cover railway/dispatch/ghost UR rows with `isSpecialURQuality=true`, a non-special truck quality above five, truck UR page/count consistency, and server isolation. Railway also combines recovered UR + retained-item predicates.
- [ ] Regular AI: complete ESC-002/004/005 method/alternative packets or resolve their questions. ESC-001 is CLOSED. ESC-003 is NOT_ASSIGNED after PM review; reopen only with a specific permissible approach or new evidence/target. No specialist is assigned.
- [ ] PM: review each completed packet, record the reason and approve only the specific permitted question; return insufficient requests with concrete missing evidence.
- [ ] Regular AI: integrate/validate each accepted specialist return; retain all live-proof gates.

All existing DEEP-BINARY/ARTIFACT-REVIEW labels below describe the evidence method needed; ordinary ownership remains with the regular AI until an ESC is approved. Continue independent work while any request is pending.

- [x] Review 6 accepts R6-014 clock/time search and R6-023 treasure option keys; close those specific research gaps.
- [x] Verify previously delayed delivery: GitHub contains `3138fe9`; SB-10 is historical.
- [x] **PM6-01 / regular AI:** `LWB-PM6-001` replaces unbounded host-probe cleanup waits with a bounded owned-process cleanup helper, preserves termination errors, and adds a deterministic termination-failure self-test to CI. Both the self-test and normal isolated host probe pass; this is test-orchestration hygiene only, not a live feature. See `evidence/lwbridge-implementation/2026-09-09-pm6-host-cleanup.json`.
- [x] Review 7 accepts R6-024–037 with static/offline limits; source/run, no-alliance and summary flow subquestions are recovered. The 47 full acceptance cases remain unsigned.
- [x] PM7-01/03: remove stale current-status claims, archive review 6/follow-ups and replace the overgrown handoff with bounded tasks. One `task.md` remains authoritative.
- [x] **PM7-A / regular AI:** `LWB-R6-038` integrates recovered published/staged option/count scopes in one service, with exact server/run isolation and one-snapshot checks. Public `map_data_options` remains gated on exact progress serialization and authoritative state/provider prerequisites.
- [ ] **PM7-B / regular AI:** recover bridge-handler linkage and complete authoritative readiness/error semantics; integrate only the supported summary provider. `LWB-R6-040` closes the host parsed-`packageKeyEnvelope` → `package-key.envelope` persistence/cleanup gap. `LWB-R6-041` narrows the host auth/device-key plus proxy CNG/BCrypt/file-loader boundary; `LWB-R6-042` preserves the exact proxy `DEVICE_KEY_PROVIDER/EXPORT/FORMAT/MISSING` labels; `LWB-R6-043` corrects their interpretation and recovers the secure proxy's hardcoded Microsoft Software Key Storage Provider, persisted GUID key name, `ECCPUBLICBLOB`/65-byte public format, `TRUNCATE` 32-byte derivation and AES-GCM 32/12/16-byte input contract. `LWB-R6-044` closes the shared producer's `serverIdSource` live/none fallbacks and active-read server-change guard. `LWB-R6-045` source-attributes secure-proxy package validation/decrypt function `RVA 0x3D260-0x3DCF5` as a direct consumer of the R6-043 AES helper and pins its `LWBP2|` plus package integrity/build diagnostics. `LWB-R6-046` recovers exact `\bridge-runtime\package-key.envelope` construction, its `0x1000`-byte bounded reader with trailing CR/LF trimming, and direct opaque path-consumer call `0x1C8E2 -> 0x3F8E0`. The exact envelope field grammar/agreement ownership, package-file caller and key/nonce/tag callsite ownership, host create/finalize provisioning dataflow, plain-proxy parity, protected handler plaintext, dispatcher failures and separate `map scan state is unavailable` emitter remain open.
- [ ] **PM7-C / regular AI:** native identity/normalization and smallest scan path, alongside R5 launch/handshake prerequisites; `LWB-R6-049` recovers the original normalized `record_key` formula and its write into the R6-047 `+0x78` slot. `LWB-R6-050` pins current `PointInfo.pointIndex/mainIndex/uuid`, their generated xLua backing tokens, constructor stores, and `ResPointInfo.gatherMarchUuid`/resource getters. `LWB-R6-051` additionally pins the original normalized `level`, `quality`, `power`, distance fallback, and shield/protect fallback field sources. `LWB-R7-001` adds a bounded current-game acquisition/display checkpoint: an instrumented v14 run captured one real server-2212 resource row and isolated `--first-live-result` displays it through the rebuilt store/query/UI without guessing resource-name or gather state. Production capture/serializer ownership, bridge/session connection, remaining resource normalization/removal and live scan lifecycle remain open.
- [ ] **PM7-D / regular AI / DEFERRED:** remaining schema/export/sort evidence and specific ESC-002/004 packets after the fresh app-initiated result, unless a named prerequisite directly blocks it.

## Prior review-12 repair ledger — current overrides in review 13 above

- [x] **PM route decision:** accept R7-003's two recorded current-client acquisitions and backend ingestion as a bounded PM10-01 slice. Full milestone and full-case acceptance stay open.
- [x] **PM12-A:** `LWB-PM12-001` fixes and isolated-tests partial-install rollback, exact backup/recovery state, interrupted recovery, dual-error preservation and cross-process helper ownership. `LWB-PM12-003` implements/offline-tests exact selected-installation path/PID targeting and removes broad image-name termination. `LWB-PM12-005` live-proved the locked-file close dependency. `LWB-PM12-006` now implements the separately observed exact-PID `Process.CloseMainWindow()` primitive as explicit rebuild policy, revalidates selected path/PID immediately before close, has no force-kill fallback, and live-proves two helper-owned closes followed by exact three-file restoration. Post-run hashes match the originals and no game/launcher/helper process remains. This closes PM12-A for the bounded current-client resource route; it does not establish original LWBridge process semantics or persistent bridge readiness.
- [x] **PM12-B / closed by PM13-03:** `LWB-PM12-002` retains duplicate Start, Stop/cancel/close and supervised-helper behavior; `LWB-PM13-003` closes the review-13 gaps with prepare-before-commit, an atomic completion/cancel decision, and pre-start helper cleanup ownership. Deterministic pre-commit Stop/Close, post-decision Stop and process-start/Close races pass. This remains bounded-route lifecycle evidence, not a full live scan claim.
- [x] **PM12-C:** `LWB-PM12-002` closes immutable same-bytes validation/import, request-owned output, request/schema/route/source/time checks, current-client build/restoration evidence and established-server continuity. `LWB-PM12-003` adds the helper-owned launchSessionId + selected profileId + exact selected-game PID/path chain and removes loaded-probe reuse. `LWB-PM12-005` live-proves that tuple at the source boundary. `LWB-PM12-006` completes the missing post-restore path twice: distinct request/session/PID tuples restore exact originals and return through desktop validation/import, with normal `map_search` returning one bounded resource row after each acquisition. This closes PM12-C for the bounded current-client resource route; unsupported fields and broader scan/session contracts remain gated.
- [ ] **PM12-D:** `LWB-PM12-004` implements/offline-tests truthful unknown occupancy. `LWB-PM12-007` then pins current `ResPointInfo.gatherMarchUuid/gatherUid` to the recovered Idle/Gathering rule and live-proves two final-byte bounded reads with both fields readable and empty/zero, importing point `32482` as source-backed known idle after exact restoration. `LWB-PM12-008` adds an offline-validated diagnostic path that drives the normal manual Map Data controls, performs two resource-only Start Reading actions, requires two distinct fresh acquisitions, and is ready to capture rendered row text plus two screenshots. The prior live launch was rejected before execution, so a live Gathering observation, resource-name mapping, and the normal-window Start Scan/display/second-refresh screenshot/interaction proof remain open. PM13-02 finds its rendered-row predicate insufficient. Native sky works for PM, while SB-97 remains a separate live-operation restriction; PM13-04 stays open.
- [ ] **PM10-01 overall:** finish A–D and demonstrate the fresh row in the real UI; do not equate backend proof or two restarts with persistent bridge readiness.
- [x] **PM10-03:** isolated importer/gate tests implemented; these do not cover PM12-A–C.
- [x] **R5-006/007, R7-001/002/003:** credit only recorded naming/framing/hello, replay and bounded fresh acquisition slices. PM10-02 visual proof remains pending under PM12-D.
- [x] **PM8-0 / PM7-A:** user-operated official recovery verified; bounded count/option service implemented. Do not repeat either.
- [ ] **ESC-005 original-pipe parity / NOT_ASSIGNED:** reviewed and deferred; reopen for a concrete feature dependency and permitted scope. No Daybreak assignment.
- [ ] **PM9-A / PM7-D:** updater, broad export/schema/sort work deferred while this user-visible milestone is open.

The regular AI may continue the ordered supported work after each committed/pushed checkpoint. The review-12 route decision supersedes earlier 'wait for PM' instructions. Keep source evidence and all 47 acceptance cases intact.

## Completed foundation

- [x] Recover and reproduce the original React/Vite feature UI in WebView2.
- [x] Preserve eight normal pages and the explicitly requested hidden Advanced view.
- [x] Preserve themes, icons, navigation, and nine languages.
- [x] Add real WebView2 JavaScript-to-C# RPC with profile injection/filtering, request/session IDs, origin checks, structured errors and cooperative timeout/cancel/event plumbing. R3/R4 real-host lifetime and interaction verification now pass; real worker integration remains below.
- [x] Add local profile/configuration persistence; PM2 failure handling and PM3 feedback/reconciliation pass isolated regressions. Real lifecycle integration remains open.
- [x] Detect the current installation and check required files/AMD64/PE32+. The exact xLua ABI selector is recovered; current-runtime lifecycle validation remains R5.
- [x] Keep unmanaged game processes distinct from a verified LWBridge-owned instance.
- [x] Recover Map Scan selected-type allowlist and `normal=8` / `fast=20` request concurrency.
- [x] Add a read-only official-runtime inspector and machine-readable current-install evidence.
- [x] Keep launch and production scanning fail-closed until bridge readiness is proven.
- [x] Complete tracked legacy source/test/tool/document cleanup and retain current LWBridge evidence/references. Do not restore old implementation.

## R1–R4 — Preserve the foundation; complete worker integration (P0)

### Review 3 preference follow-up — completed

- [x] **PM3-01 / R2/R4:** rejected saves are visible in login-free single-profile mode through the existing `profile-error` visual language, and native-host `ok` now requires visible non-empty error feedback.
- [x] **PM3-02 / R2/R4:** optimistic saves reconcile against the last confirmed persisted value. Both-fail, fail/success, success/fail, all-success and recovery cases pass in the deterministic provider matrix; both-fail/mixed/recovery cases also pass in the real isolated WebView host.
- [x] Add recurring CI coverage for the isolated host interaction matrix and new preference assertions. The three current-build recovery inspectors remain compile-checked only because their runtime validation depends on reference/current-install artifacts that are not part of game-independent CI.
- [x] **IMPLEMENTATION / test hygiene:** `tools/check_lwbridge_host_probe.ps1` now launches hidden, quotes its path-valued output argument, uses the review-4 55-second orchestration bound as explicit IMPLEMENTATION POLICY, fails on timeout/nonzero exit, and cleans up only the process it started. The isolated configuration and existing required gates are unchanged.

### Resolved PM2 defects — preserve their regression tests

- [x] **PM2-01 / R2:** `SetLocalConfig` applies validated partial fields to the locked fresh baseline; deterministic backend tests preserve another owner's reconnect/root/history fields and profile identity.
- [x] **PM2-02 / R2:** missing primary recovers a valid owned backup and stable profile identity; incompatible backup and unreadable-storage cases fail closed instead of creating a fresh identity.
- [x] **PM2-03 / R2:** owner/schema incompatibility is not treated as corruption; foreign-owner and future-schema primaries remain byte-for-byte unchanged with and without valid backups.
- [x] **PM2-04 / R3:** cancellation-source disposal is owned by request completion; noncooperative return/fault after close or cancel resolves as cancelled, cooperative cancellation still passes, and cancel/completion races drain ownership without teardown exceptions.

Run the [independent reproducer](evidence/lwbridge-implementation/pm-review-2-repro/Program.cs) together with the standard suite. The original review evidence records four historical failures; the current source now reports four `passed:true` outcomes and the corrected expectations are in the standard deterministic checks.

### Existing foundation and remaining integration

- [x] **R1 / profile contracts:** restore original implicit active-profile injection and event-envelope filtering in the generated API/native adapter; classify global commands; validate malformed payloads and profile scope consistently.
- [x] Test implicit/explicit/foreign profiles, malformed input, wrong-session responses and wrong-profile/late events through the frontend/native boundary.
- [x] **R2 implemented slice:** durable write-before-memory, isolated storage, backup/owner/schema handling and the specified PM2 recovery cases pass. PM3 rejected-save feedback and failed-overlap reconciliation now pass in provider and native-host tests.
- [x] Single-save rollback, persistent validated server history, visible save errors, rapid failed-save reconciliation and successful recovery work in isolated tests.
- [x] Inject temporary config roots for checks/capture and prove real user configuration is unchanged. Test denied writes, restart, corrupt JSON, interrupted replacement and competing writers.
- [x] **R3 implemented slice:** shared executor/registry and allowlisted subscription ownership pass cooperative delayed-service, duplicate-ID and window-close checks. The native-host follow-up also offloads backend work and gives each WebView document its own request/subscription generation.
- [x] Test browser timeout propagation plus host explicit cancel/close/reload/duplicate IDs/late completion with a controllable delayed backend service; prove cancellation prevents the service commit and success publication.
- [ ] Connect the request/event lifetime foundation to the first real lifecycle/scan service and add durable correlated diagnostics when that service exists; `append_log` remains a no-op today.
- [x] Move potentially blocking config/installation/SQLite work off the UI thread with deliberate ownership/cancellation. The isolated native WebView probe holds the real config lock for ~868 ms while a browser timer fires at ~56 ms, proving rendering remains responsive; folder selection remains UI-owned while root inspection/save run off-thread.
- [x] Implement/test real document reload/navigation invalidation: a real WebView2 reload cancels the prior request, clears prior subscriptions, rotates the document session/generation and rejects an invoke carrying the old session ID.
- [x] **R4 / verification:** split deterministic tests from installed-game diagnostics; run deterministic backend/transport tests in CI.
- [x] Requested live mode rejects missing native transport in the Node harness; read-only bootstrap suppresses auto-launch and uses isolated storage.
- [x] Verified R4 slice: actual native-host duplicate/session/reload/slow-storage/picker/close/startup handling, visible single failed-save rollback, ordered successful saves, both-fail/mixed-failure reconciliation and post-failure recovery. The host `ok` gate now includes these preference requirements.
- [x] Check AMD64 COFF machine plus PE32+ for game/xLua; include official-runtime inspector syntax checks. Exact xLua ABI selection is now recovered by `LWB-R5-002`; lifecycle integration remains R5.

## R5 — Overview lifecycle (P0 critical path; research alongside R1–R4)

- [x] Recover the host-side producer for `LaunchEnvelope` (`descriptorJson`, `launchProof`, `gameLaunchTicket`). `LWB-R5-001` locates the outer-host state machine, exact field xrefs/value sources, JSON serialization and handoff clone. This is static recovery only; proof/ticket semantics remain below.
- [ ] **DEEP-BINARY DB-01/02:** finish the unresolved launch input/ownership/validation semantics below; preserve already recovered representation/validation findings.
  - [x] `LWB-R5-003` recovers the child three-field `LaunchEnvelope` parser, descriptor expiry/shape gates, two-segment `LWPM1|launch|...` proof framing/signature/timing checks, and `LWLT1`/`LWLT2` ticket grammar with 1–300 second span, 32-hex field and 128-hex signature.
  - [x] `LWB-R5-004` recovers outer `LeaseActivationResponse.launchProof` propagation, the exact `primary_official` / `cached_reusable` / `independent_official` ticket-source labels, the `ticket_missing` transition, and cached launcher-report `pid`/ticket/expiry fields before fallback processing.
  - [x] `LWB-R5-005` recovers the child post-start polling branches: observed-length/sequence mismatch -> `LAUNCH_TICKET_OWNERSHIP_CHANGED`, stored-deadline crossing -> `LAUNCH_TICKET_CONSUMPTION_TIMEOUT`, and mapped internal result variants -> `LAUNCH_TICKET_CONSUMPTION_FAILED`. Opaque input identities, time units and exact result-variant meanings remain unresolved.
  - [ ] **DEEP-BINARY DB-01/02:** resolve remaining descriptor semantics, producer/consumer and external trust-service boundaries, `LWLT2` extra-field meaning, poll caller/input identities, units/result variants, unresolved outer-helper semantics and child argument/input construction. Respect SB-01/02; do not fabricate credentials, proofs or signatures to bypass an unresolved dependency.
- [x] Recover xLua secure/plain ABI fingerprint selection exactly. `LWB-R5-002` recovers the `LWXE1\n` prefix, ordinal/name record construction and ordering, SHA-256 digest, bundle values, and exact current-build `secure` match.
- [x] Implement owned `profile_instance_start`, `profile_instance_status`, and `profile_instance_stop` lifecycle for the independent current-v14 route; technical live proof/owner verification are preserved under `LWB-OVL-002/003`.
- [x] Recover the original per-user control-pipe name derivation. `LWB-R5-006` proves prefix + first 16 lowercase SHA-256 hex characters of UTF-8 Windows user SID and live-correlates the result against the verified reference host; session framing/request grammar remain open.
- [x] Require matching profile/session/challenge/PID readiness plus fresh heartbeat before reporting connected on the independent current-v14 route; protected original handshake parity remains separately open under DB-01/02.
- [x] Implement startup launch preference through the same lifecycle service without double-start races (`LWB-OVL-004` technical live proof; owner-visible combined O04/O05 acceptance passed under `LWB-OVR-011`).
- [x] Implement automatic reconnect/recovery with explicit eligibility, cancellation, and bounded retry behavior. `LWB-OVR-008/009` cover offline recovery/event/update behavior, `LWB-OVR-010` technically live-proves the bounded recovery cycle, and `LWB-OVR-011` records owner-visible startup/reconnect/intentional-Close acceptance plus the corrected startup-reconcile UI timeout.
- [x] Recover and implement repair/update/restart presentation and state transitions (`LWB-OVR-012` original contract; `LWB-OVR-014` production live proof).
- [ ] **EXTENDED PARITY/STRESS VALIDATION — not an O01-O06 functional blocker:** event-driven disconnect/update families retain their documented offline-tested scope; process-exit restart, app restart/repair, startup, normal Close and non-resurrection are live-proven/owner-accepted. Preserve this line for future broader 47-case release validation.

## R6 — Offline map contracts, index and result services (P0 parallel work)

This work can advance while R5 semantic research remains incomplete or a particular analysis operation is restricted. Static/offline proof does not replace later current-client proof.

- [ ] Trace every Map Data payload/result/conditional control in the original frontend and backend evidence; record unknowns explicitly.
- [x] Recover the original SQLite map-index tables/indexes, stored record identity `(kind,server_id,record_key)`, scan staging identity `(run_id,kind,server_id,record_key)`, player-mark identity `(server_id,owner_uid)`, server-clear scope and completed-kind publish transaction.
- [ ] **DEEP-BINARY DB-03:** integrate the recovered R6-049 native `record_key` rule only after current capture/typed-input mapping is established; recover remaining normalization/update/removal rules for city, resource, monster, truck, railway, dispatch, ghost and treasure records.
  - [x] `LWB-R6-029` exhausts the currently tracked official-runtime readable snapshot for direct `record_key`/scan-bridge and known row-vocabulary clues: no current-runtime text hits were present. The build-1078 `Assembly-CSharp.rdl` and active `LWScripts.data` are identified as the next concrete current-contract sources; derivation remains UNKNOWN/BLOCKED.
  - [x] `LWB-R6-047` recovers the original native map-record upsert boundary at RVA `0x27A1E3-0x27AA73`: exact 12-byte `scan_records` selects staging, staging prepends `run_id`, and the shared record layout binds `record_key` from offset `+0x78` with the same 14 record fields used by `map_records`. This narrows the upstream producer target but does not recover any per-kind key derivation or enable production ingestion.
  - [x] `LWB-R6-048` recovers the current build-1078 world-point identity surface for one resource path: protobuf world points carry separate `Id`/`Uuid`/server/world fields, `GetWorldTileInfo(int32 pointIndex)` and `GetPointInfoByUuid(int64 uuid)` are distinct lookup paths, and `GetResourcePointInfoByIndex(int32 pointIndex)` reaches `ResPointInfo`. Neither current-client identity is promoted to native `record_key`; the direct producer into R6-047 `+0x78` is still UNKNOWN/BLOCKED.
  - [x] `LWB-R6-049` recovers the original normalized `record_key` formula upstream of R6-047: first-present `pointIndex/mainIndex/pointId/index` is used as signed-decimal identity for ordinary indexed records; exact truck/railway, any record carrying nonempty `marchUuid`, or a record without a point index uses nonempty `uuid` else `marchUuid`. The builder writes that String at `+0x78/+0x88`; current-client capture-to-builder mapping and remaining typed normalization/update/removal are still UNKNOWN/BLOCKED.
- [ ] **IMPLEMENTATION:** implement transactional profile/server/run storage, consistent count/page snapshots, schema versioning/migration and checkpoint/generation infrastructure. Actual job eligibility and production run publication must use recovered contracts.
- [x] Add the recovered SQLite schema/index foundation with deterministic restart/upsert/server-scope tests; require an explicit already-derived `record_key` rather than guessing ingestion identity.
- [x] `LWB-R6-019` adds test-only transactional staging/publication infrastructure for the recovered completed-kind delete/copy scope and proves selected-run/kind/server isolation. It does not enable production publication or invent the unresolved completion/native-key gate.
- [x] `LWB-R6-020` proves the test-only publication transaction rolls back completely when failure is injected after the recovered delete and before staged copy; previous published rows remain intact. Production failure classification/retry and completion eligibility stay unrecovered.
- [x] `LWB-R6-028` adds restart-safe test-only persistence over the recovered `scan_blocks(run_id,block_index)` identity, proves same-key checkpoint replacement, file-backed reopen durability and `scan_runs` clear-cascade behavior. Original scheduling/ack/retry/status transitions and production resume eligibility remain UNKNOWN/BLOCKED.
- [x] `LWB-R6-011` recovers the original `schema_version` metadata read/upsert, `MAP_SCHEMA_TOO_NEW` error vocabulary, dispatch-assist migration SQL and `legacy_import_completed` marker. Schema migration code remains gated because the supported version constant, migration threshold/order and metadata timestamp unit are still unrecovered.
- [x] Implement persistent player mark/unmark and server-scoped clear using recovered identities/SQL scope; emit the recovered player-mark refresh event.
- [x] Implement the `LWB-R6-003` default persisted `map_search` slice: recovered pagination, `updatedAt` ordering with `record_key ASC`, `{rows,total}`, city marks/`markedOnly`, and fail-closed `MAP_QUERY_UNRECOVERED` for unsupported filters/sorts.
- [ ] Extend the existing default search with recovered remaining predicates/sorts, options and counts/summary; test actual frontend payloads for all eight tabs plus stale-query rejection. Do not reimplement completed LIMIT/OFFSET/updatedAt/markedOnly support.
- [x] `LWB-R6-004` pins all eight serialized frontend tab query families, explicit-false/zero/empty/omitted distinctions, `map_data_options`/`map_summary` consumed response fields, and frontend generation guards for stale search/options/summary responses; deterministic query-contract checks pass.
  - [x] `LWB-R6-005` recovers and implements exact backend predicates for city alliance/no-alliance, resource/monster name keys, and truck/railway retained-item membership from the verified LWBridge binary; deterministic persisted-search checks pass. Unrecovered filters/sorts remain fail-closed.
  - [x] `LWB-R6-006` recovers and implements the exact dispatch/ghost `specialOnly` and `reindeerOnly` JSON predicates from unique byte-adjacent field/SQL pairs in the verified LWBridge binary. Its earlier railway frontend-kind allowance is superseded by `LWB-R6-010`; the recovered visible selector emits `reindeerOnly` only for truck.
  - [x] `LWB-R6-007` recovers and implements the keyword predicate over `name`/`alliance_name`/`uuid`/`data_json`, exact backslash-percent-underscore escape order, literal `%...%` wrapping and four parameter copies; literal wildcard and case-insensitive deterministic searches pass.
  - [x] `LWB-R6-008` applies the documented **IMPLEMENTATION POLICY** that one `map_search` `{rows,total}` response comes from one SQLite read snapshot; a deterministic second-connection WAL writer proves count/page consistency while the concurrent write still commits.
  - [x] `LWB-R6-009` recovers the Map Data option SQL family/latest scan summary and implements the exact treasure option pair plus dispatch selected-level predicates. Reward-option `arriveTs` cutoff time semantics, quality/completion/plunder/treasure visibility and remaining sort mappings stay fail-closed.
  - [x] `LWB-R6-010` audits the original quality selector, narrows `reindeerOnly` to the truck-only visible frontend form, and preserves ordinary quality as fail-closed because `UR` renders every numeric quality `>=5` while the original filter predicate/range is still unrecovered.
  - [x] `LWB-R6-012` records byte-adjacent ordinary-quality/special-UR evidence; `LWB-R6-013` refines its scope to truck+ordinary-UR, recovers the exact frontend string mapping `n/r/sr/ssr -> 1/2/3/4`, `ur -> quality >= 5`, and implements/tests those forms while retaining unknown/numeric public forms fail-closed.
  - [x] `LWB-R6-014` recovers the shared precise wall-clock producer as `kernel32!GetSystemTimePreciseAsFileTime` converted to Unix milliseconds, the default truck/railway active-arrival cutoff, frontend-emitted truck/railway/dispatch `plunderableOnly`, dispatch/ghost `completionStatus`, and the reward-option `arriveTs` cutoff source/unit. The recovered public search forms are implemented; options still require source/run-context and complete response assembly.
  - [x] `LWB-R6-015` adds an offline/test-only persisted `map_records` option-aggregation kernel for the recovered R6-009 SQL families plus R6-014 reward cutoff. Deterministic tests cover grouping/order/deduplication, server isolation and newest non-discarded scan progress. This is explicit IMPLEMENTATION POLICY; public `map_data_options` remains fail-closed until the original source/run selector and complete counts/no-alliance/response assembly are recovered.
  - [x] `LWB-R6-016` pins the frontend count/no-alliance/summary consumer contract: exact eight kind count keys, same-server summary seeding/replacement, options-to-summary count propagation, zero-count clear reset and the `noAllianceCount > 0` filter-retention rule. Backend count/no-alliance aggregation and authoritative summary server selection remain unrecovered.
  - [x] `LWB-R6-021` extends the persisted-only R6-015 test kernel with all eight recovered frontend count keys, zero-fill for absent kinds and a server-scoped no-alliance value using the recovered null/empty city predicate. This is IMPLEMENTATION POLICY only; public source/run selection and native count assembly remain unrecovered.
  - [x] `LWB-R6-022` recovers the native `treasure:` / `supplies:` option-key prefixes and documents that direct RIP xrefs plus absolute-pointer metadata-slot tracing do not reach the formatter. Its suffix gap is superseded by `LWB-R6-023`.
  - [x] `LWB-R6-023` traces the already identified native option-assembly function and signed-decimal formatter to recover exact keys `treasure:<decimal treasureType>` and `supplies:<decimal suppliesType>`. The persisted-only R6-015 helper now emits those keys and deterministic tests cover multi-digit formatting. Public `map_data_options` remains gated on source/run selection and complete native response assembly.
  - [x] `LWB-R6-024` recovers the exact native option-source/scope candidates from the verified `map_index.rs` string cluster: `map_records` + `server_id=?1`, or `scan_records` + `server_id=?1 AND run_id=?2`. Frontend options provide only `serverId`; the native kind-count query is also source/scope-parameterized. This completes a concrete ESC-003 method packet but does not recover the selector condition, chosen run identity, exact `noAllianceCount` producer or full response assembly; public `map_data_options` remains fail-closed.
  - [x] `LWB-R6-025` recovers the native `map_summary` success envelope and data flow: the shared scan-state producer runs first, `scanState.serverId` becomes both the count-helper server argument and response `serverId`, and the original response assembles exactly `serverId`/`counts`/`scanState`. The count helper selects published versus staging rows from an internal optional run scope whose producer remains UNKNOWN/BLOCKED. The rebuild now fails `map_summary` closed with `MAP_INDEX_UNAVAILABLE` instead of returning a synthetic production success object.
  - [x] `LWB-R6-030` recovers the shared optional run-scope selector and run identity: staging exists only when `isReading=true`, scan-state `serverId` matches the positive requested server and raw `scanRunId` is nonempty; otherwise no run scope is supplied and R6-024's persisted source is selected. An internal helper/tests mirror only this decision. Public `map_data_options`/`map_summary` remain fail-closed while native `noAllianceCount`, complete options response/order/deduplication, exact public `scanProgress` serialization and summary unavailable/error integration remain unresolved.
  - [x] `LWB-R6-031` recovers native `noAllianceCount` directly from the grouped alliance row loop: NULL/empty decoded names are excluded from `alliances[]` and their group `COUNT(*)` values accumulate into no-alliance. It also pins the top-level insertion order and proves `scanProgress` selects latest non-discarded run by server without active scope or exact `scanRunId` with active scope. The persisted test kernel now mirrors the no-alliance branch. Exact scan-progress JSON serialization/empty/error behavior is the remaining public options assembly gap.
  - [x] `LWB-R6-038` completes PM7-A: the persisted-only aggregate kernel is replaced by `SelectOptionSource` + `ReadOptionAggregatesAt`, which carry the recovered published/staged server/run scope through every known option family, all eight counts and scan-progress row selection in one SQLite read snapshot. Conflicting published/staged/two-run/two-server fixtures and a concurrent WAL writer pass. Public options remain `MAP_INDEX_UNAVAILABLE`; exact `scanProgress` serialization and authoritative production state/provider integration remain open.
  - [x] `LWB-R6-032` narrows that serializer gap without inventing fields: the post-query option path uses generic tagged-value conversion at `0x1402AD389`, while the persisted latest-scan query closure identifies `0x140294188` as the concrete row-mapper callback. Broader `map_scan` live-state metadata is explicitly separated from persisted `scanProgress`. Direct callback inspection was rejected by automatic review (SB-33), no active-scan target was running, and exact keys/order/null/error behavior remain blocked; public options stay fail-closed.
  - [x] `LWB-R6-033` recovers `map_summary` command-level failure propagation: shared scan-state errors bypass server extraction/run-scope/count work and count-helper errors bypass response assembly; both flow into the normal command error return rather than fabricating an empty summary. Exact shared-producer error-code/condition mapping and production scan-state/current-server integration remain UNKNOWN/BLOCKED, so public summary stays fail-closed.
  - [x] `LWB-R6-034` reuses the proven read-only RG/RGMD metadata/CIL parser against current build-1078 `Assembly-CSharp.rdl` and recovers xLua-visible `DCPlayer.GetCurServerId()` plus `SceneManager.IsInWorld()`. `GetCurServerId` returns the cross-server field when nonnegative and otherwise a still-unnamed fallback field. This closes the R6-029 decoder setup gap and establishes concrete current-client state contracts, but the authoritative DCPlayer access path, bridge-facing shared producer, exact LWBridge error mapping and live correlation remain UNKNOWN/BLOCKED; public summary stays fail-closed.
  - [x] `LWB-R6-035` closes the current-client managed DCPlayer owner/access path: static `GameEntry.Data` returns `CustomDataManager`, whose `Player` returns `DCPlayer`; generated xLua wrappers expose both getters and `DCPlayer.GetCurServerId()`. Production remains blocked on the LWBridge-facing producer, runtime readiness/null handling, exact error mapping and live correlation.
  - [x] `LWB-R6-036` recovers the shared LWBridge scan-state producer's bridge request order and a bounded normalization slice: `getWorldMapState` precedes `getCurrentServerId`, both use the common dispatcher and recovered 5,000 ms bridge timeout, missing/non-boolean `isInWorld` becomes false, positive `homeServerId` is retained, and season/truck-match server IDs are filtered to 1..99999, deduplicated and sorted. Exact public named-error mapping plus the bridge-command-handler link to R6-035 remain UNKNOWN/BLOCKED.
  - [x] `LWB-R6-037` closes the explicit current-server unavailable mapping in `src\commands\map.rs`: after `getCurrentServerId`, the extraction branch accepts only `serverId > 0`; `serverId <= 0` constructs exact `SERVER_UNAVAILABLE` / `current server id unavailable`. The encrypted bridge-script implementation, propagated transport errors and the separate `map scan state is unavailable` condition remain UNKNOWN/BLOCKED.
  - [x] `LWB-R6-039` recovers a bounded PM7-B package-key/runtime-material seam: the host auth parser extracts `packageKeyEnvelope`/expiry beside the authorization ticket, a separate host runtime-material function constructs `authorization.challenge`/`authorization.ticket`/`package-key.envelope`/`build.manifest`, and both embedded proxies carry the matching key-envelope/package-decrypt vocabulary. The LWBP v2 script package has no `LWKE1` marker. Direct parsed-value-to-file dataflow, proxy read/decrypt dataflow, handler plaintext and live correlation remain UNKNOWN/BLOCKED; public summary/status stay fail-closed.
  - [x] `LWB-R6-040` closes R6-039's direct host persistence gap: the successful package-key branch serializes the parsed envelope as the response string plus LF, passes the recovered runtime path/content to shared persistence helper `0x14023D8FC`, reaches `NtWriteFile`, and finalizes with `MoveFileExW(..., MOVEFILE_REPLACE_EXISTING)`. Invalid/expired material reaches `DeleteFileW` on the same runtime path and returns `KEY_ENVELOPE_INVALID`. Proxy read/decrypt, plaintext handlers, readiness/error mapping and live correlation remain UNKNOWN/BLOCKED.
  - [x] `LWB-R6-041` narrows the remaining protected-loader prerequisite. One verified `src\services\auth.rs` marker cluster ties the MachineGuid fingerprint path, device-key cleanup/errors, `packageKeyEnvelope`, `LWKE1` and bridge-runtime filenames together; the host imports persistent CNG-key plus DPAPI primitives. Both proxies carry matching device-key/envelope/package-decrypt vocabulary and import CNG secret-agreement/derive-key, BCrypt decrypt/hash/signature and file/environment primitives. These are static available-primitives/boundary facts only: exact storage provider/key name, file-open/read/decrypt call graph, crypto parameters and plaintext handlers remain UNKNOWN/BLOCKED.
  - [x] `LWB-R6-042` recovers exact proxy-local `DEVICE_KEY_PROVIDER`, `DEVICE_KEY_EXPORT`, `DEVICE_KEY_FORMAT` and `DEVICE_KEY_MISSING` markers at matching raw offsets in both proxy variants. R6-043 supersedes the earlier interpretation of the first three as configuration names; the offsets/literal-ownership evidence remains valid.
  - [x] `LWB-R6-043` recovers the secure proxy's concrete device-key prerequisites: hardcoded `Microsoft Software Key Storage Provider`, persisted key `{2D337A4D-7E6C-49EF-9486-54F0A00D8A41}`, `ECCPUBLICBLOB` with `ECK1`/32-byte coordinates and 65-byte `0x04 || X || Y` public form, CNG `TRUNCATE` yielding exactly 32 bytes, and AES-GCM requiring a 32-byte key, 12-byte nonce and 16-byte tag. Exact package-key file caller/envelope ownership, host create/finalize linkage, plain-proxy parity and protected handler/readiness mapping remain UNKNOWN/BLOCKED; no public provider is enabled.
  - [x] `LWB-R6-044` recovers shared-producer server fallback semantics: exact `isInWorld=false` can fill a missing/nonpositive state server from positive `homeServerId` with `serverIdSource=live`; the current-server branch writes positive current `serverId` with `live` but raises `current server changed during map scan` when an active read has a different positive server; with no current server, exact `isReading=true` or an already-positive state server is preserved, otherwise the producer writes `serverId=0`, `serverIdSource=none`, `tileWidth=0`, and `tileHeight=0`. Protected handler linkage, dispatcher failure identities and the separate `map scan state is unavailable` emitter remain UNKNOWN/BLOCKED.
  - [x] `LWB-R6-045` recovers a distinct secure-proxy package crypto boundary: unwind function `RVA 0x3D260-0x3DCF5` directly calls AES-GCM helper `RVA 0x3CB60` at `0x3DA4A` and directly references `LWBP2|`, `package integrity invalid`, and `package build mismatch`. This proves a package validation/decrypt consumer of the R6-043 primitive but does not identify the package file caller, package-key envelope reader/parser, agreement caller, or callsite key/nonce/tag ownership.
  - [x] `LWB-R6-046` recovers the secure-proxy package-key path/read boundary without entering the restricted consumer body. Path function `RVA 0x1B300-0x1B4BF` appends exact UTF-16 suffix `\bridge-runtime\package-key.envelope`; loader `RVA 0x1C0A0-0x1D46F` passes it to bounded reader `RVA 0x1BD80-0x1C09F` with exact maximum `0x1000`, whose success path strips trailing CR/LF. A second branch passes the exact path as `RCX` to `RVA 0x3F8E0` and tests `AL`; internal envelope field grammar/agreement ownership remain UNKNOWN/BLOCKED and SB-79 is not replayed.
  - [x] `LWB-R6-026` pins the public per-kind sort columns, the frontend's ordered multi-sort toggle/default behavior, eleven scalar native sort-key parser comparisons, four direct native expression references, and the adjacent sort-expression/null-order string inventory. The item-count SQL text begins at raw `0x00C8A926` after its one-byte `0x56` descriptor at `0x00C8A925`. No production sort is newly enabled: the frontend preserves prior sorts when a new sort is selected, while the complete native ordered multi-sort/null-order assembly plus remainingLootCount/shield/distance/railway-quality data flow are still UNKNOWN/BLOCKED.
  - [x] `LWB-R6-027` recovers the native public sort-direction branch: frontend `asc` selects the 3-byte `ASC` literal, ordinary `desc` selects the 4-byte `DESC` literal, and the exact `distance` branch overrides a selected `desc` back to physical `ASC`. The reason/value transformation behind that distance special case and complete multi-sort/null-order assembly remain UNKNOWN/BLOCKED, so production behavior stays fail-closed.
  - [ ] **ARTIFACT-REVIEW DB-05 + IMPLEMENTATION:** recover the remaining ordered multi-sort/null-order/tie-break assembly, the unresolved alternate-sort mappings/gates, exact public `scanProgress` JSON serialization/empty/error behavior, and the remaining shared scan-state readiness/error mapping plus production summary integration. R6-034/035 provide current-client callables/managed ownership, R6-036 provides native bridge request order/normalization, R6-037 closes the explicit `serverId <= 0 -> SERVER_UNAVAILABLE` branch, R6-044 closes producer-side live/none fallback and active-read mismatch semantics, and R6-039–046 narrow the protected runtime-material/device-key/package-crypto boundary through the secure proxy's exact provider/key/export/KDF/AES-GCM helper contract, one package-side AES consumer, and the package-key path/read boundary. Continue from permitted caller/output semantics around opaque path consumer `RVA 0x3F8E0` or an independent artifact path to parsed-field ownership into agreement helper `RVA 0x3DD00`, then the package-file caller and key/nonce/tag callsite ownership, encrypted bridge-handler mapping, propagated transport errors and the separate `map scan state is unavailable` emitter. Preserve the supported R6-015/016/025/026/030–046 results; escalate only identified opaque calculations and do not re-recover the implemented/proven subset.
- [x] **IMPLEMENTATION + ARTIFACT-REVIEW:** build export infrastructure and recover exact scope/columns/format semantics; implement full filtered Excel export and verify it reopens with correct rows/types/large IDs.
  - [x] `LWB-R6-017` pins the city-only frontend export envelope: current city query builder at page 1/pageSize 200, exact 12 header labels/translation keys, localized sheet/yes/no labels, button eligibility/busy gates and consumed `{canceled,rowCount,path}` result fields. A deterministic backend regression confirms the recovered envelope still stops at `MAP_INDEX_UNAVAILABLE`; pageSize 200 is not treated as a proven export row limit.
  - [x] `LWB-R6-018` recovers a bounded native-format slice: sheet/default filename/dialog/error vocabulary, embedded A-L OOXML package, frozen header, column widths, autofilter/margins, styles and partial D-L field adjacency. It deliberately does not infer exact A-C mapping, protect/shield fallback or per-column types.
  - [x] `LWB-R7-057` builds **internal-only** export infrastructure without enabling `map_city_export`: one SQLite snapshot returns the full filtered/sorted City set rather than the frontend's first 200 rows; the OOXML writer reproduces the recovered six-part package/layout/style table; deterministic reopen proves 235 data rows, exact large-ID text, formula-safe strings, date cells and mark labels. Full-filter scope and per-column coercion are explicitly IMPLEMENTATION POLICY where original host behavior is still unrecovered.
  - [x] `LWB-R7-058` recovers the exact original default filename contract from hash-locked Rust format bytecode plus the bounded picker construction: `map-cities-{serverId}-{YYYY}{MM}{DD}-{HH}{mm}{ss}.xlsx`. The six timestamp fields are zero-padded widths `4/2/2/2/2/2`, use UTC from `kernel32!GetSystemTimePreciseAsFileTime`, and are corroborated by the identical `lwbridge-feedback-` timestamp suffix. One later register-filter inspection was rejected as SB-99 and was not replayed or used as evidence.
  - [x] `LWB-R7-059` recovers the original Windows save-dialog contract. LWBridge leaves `starting_directory` and custom title unset, applies filter `Excel workbook` / `xlsx`, and calls rfd 0.16 `save_file()`. Therefore the Common Item Dialog owns the remembered/default folder; existing files use the default `FOS_OVERWRITEPROMPT`; user cancel and any rfd build/show/get-result error collapse to `None`, which LWBridge serializes exactly as `{canceled:true,path:"",rowCount:0}`. `Excel workbook` is refined from a generic dialog label to the filter display name.
  - [x] `LWB-R7-060` historically closed the remaining original writer contract and enabled public City export; **superseded by owner removal `LWB-R7-066`**. The original host paginates the full filtered City query at pageSize 200 through page 1000 (200,000-row ceiling, then `MAP_EXPORT_FAILED / city export exceeded the row limit`), maps A-L exactly as Server/X/Y/Player/UID/UUID/Alliance/Level/HP/protection/marked/updated, writes UID/UUID/text as `inlineStr`, uses numeric cells for A/B/C/H/I, style-3 dates for J/L with the exact 1e11 seconds/milliseconds threshold, and falls back J from `protectEndTime` to `shieldEndTime` only when the primary key is absent. The desktop host now owns the native save dialog and returns the recovered cancel/success envelopes; deterministic XLSX reopen covers >200 frontend-page rows, large IDs, coercion, fallback and timestamp boundaries.
- [ ] Connect offline services to real native handlers and result tabs; test using explicitly labelled recovered/synthetic samples. Keep unknown semantic fields open.

## R7 - Production shared Manual Scan engine (P0; active)

- [x] Connect ordinary production `map_scan_start` to the common `ManualMapScanCommandService`/`MapScanEngine`; `LWB-R7-004` live-proves current-v16 context, one real Resource engine block and bounded Stop. The older one-view service remains support/evidence code rather than ordinary Start ownership.
- [x] **DB-04 recovered completion/start baseline:** `LWB-R6-052/053/054/055/056/058/059/060/061` now cover geometry/cardinality, derived progress/rate, direct-completion gates, Start/Stop/Clear ownership, transactional publication, terminal failure admission, event run/server identity and world readiness. `LWB-R7-055` maps those invariants to the rebuild's direct-snapshot architecture without fabricating native queue counters, and `LWB-R7-056` adds durable full-run identity. The only unrecovered branch is a producer of `resumeAvailable=true` plus its retry/resume policy; every recovered 0.3.1 state-construction/reset site writes false, so public resume remains deliberately unavailable rather than guessed.
- [x] **Durable full-run identity (`LWB-R7-056`):** profile scope is the per-profile map database; each production run now additionally persists launch session, server, world, tile geometry, home tile, ordered selected types, scan mode, concurrency and retry bound. Every checkpoint/failure/Stop/final publish revalidates that full identity transactionally before mutation, so a reused run ID with any stale/foreign dimension is rejected. Legacy `scan_runs` tables migrate additively; old/default identity rows cannot impersonate a current fully identified run. Public resume stays `resumeAvailable=false` because no original true producer is recovered.
- [x] Historical Normal/Fast mechanisms exist through the same common service with recovered concurrency `8`/`20`; these are now internal strategy building blocks only. Owner override requires removing the user-facing selector and adding backend category-aware strategy planning.
- [x] Implement Stop/cancel/failure ownership for the direct scanner: one Stop cancels the active owner and waits for its terminal task; engine cancellation writes stopped state and never publishes; source/context failures release ownership into truthful error state; stopped/failed runs cannot later publish; window shutdown calls `ManualMapScanCommandService.Close()` before lifecycle/store disposal, and `Close()` synchronously drains its terminal task.
- [x] Persist direct-scan run/block/capture staging durably and fail closed across interruption. Each logical block is checkpointed before publication; successful final publication requires exact completed coverage with zero failed blocks inside the owned transaction; stopped/failed runs remain non-publishable; stale staging is never treated as a resumable completed run.
- [x] Resolve capture completeness for the rebuild's direct-snapshot transport instead of fabricating the original native queue. Correlated point/march snapshots are admitted only for the active server/world/request footprint; selected-kind publication atomically replaces the prior dataset so absent fresh rows act as removals; collection/enumerator mismatches and bounded WorldMarch overflow fail the batch/run. Original queue-only `acks`/pending/dropped counters are not synthesized and stay unknown/null.
- [x] Drive `map_scan_status`/`bridge://map-scan-status` and the progress bar from real totals/completed/read/failed/unread/inflight/rate/acquisition/commit state. Acquisition may advance below 100%; `100%` is reserved for completed durable publication, and unsupported resume is explicitly `resumeAvailable=false` (`LWB-R7-055`).
- [x] Finish `map_scan_clear({serverId})` lifecycle semantics under `LWB-R7-054`: preserve marks, reject active scans, require exact current-live-server ownership, serialize Clear against Start, atomically clear the intended server scan/index scope and reset owner-visible progress without stale-run resurrection.
- [x] Prove the common worker with City through ordinary Manual Start, real progress, durable publication, normal Search/render, newer-run replacement and reopen (`LWB-R7-008` plus subsequent current-client full-world regressions).
- [x] Enable all original eight kinds on the same worker. `LWB-R7-036` proves mixed/all-eight shared acquisition and both modes; subsequent current-v19 per-kind checkpoints live-prove Resource/Monster/Truck/Dispatch and shared typed paths, while Railway/Ghost retain only their explicitly documented positive-population proof gaps rather than separate scanner implementations.
- [x] `LWB-R7-036` proves mixed selected types, all-eight selection, same-owned-session repeated completion and all-eight Normal/Fast. The shared Manual acquisition layer is now eligible for completion; Auto Scan must reuse it.
- [ ] Finish downstream per-kind query/options/filter/sort/navigation against committed data; Excel export is retired; keep Scan Content acquisition selection distinct from result filtering.
  - [x] `LWB-R7-048` closes current-v19 Truck ordinary-UR, reindeer-only, plunderable-only, retained-item option and retained-item filter behavior with exact live UUID-set/reopen proof.
  - [x] `LWB-R7-049` closes the public Truck alternate-sort surface: six recovered keys, ordered multi-sort, nulls-last ASC/DESC, special-UR quality=100, itemCount/itemKey coupling and stable record-key ties; exact current-v19 proof passed 15 scenarios before DB reopen and the same 15 after.
  - [x] `LWB-R7-050` implements the public Railway alternate-sort surface from hash-locked original recovery plus deterministic multi-row acceptance: five keys, ordered multi-sort, nulls-last ASC/DESC, plain quality, itemCount/itemKey coupling, protectTime zero-to-NULL and stable record-key ties. Current-v19 acquisition completed 2,500/2,500 with zero Railway rows, so positive-row live ordering remains population-pending.
  - [x] `LWB-R7-051` closes the public Resource alternate-sort surface: `level` + `updatedAt`, ordered multi-sort, nulls-last ASC/DESC and stable record-key ties; exact current-v19 proof passed 5 scenarios before DB reopen and the same 5 after across 1,870 rows.
  - [x] `LWB-R7-052` closes the public City alternate-sort surface: `level`, `health`, clock-aware `shield`, and `updatedAt`, ordered multi-sort, nulls-last ASC/DESC and stable record-key ties; exact current-v19 proof passed 9 scenarios before DB reopen and the same 9 after across 1,319 rows. Dispatch/Ghost alternate sorts were still fail-closed at this checkpoint.
  - [x] `LWB-R7-053` recovers and production-enables the shared Dispatch/Ghost alternate-sort surface: `level`, special-aware `quality`, zero-to-NULL `completionTime`, and `updatedAt`, with ordered multi-sort, nulls-last ASC/DESC and stable record-key ties. Exact current-v19 Dispatch proof completed 2,500/2,500, published/reopened 386 rows, and passed 9 scenarios before reopen plus the same 9 after. Ghost uses the same recovered/implemented contract and is deterministically tested; positive-row live acceptance is deferred until Thursday, 2026-09-24 per owner availability.
  - [x] `LWB-R7-073` closes the current-v19 Dispatch filter surface with independent live-row set comparison: five quality selectors, `specialOnly`, pending/completed `completionStatus`, `minLevel`, `maxLevel`, exact-level composition and `plunderableOnly`. Fast 2,500/2,500 published/reopened 69 rows; 12 scenarios matched exactly before and after reopen. Positive live branches included SR/SSR/UR, special, pending/completed, level and plunderable rows. The reusable verifier is shared with Ghost for the deferred positive-row run.
  - [x] `LWB-R7-074` closes current-v19 Resource result/truth filtering with independent set comparison. Fast 2,500/2,500 published/reopened 1,788 Resource rows and passed 8 scenarios before and after reopen: `resourceNameKey`, idle-only, full-only, exclude-black-tile, min/max/exact level, and the combined default. The live population had 1,783 idle, 1,665 full, zero black-tile rows and 420 exact level-3 rows; no positive black-tile exclusion case is claimed.
  - [x] `LWB-R7-075` closes the current-v19 City public filter surface with independent raw-record set comparison. Fast 2,500/2,500 published/reopened 1,546 City rows and passed 7 scenarios before and after reopen: alliance equality, no-alliance, positive `markedOnly` using one disposable-DB mark seeded against a live owner, positive identity keyword, and literal `%`/`_`/`\\` keyword escaping. No game-side mark or persistent user data was changed.
  - [x] `LWB-R7-076` closes current-v19 Monster result filtering against 5,105 published/reopened live rows. Eight exact UUID-set scenarios passed before and after reopen: `monsterNameKey`, current five-level-bucket inclusive `maxLevel`, name+max composition, identity/literal keyword escaping and the R7-013 resolved `monsterNameKeys` rebuild-policy path. The current population had no positive Zombie/Boss protection-detail or shield deadline, so those observational gates remain unchanged.
  - [x] `LWB-R7-077` closes the completed/persisted public `map_data_options` surface on current-v19. One automatic Fast all-eight scan completed 2,500/2,500, produced 6,341 rows across 6 positive kinds, and the public response exactly matched independently derived counts, City alliances/no-alliance, Resource/Monster names, Dispatch/Monster levels, Treasure descriptors, Truck/Railway retained-item options and completed scan progress; the same aggregate metrics reproduced after DB reopen. Positive active-staging live proof is not claimed because the current Fast all-eight source returns its logical blocks as one large batch before final publication.
  - [x] `LWB-R7-078` closes current-v19 public `map_summary`: the exact `{serverId,counts,scanState}` envelope exposed the owned Monster run while scanning, then exact persisted counts after 2,500/2,500 completion with 5,108 live Monsters, and after DB reopen the unique saved-profile server reproduced the same counts with `phase=unavailable` / `serverIdSource=saved_profile_index`. The active count was truthfully zero before the one-batch Fast source checkpointed; no published-empty claim was inferred.
  - [x] `LWB-R7-060` historically closed City Excel export end-to-end. **Retired by owner override 2026-09-19:** production/UI removal is now a cleanup task; this line remains evidence history only.
  - [x] `LWB-R7-061` live-proves non-moving result navigation through public `map_coordinate_jump`: current-v19 server 2212 normal-world camera moved from the player tile to an in-bounds target through the owned-session `GotoWorldPos` completion callback, then returned to the player tile. Shipped result UI wiring is static-proven: Truck/Railway use moving `map_march_follow`; every other result kind uses coordinate Jump. Truck Follow is already live-proven; Railway positive-row Follow remains population-pending.

## R8 — Automatic scanning and server travel (P0)

- [x] **ARTIFACT-REVIEW DB-06 → IMPLEMENTATION → LIVE-VALIDATION:** cross-server travel and current/home arrival truth are LIVE-PROVEN by `LWB-R7-037`; recovered invalid/busy/timeout contracts remain fail-closed, and R7-130 preserves multi-server saved history/browsing.
- [x] Implement Auto Scan as a single-owner durable scheduler: configuration, Run Now, intervals/eligibility, target sequence, failures, cancel and return-to-origin. Closed by R7-037/038/039/070, with R7-130/131 current-bundle failure-isolation/Stop/persistence/saved-server browser hardening.
- [x] Prove navigation/refresh/reconnect cannot create duplicate Auto cycles or change server context before confirmed travel. Closed by `LWB-R7-132`: generated-frontend browser acceptance proves one profile-owned scheduler survives navigation/Refresh Status/reconnect without duplicate starts, stops later targets while disconnected, and rechecks ownership after confirmed travel. Full desktop/game restart persistence and unattended deadline execution remain the separate live authority from `LWB-R7-039`.

## R9 — Conditional actions and scheduled jobs (P0)

- [ ] **ARTIFACT-REVIEW DB-06 → IMPLEMENTATION → LIVE-VALIDATION:** coordinate Jump is LIVE-PROVEN by `LWB-R7-061`; Truck moving Follow is LIVE-PROVEN by `LWB-R7-040`; Railway uses the same implemented Follow route but still needs a positive live Railway row before this family can be fully closed.
- [ ] Treasure actions: read-only refresh/status is closed by R7-068/069; R7-086 recovers/offline-tests the host validator/candidate query plus current-v19 direct ordinary claim boundary; and R7-087 closes the original frontend/status state machine (1 s polling, 1,800 cap, UUID state overlay, lucky default/persistence and row eligibility gate). State-changing `map_treasure_claim` remains blocked because protected scope filtering/order, lucky-slot scheduling, scout-slot selection/reservation and exact batch terminal enumeration are still unrecovered; live claim/scout acceptance also requires a suitable explicitly authorized target.
- [x] Scheduled Plunder read/list/status persistence is closed by R7-041 and current-code audit R7-079: public `map_plunder_jobs_list` returns persisted Dispatch plus Truck active/history rows with recovered ordering/metadata and reopen stability.
- [x] Truck schedule/cancel persistence + durable worker is implemented/offline-tested by R7-041 through R7-046. Public schedule is enabled, same-live-server only, with no implicit jump or retry after ambiguous send. Live robbery acceptance is deliberately NOT_RUN because it consumes a real action.
- [x] Dispatch schedule/execution implementation (`LWB-R7-080`/`081`/`082`/`083`/`084`): public validation/schedule/cancel, active persistence, current-v19 eligibility/derived fields, post-arm result semantics, session-correlated executor and durable single-owner worker are implemented/offline-tested. R7-084 preserves whole-batch validation, sequential guarded writes, positive-i64 local server identity and one post-success change event. Wider-than-current-v19 server IDs fail before attempt/send. Live plunder acceptance is deliberately NOT_RUN because it consumes a real action.
- [x] Dispatch Alliance Share payload/validation (`LWB-R7-085`): original 1..200 validation, exact errors, per-row 5 s bridge call, aggregate result shape, and current-v19 `Text_PointShare -> TO_ALLIANCE -> hero.dispatch.share.chat` payload/transport are recovered and offline-tested. Production send remains intentionally unavailable; live message delivery requires explicit messaging authorization.
- [ ] Prove each state-changing action's eligibility, duplicate suppression, failure behavior and authoritative outcome using suitable explicitly authorized targets.

## R10 — Acceptance and handoff (P0 release gate)

- [x] Run and record all 47 full acceptance cases from `task.md` as pass/fail/not-run/blocked with exact build and evidence paths. `LWB-R7-134` refreshes the complete 47-row matrix from R7-088 using durable R7-089–R7-133 evidence plus a fresh clean Release/browser/visual rerun; incomplete rows remain explicitly partial/blocked rather than being promoted by foundation subchecks.
- [x] Recheck production-mode behavior and the fixture visual matrix separately; `LWB-R7-135` passively proves real normal Release Overview/Map Data production composition while `LWB-R7-134` independently owns fixture/browser/pixel validation.
- [x] Deliver a fresh runnable build and exact commands; update the S/O/M ledger and R backlog with remaining unknowns. `LWB-R7-135` records the Release executable path/hash, exact build/passive-verification commands and refreshed S/O/M rows. Final integrated acceptance remains open for the explicitly listed population/action/active-restart/D02/multi-account/F04 gates.

## P1 — Remaining feature families

- [ ] Build the exact original command/event/config catalog from the verified application.
- [ ] Recover one feature family at a time using the same evidence labels and live-proof gates.
- [ ] Keep unsupported commands fail-closed until their contracts are recovered and implemented.

## Verification

Review 9: Release build (0 warnings/errors), all five backend groups with real-config preservation, frontend integrity, five preference scenarios and both transport checks pass. Fresh active script CRC/version match accepted update; sampled core hashes unchanged. No new game action, native analysis or UI/host capture. [Review evidence](evidence/lwbridge-implementation/2026-09-10-pm-review-9.json) distinguishes ordinary game recovery from all 47 unsigned rebuild acceptance cases.

See [the regular handoff](docs/implementation-handoff.md) for current verification commands, [docs/README.md](docs/README.md) for evidence and [task.md](task.md) for full acceptance. Run visual/current-runtime captures when applicable using new dated outputs; do not overwrite a historical runtime snapshot.
