# LWBridge documentation index

## Current owner priority - shared Map Data Manual Scan engine, 2026-09-13

**Treasure claim host/direct-current-v19 contract is now RECOVERED + OFFLINE-TESTED (`LWB-R7-086`), while production claiming remains BLOCKED.** The verified original accepts `claimScope` `boxes|season|single` (`single` requires `targetUuid`), defaults `prioritizeLuckySlots=true`, loads exact persisted claim candidates through the recovered completed-ordinary-or-Supplies-1/3/4 SQL with expiry/UUID guards, calls protected `claimTreasures` with a 5,000 ms timeout, and polls `getTreasureClaimStatus` separately. Current-v19 direct ordinary Treasure uses `detect.event.claim.treasure` with UUID `PutLong` and target server `PutInt`; `IsHaveGetReward(localUid)` is the UI duplicate gate, no-`errorCode` enters the official success path even when reward is absent, and the push path broadcasts the claimed UUID. Supplies is instead scout/march-driven through `CheckBtnState -> LaunchScout(SCOUT_SUPPLIES) -> StartMarch`. The rebuild now has only an internal validator/candidate-query/direct-transport/result contract; protected scope/lucky/scout batch orchestration remains `UNKNOWN/BLOCKED`, `map_treasure_claim` is still not routed, and no claim/scout/collection was executed. Evidence: [R7-086](../evidence/lwbridge-implementation/2026-09-20-r7-treasure-claim-offline-contract.json).

**Dispatch Alliance Share payload/validation is now RECOVERED + OFFLINE-TESTED (`LWB-R7-085`), with live delivery still BLOCKED.** The original `map_dispatch_share_alliance({rows})` contract accepts 1..200 rows and requires decimal UUID plus positive integer-like `serverId/x/y/cfgId`; it calls `shareDispatchTaskToAlliance` per row with a 5,000 ms host timeout and aggregates `shared/failed/sharedUuids/failedUuids`. Current-v19 is hash-locked through `Text_PointShare -> TO_ALLIANCE -> ChatHeroDispatchShare -> hero.dispatch.share.chat`; its core point payload is `x/y/sid/dispatch=1/cfgId/uuid` with optional `uname/abbr`, and Dispatch display uses localization key `456288` rather than a fabricated `oname`. The rebuild now has an internal validator/payload plan with PutInt/PutLong transport bounds, but production still cannot handle `map_dispatch_share_alliance`. No message/share was sent. Evidence: [R7-085](../evidence/lwbridge-implementation/2026-09-20-r7-dispatch-alliance-share-offline.json).

**Public Dispatch scheduling is now ENABLED + OFFLINE-TESTED (`LWB-R7-084`).** The recovered `map_dispatch_plunder_schedule({rows})` wrapper is now exposed over the R7-080 validator/store, R7-082 executor and R7-083 durable worker. The complete 1..200-row batch validates before any write; validated rows then persist sequentially, return accumulated scheduler-overlaid rows, and emit `bridge://dispatch-plunder-changed` only after the full loop succeeds. A later guarded running/terminal conflict preserves earlier writes and returns exact `MAP_DATA_ERROR / scheduled plunder job is missing` without a success event. Original schedule/cancel server identity is now durably pinned as positive signed-64-bit with no `99999` cap, so Dispatch-only persistence/cancel keys use `long`; the current-v19 execution path remains fail-closed to its proven 1..99999 transport domain before attempt/send. No live schedule or plunder was executed. Evidence: [R7-084](../evidence/lwbridge-implementation/2026-09-20-r7-dispatch-public-schedule.json).

**Dispatch durable execution is now WIRED + OFFLINE-TESTED (`LWB-R7-083`).** A single-owner worker now connects the R7-081 persistent scheduler state to the R7-082 current-v19 executor. It preserves the recovered 10-second arm lead, due-only offline deferral, expiry and daily-limit stop-all behavior, increments attempts exactly once on an active-only `scheduled|waiting_connection -> running` transition, and emits the existing Dispatch change event. Current-v19 cross-server work is intentionally not restricted to the live server because `DispatchSteal` carries `targetServer` and the executor rechecks `IsOpenCrossSteal()`. Two safety hardenings are explicit: a cancel that wins before `running` cannot be resurrected, and stale `running` rows after restart become terminal `failed / DISPATCH_PLUNDER_CLIENT_RESTARTED` instead of the original retryable state because prior send state cannot be reconstructed safely. Post-send/state-unknown outcomes remain terminal and non-retryable. Public `map_dispatch_plunder_schedule` is still fail-closed; no live plunder was run. Evidence: [R7-083](../evidence/lwbridge-implementation/2026-09-20-r7-dispatch-durable-worker.json).

**Dispatch post-arm result semantics and the internal current-v19 executor are now RECOVERED + OFFLINE-TESTED (`LWB-R7-082`).** The original result event is exactly `map.dispatch-plunder-result`, correlated by `(serverId,taskUuid)` with `success/errorCode` and optional `serverDayStartAt`; its pending result deadline is `max(now,executeAt)+30000 ms`, and an arm failure is terminal `failed` while connected but returns to `waiting_connection` when disconnected. Current-v19 dispatcher mapping is now hash-locked end-to-end: `MsgDefines.DispatchSteal = hero.dispatch.steal`, `MsgMap.DispatchSteal -> Net.Msgs.DispatchTask.DispatchStealMessage`. The internal bridge temporarily wraps that exact `HandleMessage`, preserves the original handler, parses the decimal task UUID through `CS.System.Int64.Parse`, cannot send before authoritative server time reaches `executeAt`, rechecks daily/cross-server gates, sends once, and treats post-send timeout as ambiguous/non-retryable. Host protocol tests cover success, explicit server rejection, timeout ambiguity and foreign-session rejection. Public schedule remains fail-closed and no live plunder was run; Lua live-boot proof is also not claimed. Evidence: [R7-082](../evidence/lwbridge-implementation/2026-09-20-r7-dispatch-result-executor.json).

**Dispatch current-v19 arm prerequisites are now RECOVERED + OFFLINE-TESTED (`LWB-R7-081`).** The official v19 client sends `SFSNetwork.SendMessage(MsgDefines.DispatchSteal, uuid, targetServer)` after completion/protection, already-stolen, daily-count, per-task-count and cross-server gates. The scanner now derives only source-backed schedule fields: `plunderAt = completionTime + protect_times*60000`, `stolenCount = stealList.Count`, and `maxStealCount = steal_maxtimes`; it deliberately does not map `expiredTime` to historical `taskExpireTime`, because no current-v19 Dispatch-named Lua module uses that field. Original worker recovery independently pins a 10,000 ms arm lead, `running` before `armMapPlunder`, a 5,000 ms arm host timeout and a 30,000 ms response-horizon constant. Exact expiry/armable/status/restart/offline/daily-limit SQL is implemented and deterministic-tested. Public Dispatch scheduling remains fail-closed because the arm-result/runtime branch and current-v19 bridge request/result correlation are not yet closed. No live plunder action was performed. Evidence: [R7-081](../evidence/lwbridge-implementation/2026-09-20-r7-dispatch-v19-arm-prerequisites.json).

**Dispatch plunder schedule/cancel persistence is now RECOVERED + internally OFFLINE-TESTED (`LWB-R7-080`).** The verified original wrapper requires 1..200 `rows` and validates positive server, decimal-string task UUID, completion/plunder timing, optional expiry, and steal-count eligibility with exact `INVALID_REQUEST` messages. Exact original SQLite upsert/cancel semantics are reproduced internally: fresh jobs start `scheduled/0 attempts/NULL error`; active `scheduled`/`waiting_connection` reschedules update only task/timing/update fields; running/terminal rows cannot be replaced; cancel only affects those two active states and clears `last_error`. Public `map_dispatch_plunder_cancel` is now enabled and offline-proven with exact invalid-target, NOT_FOUND, null-success and `bridge://dispatch-plunder-changed` behavior. Public scheduling remained deliberately unavailable at R7-080; R7-081 subsequently recovers the current-v19 send/eligibility chain and original arm/persistence kernel, but schedule stays blocked pending post-arm result/runtime and bridge-correlation closure. No live plunder action was performed. Evidence: [Dispatch schedule persistence](../evidence/lwbridge-implementation/2026-09-20-r7-dispatch-schedule-persistence.json).

**Scheduled Plunder read/list/status persistence is now current-code audited and closed (`LWB-R7-079`).** Public `map_plunder_jobs_list` is a read-only production command returning `{dispatchJobs,truckJobs}` from persisted Dispatch jobs plus Truck active/history rows with the recovered active-first/due-time/update-time ordering and scheduler metadata overlay; deterministic tests prove the combined envelope and DB-reopen persistence. R7-079 also reconciles the action boundary: R7-046 superseded R7-041’s historical Truck-schedule fail-closed statement, so Truck public schedule/cancel + durable worker are implemented/offline-tested, while live robbery acceptance remains NOT_RUN. At R7-079 production had no Dispatch action handler; R7-080 subsequently enables public local-scheduler cancel while schedule remains blocked. Alliance sharing remains blocked pending offline payload work plus explicit live messaging authorization. Evidence: [Scheduled Plunder read audit](../evidence/lwbridge-implementation/2026-09-20-r7-scheduled-plunder-read-audit.json).

**Public `map_summary` is now LIVE-PROVEN on current-v19 (`LWB-R7-078`).** One automatic Fast Monster scan exposed the exact `{serverId, counts, scanState}` envelope while the owned run was actively `scanning`, then completed 2,500/2,500 with zero failed/unread blocks and 5,108 live Monster rows. The completed summary exactly matched raw persisted counts, and after DB reopen the unique saved-profile server reproduced those counts with `phase=unavailable` / `serverIdSource=saved_profile_index` without promoting saved data to live readiness. The active Monster count was truthfully zero before the one-batch Fast source checkpointed; that is an active staging-scope observation, not an empty published result. Evidence: [Map summary live proof](../evidence/lwbridge-implementation/2026-09-20-r7-map-summary-live.json).

**Completed/persisted public `map_data_options` is now LIVE-PROVEN on current-v19 (`LWB-R7-077`).** One automatic Fast all-eight scan completed 2,500/2,500 with zero failed/unread blocks and published 6,341 rows across 6 positive kinds. The public response exactly matched independent raw-row derivation for every kind count, 72 City alliance options, 1,199 no-alliance Cities, 3 Resource names, 7 Monster names, 5 Dispatch levels, 36 Monster levels, one Treasure descriptor, and 18 Truck retained-item options; Zombie Boss names and Railway retained-item options were exact live zero sets. The same aggregate metrics reproduced through the public command after database reopen. Active-staging positive live acceptance is deliberately unclaimed because the current Fast all-eight source returns its logical blocks as one large batch before final publication. Evidence: [Map Data options live proof](../evidence/lwbridge-implementation/2026-09-20-r7-map-data-options-live.json).

**Monster result filters are now LIVE-PROVEN on current-v19 (`LWB-R7-076`).** A Fast 2,500/2,500 scan published/reopened 5,105 Monster rows and exact-UUID-set matched 8 current result-filter scenarios before and after reopen: `monsterNameKey`, the owner-corrected five-level-bucket inclusive `maxLevel`, name+max composition, identity keyword, literal `%`/`_`/`\\` keyword escaping, and the R7-013 resolved `monsterNameKeys` localized-name path. The selected name matched 1,584 rows; max level 20 matched 2,916; name+max matched 861; resolved name keys matched 3,101. The localized-name expansion remains explicit rebuild usability policy, not an original 0.3.1 frontend claim. This population had no positive Zombie/Boss protection-detail or shield deadline. Evidence: [Monster filter fidelity](../evidence/lwbridge-implementation/2026-09-20-r7-monster-filter-fidelity.json).

**City public result filters are now LIVE-PROVEN on current-v19 (`LWB-R7-075`).** A Fast 2,500/2,500 scan published/reopened 1,546 City rows. Independent raw-record identity sets matched all 7 public filter scenarios before and after reopen: alliance equality, no-alliance, positive `markedOnly` via one mark seeded only inside the disposable proof database, positive identity keyword, and literal `%`, `_`, `\\` keyword escaping. The live population produced alliance=70, no-alliance=1,136, marked=1, identity-keyword=1, literal `%`=0, `_`=1,546 and `\\`=413. One bounded `overview_session_unavailable` acquisition retry recovered on attempt 1/3; final scan completed with zero failed/unread blocks. No game-side mark or persistent user mark state changed. Evidence: [City filter fidelity](../evidence/lwbridge-implementation/2026-09-20-r7-city-filter-fidelity.json).

**Resource result/truth filters are now LIVE-PROVEN on current-v19 (`LWB-R7-074`).** A Fast 2,500/2,500 read-only scan published/reopened 1,788 Resource rows. Independent expected-row sets matched all 8 public/current Resource scenarios before and after reopen: sampled `resourceNameKey`, idle-only, full-only, exclude-black-tile, `minLevel`, `maxLevel`, exact level, and the combined idle+full+non-black default. The live population had 1,783 idle rows, 1,665 full rows, 420 exact level-3 rows and no black-tile rows; therefore the non-black filter is exact-set proven but its positive exclusion branch remains population-unobserved. Evidence: [Resource filter fidelity](../evidence/lwbridge-implementation/2026-09-20-r7-resource-filter-fidelity.json).

**Dispatch/Secret Task public result filters are now LIVE-PROVEN on current-v19 (`LWB-R7-073`).** A Fast 2,500/2,500 read-only scan published/reopened 69 authentic Dispatch rows. At one frozen wall clock, the live verifier independently computed row-identity sets and matched all 12 recovered public filter scenarios before and after database reopen: N/R/SR/SSR/UR quality, `specialOnly`, pending/completed `completionStatus`, `minLevel`, `maxLevel`, exact-level composition and `plunderableOnly`. The live population exercised SR=20, SSR=12, UR=37, special=6, pending=6, completed=63, exact level-4=24 and plunderable=69; N/R were valid zero-set comparisons. A same-day Railway filter follow-up completed 2,500/2,500 on server 2204 but that population had dropped to zero Railway rows, so no stale-row Railway filter claim was made and R7-071 remains its positive authority. Evidence: [Dispatch filter fidelity](../evidence/lwbridge-implementation/2026-09-20-r7-dispatch-filter-fidelity.json).

**Supplies positive-row acceptance is ready but currently population-blocked (`LWB-R7-072`).** The strict read-only Treasure-state harness now supports target-server proof and fails unless a genuine point-type-27 `WorldSuppliesPoint` is published. Server 2204 completed Fast 2,500/2,500 with zero failures/unread but exposed 3 ordinary Treasure and 0 Supplies; a second strict server-2213 run also completed 2,500/2,500 and exposed 2 ordinary Treasure, 0 Supplies, then returned to 2212. Current-v19 source recovery additionally pins the season Supplies manager/activity/config anchors, and a temporary read-only metadata sweep found no active Supplies activity on any reachable server 2149-2211; lower sampled targets were rejected by the game's travel precheck. The temporary discovery code was removed. Supplies remains open until the event/population actually exists. Evidence: [Supplies population gate](../evidence/lwbridge-implementation/2026-09-20-r7-supplies-population-gate.json).

**Railway positive-row sorting and moving Follow are now LIVE-PROVEN on current-v19 (`LWB-R7-071`).** The repaired harness first proved server 2213 had zero authoritative Railway rows, then used a temporary read-only Train-list diagnostic only to nominate server 2204; that discovery lane was removed before the checkpoint. The authoritative server-2204 Railway-only Fast scan completed 2,500/2,500 with zero failed/unread blocks, published/reopened 3 rows, passed 12 recovered sort scenarios before reopen and the same 12 after reopen, exercised `itemCount` with nonempty goods, proved meaningful relative ordering across all 3 live rows, and succeeded on public `map_march_follow` using an exact published march identity. It returned to original server 2212. All 3 rows exposed `maxLootCount` and auditable `currentGoods` totaling 46 items; optional raw `trainData` was absent in this live population. Evidence: [Railway positive Follow](../evidence/lwbridge-implementation/2026-09-20-r7-railway-positive-follow.json).

**Auto Scan core scheduler is now formally CLOSED (`LWB-R7-070`).** No new scheduler implementation was needed: historical live evidence R7-038/039 already proves repeated Run Now, ordered multi-target execution, disable-during-cycle skip, return-to-original/status truth, full desktop/game restart persistence, unattended deadline firing and next-schedule advancement. R7-070 audits the current post-R7-067 shipped bundle and adds browser/deterministic guards for per-profile enable/interval/servers/return/`nextRunAt`, top-level scheduler ownership, mid-cycle disable break, return-in-finally, 5-second polling, and backend-planned shared scans with no caller `scanMode`. Release build/full suite pass. Navigation-away/back live acceptance and a naturally occurring native travel failure remain limited evidence gaps only. Evidence: [Auto scheduler closure audit](../evidence/lwbridge-implementation/2026-09-20-r7-auto-scheduler-closure-audit.json).

**Treasure read-only state refresh/status is now LIVE-PROVEN on current-v19 (`LWB-R7-069`).** Production handles `map_treasure_state_refresh`, `map_treasure_state_refresh_all`, and `map_treasure_claim_status` through the existing owned game session, reusing recovered cache/overlay semantics from R7-068. Current-v19 source is pinned to `LWScripts.data` contentVersion 19 plus the managed `TreasurePointInfo` methods and Supplies detail/charge logic. Full deterministic acceptance passes. Final live proof on server 2212 completed Fast 2,500/2,500, published 8 Treasure rows, returned/cached/overlaid 8/8 states, and resolved all eight player states to `unclaimed`; no claim/scout/march action ran and `claimPriority` was not fabricated. `map_treasure_claim` remains blocked. Evidence: [Treasure read-only state refresh](../evidence/lwbridge-implementation/2026-09-20-r7-treasure-state-refresh.json).

**Treasure visibility/lucky ordering and the persistent claim-state cache are now RECOVERED and OFFLINE-TESTED (`LWB-R7-068`).** The original backend applies foreign-radar visibility only when `includeForeignRadarTreasures=false`: non-radar rows pass, while radar `treasureType=1` rows require the viewer alliance; absent caller identity falls back to row-embedded `viewerAllianceId`. `luckyFirst=true` prepends cached `state_json.claimPriority ASC` with default 1 and likewise falls back from caller `viewerUid` to the row identity. Production now reproduces those predicates, the recovered `(server_id,player_uid,treasure_uuid)` state join, state-over-row overlay, expiry cleanup and upsert. Release build and all six deterministic groups pass. At R7-068 the live read-only state-label refresher/status commands remained open; that follow-up is now closed by R7-069. `map_treasure_claim` stays blocked. Evidence: [Treasure query/state cache](../evidence/lwbridge-implementation/2026-09-20-r7-treasure-query-state-cache.json).

**Automatic scan strategy is now backend-owned (`LWB-R7-067`).** Manual and Auto no longer expose/persist Normal/Fast or send `scanMode`. The planner selects the fastest already-proven strategy after authoritative context: standard current-v19 1000x1000 worlds use Fast/20, dedicated Monster/Zombie Boss uses `current_fast_monster_lod2_v1`, and only proven nonstandard single City/Resource falls back to ordinary LOD0/8; unsupported nonstandard shapes fail closed. Browser and full deterministic acceptance pass, and a live server-2212 Zombie Boss Start with no caller mode selected `fast / current_fast_monster_lod2_v1 / 20` and completed 2,500/2,500 with zero failures in 5.118 s. Evidence: [automatic scan strategy](../evidence/lwbridge-implementation/2026-09-20-r7-automatic-scan-strategy.json).

**City Excel export is removed from the shipped product (`LWB-R7-066`).** This owner override intentionally supersedes the earlier R7-057..060 parity work without deleting its historical evidence. Map Data no longer contains the Export Excel control/handler/API wrapper or export translations; production has no `map_city_export` route, WinForms save dialog, OOXML writer, export-only full-snapshot storage path or export-only test suite. The retired command is regression-tested as `COMMAND_NOT_IMPLEMENTED`, a candidate-only browser check proves the generated Map Data surface contains/invokes no export path, and the complete deterministic suite is green. Evidence: [City export removal](../evidence/lwbridge-implementation/2026-09-20-r7-city-export-removal.json).

**Truck/reward game artwork is now LIVE-PROVEN on current-v19 (`LWB-R7-065`).** The public `game_asset_image({assetPath})` path now preserves the authoritative game icon path end to end: xLua calls `SpriteRenderer:LoadSpriteAuto`, the bridge crops the returned Sprite through its real `textureRect` with `Graphics.Blit`, encodes PNG/base64, and the host validates dimensions/signature and caches the exact source. Four representative Truck reward paths passed in one owned session, including extensionless item icons, a `.png` item icon, a packed atlas resource icon and an extensionless chest icon. `item406` produced a populated 153x153 image with 10,837 nontransparent pixels; the packed 60x60 atlas sprite produced 2,189 nontransparent pixels. `spriteName` remains fail-closed and is not used by Truck `currentGoods`. Evidence: [Truck game artwork](../evidence/lwbridge-implementation/2026-09-20-r7-truck-game-asset-image.json).

**Server-2207 Fast Secret Task stability is now LIVE-PROVEN under repeated use (`LWB-R7-064`).** Fourteen full investigation scans completed 2,500/2,500. The historical all-three-attempt non-rectangular footprint did not recur, so exact AOI validation remains intact and no speculative reset/fallback was introduced. Stress did reproduce an early failed probe whose success-only fields were absent; C# had masked the native failure as a parameter mismatch. Failed results now surface the native error before proven-only field checks. Final exact-code three-scan acceptance published **67 / 80 / 126** rows; iteration 1 decoded `overview_session_unavailable`, recovered on attempt 2, and iterations 2-3 were clean. Evidence: [server-2207 Fast stability](../evidence/lwbridge-implementation/2026-09-19-r7-server-2207-fast-stability.json).

**Coordinate Jump is now LIVE-PROVEN on current-v19 (`LWB-R7-061`).** The shipped Map Data UI routes Truck/Railway coordinate cells to moving `map_march_follow({serverId,marchUuid})` and every other result kind to `map_coordinate_jump({serverId,x,y})`. The new public-command proof used the owned current-client session on server 2212, proved the requested normal-world target through the `GotoWorldPos` completion callback, and then proved a return to the player tile. It started no scan and performed no attack/plunder/claim/collection/message action; game and launcher were closed afterward. Truck Follow remains live-proven from R7-040; Railway positive-row Follow was still population-pending at R7-061 and is subsequently closed by R7-071. Evidence: [coordinate Jump live proof](../evidence/lwbridge-implementation/2026-09-19-r7-coordinate-jump-live.json).

**Historical/superseded: City Excel export was recovered and public-enabled at `LWB-R7-060` (OFFLINE-TESTED), then removed from the rebuild by owner override `LWB-R7-066`.** The original host internally paginates the current filtered City query at 200 rows per page through page 1000, giving a 200,000-row ceiling before `MAP_EXPORT_FAILED / city export exceeded the row limit`. A-L are now source-proven as Server/X/Y/Player/UID/UUID/Alliance/Level/HP/protection/marked/updated; UID/UUID/text are lossless `inlineStr`, A/B/C/H/I are numeric, J/L are style-3 datetimes using the original 1e11 seconds/milliseconds threshold, and J falls from `protectEndTime` to `shieldEndTime` only when the primary key is absent. The desktop host uses the recovered UTC filename and save-dialog semantics from R7-058/059, returning exact cancel and success envelopes. Deterministic acceptance writes/reopens 235 rows, preserves a 36-digit UID, exercises coercion/fallback/unit boundaries, and the full suite is green. No interactive user save was performed during acceptance. Evidence: [row/data contract](../evidence/lwbridge-implementation/2026-09-19-r7-city-export-row-contract.json), [save-dialog contract](../evidence/lwbridge-implementation/2026-09-19-r7-city-export-dialog.json), [filename contract](../evidence/lwbridge-implementation/2026-09-19-r7-city-export-filename.json), and [City export infrastructure](../evidence/lwbridge-implementation/2026-09-19-r7-city-export-infrastructure.json).

**Durable full-run identity is now closed (`LWB-R7-056`).** The per-profile Map Data database now persists launch session, server/world, map geometry, home tile, ordered selected types, scan mode, concurrency and retry bound for each production run. Every checkpoint/failure/Stop/final publish revalidates the complete identity before mutation; any stale/foreign dimension fails with `INVALID_SCAN`. Legacy `scan_runs` schemas migrate additively. Public resume remains `resumeAvailable=false` because the verified original still has no recovered true producer. Evidence: [run identity](../evidence/lwbridge-implementation/2026-09-19-r7-map-scan-run-identity.json).

**Direct-snapshot progress/completion integrity is now closed (`LWB-R7-055`).** The shared scanner already enforced exact logical coverage, zero failed blocks, cancellation-without-publication, transactional `running -> completed` ownership and 98%-until-completed progress. R7-055 closes two remaining truthfulness gaps: every status now explicitly reports unsupported `resumeAvailable=false`, and all bounded WorldMarch enumerations (Truck/Railway, Monster/Zombie Boss, protection targets) must prove exhaustion at the 50,000 safety limit rather than silently accepting a truncated prefix. The rebuild intentionally does not fabricate the original bot's native pending/ack/drop queue counters; direct correlated snapshots fail closed on enumeration/identity mismatch and selected-kind publication represents removals by authoritative replacement. Evidence: [completion integrity](../evidence/lwbridge-implementation/2026-09-19-r7-map-scan-completion-integrity.json).

**Clear Map Data ownership is now production-closed (`LWB-R7-054`).** `map_scan_clear({serverId})` is routed through the same Manual Scan owner as Start/Stop/Status. It preserves the original active-scan rejection (`SCAN_RUNNING / stop the map scan first`) and exact current-live-server gate (`SERVER_UNAVAILABLE / current server id unavailable`), serializes Clear against Start, clears only that server's scan/index rows transactionally, preserves player marks and other servers, then publishes idle/zero progress. A saved/replay-only backend cannot clear. All destructive acceptance used in-memory stores; no real user Map Data was deleted. Evidence: [Clear ownership](../evidence/lwbridge-implementation/2026-09-19-r7-map-scan-clear-ownership.json).

**Dispatch/Ghost alternate sorting is now recovered and production-enabled (`LWB-R7-053`); Dispatch is LIVE-PROVEN on current-v19, while Ghost positive-row live acceptance is time-window pending.** Both kinds expose the same public keys: `level`, `quality`, `completionTime`, and `updatedAt`. Original recovery proves special missions sort as effective quality 100, zero completion time sorts as NULL, ordered multi-sort is preserved, NULL stays last for ASC/DESC, and `record_key ASC` is the stable tie-break. Final read-only Fast Dispatch proof completed 2,500/2,500 in **145.413 s**, published/reopened **386 Dispatch rows**, and passed **18 live sort assertions** across pre/post reopen, including 20 special rows and 380 nonzero completion times. Ghost has the same shared implementation and deterministic acceptance, but its positive-row live proof is explicitly deferred until **Thursday, September 24, 2026**. Evidence: [Dispatch/Ghost sort fidelity](../evidence/lwbridge-implementation/2026-09-19-r7-dispatch-ghost-sort-fidelity.json).

**City alternate sorting is now LIVE-PROVEN on current-v19 (`LWB-R7-052`).** The complete public City sort set is `level`, `health`, `shield`, and `updatedAt`, including the recovered dual-unit active-shield clock rule, ordered multi-sort, nulls-last ASC/DESC and stable record-key ties. Final read-only Fast proof completed 2,500/2,500 in **132.903 s**, published/reopened **1,319 City rows**, and passed **18 live sort assertions** across pre/post reopen; the population included 140 active shield values with 137 distinct deadlines. Evidence: [City sort fidelity](../evidence/lwbridge-implementation/2026-09-19-r7-city-sort-fidelity.json).

**Resource alternate sorting is now LIVE-PROVEN on current-v19 (`LWB-R7-051`).** The recovered public Resource sort set is exactly `level` and `updatedAt`, with ordered multi-sort, nulls-last ASC/DESC, plain indexed level and stable record-key ties. Final read-only Fast proof completed 2,500/2,500 in **126.029 s**, published/reopened **1,870 Resource rows**, and passed **10 live sort assertions** across pre/post reopen. Evidence: [Resource sort fidelity](../evidence/lwbridge-implementation/2026-09-19-r7-resource-sort-fidelity.json).

**Historical checkpoint `LWB-R7-050`: Railway alternate sorting was IMPLEMENTED/OFFLINE-TESTED there; the live positive-row ordering gap is subsequently closed by `LWB-R7-071`.** The recovered public Railway sort set is `quality`, `power`, `itemCount`, `protectTime`, and `updatedAt`, with ordered multi-sort, nulls-last ASC/DESC, plain Railway quality, itemCount/itemKey coupling and stable record-key ties. Current-v19 Fast acquisition completed 2,500/2,500 but exposed zero Railway rows, so the positive-row gate correctly withheld live acceptance. Evidence: [Railway sort fidelity](../evidence/lwbridge-implementation/2026-09-19-r7-railway-sort-fidelity.json).

**Truck alternate sorting is now LIVE-PROVEN on current-v19 (`LWB-R7-049`).** The recovered public Truck sort set is `quality`, `power`, `itemCount`, `remainingLootCount`, `arriveTime`, and `updatedAt`, with ordered multi-sort, nulls-last ASC/DESC, special-UR quality=100, itemCount/itemKey coupling, and stable record-key tie-breaking. Final read-only Fast proof completed 2,500/2,500 in **130.175 s**, published/reopened **469 Trucks**, and passed **30 live sort assertions** across pre/post reopen. Railway is subsequently recovered/implemented by `LWB-R7-050`, Resource by `LWB-R7-051`, City by `LWB-R7-052`, and Dispatch/Ghost by `LWB-R7-053` (Dispatch live-proven; Ghost positive-row live proof pending). Evidence: [Truck sort fidelity](../evidence/lwbridge-implementation/2026-09-19-r7-truck-sort-fidelity.json).

**Truck result filters/options are now LIVE-PROVEN on current-v19 (`LWB-R7-048`).** Failure-first acceptance corrected special-UR/reindeer plunderability by materializing original `remainingLootCount` from the shipped frontend effective-max rule, then restored retained-item options by resolving game-owned reward names/icons through `ItemTemplateManager` / `RewardManager` with per-reward caching. Final read-only Fast proof completed 2,500/2,500 in **131.059 s**, published/reopened **435 Trucks**, covered `remainingLootCount` on all 435, produced **18** retained-item options, and proved selective item `reward:7:600002` returned exactly **283/435** rows before and after reopen. No consuming action was used. Evidence: [Truck filter/options](../evidence/lwbridge-implementation/2026-09-19-r7-truck-filter-options.json).

**Truck scan goods fidelity is now LIVE-PROVEN on current-v19 (`LWB-R7-047`).** The Fast Truck scanner now parses the live XLua enum form (`Truck: 1`), resolves authoritative Lua TrainData through `LWTrainDataManager`, captures game-owned `GetCurRewardData()` plus exact `maxLootPerTrain`, and performs reward aggregation once after full acquisition. Full Truck `TrainData` JSON remains off the hot path. The final cleaned read-only run completed 2,500/2,500 blocks in **125.451 s**, published/reopened **244 Trucks**, populated current goods on all 244 (**1,782 items**) and max-loot on all 244, preserved 19 special-UR rows, and retained zero full Truck TrainData JSON rows. Exact v19 restoration and cleanup passed; no robbery or consuming action was used. Evidence: [Truck goods fidelity](../evidence/lwbridge-implementation/2026-09-19-r7-truck-goods-fidelity.json).

**PM17 correction delivery remains complete.** `LWB-PM17-001/002` are IMPLEMENTED/OFFLINE-TESTED at `f24fef39bbe41a8655595da2c9c9da1bcb9e9811` with GitHub Actions `34711920482` SUCCESS. The corrected-build Overview live regression remains prepared but NOT RUN.

**Public Truck Scheduled Plunder is now IMPLEMENTED/OFFLINE-TESTED (`LWB-R7-046`), but not live-proven.** Original 0.3.1 recovery pins `map_truck_plunder_schedule({rows})`: `rows` is required, batch size is 1..200, malformed payloads return `INVALID_REQUEST` with exact messages `truck rows are required`, `select between 1 and 200 trucks`, or `truck scheduling data is invalid`, and each row requires positive server/execute/max-loot data, a decimal-string `uuid`, and literal `robTimes < maxLootCount`. The rebuild validates the complete public batch before the first write, feeds the R7-044 archive/reschedule transaction, returns the original-compatible unit/null success, and emits the Scheduled Plunder change event. `expireAt` stays nullable when the current-v19 row does not source-prove it; it is never synthesized from `arriveTs`. R7-045 worker safety remains unchanged: same-live-server execution only, no implicit jump, and no retry after ambiguous send/crash. No live robbery was attempted because it consumes a real daily action. Evidence: [Truck public schedule](../evidence/lwbridge-implementation/2026-09-19-r7-truck-public-schedule.json).

**Truck durable worker is now OFFLINE-TESTED (`LWB-R7-045`).** The original worker contract is now source-backed: 10,000 ms arm lead, `running` transition with one attempt increment, `invalid scheduled target` terminal failure before increment, `server response timeout` terminal failure after send, due-offline `waiting_connection / game disconnected`, and `bridge://truck-plunder-changed`. The rebuild attaches a single Truck worker to the existing `ManualMapScanCommandService`/`CurrentClientMapBlockSource`, shares the same game-operation gate as scan/jump/follow, and never creates a second live bridge owner. Sent/ambiguous outcomes are never retried. A stale `running` row after process death is conservatively terminalized as unknown instead of reproducing the original retryable `client restarted` behavior, because the rebuild cannot prove whether `train.attack` was already sent. Current-v19 execution is restricted to the current live server and never performs an implicit server jump. Public `map_truck_plunder_schedule` remains fail-closed pending public payload/result contract proof. Evidence: [Truck durable worker](../evidence/lwbridge-implementation/2026-09-19-r7-truck-durable-worker.json).

**Truck archive/reschedule persistence is now OFFLINE-TESTED (`LWB-R7-044`).** Original 0.3.1 disassembly now pins the complete persistence transaction: only `succeeded`/`failed`/`cancelled`/`expired` prior attempts archive; old string `jobId` is reused or legacy rows receive `legacy-{serverId}-{trainUuid}-{createdAt}`; fresh attempts receive `truck-{unixMs}-{u64LowerHex}`; stale `battleWon`/`plunderRewards` are removed; history uses `ON CONFLICT(job_id) DO NOTHING`; the active upsert preserves attempts only for `scheduled`/`waiting_connection`, resets otherwise, deliberately preserves conflict-row `created_at`, and refuses to replace `running`. Production now reproduces that transaction internally and verifies the scheduled row before commit. The exact original entropy-generator algorithm is not claimed; the rebuild uses a cryptographic u64 source. Public scheduling remains fail-closed until durable worker/restart/ambiguity ownership is proven. Evidence: [Truck archive/reschedule](../evidence/lwbridge-implementation/2026-09-19-r7-truck-archive-reschedule.json).

**Truck result/reward reconciliation is now OFFLINE-TESTED (`LWB-R7-043`).** Current-v19 `FakePVPLogic` proves Truck rewards are `msg.reward` plus `extraPlunder`/`retake`; the rebuild now normalizes those entries through the game Item/Reward managers into the Scheduled Plunder `plunderRewards` shape and persists `battleWon` + rewards back into the active `truck_json`. Recovered 0.3.1 SQL shows this is a distinct `record truck plunder result` update: successful attempts remain in `truck_plunder_jobs` as `succeeded` (attempt count preserved) until a later reschedule archives the previous attempt into `truck_plunder_history`. Incomplete reward display normalization remains a proven non-retryable battle result. Public Truck scheduling is still fail-closed pending archive/reschedule + durable worker ownership. Evidence: [Truck result reconciliation](../evidence/lwbridge-implementation/2026-09-19-r7-truck-result-reconciliation.json).

**Current-v19 Truck quick-rob execution is now INTERNAL/OFFLINE-TESTED (`LWB-R7-042`).** Current source recovery replaces the missing injected `armMapPlunder` seam with the game-owned chain: exact live `WorldMarch`/`TrainData` identity -> `RailwayUtil.ClickAttackTrain(trainData,true)` -> default free highest-power robbery formation -> `LWMyStationDataManager.TryAttackTrain` -> `train.attack` -> success-only `TrainSkirmishDataReceived` / terminal `TrainAttackReceived`. The bridge preserves march/train IDs as decimal strings instead of Lua doubles, derives `battleWon = not battleData.topPlayerWin`, and treats every post-send timeout as non-retryable ambiguous state. The protocol remains internal and **does not enable `map_truck_plunder_schedule`**; reward-object/history reconciliation and durable scheduling are still pending. No live robbery was attempted. Evidence: [Truck quick-rob protocol](../evidence/lwbridge-implementation/2026-09-19-r7-truck-quick-rob-protocol.json).

**Scheduled Plunder persistence/listing is IMPLEMENTED/OFFLINE-TESTED (`LWB-R7-041`; current boundary audited by `LWB-R7-079`).** The rebuild reproduces the original combined `map_plunder_jobs_list` envelope over persisted Dispatch jobs plus Truck active/history rows, including recovered active-first ordering and scheduler metadata (`scheduleStatus`, attempts/error, scheduled/update time, execute/plunder time). `map_truck_plunder_cancel({serverId,trainUuid})` applies the recovered SQL transition only to `scheduled`/`waiting_connection`, clears `last_error`, persists across reopen, and returns `NOT_FOUND / scheduled truck job not found` when no cancellable job remains. R7-041’s then-current statement that `map_truck_plunder_schedule` stayed fail-closed is historical: R7-042–046 subsequently supplied the current-v19 executor/result/archive/worker/public contract, so Truck public scheduling is now enabled but live robbery acceptance remains deliberately unclaimed. Dispatch scheduling is still unimplemented; R7-080 subsequently enables/offline-proves public local-scheduler cancel.

**Moving Truck Follow is now LIVE-PROVEN (`LWB-R7-040`).** Original 0.3.1 recovery pins `map_march_follow({serverId,marchUuid})`, exact `INVALID_MARCH / server ID and march UUID are required`, native `gotoWorldMarch`, and a 5,000 ms native timeout with no frontend coordinates/world/type arguments. Current-v19 `GoToUtil.JumpToMarchByUuid(marchUuid,serverId,0)` follows loaded marches directly and uses `MsgDefines.GetMarchPos` plus `GetMarchPosMessage -> MoveToWorldMarchAndOpen` when the march is not locally loaded. A separate live defect was fixed at the map-index boundary: every `map_search` row now receives its authoritative indexed `server_id`, so the recovered stale-server guard no longer blocks row actions on otherwise valid Truck/Train rows. A rendered Truck **Follow** click then returned a correlated `proven` result on the current server through `GoToUtil.JumpToMarchByUuid`, with a real moving-march position observed. Current Railway population was empty, so positive Railway Follow remains population-pending rather than claimed. Evidence: [moving-march Follow proof](../evidence/lwbridge-implementation/2026-09-19-r7-moving-march-follow.json).

**Auto Scan core scheduler acceptance is now LIVE-PROVEN (`LWB-R7-039`).** A real two-target cycle scanned both configured targets in order with the shared Fast Zombie Boss worker (2,500/2,500, zero failed/unread for each) and returned to the original server. Disabling Auto during the first target let that scan finish and skipped the remaining target. After a full desktop/game restart, a persisted future `nextRunAt` fired automatically 741 ms after its deadline with no Run Now or Auto UI interaction and advanced the next schedule. A controlled frontend-only `server_jump` failure proves scheduler catch/finally behavior without claiming native travel failure. Evidence: [Auto scheduler completion proof](../evidence/lwbridge-implementation/2026-09-19-r7-auto-scheduler-complete.json). Navigation-away survival and a naturally occurring native travel failure remain limited gaps.

**The actual Auto Scan parent scheduler is now LIVE-PROVEN for repeated cross-server Zombie Boss cycles (`LWB-R7-038`).** The parent sanitizer now retains the dedicated `zombie_boss` selection instead of silently falling back to the recovered Auto defaults. A fresh desktop/game session initializes unknown idle Map Scan server state from the exact owned heartbeat before any jump/scan; successful `server_jump` also updates shared status immediately. Two consecutive real UI Run-now cycles each started on the original server, jumped to a different target, completed 2,500/2,500 Fast Zombie Boss blocks with zero failed/unread, returned to the original server, and left status correctly on the original server before the next cycle. Aggregate-only evidence: [actual Auto scheduler proof](../evidence/lwbridge-implementation/2026-09-19-r7-auto-scheduler-cross-server.json). One multi-target-list cycle plus disable/stop/failure behavior remain open; naturally elapsed unattended scheduling after full app restart is not separately live-proven.

**Cross-server travel is now TECHNICALLY LIVE-PROVEN and the first Auto Scan backend cycle is LIVE-PROVEN (`LWB-R7-037`).** Production `server_jump({serverId})` now uses the owned current-v19 Overview session, recovered current-client `GoToServerPreCheck` plus `CrossServerUtil.OnCrossServer/OnBackSelfServer`, and authoritative `GetCurServerId()` arrival proof. The recovered original invalid-ID, busy-operation, and timeout public contracts are preserved. A bounded live run proved same-server no-op followed by **original -> different target -> original**. A second owned run then executed the Auto Scan backend sequence `server_jump(current) -> map_scan_start(fast,zombie_boss)` and completed 2,500/2,500 with zero failed/unread blocks in **19.363 s**, publishing **56 Zombie Bosses**. This closed the travel blocker and proved reuse of the Manual engine; the actual parent scheduler and return-status portion is subsequently live-proven by `LWB-R7-038`. Evidence: [server jump + Auto Zombie cycle](../evidence/lwbridge-implementation/2026-09-19-r7-server-jump-auto-zombie-cycle.json).

**Mixed/all-eight shared Manual Scan is now LIVE-PROVEN (`LWB-R7-036`).** The production service accepts any mixture of the recovered original eight Map Data kinds in one shared run; dedicated Zombie Boss remains a separate ninth kind and is mutually exclusive because its normalizer intentionally emits the dedicated dataset only in a standalone run. A same-owned-session mixed Normal -> all-eight Normal proof completed 2,500/2,500 twice with zero failed/unread, and a second same-owned-session all-eight Normal -> Fast proof completed 2,500/2,500 twice at recovered concurrency 8 / 20. The final Fast publication reopened with exact per-kind counts. Manual multi-select and Normal/Fast controls are restored. Shared Manual selection/mode acceptance is closed; Auto Scan is next. Ghost positive-row proof remains population-dependent. Evidence: [mixed/all-eight Manual proof](../evidence/lwbridge-implementation/2026-09-19-r7-mixed-all-eight-manual.json).

**Treasure ordinary Manual Start is now LIVE-PROVEN (`LWB-R7-035`).** Current-v19 Treasure-only Normal Start completed 2,500/2,500 with zero failed/unread blocks in 123.150 s and published/reopened exactly one authentic live Treasure row. The row was native point type 21 `TreasurePointInfo`, preserved the recovered ordinary Treasure type/source identity, and carried source-backed rewarded/digging/reward-max/remaining-box counts. Production also supports the separately recovered point type 27 `WorldSuppliesPoint` + `TableName.LWIceSupplies` shape with strict mutually exclusive `treasureType`/`suppliesType` validation; this live population contained zero Supplies rows, so positive Supplies ingestion remains live-unobserved rather than fabricated. Exact v19 compatibility/restoration and cleanup passed. Evidence: [Treasure full Manual proof](../evidence/lwbridge-implementation/2026-09-18-r7-treasure-full-manual.json).

**Resource authoritative full-state/filter acceptance is LIVE-PROVEN (`LWB-R7-034`).** Current-v19 Resource-only Normal Start completed 2,500/2,500 with zero failed/unread in 131.208 s, published/reopened 838 rows, and supplied type/level/name/max/occupancy/black-tile classification on every row. All 782 final idle rows received authoritative `WorldGetDetail` amounts: 696 full, 86 partial, zero invalid pairs; the default Idle+Full+Exclude-black query returned 696. Monster-style LOD2 returned zero Resource rows and is therefore rejected for complete Resource discovery. Async detail requests overlap the LOD0 sweep. A Lua 5.3 bootstrap regression caused by 201 top-level locals was fixed by collapsing Resource detail state/helpers to one runtime table (192 locals) and is now guarded deterministically. Evidence: [Resource full-truth acceptance](../evidence/lwbridge-implementation/2026-09-18-r7-resource-full-truth.json).

**Current Home package is now Lua v19 and its stale-version-marker compatibility regression is corrected (`LWB-OVR-018`).** The untouched launcher applied `19 <- 18`, then independently reported local Lua 19 current against remote 19 while LocalLow `version.txt` remained 18. Policy `lwbridge-current-client-critical-anchors-2` therefore treats the LWLF header plus exact size/CRC metadata and unchanged recovered executable/xLua/managed/critical-Lua anchors as authoritative; `version.txt` is still required and numeric, may lag, and may never exceed the package header. Real manual and auto-start Overview cycles both reached Connected and closed/restored cleanly on v19. Aggregate evidence: [v19 marker compatibility](../evidence/lwbridge-implementation/2026-09-18-v19-version-marker-compat.json).

**Railway/Train ordinary Manual Start is now LIVE-PROVEN (`LWB-R7-031`).** On current v19, a Railway-only Normal run completed all 2,500 logical blocks with zero failed/unread blocks in 112.327 s, published one authentic Railway row and reopened exactly one. That live row carried source-backed train type, quality, power, config/raw train data, arrival time, robbery count, march identity and protection time. This dynamic population exposed no `maxLootCount` or nonempty `currentGoods`, so neither is claimed or synthesized. Exact v19 compatibility/restoration and cleanup passed. Dispatch/Secret Task is next on this same scanner. Evidence: [Railway full Manual proof](../evidence/lwbridge-implementation/2026-09-18-r7-railway-full-manual.json).

**Ghost Ops scanning is IMPLEMENTED/OFFLINE-TESTED but positive-row LIVE acceptance is pending (`LWB-R7-033`).** Current-v19 recovery pins world point type 29 `GHOSTRECON_POINT`, native `GhostreconPointInfo`, `WorldPointManager.GetGhostreconPointInfoByIndex`, and `TableName.LwGhostreconTask`. A strict Ghost-only Normal live run reached the completed 2,500-block publication gate with zero failed/unread blocks and exact LOD0/10x100 restoration, but the current map contained zero Ghost rows; the harness therefore failed rather than publishing an empty acceptance. Build and all six deterministic groups pass. Mixed/all-eight acceptance is closed under `LWB-R7-036`; Auto Scan is next while Ghost still awaits a positive population. Evidence: [Ghost live-pending proof](../evidence/lwbridge-implementation/2026-09-18-r7-ghost-live-pending.json).

**Dispatch/Secret Task ordinary Manual Start is now LIVE-PROVEN (`LWB-R7-032`).** On current v19, a Dispatch-only Normal run completed all 2,500 logical blocks with zero failed/unread blocks in 105.782 s, published 74 authentic Secret Task rows and reopened exactly 74. Every row preserved point type 17, `HeroDispatchMissionPointInfo`, config ID, level, quality, special flag and exact source identity; all 74 also carried mission timing/owner/list/name-key fields, while alliance ID was present on 70. Exact v19 compatibility/restoration and cleanup passed. Ghost Ops remains live-pending under `LWB-R7-033`; Treasure is subsequently LIVE-PROVEN under `LWB-R7-035`, so mixed/all-eight acceptance is next. Evidence: [Dispatch full Manual proof](../evidence/lwbridge-implementation/2026-09-18-r7-dispatch-full-manual.json).

**The v18 auto-update regression remains corrected.** `LWB-OVR-017` proves the official `18 <- 17` delta failed only when the cached settle path let the launcher see LWBridge-modified candidate bytes; the same patch succeeded immediately against restored untouched v17. Current v18 package `e4f875a8...d9d2a1` passes all recovered critical compatibility anchors. The lifecycle now self-heals this exact CRC condition with one restore -> forced untouched settle/update -> compatibility recheck -> helper retry, while unrelated failures remain fail-closed.

**Home current-v17 regression is TECHNICALLY LIVE-PROVEN and Map Data work has resumed.** `LWB-OVR-015` replaces the obsolete exact Lua-package pin with a fail-closed critical-anchor compatibility check. Current content v17 passed both manual Launch/Close and Open-games-at-startup/Close production lifecycle cycles with exact v17 restoration. The owner then returned to Map Data and reported live scan-speed/progress/Stop defects; `LWB-R7-012` below closes that checkpoint.

**Monster/Zombie Boss repeated-scan recovery is now LIVE-PROVEN (`LWB-R7-030`).** Owner-reported scans could suddenly crawl because an LOD2 error silently entered the old LOD0 coverage path, and one failed run left the AOI grid at 1000/1 after svLod/camera had returned to normal. Production is now fast-only for Monster/Zombie Boss and gives zoom restoration one bounded native `WorldPointManager.UpdateLWAoi_Normal(true)` recovery attempt. Two scans in one owned game session completed 2,500/2,500 in 38.414 s and 30.715 s with exact 10x100 restoration. A final clean production run naturally exercised the recovery branch and still completed 2,500/2,500 in 41.053 s, recovering 10x100/LOD0 with the camera stable. Exact v18 restoration/compatibility and cleanup passed.

**Monster transient same-session recovery is now LIVE-PROVEN (`LWB-R7-027`).** A failure-first 7-second forced readiness pause reproduced 94 `game connection unavailable` block failures at 2,406/2,500. The corrected source retains the exact full-world run/session anchor across instantaneous readiness loss without weakening stale-session rejection. Repeating the same pause completed 2,500/2,500 with zero failures in 18.294 s, published/reopened 6,912 Monster rows with Distance on every row, and produced 12/12 required Zombie Boss detail replies with 12 active deadlines. Exact v18 restoration/compatibility and cleanup passed.

**Monster whole-world fast scan is now LIVE-PROVEN (`LWB-R7-026`).** Current-v18 can provide the complete Monster march population through one native coarse LOD2 world snapshot and restore the exact original camera/LOD afterward. Two ordinary Normal Monster runs completed 2,500/2,500 with zero failures in **11.418 s** and **10.903 s**, publishing/reopening 7,271 / 7,194 dynamic rows with Distance on every row. Automatic Zombie Boss detail remained first-scan/no-click: 10/10 and 9/9 required replies produced 10 and 9 active future deadlines. At that R7-026 checkpoint invalid/unavailable LOD2 acquisition still fell back to conservative exact LOD0 coverage; `LWB-R7-030` supersedes that behavior with fast-only failure plus bounded native AOI recovery.

**Zombie Boss first-scan Remaining is now LIVE-PROVEN (`LWB-R7-025`).** A fresh current-v18 Normal Monster run required no Jump, manual boss click or priming second scan and completed 2,500/2,500 with zero failures in 108.144 s. It published/reopened 4,735 Monsters, found 21 eligible Zombie Bosses, issued only 2 serialized protection-detail requests, received 2/2 authoritative replies and persisted 2 future shield deadlines. The run also live-measured the v18 native footprint at 3x10 AOIs, replacing the v17 5x10 full-world assumption with a conservative 340-request exact-coverage planner. Monster is no longer the category gate; Railway/Train is next.

**Zombie Boss positive protection deadlines are now LIVE-PROVEN in the backend.** `LWB-R7-023` records a fresh current-v18 ordinary Monster scan completing 2,500/2,500 with zero failures in 73.206 s and publishing 13,462 Monster rows. Of 97 exact Invasion Zombie Boss rows, 40 received authoritative protected replies and all 40 persisted positive protection deadlines; six were still future at publication. The then-current final retry sent 57 retries for the 57 unanswered bosses; R7-024 later supersedes overlapping retry sends after recovering the message's shared-UUID correlation constraint. The recovered frontend generator check and local-only Monster countdown browser regression pass. Owner-visible positive Remaining is still pending one normal UI observation.

**Black-screen bootstrap regression is fixed and LIVE-PROVEN.** `LWB-R7-022` traces the owner black screen to one stale `M.Pump()` call to the removed `pump_monster_protection_batch_retry` helper. The call is removed, a deterministic guard pins the replacement retry path, and two current-v18 lifecycle cycles (manual and auto-start) both reached Connected and closed cleanly. Exact original v18 is restored. The active functional gate returns to the R7-021 Zombie Boss positive Remaining retest.

**Zombie Boss active Remaining extraction is now the active owner gate.** `LWB-R7-021` uses the owner's successful 2,500/2,500 scan plus screenshot to isolate the final defect: authoritative `isProtected=true` replies could still carry deadline `0` because the local manager lost the boss object before the reply arrived. The scanner now preserves the source-backed `createTime + monster_invasion.k12 * 1000` deadline while loaded and applies it only to authoritative protected replies; the unconditional 40 ms duplicate retry is replaced with one final retry for still-unanswered UUIDs. Owner positive-timer retest is pending.

**Historical Monster v18 scan-resilience/detail-transport checkpoint.** `LWB-R7-018/019` recover the nonblocking protection-detail transport and prove a technical 2,500/2,500 run in 82.962 s. `LWB-R7-020` diagnoses the later ordinary UI validator failure caused by cross-AOI UUID carry-over. Subsequent R7-021/023 runs supersede the pending-scan statement: ordinary Normal Monster scanning now completes 2,500/2,500 with zero failures; only owner-visible positive Remaining remains pending.

**Player City is LIVE-PROVEN through the selected owner-visible path.** [`LWB-PC-001`](../evidence/lwbridge-implementation/2026-09-13-player-city-live-proof.json) records two distinct fresh city-only current-client acquisitions, correct active-profile/server persistence, normal `map_search`/summary, a strictly newer second capture, exact restoration and clean ownership. [`LWB-PC-002`](../evidence/lwbridge-implementation/2026-09-13-player-city-reopen-proof.json) separately reopens the same profile/store and returns the saved city through normal `map_search`. [`LWB-PC-003`](../evidence/lwbridge-implementation/2026-09-13-player-city-owner-check.json) records the owner-visible normal Map Data row, exact-session Search/render correlation, clean shutdown, and the fact that initial page load performs an automatic saved-data City query before the manual Search click.

**The Review 18 owner check is complete.** Attempt `20260913T042954Z-c20cd9c6-2b81ad37` used the reviewed read-only package, showed the persisted Player City row in the normal page, correlated the later owner Search to that row, exited cleanly and recorded no identity leak. No fresh scan/game action was part of the owner check. `LWB-R7-004` LIVE-PROVES source-backed current-v16 context, arbitrary Resource blocks, ordinary production Start, one real Normal block and bounded Stop-to-idle. `LWB-R7-005` LIVE-PROVES arbitrary Player City block coverage, including fresh zero-city AOI cells that stay on the requested block instead of invoking the legacy home-city fallback. [`LWB-R7-006`](../evidence/lwbridge-implementation/2026-09-15-r7-player-city-footprint-proof.json) now LIVE-PROVES current-v16 Player City AOI-footprint reuse: both a populated 20x20 block and an empty edge block satisfied all four planned 10x10 cells from exactly one fresh current-view response using `WorldPointManager._curViewIndex`. `LWB-R7-007` separately LIVE-PROVES the dedicated fast Player City world scan. `LWB-R7-008` LIVE-PROVES Player City through the **ordinary Manual Start production path**: 10,000 AOIs / 2,500 logical blocks, zero failed blocks, 2,592 Cities published/reopened and 51.524 s scan wall time. `LWB-R7-009` adds truthful native acquisition progress plus owner selection/Jump fixes. [`LWB-R7-010`](../evidence/lwbridge-implementation/2026-09-16-r7-manual-full-monster-proof.json) now LIVE-PROVES Monster-only ordinary Manual Start over the same full-world coverage, publishing/reopening 6,934 game-classified monsters/bosses in 71.161 s. Monster acquisition is generic and not tied to Zombie Invasion. `LWB-R7-011` additionally restores the reference-backed offline `lastwar_localize` cache path and offline-proves readable Monster names plus UI-language re-localization without committing game-owned locale payloads. [`LWB-R7-012`](../evidence/lwbridge-implementation/2026-09-16-r7-map-scan-speed-stop.json) then LIVE-PROVES the current-v17 `5x10` native AOI footprint, reduces full-world acquisition from 250 to 200 requests while retaining the exact 10,000-cell gate, completes Monster runs in 43.620 s Normal / 70.746 s Fast under different live populations, and repairs production progress/summary plus one-Stop-to-idle integration. [`LWB-R7-013`](../evidence/lwbridge-implementation/2026-09-17-r7-monster-usability.json) records the first Monster result-usability slice. [`LWB-R7-014 / LWB-OVR-016`](../evidence/lwbridge-implementation/2026-09-17-r7-owner-map-fixes.json) supersedes its presentation policy: Remaining is shield-only, Monster level is `<= selected`, Level/Distance sorting is wired, live current-v17 Distance is populated for every row in the latest 12,731-row full-world proof, camera restoration occurs in the same request callback, and unchanged starts reuse exact official-settlement evidence instead of doing the visible double-launch warm-up. [`LWB-R7-015`](../evidence/lwbridge-implementation/2026-09-17-r7-resource-full-manual.json) LIVE-PROVES Resource-only ordinary Manual Start on current v17: 2,500/2,500 logical blocks, zero failures, 442 Resources published/reopened in 70.958 s Normal, with source-backed type/level/occupancy fields on every row in that population. [`LWB-R7-016`](../evidence/lwbridge-implementation/2026-09-17-r7-truck-full-manual.json) now LIVE-PROVES Truck-only ordinary Manual Start on the same planner: 2,500/2,500 logical blocks, zero failures/unread blocks, 175 Trucks published/reopened in 71.321 s Normal, with authoritative type/quality/power/train data and normalized `arriveTs`/`robTimes` on every row in that live population. Railway/Train is next, followed by Dispatch/Secret Task, Ghost Ops, Treasure, mixed selections and all eight. S03/S06 remain deferred and S02 unfinished/unassigned.

### Retained earlier resource/PM15 checkpoint — not the active assignment


> Review 15 at `154ce35` accepted the owner no-context result and returned PM15-01/02. Web implemented both repairs at `1e9af6e`; `LWB-PM15-001` is now **READY_FOR_PM_REVIEW** with isolated regressions and exact package identity. Do not repeat the owner check. Fresh acquisition remains SB-97 blocked. No Daybreak task. [Current review](reviews/2026-09-11-review-15-owner-evidence.md) and [Web prompt](team-workflow.md).


This directory contains current LWBridge reconstruction and official-client evidence. Start here instead of browsing files by date or guessing which notes are authoritative.

**Mandatory before working:** [../AGENTS.md](../AGENTS.md) defines the user's repository-wide reverse-engineer-first, no-invented-values, immediate-finding-documentation and checkpoint commit/push rules. Every AI and contributor must follow them. New findings require source identity/hash, an exact locator, reproduction steps, evidence status and limitations; update this index and the feature ledger when adding durable material.

## Current priorities

**Current team:** Web handles implementation and automatic technical capture; the owner supplies only guided screenshots/descriptions; PM audits. No separate Sol task. Player City `LWB-PC-001/002/003` is closed for the selected live-acquire/persist/Search/reopen/owner-visible path. Do not repeat the owner check merely because the page auto-loads saved data. The active work is the shared Manual Scan engine documented in [`map-data-delivery.md`](map-data-delivery.md) and [`team-workflow.md`](team-workflow.md): build the common world-scan lifecycle first, prove it with Player City, then enable Resource and the remaining kinds on that same worker. SB-97 remains specific to the denied Resource operation. Daybreak is unassigned.

[Review 14](lwbridge-project-status.md) audits `7ca6d5c`. Four resource repairs are closed at stated scopes; PM13-04 remains the final fresh resource-only normal-window test. **Controlled-test candidate, not a completed live feature or two-page release.** Use [the concrete test checklist](user-test-checklist.md) and [regular handoff/prompts](implementation-handoff.md). Computer Use capability testing is separate from the real-game result and does not clear SB-97.

[Review 13 and contributor follow-ups](reviews/2026-09-11-review-13-and-followups.md) preserve prior findings. PM12-A/B/C bounded acceptance remains; PM12-D is open. Monster acquisition/search is next only after resource acceptance. No Daybreak task; original-pipe/updater/export expansion remains deferred.

## Reading order

Start with the active [`map-data-delivery.md`](map-data-delivery.md) and [`team-workflow.md`](team-workflow.md), then [review 18](reviews/2026-09-13-review-18-player-city.md), [current PM status](lwbridge-project-status.md), [restriction outcomes and requests](daybreak-escalations.md), and [the rough completion estimate](lwbridge-completion-estimate.md). Read [the regular-AI task](implementation-handoff.md) or [the specialist task](deep-binary-handoff.md) as appropriate. The [review 8/follow-up narrative](reviews/2026-09-10-review-8-and-followups.md) and earlier reviews are historical. Clock/keys/cleanup and option source/no-alliance subquestions are resolved. ESC-003 has a reviewed NOT_ASSIGNED decision; public integration and serializer/export/schema/lifecycle gaps remain tracked.

1. [`lwbridge-feature-ledger.md`](lwbridge-feature-ledger.md) — current implementation/proof matrix for Overview and Map Data.
2. [`lwbridge-overview-recovery.md`](lwbridge-overview-recovery.md) — O04/O05 original recovery contracts, current-client observation mapping and rebuild recovery-core validation.
3. [`official-runtime-architecture.md`](official-runtime-architecture.md) — current installed Last War runtime baseline and official launcher observations.
4. [`lwbridge-architecture.md`](lwbridge-architecture.md) — recovered LWBridge application/runtime architecture.
5. [`lwbridge-ui.md`](lwbridge-ui.md) — recovered frontend reproduction and visual verification.
6. [`lwbridge-injection.md`](lwbridge-injection.md) — recovered bootstrap/injection lifecycle, fail-closed states, descriptor checkpoint, and remaining gaps.
7. [`lwbridge-map-scan.md`](lwbridge-map-scan.md) — recovered Map Scan request/native-capture contract and remaining gaps.
8. [`lwbridge-artifact-evidence.json`](lwbridge-artifact-evidence.json) — machine-readable artifact evidence supporting recovered LWBridge structure.

## Planning

- [`../task.md`](../task.md) is the primary AI instruction file and full Overview + Map Data acceptance specification.
- [`../BACKLOG.md`](../BACKLOG.md) is the current progress checklist, renamed from `TASKS.md`. Do not recreate the old name or duplicate the requirements.

- [Regular task](implementation-handoff.md) and [specialist task](deep-binary-handoff.md) provide distinct ownership/delivery instructions; [ESC register](daybreak-escalations.md) records requests, reasons and decisions. Both tasks inherit `task.md`.

## Visual and machine-readable evidence

- [Auto scheduler completion proof](../evidence/lwbridge-implementation/2026-09-19-r7-auto-scheduler-complete.json) records `LWB-R7-039`: one real multi-target cycle, disable-during-cycle skip behavior, deterministic scheduler failure/finally proof, and persisted clock-triggered execution after full desktop/game restart with no Run Now click.
- [Actual Auto scheduler cross-server proof](../evidence/lwbridge-implementation/2026-09-19-r7-auto-scheduler-cross-server.json) records `LWB-R7-038`: dedicated Zombie Boss config persistence, fresh-session heartbeat server initialization, two consecutive real UI scheduler cycles, shared-scanner 2,500/2,500 completion, configured return-to-original, truthful post-return status, restored user Auto config and clean owned-game stop.
- [Mixed/all-eight Manual proof](../evidence/lwbridge-implementation/2026-09-19-r7-mixed-all-eight-manual.json) records `LWB-R7-036`: mixed and all-eight same-session runs, repeated Normal/Fast all-eight completion, exact reopen counts, restored multi-select UI, and dedicated Zombie Boss exclusivity.
- [Treasure full Manual proof](../evidence/lwbridge-implementation/2026-09-18-r7-treasure-full-manual.json) records `LWB-R7-035`: current-v19 2,500-block Treasure completion, one authentic ordinary Treasure row with exact reopen equality and source-backed claim-count fields, strict recovered Supplies shape support, and an explicit zero-live-Supplies limitation.
- [Resource full-truth acceptance](../evidence/lwbridge-implementation/2026-09-18-r7-resource-full-truth.json) records `LWB-R7-034`: current-v19 2,500-block Resource completion, authoritative remaining/full amounts for every final idle row, level/name/max/occupancy/black-tile coverage, default full-resource filtering, the rejected zero-row LOD2 shortcut, asynchronous detail proof and the Lua-local bootstrap guard.
- [Monster/Zombie Boss fast-only recovery acceptance](../evidence/lwbridge-implementation/2026-09-18-r7-monster-fast-only-recovery.json) records `LWB-R7-030`: no slow LOD0 fallback, same-owned-session repeated-scan acceptance, bounded native AOI restore recovery, a final clean run that naturally exercised recovery, exact v18 compatibility and clean lifecycle restoration.
- [Monster transient-readiness resume acceptance](../evidence/lwbridge-implementation/2026-09-18-r7-monster-transient-readiness-resume.json) records `LWB-R7-027`: failure-first 7-second same-owned-session readiness loss, the early full-world resume-anchor correction, corrected 2,500/2,500 zero-failure publication/reopen, exact v18 restoration and fail-closed stale-session ownership.
- [Monster LOD2 fast-scan acceptance](../evidence/lwbridge-implementation/2026-09-18-r7-monster-lod2-fast-scan.json) records `LWB-R7-026`: one native whole-world LOD2 Monster snapshot with exact LOD0/camera restoration, repeated 11.418 s / 10.903 s production scans and automatic active Zombie Boss deadlines. Its historical LOD0 fallback is superseded by `LWB-R7-030` fast-only recovery.
- [Monster first-scan Remaining acceptance](../evidence/lwbridge-implementation/2026-09-18-r7-monster-first-scan-remaining.json) records `LWB-R7-025`: current-v18 3x10 footprint correction, one fresh no-click/no-Jump/no-priming Normal scan, 2/2 serialized Zombie Boss protection replies, two active future deadlines, exact publish/reopen equality and clean official-v18 restoration.
- [Zombie Boss serialized-detail recovery](../evidence/lwbridge-implementation/2026-09-18-r7-zombie-boss-serial-detail.json) records `LWB-R7-024`: exact current-v18 MsgMap/SFSNetwork/detail-message hashes and locators, the shared `_uuid` correlation constraint, serialized background enrichment, removal of the per-AOI wait, and green offline validation. Owner first-scan live proof remains pending.
- [Automatic Zombie Boss protection batch evidence](../evidence/lwbridge-implementation/2026-09-17-r7-zombie-boss-protection-batch.json) records `LWB-R7-019`: current-v18 click/request/response recovery, run-bound automatic batching, removed per-AOI waits, the rejected dwell experiment, final 82.962 s complete run, 81 requests/retries and 50 authoritative replies, plus the absence of an active positive deadline in that dynamic population.
- [Monster v18 scan-resilience evidence](../evidence/lwbridge-implementation/2026-09-17-r7-monster-protection-scan-resilience.json) records `LWB-R7-018`: the owner-observed 4.5% reset loop, persisted protection-timeout failure, non-blocking optional-detail correction, stale-correlation cleanup, no-unrelated-shield fallback, v18 restoration/compatibility and green offline validation. `LWB-R7-019` supplies the subsequent live completion proof.
- [v18 auto-update recovery evidence](../evidence/lwbridge-implementation/2026-09-17-v18-auto-update-recovery.json) records `LWB-OVR-017`: the modified-base 18<-17 CRC failure, successful untouched official v18 application, unchanged critical anchors, bounded one-retry recovery implementation and green offline validation.
- [Home v17 automatic-compatibility evidence](../evidence/lwbridge-implementation/2026-09-16-home-v17-auto-compat.json) records `LWB-OVR-015`: current v17 identities/critical anchors, fail-closed compatibility policy, two real manual/startup lifecycle cycles, exact Close/restoration, and the structured `GAME_UPDATE_UNSUPPORTED` fallback for a future incompatible update.
- [Map Data speed/Stop acceptance](../evidence/lwbridge-implementation/2026-09-16-r7-map-scan-speed-stop.json) records `LWB-R7-012`: current-v17 5x10 footprint measurements, 200-request exact-coverage planner, two live full-Monster timings, dedicated single-Stop-to-idle proof, and the status/summary integration repair.
- [Resource full Manual acceptance](../evidence/lwbridge-implementation/2026-09-17-r7-resource-full-manual.json) records `LWB-R7-015`: current-v17 Resource-only 2,500-block completion, zero failures, exact publish/reopen equality, source-backed type/level/gather occupancy aggregates, and final compatibility/cleanup validation.
- [Monster result usability evidence](../evidence/lwbridge-implementation/2026-09-17-r7-monster-usability.json) records `LWB-R7-013`: sanitized current-v17 timer-field aggregates plus the offline/browser-proven localized visible-name search, exact-level filtering/options and ticking countdown implementation.
- [Owner Monster/Home correction evidence](../evidence/lwbridge-implementation/2026-09-17-r7-owner-map-fixes.json) records `LWB-R7-014` / `LWB-OVR-016`: shield-only Remaining, inclusive-maximum Monster level filtering, working Level/Distance sorts, authoritative home-tile/Distance live proof, same-tick camera restoration and exact-package official-settle warm-up reuse.
- [Monster localization recovery](../evidence/lwbridge-implementation/2026-09-16-r7-monster-localization.json) records `LWB-R7-011`: verified reference command/manifest contract, fail-closed cache extraction/loading, real-cache backend proof, and synthetic Map Data English -> `zh-CN` re-localization proof with no committed game-owned locale payloads.
- [Review 18 Player City audit](reviews/2026-09-13-review-18-player-city.md) accepts implementation `7da8665` / CI `34726315539` for the read-only saved-row owner check, while retaining the targeted-city fallback, Resource SB-97, deferred Overview work and non-Player-City Map Data as open.
- [Player City owner-visible acceptance](../evidence/lwbridge-implementation/2026-09-13-player-city-owner-check.json) records `LWB-PC-003`: exact reviewed build identity, owner attempt/session completion, two sanitized City Search/render pairs, same saved city/profile, clean cleanup, and the automatic initial saved-data query that explains why the row is visible before pressing Search.

- [Overview owner acceptance evidence](../evidence/lwbridge-implementation/2026-09-11-ovl-owner-acceptance.json) records `LWB-OVL-003`: the normal Overview owner session, exact tested package identity, automatic readiness/Close/restoration bundle hashes, owner screenshot digest/dimensions, and explicit "yup all working" acceptance. This closes only the selected Launch/message/Close milestone; wait for the owner to choose the next feature.

- [Overview live lifecycle evidence](../evidence/lwbridge-implementation/2026-09-11-ovl-live-lifecycle.json) records `LWB-OVL-002`: implementation `dc82bc9`, CI `34603661402`, two distinct technical live Launch/ready/message/Close/restore cycles, exact current-client/package/build hashes, automatic per-session capture, and the remaining owner-visible normal Overview gate.
- [PM13 saved-browse context evidence](../evidence/lwbridge-implementation/2026-09-10-pm13-saved-browse-context.json) records `LWB-PM13-001`: the recovered frontend summary -> scanState -> search server flow, the single-server saved-context implementation policy and zero/multiple-server/profile-isolation/reopen tests. Its original direct-root absence blocker is superseded by `LWB-PM13-001B`.
- [PM13 saved-target rediscovery/reopen evidence](../evidence/lwbridge-implementation/2026-09-10-pm13-saved-target-rediscovered.json) plus [DOM/screenshot evidence](../evidence/lwbridge-implementation/pm13-saved-reopen/) records the exact package-local config/database hashes, source-backed server-2212 row, two ordinary app sessions, explicit Resource Search after restart, and cleanup back to the direct-desktop prestate. This closes PM13-01 saved-display proof without claiming a new acquisition.
- [PM13 resource-feedback evidence](../evidence/lwbridge-implementation/2026-09-11-pm13-resource-feedback.json) records `LWB-PM13-002`: recovered formatter/catch locators, rebuild-only nine-language guidance, distinct empty/missing-context/query states, passing browser/backend/regression checks and the retained PM13-04 connected-window gate.
- [PM13 cancellation/ownership evidence](../evidence/lwbridge-implementation/2026-09-11-pm13-cancel-ownership.json) records `LWB-PM13-003`: prepare-before-commit, the atomic completion/cancel decision, helper-start cleanup ownership, four deterministic boundary races, preserved PM12 lifecycle checks and the retained PM13-02/04 gates.
- [PM13 proof-correlation evidence](../evidence/lwbridge-implementation/2026-09-11-pm13-proof-correlation.json) records `LWB-PM13-004`: explicit normal Resource Search after each acquisition, immutable tuple -> request/query -> exact row -> render correlation, adversarial stale/loading/empty rejection, passing full offline verification, and the retained PM13-04 live gate.
- [PM13 normal-window gate evidence](../evidence/lwbridge-implementation/2026-09-11-pm13-normal-window-gate.json) plus [screenshots/observations](../evidence/lwbridge-implementation/pm13-normal-window-feedback/) records `LWB-PM13-005`: ordinary-window repaired feedback/missing-context behavior, unchanged unsupported selection, no game/helper launch, exact direct-root cleanup, current tool-capability discovery, and the still-blocked SB-97 fresh-live gate.
- [PM13 reopen-proof correction evidence](../evidence/lwbridge-implementation/2026-09-11-pm13-reopen-proof-correction.json) plus [saved-reopen harness evidence](../evidence/lwbridge-implementation/pm13-saved-reopen-harness/) records `LWB-PM13-006`: recovered `x,y + Jump` coordinate-button shape, corrected production snapshot, real-shape browser regression, repeatable ordinary-window saved-reopen verifier, exact historical server-2212 validation and retained SB-97 two-read gate.
- [PM13 Computer Use capability evidence](../evidence/lwbridge-implementation/2026-09-11-pm13-computer-use-capability.json) records `LWB-PM13-007`: installed bundled skill/version/hash, its required `node_repl` + `@oai/sky` supported path, the current chat's missing `node_repl` bridge, empty global npm `@oai/sky`, and the retained no-reroute/SB-97 boundary.
- [PM13 owner evidence package](../evidence/lwbridge-implementation/2026-09-11-pm13-owner-evidence-package.json) records `LWB-PM13-008`: tested repo-root double-click collector, passive actual Resource `map_search` request/result + rendered-table correlation, profile/store/current-client/cleanup capture, exact Release build identity, beginner owner guide and retained SB-97 fresh-acquisition blocker.
- [PM13 owner no-saved-context result](../evidence/lwbridge-implementation/2026-09-11-pm13-owner-no-saved-context.json) records `LWB-PM13-009`: the owner attempt against reviewed build `2d3915d`, empty published-server/Resource state, normal `MAP_SAVED_CONTEXT_UNAVAILABLE` UI result, the collector's false `INCOMPLETE` cause, the repaired `COMPLETE_NO_SAVED_CONTEXT` branch, and retained SB-97 boundary.
- [PM15 owner-evidence hardening](../evidence/lwbridge-implementation/2026-09-11-pm15-owner-evidence-hardening.json) records `LWB-PM15-001`: distinct collector session/PID-owned reopen proof, explicit failed process/evidence health, seven isolated PM15 regressions, exact build/collector identity, and the unchanged no-owner-retest/SB-97 boundary. Supporting [regression output](../evidence/lwbridge-implementation/pm15-owner-evidence/isolated-regressions.json) and [package identity](../evidence/lwbridge-implementation/pm15-owner-evidence/package-identity.json) are durable.
- [PM16 process-incarnation correction](../evidence/lwbridge-implementation/2026-09-12-pm16-process-incarnation.json) records `LWB-PM16-001`: the pre-fix reused-PID/path reproduction, creation-time ownership policy, desktop/helper boundary regressions, fail-closed unknown identity handling and already-exited restoration path. PM16-02 is delivered/offline-tested at `9f18194`; PM16-01 follow-up is delivered at `c3d77e2`.
- [PM16 selected-root correction](../evidence/lwbridge-implementation/2026-09-12-pm16-selected-root.json) records LWB-PM16-002: the two pre-fix backend/lifecycle propagation failures, stopped journal-free rebind policy, active/repair retarget rejection, invalid/cancel preservation, exact-path process status, PowerShell 7.6.6 validation setup and the final green offline suite. PM16-01 is READY_FOR_PM_REVIEW; no new real-game run was required.
- [PM16 evidence reconciliation](../evidence/lwbridge-implementation/2026-09-12-pm16-evidence-reconciliation.json) records `LWB-PM16-003`: exact corrected-code CI/build/helper identity, successor O01/O06 dispositions, saved `LWB-OVR-012` disassembly/tool-version provenance and remaining S02/S03/S06 limits. The durable [saved disassembly excerpt](../evidence/lwbridge-implementation/2026-09-12-overview-repair-contract-saved-disassembly.txt) avoids inventing the unavailable historical generator command.
- [Overview recovery static evidence](../evidence/lwbridge-implementation/2026-09-12-overview-recovery-static.json), [offline recovery-core evidence](../evidence/lwbridge-implementation/2026-09-12-overview-recovery-core-offline.json), [event/update completion evidence](../evidence/lwbridge-implementation/2026-09-12-overview-recovery-event-update-offline.json), [owner O04/O05 acceptance](../evidence/lwbridge-implementation/2026-09-12-overview-owner-o04-o05-acceptance.json), [original repair contract](../evidence/lwbridge-implementation/2026-09-12-overview-repair-contract.json), [O01 closure](../evidence/lwbridge-implementation/2026-09-12-overview-game-root-closure.json), [O06 live repair closure](../evidence/lwbridge-implementation/2026-09-12-overview-repair-live.json), plus [recovery findings](lwbridge-overview-recovery.md) preserve `LWB-OVR-005` through `LWB-OVR-014`. Review 16 supersedes the former zero-open conclusion: PM16-01/02 are corrected, while whole-page S02/S03/S06 remain open.
- [OVL-01/02/03/04 offline lifecycle evidence](../evidence/lwbridge-implementation/2026-09-11-ovl-offline-lifecycle.json) plus [current-v14 UI recovery excerpt](../evidence/lwbridge-implementation/2026-09-11-ovl-current-ui-contract.txt) records `LWB-OVL-001`: explicit independent-policy launch/ready/renderer/close implementation, current UI root/TMP/Canvas source hashes and locators, deployed helper check-only proof, zero-warning Release build and deterministic stale/foreign/duplicate/ownership regressions. It is not LIVE-PROVEN; the real Overview cycle remains the active gate.
- [Web -> Sol current-client handoff fingerprint](../evidence/official-runtime/2026-09-11-web-sol-handoff.json) refreshes the read-only build-1078/v14 runtime identity used by the `READY_FOR_SOL` packet, including the exact package/xLua/Assembly hashes and current table identity; it is preparation evidence, not live feature proof.
- [PM12 lifecycle/result-integrity evidence](../evidence/lwbridge-implementation/2026-09-10-pm12-lifecycle-result-integrity.json) records `LWB-PM12-002`: duplicate Start/Stop/cancel/timeout/close behavior, immutable request-owned ingestion, source/server/time/build/restoration gates, passing deterministic/browser checks and the remaining first-session identity limit.
- [PM12 selected-session identity evidence](../evidence/lwbridge-implementation/2026-09-10-pm12-session-identity.json) records `LWB-PM12-003`: exact selected game-path/PID filtering, helper-owned launch-session/profile/PID correlation, removal of broad process termination, passing offline regressions and the remaining live/native-UI limit.
- [PM12 resource occupancy evidence](../evidence/lwbridge-implementation/2026-09-10-pm12-resource-occupancy-unknown.json) records `LWB-PM12-004`: recovered Idle/Gathering inference, the bounded source's missing occupancy proof, rebuild-only unknown rendering, removal of the replay-only DOM workaround, passing deterministic/frontend/browser checks and the remaining normal-window live proof gate.
- [PM12 live restoration blocker evidence](../evidence/lwbridge-implementation/2026-09-10-pm12-live-restoration-blocker.json) records `LWB-PM12-005`: a fresh helper-owned server-2212 resource result, exact profile/launch-session/game-PID correlation at the source/result boundary, the locked-file restoration failure, the preserved recovery state, and verified exact original-byte recovery after an operator-scoped normal close. The companion [request-owned result](../evidence/lwbridge-implementation/2026-09-10-pm12-live-restoration-blocker-result.json) is the immutable live source payload; the operator close is recovery evidence, not production functionality.
- [PM12 scoped-close/restored-import evidence](../evidence/lwbridge-implementation/2026-09-10-pm12-scoped-close-restored-import.json) records `LWB-PM12-006`: exact owned-PID/path revalidation, the rebuild-policy `Process.CloseMainWindow()` path with no force fallback, isolated regressions, two final-byte live closes/restores and successful desktop post-restore immutable import/search. The companion [raw two-read proof](../evidence/lwbridge-implementation/2026-09-10-pm12-live-restored-import.json) preserves the request/session/PID/source/restore envelopes.
- [PM12 source-backed resource occupancy evidence](../evidence/lwbridge-implementation/2026-09-10-pm12-resource-occupancy-source.json) records `LWB-PM12-007`: recovered LWBridge gather-field semantics, current `ResPointInfo` ownership, probe/import/frontend locators and hashes, passing offline checks, two final-byte live idle observations and exact restoration. The [final raw proof](../evidence/lwbridge-implementation/2026-09-10-pm12-resource-occupancy-live-proof.json) preserves both request/source/import/restore envelopes; the [first attempt](../evidence/lwbridge-implementation/2026-09-10-pm12-resource-occupancy-live-proof-attempt1.json) is retained as incomplete because its older probe bytes omitted the false occupied value.
- [PM12 normal-window proof-harness evidence](../evidence/lwbridge-implementation/2026-09-10-pm12-normal-ui-proof-harness.json) records `LWB-PM12-008`: recovered Map Data type ordering, the diagnostic two-click normal-window orchestration, successful offline validation, the prior live-launch restriction, and the separately recovered raw `resourceName` / public `resourceNameKey` endpoints with their numeric mapping deliberately left unknown.
- [Review 9 evidence](../evidence/lwbridge-implementation/2026-09-10-pm-review-9.json): eight-commit audit, fresh validation, attribution corrections and next tasks.
- [PM9 update comparison](../evidence/official-runtime/2026-09-10-pm9-update-comparison.json), [fresh runtime](../evidence/official-runtime/2026-09-10-pm9-official-runtime.json), [fresh update health](../evidence/official-runtime/2026-09-10-pm9-update-health.json): LWB-PM9-001 unchanged/changed source facts and limits.

- [Review 8](../evidence/lwbridge-implementation/2026-09-10-pm-review-8.json) — nine-commit audit, fresh checks and task decisions.
- [Update health](../evidence/official-runtime/2026-09-10-lua-update-health.json), [retry health](../evidence/official-runtime/2026-09-10-pm8-retry-update-health.json), [seventh-retry health](../evidence/official-runtime/2026-09-10-pm8-003-update-health.json), [recovered health](../evidence/official-runtime/2026-09-10-pm8-004-recovered-update-health.json), [recovery surface](../evidence/official-runtime/2026-09-10-pm8-recovery-surface.txt), [recovered runtime](../evidence/official-runtime/2026-09-10-pm8-004-official-runtime.json) and [recovery comparison](../evidence/official-runtime/2026-09-10-pm8-004-runtime-comparison.json) — LWB-PM8-001/002/003/004 failure history, official recovery surface, successful cleanup/reinstall/startup and exact unchanged/changed observations.

- [Review 7 evidence](../evidence/lwbridge-implementation/2026-09-09-pm-review-7.json) records the 15-commit audit, fresh checks, source hashes, limits and PM7 task/escalation decisions.

- [Review 6 evidence](../evidence/lwbridge-implementation/2026-09-09-pm-review-6.json) records R6-014–023 audit results, current restriction outcomes and the estimate basis.

- [PM6 host cleanup evidence](../evidence/lwbridge-implementation/2026-09-09-pm6-host-cleanup.json) records `LWB-PM6-001`: bounded owned-process cleanup, preserved termination errors, the deterministic failure-path self-test, normal host-probe revalidation and its test-only limits.

- [Review 5 evidence](../evidence/lwbridge-implementation/2026-09-09-pm-review-5.json) records combined R6 progress, PM5-01 correction, fresh checks and reported restrictions.

- [`ui-reproduction/`](ui-reproduction/) contains the current deterministic UI regression evidence.
- [`task-reference/`](task-reference/) contains the user-supplied Overview and Map Data visual references.
- [`../evidence/lwbridge-0.3.1/`](../evidence/lwbridge-0.3.1/) contains immutable recovered frontend evidence.
- [`../evidence/lwbridge-implementation/`](../evidence/lwbridge-implementation/) contains rebuild milestone evidence.
- [Review 4 evidence](../evidence/lwbridge-implementation/2026-09-08-pm-review-4.json) records the audited implementation, fresh check results, retained safety-review restrictions and precise proof limits.
- [`../evidence/lwbridge-implementation/2026-09-08-pm2-foundation-fix.json`](../evidence/lwbridge-implementation/2026-09-08-pm2-foundation-fix.json) records the PM2 repair source hashes, exact locators, commands, results and remaining limits.
- [`../evidence/lwbridge-implementation/2026-09-08-r3-native-host.json`](../evidence/lwbridge-implementation/2026-09-08-r3-native-host.json) records the real isolated WebView2 reload/session and slow-storage responsiveness checkpoint.
- [`../evidence/lwbridge-implementation/2026-09-08-r4-native-host-interactions.json`](../evidence/lwbridge-implementation/2026-09-08-r4-native-host-interactions.json) records the recovered picker result contract and the completed controlled native-WebView duplicate/preference/picker/closed-window interaction matrix.
- [`../evidence/lwbridge-implementation/2026-09-08-pm3-preference-fix.json`](../evidence/lwbridge-implementation/2026-09-08-pm3-preference-fix.json) records PM3-01/02: visible rejected-save feedback, confirmed-value rollback, deterministic rapid-save outcome matrix, strengthened real WebView host gates and recovery behavior.
- [`../evidence/lwbridge-implementation/2026-09-08-r5-launch-envelope-producer.json`](../evidence/lwbridge-implementation/2026-09-08-r5-launch-envelope-producer.json) records `LWB-R5-001`: exact outer-host `LaunchEnvelope` field/value locators, JSON serialization/handoff and the remaining proof/ticket/child-input blockers.
- [`../evidence/lwbridge-implementation/2026-09-08-r5-xlua-abi-selector.json`](../evidence/lwbridge-implementation/2026-09-08-r5-xlua-abi-selector.json) records `LWB-R5-002`: exact `LWXE1\n` export-fingerprint construction, ordinal/name ordering, SHA-256 selector, bundled secure/plain values and current-client secure classification.
- [`../evidence/lwbridge-implementation/2026-09-08-r5-launch-validation.json`](../evidence/lwbridge-implementation/2026-09-08-r5-launch-validation.json) records `LWB-R5-003`: child `LaunchEnvelope` parsing, recovered descriptor safety gates, two-segment launch-proof validation and `LWLT1`/`LWLT2` ticket grammar/timing, with producer/ownership/consumption gaps kept open.
- [`../evidence/lwbridge-implementation/2026-09-08-r5-launch-material.json`](../evidence/lwbridge-implementation/2026-09-08-r5-launch-material.json) records `LWB-R5-004`: outer `LeaseActivationResponse.launchProof` propagation, exact ticket-source labels, missing-ticket transition and cached launcher-report reuse/fallback fields, with signing/ownership/child-input gaps kept open.
- [`../evidence/lwbridge-implementation/2026-09-08-r5-ticket-consumption.json`](../evidence/lwbridge-implementation/2026-09-08-r5-ticket-consumption.json) records `LWB-R5-005`: child post-start ticket polling, exact replacement/timeout/failure branches and opaque comparison inputs, with caller identities, units and result variants kept open. The linked focused disassembly excerpt is durable evidence for the checkpoint.
- [`../evidence/lwbridge-implementation/2026-09-10-r5-control-pipe-contract.json`](../evidence/lwbridge-implementation/2026-09-10-r5-control-pipe-contract.json) records `LWB-R5-006`: exact per-user control-pipe prefix/suffix derivation, secure-proxy locator/hash identity, bounded original-host pipe-name correlation and the remaining session/framing/request-grammar gate.
- [`../evidence/lwbridge-implementation/2026-09-10-r5-session-frame-hello-contract.json`](../evidence/lwbridge-implementation/2026-09-10-r5-session-frame-hello-contract.json) records `LWB-R5-007`: hash-locked recovery of the 4-byte little-endian control-pipe frame, exact secure-proxy `hello` JSON shape, endpoint roles, host handshake/error marker boundary and the still-unrecovered `hello.ack`/heartbeat/request-result grammar.
- [`../evidence/lwbridge-implementation/2026-09-08-r6-map-query-frontend.json`](../evidence/lwbridge-implementation/2026-09-08-r6-map-query-frontend.json) records `LWB-R6-003`: exact frontend Map Data query-builder/sort/page/export-envelope locators plus the implemented default persisted-search slice and its fail-closed filter/sort limits.
- [`../evidence/lwbridge-implementation/2026-09-08-r6-map-frontend-consumers.json`](../evidence/lwbridge-implementation/2026-09-08-r6-map-frontend-consumers.json) records `LWB-R6-004`: exact Map Data option/summary response consumers, per-tab conditional query emission and search/options/summary stale-generation guards; the eight real tab envelope families are pinned by offline checks while unrecovered aggregation/filter SQL remains fail-closed.
- [`../evidence/lwbridge-implementation/2026-09-08-r6-map-query-backend.json`](../evidence/lwbridge-implementation/2026-09-08-r6-map-query-backend.json) records `LWB-R6-005`: verified-binary file offsets and exact backend predicates for city alliance/no-alliance, resource/monster name keys, and truck/railway `currentGoods` item membership. Its time-dependent/quality limits are historical; `LWB-R6-013/014` later close ordinary quality and the recovered frontend completion/plunderability clock slice, while treasure visibility and alternate sorts remain blocked.
- [`../evidence/lwbridge-implementation/2026-09-08-r6-map-query-boolean-filters.json`](../evidence/lwbridge-implementation/2026-09-08-r6-map-query-boolean-filters.json) records `LWB-R6-006`: unique, byte-adjacent verified-binary field/predicate pairs for dispatch/ghost `specialOnly` and `reindeerOnly`. Its original truck/railway frontend-kind correlation is historical; `LWB-R6-010` supersedes that allowance and proves the visible reindeer selector is truck-only.
- [`../evidence/lwbridge-implementation/2026-09-08-r6-map-keyword.json`](../evidence/lwbridge-implementation/2026-09-08-r6-map-keyword.json) records `LWB-R6-007`: exact keyword SQL, backslash/percent/underscore escaping order, literal-substring wrapping and four-parameter construction from bounded verified-binary data flow; the predicate is implemented and offline-tested.
- [`../evidence/lwbridge-implementation/2026-09-08-r6-map-search-snapshot.json`](../evidence/lwbridge-implementation/2026-09-08-r6-map-search-snapshot.json) records `LWB-R6-008`: the explicit rebuild policy that count and page reads share one SQLite snapshot, plus the deterministic second-connection WAL-writer regression proving a stable `{rows,total}` generation without blocking the committed write.
- [`../evidence/lwbridge-implementation/2026-09-08-r6-map-options-advanced-filters.json`](../evidence/lwbridge-implementation/2026-09-08-r6-map-options-advanced-filters.json) and its [focused static excerpt](../evidence/lwbridge-implementation/2026-09-08-r6-map-options-advanced-filters.txt) record `LWB-R6-009`: recovered option SQL/latest scan-summary ranges plus implemented treasure option-pair and dispatch selected-level predicates. At that checkpoint the reward `arriveTs` cutoff and other time-dependent mappings were blocked; `LWB-R6-014` below closes the clock/cutoff and recovered public completion/plunderability slice.
- [`../evidence/lwbridge-implementation/2026-09-08-r6-map-quality-selector-kind-gates.json`](../evidence/lwbridge-implementation/2026-09-08-r6-map-quality-selector-kind-gates.json) and its [focused frontend excerpt](../evidence/lwbridge-implementation/2026-09-08-r6-map-quality-selector-kind-gates.txt) record `LWB-R6-010`: exact quality-capable kinds, ordinary selector values, numeric display mapping and the corrected truck-only `reindeerOnly` frontend gate. Ordinary quality SQL/range semantics remain blocked.
- [`../evidence/lwbridge-implementation/2026-09-08-r6-map-schema-metadata.json`](../evidence/lwbridge-implementation/2026-09-08-r6-map-schema-metadata.json) and its [focused static excerpt](../evidence/lwbridge-implementation/2026-09-08-r6-map-schema-metadata.txt) record `LWB-R6-011`: exact schema-version metadata read/upsert strings, the `MAP_SCHEMA_TOO_NEW` branch vocabulary, dispatch-assist migration SQL and legacy-import completion marker. The supported schema number, migration threshold and timestamp unit remain blocked, so no migration behavior was guessed.
- [`../evidence/lwbridge-implementation/2026-09-08-r6-map-quality-special-ur-exclusion.json`](../evidence/lwbridge-implementation/2026-09-08-r6-map-quality-special-ur-exclusion.json) and its [focused static excerpt](../evidence/lwbridge-implementation/2026-09-08-r6-map-quality-special-ur-exclusion.txt) record `LWB-R6-012`: the byte-adjacent `quality`/special-UR exclusion evidence. `LWB-R6-013` below refines its earlier broad interpretation to truck+ordinary-UR only.
- [`../evidence/lwbridge-implementation/2026-09-08-r6-map-quality-filter.json`](../evidence/lwbridge-implementation/2026-09-08-r6-map-quality-filter.json), its [focused static excerpt](../evidence/lwbridge-implementation/2026-09-08-r6-map-quality-filter.txt), and [offline implementation validation](../evidence/lwbridge-implementation/2026-09-08-r6-map-quality-implementation.json) record `LWB-R6-013`: exact `n/r/sr/ssr -> quality = 1/2/3/4`, `ur -> quality >= 5`, plus the additional `isSpecialURQuality = 0` predicate only for truck+UR, now implemented and deterministic-test covered.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-time-filters.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-time-filters.json) records `LWB-R6-014`: the shared `GetSystemTimePreciseAsFileTime` -> Unix-millisecond clock, truck/railway active-arrival and plunderability SQL, dispatch plunderability expiry/steal predicates, dispatch/ghost completion-status comparisons, and the reward-option `arriveTs` cutoff producer/unit. Public options remain gated on source/run-context and complete response assembly rather than the clock.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-persisted-options.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-persisted-options.json) records `LWB-R6-015`: an explicitly offline/test-only `map_records` option-aggregation kernel using the recovered R6-009 SQL families and R6-014 reward cutoff. Deterministic grouping/order/server-scope/progress tests pass while the public `map_data_options` command remains fail-closed because the original dynamic source/run selector and full response assembly are still unknown.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-count-consumers.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-count-consumers.json) records `LWB-R6-016`: hash-locked frontend count/no-alliance/summary semantics, including the eight exact count keys, same-server count propagation/reset rules and no-alliance filter fallback. It does not infer backend count SQL, `noAllianceCount` aggregation or summary server selection.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-export-frontend.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-export-frontend.json) records `LWB-R6-017`: exact city-export frontend eligibility, shared query page 1/pageSize 200, 12-column header/sheet/yes/no option envelope and `{canceled,rowCount,path}` consumer behavior, plus a deterministic regression that keeps the native writer fail-closed.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-export-native-structure.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-export-native-structure.json) and its [bounded raw excerpt](../evidence/lwbridge-implementation/2026-09-09-r6-map-export-native-structure.txt) record `LWB-R6-018`: native export dialog/default/error vocabulary, embedded A-L OOXML package/layout/styles and partial row-field adjacency. Export remains fail-closed because full row typing/scope/pagination and large-ID behavior are not recovered.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-publish-rollback.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-publish-rollback.json) records `LWB-R6-020`: deterministic mid-transaction failure injection proves the test-only staged publication transaction preserves the previous published kind/server and publishes no partial replacement.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-persisted-counts.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-persisted-counts.json) records `LWB-R6-021`: the persisted-only options test kernel now carries all eight recovered frontend count keys plus a server-scoped no-alliance value in the same SQLite snapshot, without enabling the unrecovered public source/run selector.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-treasure-option-key-prefixes.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-treasure-option-key-prefixes.json) records `LWB-R6-022`: native `treasure:`/`supplies:` option-key prefixes, exact raw offsets, and the exhausted direct-RIP/absolute-pointer metadata xref routes; its suffix gap is superseded by `LWB-R6-023`.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-treasure-option-descriptors.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-treasure-option-descriptors.json) records `LWB-R6-023`: bounded native option-assembly data flow, direct length-prefixed descriptor references, verified `treasureType` field identity and shared signed-decimal formatter proving exact `treasure:<decimal treasureType>` / `supplies:<decimal suppliesType>` option keys.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-treasure-option-key-implementation.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-treasure-option-key-implementation.json) records the R6-023 persisted-only implementation scope, multi-digit deterministic cases and passing Release/backend validation while keeping public options fail-closed.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-option-source-candidates.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-option-source-candidates.json) records `LWB-R6-024`: the exact native source/scope candidates immediately preceding the option SQL family (`map_records` + `server_id=?1`, `scan_records` + `server_id=?1 AND run_id=?2`), the source/scope-parameterized kind-count helper, and the permitted-method inventory for ESC-003. The frontend supplies only `serverId`; the branch that chooses the source/run and exact `noAllianceCount`/full response assembly remain blocked, so public options stay fail-closed.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-summary-native-flow.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-summary-native-flow.json) records `LWB-R6-025`: native `map_summary` obtains shared scan state first, derives the response/count server from `scanState.serverId`, invokes the source/scope-aware count helper, and assembles exactly `{serverId, counts, scanState}`. Its then-open optional run-scope producer is superseded by `LWB-R6-030`; production summary still rejects because complete unavailable/error integration remains open.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-option-source-selector.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-option-source-selector.json) records `LWB-R6-030`: the recovered helper `0x140344F1D` supplies the active `scanRunId` scope only for `isReading=true` on the requested server with a nonempty run id; otherwise the R6-024 persisted source is used. The decision is mirrored only by an internal offline-tested helper while native no-alliance/full option/scan-progress assembly and complete summary error integration remain blocked.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-option-response-assembly.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-option-response-assembly.json) records `LWB-R6-031`: native alliance rows with NULL/empty decoded names are excluded from `alliances[]` and their grouped counts accumulate directly into `noAllianceCount`; the exact top-level option insertion order is recovered; and `scanProgress` chooses newest non-discarded run by server without run scope or exact `scanRunId` with active scope. Exact scan-progress JSON serialization/empty-state/error behavior remains open, so the public command stays fail-closed.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-scan-progress-serializer-edge.json`](../evidence/lwbridge-implementation/2026-09-09-r6-scan-progress-serializer-edge.json) records `LWB-R6-032`: the common option tail passes the selected scan-run value through generic tagged-value conversion, while the persisted latest-scan query closure identifies `0x140294188` as the smallest row-mapper boundary. Broader live `map_scan` state metadata is separated from persisted `scanProgress`; exact public fields/null/error behavior remain blocked and no production command is enabled.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-summary-error-propagation.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-summary-error-propagation.json) records `LWB-R6-033`: native `map_summary` propagates shared scan-state and count-helper failures through the command error path, bypassing downstream work/response assembly instead of manufacturing an empty success object. Exact shared-producer error-code/condition mapping and production scan-state integration remain blocked, so the public summary command stays fail-closed.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-current-server-world-contract.json`](../evidence/lwbridge-implementation/2026-09-09-r6-current-server-world-contract.json) records `LWB-R6-034`: the current build-1078 `Assembly-CSharp.rdl` is now reproducibly readable with `tools/inspect_lastwar_rdl_metadata.py`; it exposes xLua-callable `DCPlayer.GetCurServerId()` and `SceneManager.IsInWorld()` contracts. This narrows the current-server/world-state seam but does not invent the missing DCPlayer access path, bridge transport or LWBridge error mapping, so public summary remains fail-closed.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-current-server-access-chain.json`](../evidence/lwbridge-implementation/2026-09-09-r6-current-server-access-chain.json) records `LWB-R6-035`: current build-1078 managed ownership is traced as static `GameEntry.Data -> CustomDataManager.Player -> DCPlayer.GetCurServerId()`, and generated xLua wrappers expose every hop. Bridge invocation/readiness/error semantics remain unproven, so public summary stays fail-closed.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-shared-scan-state-producer.json`](../evidence/lwbridge-implementation/2026-09-09-r6-shared-scan-state-producer.json) records `LWB-R6-036`: the verified LWBridge shared state producer calls `getWorldMapState` then `getCurrentServerId` through the same bridge dispatcher with the recovered 5,000 ms timeout, normalizes missing/non-boolean `isInWorld` to false, inserts only positive `homeServerId`, and filters/deduplicates/sorts season/truck-match server lists. Exact named-error mapping and the command-handler link to the current-client managed chain remain blocked.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-current-server-error-mapping.json`](../evidence/lwbridge-implementation/2026-09-09-r6-current-server-error-mapping.json) records `LWB-R6-037`: `src\commands\map.rs` sends `getCurrentServerId`, extracts `serverId`, and when that extraction branch yields `<= 0` constructs exact code `SERVER_UNAVAILABLE` with message `current server id unavailable`. The encrypted bridge-script handler and the separate `map scan state is unavailable` condition remain unrecovered.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-bridge-runtime-material.json`](../evidence/lwbridge-implementation/2026-09-09-r6-bridge-runtime-material.json) records `LWB-R6-039`: the host auth parser extracts `packageKeyEnvelope`/expiry, a separate host function constructs the sibling `package-key.envelope` runtime path, both proxies contain the matching key-envelope/package-decrypt vocabulary, and the protected LWBP v2 `bridge-scripts.dat` contains no `LWKE1` marker. Direct parsed-value-to-file dataflow and plaintext command handlers remain blocked.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-runtime-material-persistence.json`](../evidence/lwbridge-implementation/2026-09-09-r6-runtime-material-persistence.json) records `LWB-R6-040`: the successful host package-key branch serializes the parsed `packageKeyEnvelope` as the response string plus LF, persists it through `NtWriteFile` and replace-existing `MoveFileExW`, and deletes the same runtime path with `DeleteFileW` before returning `KEY_ENVELOPE_INVALID` for invalid/expired material. Proxy read/decrypt and protected handler semantics remain blocked.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-device-key-boundary.json`](../evidence/lwbridge-implementation/2026-09-09-r6-device-key-boundary.json) records `LWB-R6-041`: the verified host auth-service marker cluster ties MachineGuid/device-key states to `packageKeyEnvelope`, `LWKE1` and runtime material, while both embedded proxies carry matching device-key/envelope/package-decrypt vocabulary plus CNG/BCrypt/file-loader imports. This narrows the protected-loader prerequisite without assigning unrecovered crypto parameters, call ordering or plaintext handlers.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-proxy-device-key-config.json`](../evidence/lwbridge-implementation/2026-09-09-r6-proxy-device-key-config.json) records `LWB-R6-042`: both embedded proxies carry exact `DEVICE_KEY_PROVIDER`, `DEVICE_KEY_EXPORT`, `DEVICE_KEY_FORMAT` and `DEVICE_KEY_MISSING` markers at identical offsets. This evidence remains the marker-ownership record; R6-043 supersedes its earlier configuration-name interpretation with secure-proxy branch-level semantics.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-proxy-device-key-crypto.json`](../evidence/lwbridge-implementation/2026-09-09-r6-proxy-device-key-crypto.json) records `LWB-R6-043`: secure-proxy control flow supersedes R6-042's configuration-name interpretation and proves those labels are diagnostics. It recovers the hardcoded Microsoft Software Key Storage Provider, persisted GUID key, `ECCPUBLICBLOB`/65-byte public-key format, CNG `TRUNCATE` 32-byte derivation and AES-GCM 32-byte key/12-byte nonce/16-byte tag requirements. Exact package-key caller/envelope layout, plain-proxy parity, protected handlers and readiness mapping remain blocked.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-scan-state-source-selection.json`](../evidence/lwbridge-implementation/2026-09-09-r6-scan-state-source-selection.json) records `LWB-R6-044`: the shared producer's exact `serverIdSource="live"` home/current-server fallbacks, active-read server-mismatch error, and `serverId=0`/`serverIdSource="none"` fallback are now source-attributed. The separate `map scan state is unavailable` emitter, protected handlers and dispatcher failure identities remain blocked, so summary/status stay fail-closed.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-proxy-package-crypto-boundary.json`](../evidence/lwbridge-implementation/2026-09-09-r6-proxy-package-crypto-boundary.json) records `LWB-R6-045`: secure-proxy unwind function `RVA 0x3D260-0x3DCF5` directly calls the recovered AES-GCM helper at `RVA 0x3DA4A -> 0x3CB60` and owns exact `LWBP2|`, `package integrity invalid`, and `package build mismatch` references. This source-attributes a package validation/decrypt consumer of the R6-043 AES primitive while leaving the package-key envelope reader/parser, package file caller, key/nonce/tag callsite ownership, plain-proxy parity, and protected handlers blocked.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-proxy-package-key-reader.json`](../evidence/lwbridge-implementation/2026-09-09-r6-proxy-package-key-reader.json) records `LWB-R6-046`: secure-proxy path function `RVA 0x1B300-0x1B4BF` appends exact UTF-16 `\bridge-runtime\package-key.envelope`; loader `RVA 0x1C0A0-0x1D46F` passes that path to bounded text reader `RVA 0x1BD80-0x1C09F` with a `0x1000` maximum and later passes the same exact path to opaque consumer `RVA 0x3F8E0`. The reader strips trailing CR/LF; the restricted consumer body, field grammar and agreement ownership remain blocked.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-option-source-integration.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-option-source-integration.json) records `LWB-R6-038` / PM7-A: the recovered published/staged source selector and exact server/run scope now feed one option/count aggregate service under one SQLite read snapshot. Conflicting published/staged/two-run/two-server fixtures and a concurrent WAL writer pass while public `map_data_options` remains fail-closed on the unresolved progress/state prerequisites.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-sort-inventory.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-sort-inventory.json) records `LWB-R6-026`: hash-locked frontend ordered multi-sort state/per-kind columns, eleven scalar native sort-key parser comparisons, four direct expression references and the adjacent expression/null-order string inventory. It corrects the item-count SQL text start to raw `0x00C8A926` after its `0x56` descriptor byte. Complete native multi-sort/null/tie-break composition and several key-specific data-flow branches remain blocked, so no alternate production sort is enabled.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-sort-validation.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-sort-validation.json) records R6-026 validation: the hash-locked verifier and JSON checks pass, the Release build has zero warnings/errors, and the deterministic backend suite remains fully green while production source/tests are unchanged. No game/launcher process was running, so this is not live proof.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-sort-direction.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-sort-direction.json) records `LWB-R6-027`: fixed-byte recovery of the public native direction branch, proving `asc -> ASC`, ordinary `desc -> DESC`, and the exact `distance desc -> ASC` override. The distance value mechanism and full multi-sort/null ordering remain blocked, so no production sort is newly enabled.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-map-staged-publish.json`](../evidence/lwbridge-implementation/2026-09-09-r6-map-staged-publish.json) records `LWB-R6-019`: test-only transactional replacement of one published kind/server from one staging run, preserving the recovered staging identity and leaving completion/native-key eligibility fail-closed.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-scan-block-checkpoint-infrastructure.json`](../evidence/lwbridge-implementation/2026-09-09-r6-scan-block-checkpoint-infrastructure.json) records `LWB-R6-028`: test-only restart-safe checkpoint persistence over the recovered `scan_blocks(run_id,block_index)` identity, same-key replacement, file-backed reopen durability and clear-cascade verification. Original scheduling/ack/retry/status transitions and production resume eligibility remain blocked.
- [`../evidence/lwbridge-implementation/2026-09-09-r6-record-key-current-runtime-search.json`](../evidence/lwbridge-implementation/2026-09-09-r6-record-key-current-runtime-search.json) records `LWB-R6-029`: a current-runtime readable-evidence search found no direct record-key/scan-bridge or selected known map-row vocabulary in the tracked build-1078 snapshot. It identifies the current `Assembly-CSharp.rdl` and active `LWScripts.data` as the next concrete sources without guessing their contents.
- [`../evidence/lwbridge-implementation/2026-09-10-r6-map-record-upsert-boundary.json`](../evidence/lwbridge-implementation/2026-09-10-r6-map-record-upsert-boundary.json) and its [focused native excerpt](../evidence/lwbridge-implementation/2026-09-10-r6-map-record-upsert-boundary.txt) record `LWB-R6-047`: exact native published/staging upsert selection, staging `run_id` prefix, shared 14-field record bind order and the authoritative `record_key` input slot at record offset `+0x78`. The helper consumes an already-built record, so per-kind key derivation and production ingestion remain blocked.
- [`../evidence/lwbridge-implementation/2026-09-10-r6-current-world-point-identities.json`](../evidence/lwbridge-implementation/2026-09-10-r6-current-world-point-identities.json) and its [focused current-RDL excerpt](../evidence/lwbridge-implementation/2026-09-10-r6-current-world-point-identities.txt) record `LWB-R6-048`: build-1078 world-point protobuf identity fields plus separate typed `pointIndex` and `uuid` lookup paths around resource data. Neither is promoted to native `record_key`; the direct producer into R6-047 `+0x78` remains open.
- [`../evidence/lwbridge-implementation/2026-09-10-r6-map-record-identity.json`](../evidence/lwbridge-implementation/2026-09-10-r6-map-record-identity.json), [builder dump](../evidence/lwbridge-implementation/2026-09-10-r6-map-record-identity-builder.txt) and [decimal-key formatter dump](../evidence/lwbridge-implementation/2026-09-10-r6-map-record-key-format.txt) record `LWB-R6-049`: the original builder's normalized `record_key` decision and final `+0x78/+0x88` write, including first-present point aliases, signed-decimal point identity and the `uuid`/`marchUuid` fallback for truck/railway, march-backed and indexless records. Current-client capture-to-builder mapping and remaining normalization/removal stay blocked.
- [`../evidence/lwbridge-implementation/2026-09-10-r6-current-point-identity-surface.json`](../evidence/lwbridge-implementation/2026-09-10-r6-current-point-identity-surface.json) records `LWB-R6-050`: hash-gated current build-1078 `PointInfo.pointIndex/mainIndex/uuid` ownership through generated xLua getters, the exact `isMainPoint` comparison and `WorldPointInfo` constructor stores, plus `ResPointInfo.gatherMarchUuid` and resource getters. Modified source call tokens, native capture ownership and direct R6-049 builder dataflow remain blocked.
- [`../evidence/lwbridge-implementation/2026-09-10-r6-map-normalized-columns.json`](../evidence/lwbridge-implementation/2026-09-10-r6-map-normalized-columns.json) records `LWB-R6-051`: fixed-locator original builder verification for `level`, `quality`, `power`, `distance`/`distanceFromHome`, and `shieldEndTime`/`protectEndTime` into the normalized slots consumed by R6-047. Current-client serializer ownership and live ingestion remain blocked.
- [`../evidence/lwbridge-implementation/2026-09-13-r6-map-scan-geometry.json`](../evidence/lwbridge-implementation/2026-09-13-r6-map-scan-geometry.json) records `LWB-R6-052`: hash-locked recovery of the positive map-dimension gate, exact `ceil(tileWidth/20) * ceil(tileHeight/20)` scan-grid cardinality, initial total/read/unread/failed/inflight counters, and the scalar no-failure direct-completion prerequisite. Block traversal/order/request coordinates, scheduler/tick/retry behavior, the additional non-scalar completion input and final publication eligibility remain blocked.
- [`../evidence/lwbridge-implementation/2026-09-13-r6-map-scan-progress.json`](../evidence/lwbridge-implementation/2026-09-13-r6-map-scan-progress.json) records `LWB-R6-053`: the shared scan-state normalizer derives unread work from total/completed/failed counts and computes one-decimal `progressPercent`; non-completed status is capped at `98.0`, while positive-total exact `completed` status permits `100.0`. The scheduler/live counters and final publication gates remain separate open work.
- [`../evidence/lwbridge-implementation/2026-09-13-r6-map-scan-scheduler-progress.json`](../evidence/lwbridge-implementation/2026-09-13-r6-map-scan-scheduler-progress.json) records `LWB-R6-054`: `map.scan.progress`/`map.scan.complete` counter normalization, `INVALID_SCAN_PROGRESS` for completed+failed beyond total, inflight bounded by concurrency/remaining work, unread after inflight, and two-decimal completed-blocks-per-second scan rate. Coordinate generation, request scheduling, retries and capture drain/publication remain open.
- [`../evidence/lwbridge-implementation/2026-09-13-r6-map-scan-completion-safety.json`](../evidence/lwbridge-implementation/2026-09-13-r6-map-scan-completion-safety.json) records `LWB-R6-055`: native pending-record aggregation across points/marches/removals/acks, separately tracked dropped records with positive dropped values entering failure before publishing, exact direct-completion coverage/zero-failed-block checks, the production caller's zero extra failed-batch argument, and recovered Stop state reset/conditional `stopMapScan`. A positive pending count alone is not yet proven to reject publication; traversal/tick/retry/ack-drain sequencing remains open.
- [`../evidence/lwbridge-implementation/2026-09-13-r6-map-scan-start-resume.json`](../evidence/lwbridge-implementation/2026-09-13-r6-map-scan-start-resume.json) records `LWB-R6-056`: exact Start ownership failures `GAME_CONNECTION_UNAVAILABLE` and duplicate `SCAN_RUNNING`, plus optional resume routing that reuses current state only when requested `resume:true` and current `resumeAvailable=true`. Every recovered constructor/reset writes resume availability false, so production resume remains unclaimed.
- [`../evidence/lwbridge-implementation/2026-09-13-r6-map-scan-clear-ownership.json`](../evidence/lwbridge-implementation/2026-09-13-r6-map-scan-clear-ownership.json) records `LWB-R6-057`: Clear rejects active scans, requires a positive requested server matching the current scan-state server with exact `serverIdSource=live`, then performs the recovered server-scoped clear and republishes idle state. The rebuild validator remains offline until the shared scanner owns an authoritative live server source; late-result generation invalidation remains open.
- [`../evidence/lwbridge-implementation/2026-09-14-r6-map-scan-terminal-publication.json`](../evidence/lwbridge-implementation/2026-09-14-r6-map-scan-terminal-publication.json) records `LWB-R6-059`: positive `failedBlocks` and present nonempty `lastError` populate the terminal failure list and route through shared failure/Stop cleanup; only an empty terminal list enters `publishing` and direct completion. `pendingAcks` is not an input to this specific branch, while acknowledgement consumption/drain scheduling remains unresolved.
- [`../evidence/lwbridge-implementation/2026-09-14-r6-map-scan-event-admission.json`](../evidence/lwbridge-implementation/2026-09-14-r6-map-scan-event-admission.json) records `LWB-R6-060`: progress/complete/error events pass an active-scan admission helper before processing; present nonempty `scanRunId` and present positive `serverId` must match the active run/server, while missing identity is tolerated rather than synthesized.
- [`../evidence/lwbridge-implementation/2026-09-14-r6-map-scan-world-readiness.json`](../evidence/lwbridge-implementation/2026-09-14-r6-map-scan-world-readiness.json) records `LWB-R6-061`: `enterWorldMap` uses a 5,000 ms request timeout, readiness waits up to 10,000 ms with 500 ms polls, timeout is exact `WORLD_MAP_FAILED / failed to enter world map`, and the post-readiness current server must be positive with exact `serverIdSource=live` or Start fails `SERVER_UNAVAILABLE / current server id unavailable`.
- [`../evidence/lwbridge-implementation/2026-09-10-first-live-resource-source.json`](../evidence/lwbridge-implementation/2026-09-10-first-live-resource-source.json) records `LWB-R7-001`: build-1078/Lua-v14 launcher correlation, hash-identified live diagnostics, the exact selected resource record, bounded adapter policy, visible values and remaining production limits. The linked [focused capture record](../evidence/lwbridge-implementation/2026-09-10-first-live-resource-capture.json), [WebView diagnostics](../evidence/lwbridge-implementation/2026-09-10-first-live-resource-ui.json) and [screenshot](../evidence/lwbridge-implementation/2026-09-10-first-live-resource.png) are the durable replay/display proof; the 7 MB full live capture stays outside Git.
- [`../evidence/lwbridge-implementation/2026-09-10-pm10-replay-acquisition-recipe.json`](../evidence/lwbridge-implementation/2026-09-10-pm10-replay-acquisition-recipe.json) records `LWB-R7-002`: the ancestor commit and hashes for the historical acquisition tools, v14 candidate identity, exact guarded run/restoration contract, PM10 replay isolation/labeling behavior, focused regression coverage, operation-specific restrictions and the still-open fresh app-acquisition gate.
- [`../evidence/lwbridge-implementation/2026-09-10-fresh-resource-live-proof.json`](../evidence/lwbridge-implementation/2026-09-10-fresh-resource-live-proof.json) records `LWB-R7-003`: two distinct rebuild-generated request IDs and fresh response times, current-client/package/probe fingerprints, exact restoration evidence, the source row from `WorldPointManager._pointInfos`, normal `MapDataStore` search/summary output, and explicit `originalPipeClaimed=false` limits.
- [`../evidence/lwbridge-implementation/2026-09-10-pm12-recovery-regression.json`](../evidence/lwbridge-implementation/2026-09-10-pm12-recovery-regression.json) records `LWB-PM12-001`: isolated failures after every install/restore stage, durable interrupted recovery, exact original triplet hashes, cross-process operation-lease rejection/release and dual-error preservation. It changes no installed game file and leaves exact live installation/PID targeting open.
- [`../evidence/official-runtime/`](../evidence/official-runtime/) contains read-only current-client runtime snapshots.

## Evidence labels

- **RECOVERED** — statically recovered from the verified LWBridge artifact or embedded assets.
- **IMPLEMENTED/OFFLINE-TESTED** — implemented and verified without claiming a live game result.
- **LIVE-PROVEN** — observed against the current installed client with authoritative evidence.
- **UNKNOWN/BLOCKED** — still unresolved.

Temporary analysis under `.codex-live/lwbridge-*` is convenience material only. Durable conclusions belong in this directory or under `evidence/`.

## 2026-09-14 current-client v16 update-safety

- [`lastwar-v16-update-safe-lifecycle.md`](lastwar-v16-update-safe-lifecycle.md) records `LWB-V16-001/002/003`: the modified-package patch failure diagnosis, exact supported v16 identity and unchanged compatibility anchors, plus a live-proven full update-safe lifecycle from untouched official launcher settlement through temporary candidate READY and exact v16 restoration. Reproduce the current fingerprint/critical-entry checks with `python tools/check_v16_update_safe_prereqs.py`; machine evidence for the successful live cycle is under `evidence/official-runtime/2026-09-15-v16-live-lifecycle/`.
