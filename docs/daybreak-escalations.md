# Daybreak escalation register

Updated 2026-09-09, regular-AI continuation through `LWB-R6-024`. This is the decision/attempt log shared by [the regular task](implementation-handoff.md), [the specialist task](deep-binary-handoff.md) and the project manager. Mandatory rules are in [AGENTS.md](../AGENTS.md) section 6.

## State and ownership rules

`NEEDS_INFORMATION` → `READY_FOR_PM_REVIEW` → `APPROVED` → `ASSIGNED` → `RETURNED_FOR_INTEGRATION` → `CLOSED`. `NOT_ASSIGNED` records work retained by the regular AI or a request declined with a reason. PM approval is a workflow decision, not permission to evade platform restrictions.

The regular AI fills the packet and continues independent work. The PM checks whether relevant permitted methods have actually been exhausted and whether specialist analysis offers a specific benefit. A DB label alone assigns nothing. Current specialist technical assignments: **none**.

## ESC-001 — completion/reward cutoff source and units

- **Status/owner:** CLOSED by PM review 6; resolved by the regular AI through R6-014. DB-05/R6. No Daybreak task required for this resolved contract; live feature gates remain separate.
- **Question:** what produces the bound cutoff for completion-status comparisons and reward-option `arriveTs` filtering, with what units and server/profile context? Do not assume both use one clock.
- **Sources:** LWBridge 0.3.1 SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`; [R6-009 option evidence](../evidence/lwbridge-implementation/2026-09-08-r6-map-options-advanced-filters.json), [R6-004 frontend consumers](../evidence/lwbridge-implementation/2026-09-08-r6-map-frontend-consumers.json), and [R6-014 clock/time-filter evidence](../evidence/lwbridge-implementation/2026-09-09-r6-map-time-filters.json).
- **Observed attempts/results:** repository/evidence searches and saved R6-009/R6-004 review narrowed the unresolved producer; bounded `pefile 2024.8.26` + `Capstone 5.0.6` disassembly then traced the options call at preferred-image VA `0x14025A1B5` and `map_search` call at `0x140272353` to helper `0x14023F1C0-0x14023F244`. Import-table resolution identifies IAT `0x1407B94B0` as `kernel32.dll!GetSystemTimePreciseAsFileTime`. The helper subtracts FILETIME epoch `116444736000000000` and converts 100-ns ticks to Unix milliseconds. Exact predicate/xref and parameter-append checks are reproducible with `python tools\inspect_lwbridge_map_time_filters.py ..\LW\lwbridge-0.3.1.exe --json`.
- **Resolved result:** both the reward-option `arriveTs` cutoff and completion-status comparisons use the same sampled Unix-millisecond wall clock in this verified reference. `completionStatus=pending` is null/nonpositive/future `completionTime`; `completed` is positive `completionTime <= now`. The same sample supplies truck/railway active-arrival filtering and dispatch/ghost/treasure native plunder-expiry handling. Frontend public plunderability remains limited to truck/railway/dispatch per R6-004.
- **Limits:** this resolves the original static producer/source/unit question; it is not live-game proof and does not resolve option public run-context/source selection, treasure visibility/lucky semantics or current-client gameplay contracts. No specialist capability gap remains for ESC-001.

## ESC-002 — schema version and migration prerequisites

- **Status/owner:** NEEDS_INFORMATION; regular AI prepares, PM reviews. R6 storage. Not assigned to Daybreak.
- **Question:** supported schema-version constant, migration thresholds/order and metadata timestamp producer/unit. A version found in one database would not alone prove all supported transitions.
- **Sources/known:** same verified reference identity as ESC-001; [R6-011 metadata](../evidence/lwbridge-implementation/2026-09-08-r6-map-schema-metadata.json) and [excerpt](../evidence/lwbridge-implementation/2026-09-08-r6-map-schema-metadata.txt) establish schema metadata/future-schema/legacy-import markers. Production migration is not implemented.
- **Observed attempts:** repository/schema/store/test searches, bounded printable-string observations and saved-evidence review are recorded in the regular task. A read-only query of the existing user-profile database and a later bounded schema-range read were denied (SB-04/SB-12); do not retry it via Daybreak. The current evidence does not list every relevant permitted alternative or establish a specialist-only capability gap.
- **Missing before PM approval:** method/tool/locator/output inventory, readable artifacts/import/migration producer search results, why remaining permitted approaches cannot resolve the constant/branch, and the narrow permitted question for the specialist. Retain unknown version/timing values.
- **Why consider specialist:** a genuinely unresolved native version/branch transformation may benefit from specialist analysis after that packet is complete; model choice alone is not justification.
- **Return criteria if approved:** exact constants/transition gates/timestamp source with provenance and limits, plus deterministic migration test requirements. The regular AI implements/tests the supported contract.

## ESC-003 — public options source/run and native response assembly

- **Status/owner:** READY_FOR_PM_REVIEW after `LWB-R6-024`; regular AI retains ownership pending PM decision. DB-05/R6; no specialist assignment yet.
- **Exact gap:** how public `map_data_options` chooses persisted versus active-run/staged data, applies server/run context, and builds complete native counts/no-alliance/option response. Latest-scan SQL and persisted helper output do not establish that branch.
- **Sources:** verified reference identity above; [R6-015](../evidence/lwbridge-implementation/2026-09-09-r6-map-persisted-options.json), [R6-016](../evidence/lwbridge-implementation/2026-09-09-r6-map-count-consumers.json), [R6-021](../evidence/lwbridge-implementation/2026-09-09-r6-map-persisted-counts.json), [R6-023](../evidence/lwbridge-implementation/2026-09-09-r6-map-treasure-option-key-implementation.json), and [R6-024 source candidates/method inventory](../evidence/lwbridge-implementation/2026-09-09-r6-map-option-source-candidates.json).
- **Permitted methods/results:** (1) verified the LWBridge SHA-256 and used ripgrep 15.2.0 raw offsets plus bounded Python 3.12.10 printable reads to recover the exact adjacent source/scope literals; (2) pefile 2024.8.26 mapped them to preferred-image VAs; (3) recovered frontend evidence proves `map_data_options` supplies only `serverId`, so run choice is backend-owned; (4) recovered schema proves the candidate pairs are `map_records` + `server_id=?1` and `scan_records` + `server_id=?1 AND run_id=?2`; (5) a separate native `SELECT kind,COUNT(*) FROM <source> WHERE <scope> GROUP BY kind` helper plus `read/query/prepare map counts` strings proves count aggregation is source/scope-parameterized; (6) the existing persisted helper and tests validate only the published-source SQL as explicit implementation policy; (7) saved bounded R6-023 option-assembly evidence has no direct source/scope-VA hit and therefore does not prove the branch; (8) maintained official-runtime readable evidence/docs/inspector contain zero matches for this LWBridge-local persistence vocabulary; (9) no recovered `map_index.rs`/`map_scan.rs` source exists locally; and (10) no LastWar/LWBridge process was running, so an active-run observation target was unavailable. SB-06 and SB-11 were not retried or rerouted.
- **Narrowed recovered fact:** the native public-options abstraction has exactly two evidenced candidates: published `map_records` scoped by `server_id=?1`, or staging `scan_records` scoped by `server_id=?1 AND run_id=?2`. This does **not** identify the condition that selects one or the run identity supplied to the staging branch.
- **Why unresolved after permitted methods:** readable strings/schema/frontend establish the selector inputs but not the native control-flow predicate; saved bounded option assembly does not contain a source/scope reference; current-client readable artifacts do not expose the LWBridge-local selector; no original Rust source or live active-run target is available. The exact native `noAllianceCount` producer and complete response assembly also remain unproven. Public command remains `MAP_INDEX_UNAVAILABLE`.
- **Specific specialist value if PM approves:** interpret the already identified bounded native option function and saved/native control-flow evidence to recover only the persisted-versus-staging decision, staging run identity source, and exact count/no-alliance/full response assembly. The specialist must not replay SB-06/SB-11 or use a denied whole-executable source-xref scan as a workaround.
- **Remaining alternatives considered:** original Rust source is absent; readable frontend/current-client artifacts are exhausted for this selector; live observation requires a supported active scan target that is not currently available. A future regular-AI path remains possible if a new permitted bounded caller/control-flow source is identified, so PM may return this for further regular investigation instead of assignment.
- **Return/acceptance:** exact source/run/server decision table, fields/count behavior/empty states and conflicts, reproducible evidence and which public implementation is now supported. Regular AI integrates/tests; native ingestion/live accuracy remains separate.

## ESC-004 — city-export writer semantics

- **Status/owner:** NEEDS_INFORMATION, PM-seeded in review 6; regular AI prepares. R6/M09; no specialist assignment yet.
- **Exact gap:** complete A–C/J row mapping, protect/shield fallback, per-column string/numeric/date typing, internal pagination/full-filter scope, filename/directory/cancellation semantics and large-ID preservation.
- **Sources:** verified reference identity above; [R6-017 frontend contract](../evidence/lwbridge-implementation/2026-09-09-r6-map-export-frontend.json), [R6-018 native structure](../evidence/lwbridge-implementation/2026-09-09-r6-map-export-native-structure.json) and [saved excerpt](../evidence/lwbridge-implementation/2026-09-09-r6-map-export-native-structure.txt).
- **Attempts known:** frontend envelope recovered; raw OOXML/package/default/field-adjacency reads succeeded. A template-to-helper xref/disassembly pass and a bounded frontend field snippet were denied (SB-08/09). Original workbook output and complete runtime mapping were not proven.
- **Why unresolved:** static package templates are not sufficient for the writer's per-row data flow or pagination; no production writer enabled.
- **Required next record:** permitted methods tried, tools/locators/output and why existing readable assets/templates/saved traces cannot answer each remaining field; alternatives considered and exact specialist value. Do not repeat SB-08/09 under another executor/model.
- **Return/acceptance if later approved:** source-attributed per-column mapping/typing and full-filter writer behavior with unknowns preserved. Regular AI then implements isolated workbook round-trip/large-ID/cancel/failure tests before enabling export.

## Review 6 outcome dashboard — a restriction is not a contract status

| Question / restriction | Current contract or delivery outcome | Escalation outcome |
|---|---|---|
| Clock/completion/reward cutoff; SB-05 | Resolved in recorded R6-014; backend boundary tests pass | ESC-001 CLOSED; no Daybreak needed for this fact. |
| Treasure option key suffix; SB-13/15 | Resolved in recorded R6-023; persisted-helper tests pass | No open key-format request. Other option contracts remain unresolved. |
| Delivery; SB-10 | Resolved; review 6 independently saw GitHub at `3138fe9` | Not a binary-specialist task. |
| Frontend count/context reading; SB-07/14 | Limited consumer/documentation result exists | No transfer of the denied command; native assembly handled separately in ESC-003. |
| Public options source/full assembly; SB-06/11 | R6-024 narrows the source candidates but the selector/full assembly remain open | ESC-003 READY_FOR_PM_REVIEW; no specialist assigned. |
| Summary server/count/scan-state flow; SB-17/18 | R6-025 resolves the successful envelope and `scanState.serverId` count-server path; optional run-scope producer and unavailable/error integration remain open | Continue regular permitted investigation; no new specialist request from the restrictions alone. |
| Alternate `map_search` sorts; SB-20–23 | R6-026 resolves public per-kind sort columns, frontend ordered-sort mutation, eleven scalar parser comparisons, four direct expression references and the adjacent native expression/null-order inventory. Complete ordered multi-sort/null/tie-break composition and several key data-flow branches remain open. | Still investigating with the fixed-byte/raw-string/frontend verifier and other permitted bounded methods; no ESC request from these restrictions alone. |
| Sort direction special case; SB-24 | R6-027 resolves public physical direction selection as `asc -> ASC`, ordinary `desc -> DESC`, and exact `distance desc -> ASC`; the distance value mechanism remains open | Direction-literal question resolved by a permitted fixed-byte verifier. Continue regular investigation; no transfer of SB-24. |
| Complete export mapping/typing/pagination; SB-08/09 | Still open; partial structure only | ESC-004 NEEDS_INFORMATION. |
| Schema/migrations; SB-04/12 | Still open | ESC-002 NEEDS_INFORMATION. |

The regular AI must report **resolved by finding X**, **still investigating with next method Y**, or **ESC request pending with missing packet fields Z**. "No Daybreak request created" says nothing about whether a feature is solved. A restriction remains historical evidence even if a separate permitted result later resolves its associated question. Review 6 records prior outcomes without endorsing rerouting denied operations.

## Retained regular-AI work — NOT_ASSIGNED

Options/count/no-alliance assembly, the remaining summary run-scope producer, alternate sorts, export semantics, host-test hygiene, supported-query regressions, storage/generation engineering, and DB-01–04/06 remain regular-AI work. `LWB-R6-022` recorded a narrow treasure-option-key attempt: raw prefix recovery succeeded, while direct RIP xrefs and absolute-pointer string-metadata tracing returned no formatter reference. `LWB-R6-023` then resolved that suffix question without Daybreak by restricting analysis to the already identified option-assembly function and tracing its length-prefixed descriptors, branch values and shared decimal formatter. `LWB-R6-025` similarly advances summary without assignment: it recovers `{serverId,counts,scanState}`, proves that counts use `scanState.serverId`, and narrows the remaining summary gap to the internal optional run-scope producer plus unavailable/error integration. `LWB-R6-026` advances alternate-sort recovery without assignment by pinning the public ordered-sort state, per-kind columns, eleven scalar parser comparisons, four direct expression references and the adjacent expression/null-order inventory while leaving complete composition and several data-flow branches explicitly open. Exact treasure keys are `treasure:<decimal treasureType>` and `supplies:<decimal suppliesType>`. Public options, summary and alternate sorts remain regular-AI owned because their remaining production integration contracts are still open. Open an individual ESC record only when the mandatory attempts establish the need.

## Recorded operation restrictions

| ID | Source and operation | Evidence / effect |
|---|---|---|
| SB-01 | [R5-004](../evidence/lwbridge-implementation/2026-09-08-r5-launch-material.json), new fixed-address launch-material verifier execution | Historical safety-review denial. Existing findings have limited provenance; no replay assigned. |
| SB-02 | [R5-005](../evidence/lwbridge-implementation/2026-09-08-r5-ticket-consumption.json), broader helper/binary disassembly | Historical report: reviewer could not determine safety status. Saved bounded excerpt is not fresh extraction. |
| SB-03 | [R6-009](../evidence/lwbridge-implementation/2026-09-08-r6-map-options-advanced-filters.json), convenience executable verifier | Recorded denied after earlier successful bounded observations; exact rejected invocation is not reproduced in the metadata. Preserve that limit. |
| SB-04 | [R6-011](../evidence/lwbridge-implementation/2026-09-08-r6-map-schema-metadata.json), read-only existing user-profile database query | Recorded automatic-review denial; no database contents accepted as evidence. |
| SB-05 | Task **Read Map Scan Recovery Docs**, turn `01a081a6-77cb-7893-974c-af50f0104e7b`, completion-status context/range reads | Task reports reviewer could not determine safety status. Review 5 preserves the report in its evidence; it did not retry either operation or acquire raw rejection telemetry. |
| SB-06 | Regular-AI R6 options continuation, 2026-09-09, creation of a new hash-locked bounded PE xref inspector for the `map_data_options` dynamic source/run selector | Automatic review rejected the operation because it could not determine safety status. The tool was not created/executed and the operation was not retried or rerouted. R6-015 instead continues with the already recovered SQL families in an explicitly persisted-only offline helper; the original dynamic source selector remains UNKNOWN/BLOCKED and no Daybreak assignment is requested. |
| SB-07 | Regular-AI R6 count continuation, 2026-09-09, literal here-string command to print additional bounded frontend count/no-alliance snippets | Automatic review rejected the operation because it could not determine safety status. The same command was not retried or rerouted. R6-016 uses only snippets already obtained by the preceding successful escaped frontend read and a new hash-locked deterministic inspector over the immutable recovered frontend assets. |
| SB-08 | Regular-AI R6 export continuation, 2026-09-09, fresh Capstone/pefile xref-disassembly pass from the embedded XLSX template region into runtime helper functions | Automatic review rejected the operation because it could not determine safety status. The command was not retried or rerouted. R6-018 uses only successful bounded raw-byte/string reads and leaves helper control flow, internal pagination and exact row typing UNKNOWN/BLOCKED; no Daybreak assignment is requested from this restriction alone. |
| SB-09 | Regular-AI R6 export continuation, 2026-09-09, a bounded Python snippet extraction around `protectEndTime`/`shieldEndTime` and related city fields in the immutable recovered MapDataPanel asset | Automatic review rejected the operation because it could not determine safety status. The operation was not retried or rerouted. Existing R6-017 frontend evidence and successful native raw-byte reads are sufficient for the partial R6-018 checkpoint; the exact native protect/shield fallback remains UNKNOWN/BLOCKED. |
| SB-10 | Regular-AI R6-018 delivery, 2026-09-09, an earlier `git push origin research/offline-controller` combined with remote-head verification | Automatic review initially rejected that command because it could not determine safety status. After the branch state later changed with additional completed checkpoints, a fresh delivery attempt succeeded: commits through `897414b084217092bca2aba9e34dbcd93ae1891f` were pushed and `HEAD` matched `origin/research/offline-controller`. Preserve the original rejection as historical environment evidence; GitHub delivery is no longer blocked. |
| SB-11 | Regular-AI post-R6-018 continuation, 2026-09-09, broad `rg` search across saved option evidence, Map Data store/tests and map-scan docs for remaining count/no-alliance assembly clues | Automatic review rejected the command because it could not determine safety status. The search was not retried or rerouted. No new count/no-alliance claim is accepted from this attempt; R6-016's backend aggregation gap remains UNKNOWN/BLOCKED. |
| SB-12 | Regular-AI storage continuation, 2026-09-09, bounded raw-byte printable-string read of the verified LWBridge schema metadata/migration range `0x00C82C80-0x00C82F80` | Automatic review rejected the command because it could not determine safety status. The read was not retried or rerouted. Existing R6-011 evidence remains authoritative; supported schema version, migration threshold/order and metadata timestamp producer/unit remain UNKNOWN/BLOCKED. |
| SB-13 | Regular-AI R6-022 continuation, 2026-09-09, rerun of the enhanced whole-executable descriptor-xref pass after adding length-prefixed `treasure:` / `supplies:` descriptor targets | Automatic review rejected the operation because it could not determine safety status. The denied whole-executable scan was not retried or rerouted. R6-023 instead uses a distinct bounded analysis of the already known option-assembly function `0x140257AA4-0x14025B436`; that permitted path resolved the key-format question. |
| SB-14 | Regular-AI R6-023 continuation, 2026-09-09, one combined repository read/search command covering escalation tail, R6-022 documentation and tool candidates | Automatic review rejected the combined command because it could not determine safety status. It was not replayed. Subsequent independent narrow reads of already known repository files provided the needed documentation context; the denied combined operation contributed no technical finding. |
| SB-15 | Regular-AI R6-023 continuation, 2026-09-09, a targeted PowerShell filter over the newly generated bounded-inspector JSON for selected stack/register references | Automatic review rejected that filtering command because it could not determine safety status. It was not replayed. The successful bounded evidence file was preserved, and narrower direct reads of its already generated branch instruction arrays were sufficient to trace the formatter and normalized values. |
| SB-16 | Regular-AI R6-024 continuation, 2026-09-09, a structured PowerShell `ConvertFrom-Json` parse of the already generated R6-023 evidence to inspect selected fields | Automatic review rejected the operation because it could not determine safety status. It was not replayed or rerouted. Plain bounded file reads and raw-string evidence were sufficient for R6-024; the denied structured parse contributed no technical finding. |
| SB-17 | Regular-AI R6-025 continuation, 2026-09-09, fresh bounded dumps for native helpers `0x140345030`, `0x140344F1D` and `0x1400FF37C` after the successful `map_summary`/scan-state/count-helper trace | Automatic review rejected the operation because it could not determine safety status. It was not retried or rerouted. The successful bounded caller/count-helper evidence still proves the R6-025 envelope/server/count flow; the exact optional run-scope producer remains UNKNOWN/BLOCKED. |
| SB-18 | Regular-AI R6-025 continuation, 2026-09-09, a PowerShell range-read of the already generated `.codex-map-summary-disasm.txt` scratch output | Automatic review rejected the operation because it could not determine safety status. It was not retried or rerouted and contributed no technical finding. Previously returned bounded excerpts remain the accepted evidence for R6-025. |
| SB-19 | Regular-AI R6-025 validation, 2026-09-09, one combined command that attempted scratch cleanup, JSON parsing, inspector help and Git diff/status checks | The outer execution policy rejected the combined command before execution. It was not repeated. Scratch cleanup, JSON validation, inspector invocation checks and diff checks were subsequently run as narrower permitted commands, so this restriction blocks no checkpoint validation and contributed no technical finding. |
| SB-20 | Interrupted regular-AI post-R6-025 sort continuation, 2026-09-09, a broad search intended to collect alternate-sort code/evidence context | Automatic review rejected the operation because it could not determine safety status. It was not replayed or rerouted and contributed no technical finding. R6-026 instead uses narrow immutable frontend reads, fixed raw-byte checks and exact raw-string evidence. |
| SB-21 | Regular-AI R6-026 continuation, 2026-09-09, one combined command that mixed frontend/source/test reads with a fresh bounded `--mapping-disassembly` execution | Automatic review rejected the operation before execution because it could not determine safety status. It was not retried or rerouted. Subsequent narrow source/frontend reads and the fixed-byte verifier supply the accepted R6-026 evidence; the denied disassembly contributes nothing. |
| SB-22 | Regular-AI R6-026 continuation, 2026-09-09, a combined repository `rg`/read command for query-contract, store, tests and frontend evidence | Automatic review rejected the operation before execution because it could not determine safety status. It was not replayed. Narrow single-file reads subsequently succeeded and are the only accepted source evidence from that step. |
| SB-23 | Regular-AI R6-026 continuation, 2026-09-09, a bounded pointer-target calculation over the already identified SIMD constant-load bytes near `0x140274B1D` | Automatic review rejected the operation because it could not determine safety status. It was not retried or rerouted. R6-026 therefore records the 18-byte comparison only as an unlabeled candidate and keeps `remainingLootCount` parser-key proof UNKNOWN/BLOCKED. |
| SB-24 | Regular-AI post-R6-026 continuation, 2026-09-09, a fresh fixed raw-byte range request over the native quality branch `0x140274C20-0x140274CE0` | Automatic review rejected the operation because it could not determine safety status. It was not retried or rerouted and contributed no quality finding. R6-027 instead closes the already evidenced direction-literal branch using a separate fixed-byte verifier; quality remains independently open. |

Review 5 itself recorded no new denial; SB-06 through SB-24 occurred in post-review regular-AI continuation. Restrictions describe operations, not every analysis of the executable or saved frontend. Missing tools, unresolved contracts, external services and unavailable live targets must be labelled separately. Do not reroute denied operations to Daybreak or CI.

## Required request template — copy for each new ESC entry

1. **ESC ID/date/status/owner/base commit:** name the exact DB/R/S/O/M item and requesting task.
2. **Precise question and impact:** the missing fact, dependent behavior, what can continue independently.
3. **Source identity:** reference/current-client distinction, artifact paths/build/SHA-256, known locators and existing finding IDs.
4. **Known facts versus hypotheses:** preserve unknown values and contradictory findings.
5. **Attempt log:** method; tool/version/setup; command or bounded steps/locator; result/error; durable output; interpretation/why insufficient. Do not list a method as tried without evidence.
6. **Alternatives exhausted:** readable JS/Lua/managed/resources/SQL/logs, source/caller/data-flow approaches, suitable available tools/custom scripts; explain the relevant methods you considered and why each cannot answer. Do not require irrelevant techniques or prohibited attempts.
7. **Blocker class:** research/capability, setup, missing target, external dependency or environment restriction. Include exact denied operation/tool/reason if applicable; preserve restrictions.
8. **Why Daybreak:** specific specialist value, the smallest permitted scope, expected input/output, and why the regular AI cannot progress further with its identified permitted approaches.
9. **Acceptance and return:** provenance/type/unit/state requirements, what code is unblocked, validation still needed, file ownership and integration owner.
10. **PM decision and return log:** rationale, approved bounded scope or missing evidence, assigned task if any, findings/commit/push verification, integration tests and closure reason.

ESC-001 is closed as a resolved contract. ESC-002/003/004 are deliberately incomplete request packets, not a claim that every permitted method was exhausted. The regular AI must complete the attempt/alternative/reason fields or close a question with new supported findings; the PM then decides assignment. No technical Daybreak request is currently approved.
