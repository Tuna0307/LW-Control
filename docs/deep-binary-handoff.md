# Daybreak task — specialist escalation only

Updated 2026-09-09 by project-manager review 5 against `d4e9790`. Read [AGENTS.md](../AGENTS.md) section 6, [task.md](../task.md), [the current audit](lwbridge-project-status.md) and [the escalation register](daybreak-escalations.md). This replaces the earlier broad binary-research assignment; its [historical catalog](reviews/2026-09-09-prior-deep-binary-handoff.md) remains a source map, not an active queue.

## What your previous task actually completed

The task **Start deep binary handoff** delivered `6e1d80f` (R6-006 boolean filters) and `d5be1d8` (R6-007 keyword escaping/search). Those checkpoints are integrated. The regular AI subsequently corrected the reindeer UI kind gate to truck-only (R6-010) and added R6-008/009/011/012/013. Credit the exact slices; DB-01–06 as a whole and live game functionality are not complete.

## Assignment gate

**There is currently no newly approved technical Daybreak escalation.** ESC-001/002 are PM-seeded `NEEDS_INFORMATION` records; the regular AI must complete their attempt/alternative evidence first. Missing evidence is not to be invented. Do not begin the old DB-01–06 checklist just because it appears in historical instructions.

When an ESC entry is `APPROVED` and specifically assigned to you, take only that entry's bounded question. Confirm its reviewed commit, ownership, source hashes, attempted methods and restrictions. If the packet is insufficient, return exactly what is missing to the PM; do not expand your own assignment to all remaining research.

## Specialist execution and return contract

1. Review the saved evidence and prior failed hypotheses first. Reuse valid work, including corrections/superseding findings. Distinguish a source fact from another AI's claim.
2. Confirm that the requested scope is permitted in your environment. A capability gap may justify tool setup, but an operation denied elsewhere must not be rerouted to you as a bypass. Do not rerun denied commands through a new model/executor or treat installation as permission.
3. Select a narrow evidence-producing approach that addresses the documented research/capability gap. Document tool/source/version/path/locator and intermediate conclusions. If no permitted method can progress, return the exact restriction and the remaining uncertainty; do not guess.
4. Record confirmed findings immediately under the existing subject document/evidence index with all mandatory provenance fields. Preserve prior findings and explain corrections. Do not copy raw credentials, user databases or unrelated private data into Git.
5. Return an explicit table: ESC ID; question answered; source/hash/locator; RECOVERED versus saved-evidence interpretation; exact result/types/units; remaining unknowns; which code/features are unblocked; validation required; commit/remote.
6. Set the entry to `RETURNED_FOR_INTEGRATION`; the regular AI and PM verify/integrate and close it. Completion of your research checkpoint does not sign off a feature or authorize a live action.

No live launch/injection, travel, claim, plunder, ticket fabrication or messaging is assigned by this research supplement. Follow any later explicit assignment within higher-priority restrictions. Preserve the login-free UI and fail-closed unknown operations.

## Topic map — not model assignments

| Topic | Still unresolved / ownership |
|---|---|
| DB-01/02 launch | Remaining inputs/helper/ownership/caller/unit/variant semantics and external service boundaries; regular AI first. Historical SB-01/02 remain operation-specific restrictions. |
| DB-03 native maps | Per-kind keys/types/normalization/update/removal; regular AI first. |
| DB-04 scheduler/capture | Coverage/order/timing, queue/drop/ack, cancellation/resume/completion; regular AI first. |
| DB-05 queries | Completion/reward clocks, plunderability, treasure visibility/lucky, alternate sorts; do not redo implemented keywords, boolean gates, treasure selectors, levels or quality. |
| DB-06 status/actions | Authoritative pending/status/travel/actions/job semantics; regular AI first and future live proof remains separate. |

The user wants specialist time spent only where documented regular-AI attempts have reached a real research/capability limit. If there is no approved request, do not manufacture a technical assignment merely to keep the specialist busy.
