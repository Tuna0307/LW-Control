# LWBridge Overview and Map Data feature ledger

Checkpoint: 2026-09-10. Status vocabulary follows `task.md`: **RECOVERED** is
original static behavior, **IMPLEMENTED/OFFLINE-TESTED** is rebuild behavior
proved without a live state change, **LIVE-PROVEN** requires current-client
before/after evidence, and **UNKNOWN/BLOCKED** remains open.

## Project-manager review 11 — implementation `37a1dac` (2026-09-10)

[Current audit](lwbridge-project-status.md) accepts R7-002 replay hardening and fresh passing tests, plus R5-006 pipe-path construction. The new replay UI has pending visual proof (PM11-02). At the audited `37a1dac` checkpoint PM10-01 had no fresh app acquisition; post-review `LWB-R7-003` below now supplies two bounded fresh current-client reads and awaits PM acceptance. No full case is newly signed off without that review; do not repeat accepted replay or pipe-name work.

**PM8-0 closed by the user's reinstall**; AI verified the resulting official update/startup. [PM9-A](lastwar-update-readiness.md) is TODO for future update-readiness/compatibility integration. Main build/sampled core hashes unchanged; scripts 12 -> 14; table changed earlier. No rebuilt feature is LIVE-PROVEN by official-game recovery.

[ESC decisions](daybreak-escalations.md) unchanged: none assigned. The [25–30% estimate](lwbridge-completion-estimate.md) is the historical review-9 planning baseline, not a newly measured score. **0/47 full acceptance cases signed off**.

Current delivery order was changed by the user after review 9: [first real connection/resource/display demonstration](first-live-result.md); PM9-A is deferred. `LWB-R7-001` now proves a bounded current-game resource acquisition plus isolated rebuilt display for one row. Production bridge/session ownership and fresh `map_scan_start` remain open. Standing game/process/Computer Use testing permission is recorded in AGENTS.md.

`LWB-R7-002` implements PM10-02 (visual verification pending) and closes PM10-03 as IMPLEMENTED/OFFLINE-TESTED and preserves the reusable R7-001 acquisition recipe/source under PM10-01. Saved replay now has a dedicated in-memory store, visible capture provenance, automatic Resource selection, disabled acquisition controls and unavailable/replay scan state. Focused importer/isolation/production-gate checks pass. PM10-01 remains open for fresh app-initiated acquisition and the required second fresh read.

`LWB-R5-006` recovers the original per-user control-pipe path and live-correlates the derived name against the verified LWBridge host. At that checkpoint the rebuild helper was offline-tested while session framing/validation and host-to-proxy request grammar remained the first missing connection contract; R5-007 below supersedes the framing/hello part of that limit.

`LWB-R5-007` narrows that connection gap. The secure proxy is the pipe client and the original host is the named-pipe server. The proxy uses a 4-byte little-endian payload-length prefix, accepts payload lengths `1..0x800000`, and builds `hello` as protocol version `1` with top-level `profileId`, `instanceId`, empty `requestId`, numeric `timestamp`, and payload `token`, numeric `pid`, `buildId`. The original host's `bridge_pipe.rs` metadata contains handshake validation paths for those identity fields plus exact `hello.ack`, disconnect, timeout and write-failure vocabulary. `LWBridgeControlPipeProtocol` implements only the recovered frame codec, hello parser and an explicitly labelled identity-comparison policy. Exact `hello.ack` serialization, heartbeat freshness/readiness and command/result correlation remain **UNKNOWN/BLOCKED**, so no production session service or fresh acquisition is claimed.

Post-review `LWB-R7-003` adds a separate bounded live path without filling those unknown pipe fields. The normal non-isolated app now wires `LiveResourceProbeCommandService` for resource-only `map_scan_start`; it starts a hash-gated current-client helper, requires a new `StartViewRequest` + `UpdateViewRequest(true)` response, reads one source row from `WorldPointManager._pointInfos`, validates request/state/route before persistence, writes it to the normal profile `MapDataStore`, and returns it through the recovered `map_search`/summary surfaces. Two independent requests at `2026-09-10T05:14:45Z` and `2026-09-10T05:15:09Z` completed with exact package restoration; a focused foreign-result regression verifies correlation failure cannot mutate the normal index. This route is **LIVE-PROVEN** for the bounded acquisition and **IMPLEMENTATION POLICY** for transport. It does not set `xluaOnline`, does not enable other map kinds and does not close original-pipe parity. PM review and normal-window visual proof remain pending.

## Milestone A environment evidence

| Item | Current evidence |
|---|---|
| Reference executable | `lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff` |
| Branch / starting HEAD | `research/offline-controller` / `cb762a1ad2bb6a856f0834673217ecf8da01e204` |
| Installed root | `%LOCALAPPDATA%\FunFly\Last War-Survival Game` |
| Official launcher | SHA-256 `B6E29176F64C11A6F203D414FB50EE1E02293BE318EFDA9C33CF221C83D763BC` |
| Current `Game\LastWar.exe` | SHA-256 `DF5ABCF8618D48500BEFA9F587B509ED4F58373FF34932BB87CE217F0CF267D5`; file/product version `2019.4.40.16762411` |
| Current original xLua | SHA-256 `21EB704AFDB7E528F4B90FA1B90BF414C221B06BA990D625AAAAED31B292740F` |
| Baseline process state | No matching `LastWar` or `LastWarLauncher` process during the self-check |
| Official runtime evidence | `docs/official-runtime-architecture.md` + `evidence/official-runtime/2026-09-10-pm8-004-official-runtime.json`; read-only inspectors reproduce version/hash/PE/container/hot-update/launcher/startup evidence |

## Shared / Overview matrix

| ID | Original contract / trigger | Rebuild status | Proof / remaining gate |
|---|---|---|---|
| S01 Game status | `get_status(profileId)`, `proxy_status(profileId)`, `bridge://status` | IMPLEMENTED/OFFLINE-TESTED foundation | Native adapter reports real process existence separately from `xluaOnline=false`; bridge heartbeat/session readiness is still UNKNOWN/BLOCKED until bootstrap transport is complete. |
| S02 Pending-task counter | `get_status.pending` | UNKNOWN/BLOCKED | Exact original semantics still need recovery; do not treat an arbitrary queue length as authoritative. |
| S03 Refresh Status | status + proxy + recovery calls; recovered frontend also uses allowlisted `call_lua("getStatus", ...)` | PARTIAL IMPLEMENTED | Real local status calls route through native RPC. Current runtime `getStatus` call is BLOCKED on bridge bootstrap. |
| S04 Theme | original local UI state + `set_window_theme` | IMPLEMENTED/OFFLINE-TESTED | Existing fixture captures remain clean after native adapter integration. |
| S05 Language | nine recovered bundles + `lastwar_localize` | PARTIAL | Nine UI bundles preserved. Runtime-derived names remain BLOCKED until live bridge/localization path is available. |
| S06 Cross-server | `server_jump({serverId})`, history commands | PARTIAL IMPLEMENTED / BLOCKED live travel | Recovered history migration/set contract is persisted per local profile: integer server IDs 1–99999, unique, capped at five. Authoritative current-server travel and conflict handling remain unimplemented. |
| S07 Navigation | recovered React `Activity` views/listeners | IMPLEMENTED/OFFLINE-TESTED foundation | All nine deterministic desktop captures pass after adapter integration. Live scheduler ownership still needs audit. |
| O01 Game root | `game_root_status()`, `game_root_select()` | RECOVERED result contract + PARTIAL IMPLEMENTED/OFFLINE-TESTED | Recovered Overview reads `{canceled,valid}` from picker result. Native folder picker now preserves cancel as `{canceled:true}` and returns invalid status for the recovered `INVALID_GAME_ROOT` branch; real WebView probe verifies busy/cancel/invalid. Required-file + AMD64 (`0x8664`) PE32+ checks pass. `LWB-R5-002` now reproduces the exact xLua export ABI selector and classifies current build `1.0.361 / 1078` as the bundle's `secure` ABI. |
| O02 Launch Game | `profile_instance_start({profileId,closeUnmanaged:true})` | RECOVERED API + host envelope/ABI selector + partial proof/ticket lifecycle / UNKNOWN-BLOCKED bootstrap | Original staged launcher/hook/proxy sequence recovered. `LWB-R5-001` proves the outer host `LaunchEnvelope` producer/handoff. `LWB-R5-002` proves the `LWXE1\n` + ordinal/name SHA-256 ABI algorithm and exact secure/plain values; the current official xLua matches `secure`. `LWB-R5-003` proves child `LaunchEnvelope` parsing, descriptor expiry/shape gates, two-segment proof signature/timing checks and `LWLT1`/`LWLT2` ticket grammar. `LWB-R5-004` records previously observed static outer `LeaseActivationResponse.launchProof` propagation, the exact `primary_official` / `cached_reusable` / `independent_official` ticket-source taxonomy, `ticket_missing` transition and cached `pid`/ticket/expiry fields before fallback. `LWB-R5-005` records from retained bounded disassembly the child post-start poll branches: length/sequence mismatch -> `LAUNCH_TICKET_OWNERSHIP_CHANGED`, stored-deadline crossing -> `LAUNCH_TICKET_CONSUMPTION_TIMEOUT`, and mapped result variants -> `LAUNCH_TICKET_CONSUMPTION_FAILED`; opaque input identities, units and variant semantics remain open. Producer/consumer and external trust-service boundaries, remaining descriptor semantics, `LWLT2` extra-field meaning, poll caller/input/unit/variant meanings and child argument construction remain open (DB-01/02). The `004` verifier and broader `005` analysis have the recorded SB-01/02 restrictions; neither was retried in review 4. Rebuild fails closed with `OVERVIEW_LAUNCH_BOOTSTRAP_UNRECOVERED`; no shortcut launch. |
| O03 Close Game | `profile_instance_stop({profileId,instanceId})` | RECOVERED API / BLOCKED | No owned instance exists yet; rebuild refuses to stop an unmanaged game. Ownership/cleanup comes after O02. |
| O04 Launch at app startup | original default on unless stored false; reconcile/start lifecycle | PARTIAL IMPLEMENTED/OFFLINE-TESTED preference | Native config persists the preference; actual React/WebView tests verify visible rejected-save feedback, rollback to the last confirmed persisted value, ordered rapid saves, both-fail/mixed failures and successful recovery. Fixture/probe modes suppress live startup. Reconcile still does not launch until O02 is complete. |
| O05 Auto reconnect | `set_automation(name:"autoForceUpdateReload",enabled)` / `auto_force_update_reload` | RECOVERED mapping + PARTIAL IMPLEMENTED | Preference is persisted and surfaced. Recovery state machine and live disconnect handling remain BLOCKED on O02/bridge readiness. |
| O06 Recovery/repair UI | `game_recovery_status`, `bridge://game-recovery` and recovered state labels | PARTIAL | Native event boundary exists; real waiting/updating/repairing/launching/verifying/maintenance transitions remain UNKNOWN/BLOCKED. |

## Map Data matrix

| ID | Original contract / trigger | Status | Next proof required |
|---|---|---|---|
| M01 Start/stop/mode/type selection | `map_scan_start(payload)`, `map_scan_stop()` | PARTIAL LIVE-PROVEN BOUNDED RESOURCE START / ORIGINAL LIFECYCLE BLOCKED | `LWB-R7-003` wires normal resource-only `map_scan_start` to a fresh current-client request and repeats it. Other selected types fail closed; original stop/block scheduling/resume/completion and bridge-session ownership remain UNKNOWN/BLOCKED. |
| M02 Native capture | proxy/native map capture path | LIVE-PROVEN BOUNDED CURRENT-CLIENT ROUTE / ORIGINAL PIPE BLOCKED | `LWB-R7-001` proved current-game source acquisition; `LWB-R7-003` now initiates two fresh reads from the rebuild, each requiring a new `StartViewRequest` + `UpdateViewRequest(true)` response before reading `_pointInfos`. The direct command-file/helper transport is IMPLEMENTATION POLICY and does not claim original LWBridge pipe capture ownership. |
| M03 Typed persistent index | map index service | RECOVERED SCHEMA / IMPLEMENTED-OFFLINE-TESTED FOUNDATION + BOUNDED REAL-ROW ADAPTER | Original SQLite tables/indexes and stored identity `(kind,server_id,record_key)` are recovered and reproduced. `LWB-R6-011` also recovers schema-version metadata/future-schema/legacy-import strings, but versioned migration stays gated on the supported version constant, migration threshold and timestamp unit. `LWB-R6-019` adds test-only transactional one-kind/server replacement from one staging run and proves run/kind/server isolation; `LWB-R6-020` proves an injected failure between delete and staged copy rolls the transaction back to the previous published rows. `LWB-R6-028` adds test-only restart persistence over the recovered `scan_blocks(run_id,block_index)` identity and proves reopen plus clear-cascade behavior. `LWB-R6-029` records that the tracked build-1078 official-runtime readable snapshot contains no direct record-key/scan-bridge or selected map-row vocabulary, and identifies current `Assembly-CSharp.rdl`/active `LWScripts.data` as the next bounded sources. `LWB-R6-034` closes the RDL decoder setup gap by restoring a proven read-only RG/RGMD metadata/CIL inspector into this repository; `LWB-R6-035` extends it with type-reference searches that recover managed ownership links such as `GameEntry.Data -> CustomDataManager.Player`. `LWB-R6-047` pins the original native map/staging upsert helper and shared 14-field input layout; `LWB-R6-048` pins current build-1078 point-index/UUID lookup dimensions; `LWB-R6-049` closes the original normalized `record_key` rule and its write to `+0x78`: ordinary indexed records use signed-decimal point identity, while truck/railway, records carrying `marchUuid`, or records without a point index use nonempty `uuid` else `marchUuid`; `LWB-R6-050` pins current `PointInfo.pointIndex/mainIndex/uuid` and `ResPointInfo.gatherMarchUuid` ownership plus the exact xLua backing tokens and constructor stores without naming unresolved source calls; `LWB-R6-051` pins original raw-to-normalized `level`, `quality`, `power`, `distance`/`distanceFromHome`, and `shieldEndTime`/`protectEndTime` sources and fallback order. `LWB-R7-001` adds an isolated IMPLEMENTATION POLICY adapter that imports one hash-attributed live capture row into `MapDataStore` and displays it via `map_search`; `LWB-R7-002` guarantees that replay owns a separate in-memory store and cannot silently contaminate a normal production index. Unsupported `resourceNameKey` and gather occupancy stay unknown. Direct production capture/serializer ownership, remaining typed normalization/removal, completion eligibility and retry semantics remain UNKNOWN/BLOCKED. |
| M04 Progress/failure/stop/resume | `map_scan_status`, `bridge://map-scan-status` | PARTIAL BOUNDED STATUS / ORIGINAL LIFECYCLE BLOCKED | R7-003 returns source-backed server/request/phase/selected-type state for the one-view acquisition and keeps unmeasured block/retry/native metrics null. Original scheduler/ack/retry/cancel/resume/completion, heartbeat readiness and full progress serialization remain open. |
| M05 Clear | `map_scan_clear({serverId})` | IMPLEMENTED/OFFLINE-TESTED SLICE | Backend/store transaction deletes selected-server scan runs and records, preserving marks and other servers; tests pass. Active-run cancellation, generation/late-response rejection and failure/restart cases remain open. |
| M06 Result tabs/actions | `map_data_options`, `map_search`, `map_summary` plus conditional commands | PARTIAL LIVE-PROVEN RESOURCE SEARCH/SUMMARY | [Map Scan findings R6-003–045](lwbridge-map-scan.md) preserve the recovered query/aggregate contracts. R7-003 proves the normal profile store returns the freshly acquired resource through `map_search` and `map_summary` reports `resource: 1`. Public `map_data_options`, unrelated actions, original bridge readiness and unsupported filters/handlers remain fail-closed. |
| M07 Search/filter/sort/page/live refresh | `map_search({kind,query})` | PARTIAL IMPLEMENTED/OFFLINE-TESTED | `LWB-R6-003` implements server/kind scope, recovered `LIMIT/OFFSET`, `updatedAt asc|desc` plus `record_key ASC`, `{rows,total}`, city mark projection and `markedOnly`. `LWB-R6-004` recovers the frontend stale-search guard and eight-tab envelope distinctions. `LWB-R6-005/006/007/009/010/013/014` recover and offline-test the supported keyword, city, name-key, retained-item, truck-reindeer, special, treasure-pair, dispatch-level, quality, completion-status and frontend plunderability predicates. `R6-014` also restores the original always-on truck/railway active-arrival cutoff from one sampled precise Unix-ms wall clock. `LWB-R6-008` keeps count/page on one SQLite read snapshot as an explicit rebuild policy and proves it against a concurrent WAL writer. `LWB-R6-026` recovers the frontend ordered multi-sort toggle and exact public per-kind sort columns, pins eleven scalar native parser comparisons, four direct expression references, and the adjacent sort/null-order string inventory; it also corrects item-count SQL text to raw `0x00C8A926` after a descriptor byte at `0x00C8A925`. `LWB-R6-027` recovers physical direction selection as `asc -> ASC`, ordinary `desc -> DESC`, with an exact `distance desc -> ASC` override. The distance value mechanism and complete native multi-sort/null/tie-break composition plus remainingLootCount/shield/distance/railway-quality mappings remain open, so treasure visibility/lucky semantics and every non-`updatedAt` production sort stay fail-closed with IMPLEMENTATION POLICY `MAP_QUERY_UNRECOVERED`. |
| M08 Mark/unmark | `map_player_mark_set`, `bridge://player-mark-changed` | IMPLEMENTED/OFFLINE-TESTED | Original mark key `(server_id,owner_uid)`, upsert/delete, clear-survival and frontend refresh event are recovered and reproduced. Live player-tracker relocation/state transitions remain unproven. |
| M09 Export | `map_city_export` | RECOVERED FRONTEND + PARTIAL NATIVE FORMAT / BLOCKED WRITER | `LWB-R6-017` pins city-only eligibility, positive selected data server, shared city query page 1/pageSize 200, exact twelve frontend header values/translation keys, localized sheet/yes/no options and consumed `{canceled,rowCount,path}`. `LWB-R6-018` recovers native default/dialog/error vocabulary plus the embedded A-L OOXML package, frozen header, widths, autofilter/margins, styles and partial D-L row-field adjacency. The rebuild still stops at `MAP_INDEX_UNAVAILABLE`: exact A-C mapping, protect/shield fallback, per-column types, internal pagination/full filtered scope, filename timestamp/default directory and lossless large-ID reopen proof remain UNKNOWN/BLOCKED. |
| M10 Jump/follow | `map_coordinate_jump`, `map_march_follow` | RECOVERED API / BLOCKED | Authoritative game focus/follow outcome. |
| M11 Auto Scan | recovered frontend orchestration + scan/server-jump calls | RECOVERED frontend / BLOCKED | Single-owner durable scheduler, multi-server travel, return-to-origin. |
| M12 Treasure | treasure refresh/claim/status commands | RECOVERED API / BLOCKED | Per-player state and authoritative claim result with suitable target. |
| M13 Dispatch plunder schedule | dispatch schedule/cancel commands | RECOVERED API / BLOCKED | Durable job identity, expiry/eligibility and outcome reconciliation. |
| M14 Truck/train actions | truck schedule/cancel and recovered row actions | RECOVERED API / BLOCKED | Current eligibility and authoritative plunder/train outcomes. |
| M15 Scheduled Plunder tab | `map_plunder_jobs_list` | RECOVERED API / BLOCKED | Durable job store and state reconciliation. |
| M16 Alliance share | `map_dispatch_share_alliance` | RECOVERED API / BLOCKED | Offline payload validation first; live delivery only under messaging authorization. |

## Native adapter implementation ledger

| Capability | Original source | Rebuild implementation | Validation |
|---|---|---|---|
| Command names/payload wrappers | `evidence/lwbridge-0.3.1/frontend/assets/api-ClPPi2JT.js` | `src/LWBridge.Desktop/LWBridgeBackend.cs` | Recovered wrapper strings inspected; unsupported production commands reject explicitly. |
| WebView command/event transport | original Tauri invoke/listen wrapper + rebuild host policy | `LWBridgeWindow.cs` + `WebUi/preview-host.js` + request/subscription registries | Active-profile injection/event envelopes restored. Isolated native WebView proves duplicate active-ID rejection, reload rotation/cancel/subscription reset, stale-session and external-navigation rejection, structured errors, responsive slow storage and no late publication after real window close. Real lifecycle-worker state mutation remains gated on R5/R7. |
| Fixture/read-only isolation | rebuild requirement | `preview-host.js`, `LocalConfigStore`, bootstrap/probe modes | Fixture commands cannot reach live handlers; capture/read-only modes remain isolated. The native host probe uses a unique temp config plus in-memory map DB, reports startup auto-launch suppressed, touches no user config and performs no live-game command. `LWB-PM6-001` also bounds cleanup of the owned probe child and CI exercises a deterministic termination-command-failure path without touching unrelated processes. Requested live without native transport still fails visibly. |
| Stable local profile | original profile routing + rebuild persistence policy | versioned config plus recovered U/W routing | Specified PM2 config/identity regressions pass. Single-save rollback, visible errors, rapid both-fail/mixed-success reconciliation and post-failure recovery pass in provider and real native-host tests. |
| Game-root validation | `game_root_status`, `game_root_select` | `GameInstallationService.cs` + native picker | Installed/missing-root diagnostics and recovered cancel/invalid picker behavior pass in the isolated real WebView host. Both installed `LastWar.exe` and `xlua.dll` report AMD64 `0x8664/PE32+`; `LWB-R5-002` separately proves the exact export ABI fingerprint selector and current secure classification. |
| Launch safety gate | `profile_instance_start` | `LWBridgeBackend.cs` | Self-check requires `OVERVIEW_LAUNCH_BOOTSTRAP_UNRECOVERED`; no unmanaged launch is attempted. |
| Bounded fresh resource acquisition | recovered Map Data `map_scan_start` wrapper + current-client `WorldPointManager` source | `LiveResourceProbeCommandService.cs`, `LiveResourceProofRunner.cs`, `tools/run_live_resource_probe.py`, `tools/current_live_resource_probe.lua` | `LWB-R7-003` performs two distinct rebuild-generated requests, waits for a fresh view-response flag transition, imports one resource row into the normal profile store, returns it through `map_search`/summary, and verifies exact script-package restoration. Direct transport is IMPLEMENTATION POLICY; original pipe/session remains blocked. |

## Current verification commands

```powershell
python tools/build_lwbridge_frontend.py --check
python tools/inspect_official_runtime.py --output evidence/official-runtime/2026-09-08-official-runtime.json
dotnet build src/LWBridge.Desktop/LWBridge.Desktop.csproj --configuration Release
dotnet run --project tests/LWBridge.Desktop.Checks/LWBridge.Desktop.Checks.csproj --configuration Release
node tools/check_lwbridge_live_transport.cjs
node tools/check_lwbridge_transport_boundary.cjs
./tools/capture_lwbridge_ui.ps1
```

Review 9 reran build/backend and frontend/preference/transport checks; refreshed read-only runtime/health comparison. No new binary-analysis/game action/host screenshot matrix. See [review evidence](../evidence/lwbridge-implementation/2026-09-10-pm-review-9.json).
