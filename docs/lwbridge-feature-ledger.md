# LWBridge 0.3.1 parity feature ledger

**Current through:** `LWB-R8-078`, 2026-09-27.

This ledger now tracks parity with the original program, not whether our reconstruction happens to work.

## Whole-product surfaces

| Surface | Current parity state | Notes |
|---|---|---|
| Reference artifact | EXACT_BYTES authority | Hash reverified 2026-09-24 |
| Retained frontend assets | EXACT_BYTES-derived | Original chunks/styles/icons/locales recovered; transformed boundary and prior retained-scope product deviations still require cleanup. R8-067 inventories 104 exact reference API commands and 15 product event literals; after 10 explicit account/activation exclusions, 94 retained frontend commands remain, with 61 specific production routes and 33 unrouted commands. R8-068 closes generic Automation Configure/Start/Stop at the owner-excluded authorization-derived `premium`/`admin` request boundary. R8-069 closes Chat Automation Configure/Run Pending, and R8-070 closes City Layout Validate/Apply Start/Apply Cancel, all at mandatory owner-excluded authorization-state admission. R8-071 closes Dispatch Assist Schedule/Cancel/Retry at their recovered live-provider, DB-mutation, and mandatory owner-excluded authorization boundaries. R8-072 closes VIP18 Base Apply/Restore at exact provider, post-provider config mutation, and non-atomic ordering boundaries. R8-073 closes Equipment Preset Apply, Resource Automation Run, and Trade Station Configure at their exact admission/provider/config/planner boundaries. R8-074 closes Dispatch Alliance Share and Treasure Claim at exact host/provider/database boundaries. All 33 unrouted retained frontend commands are now audited/fenced, with 0 genuinely unclosed. Native-only recovered handlers prove this is not yet the complete native command inventory |
| Auth/account flows | DEPENDENCY RECOVERY ALLOWED / standalone UI non-priority | Owner direction changed 2026-09-26: authentication/authorization/entitlement/account-session internals may be recovered when required by retained features. Do not invent credentials, capacity, premium/admin or role state. Login/Register/account-management product UI remains non-priority unless the retained-state pipeline requires it; historical authorization fences must be revisited rather than treated as permanent exclusions |
| Overview | EQUIVALENT_REIMPLEMENTATION / PARTIAL profile registry | R8-029 replaces the synthetic `profile_list` object with an evidence-backed controller registry for the normal retained single-profile path; R8-030 restores exact `profile_note_set`; R8-031 restores `profile_reorder`; R8-032 restores the fixed `profile_primary_set` guard and primary uniqueness index; R8-034 restores `profile_select` guards, selected-profile persistence, and best-effort verified game-window focus; R8-038 restores exact `game_root_status` public field/candidate schema, source ordering, empty state, lightweight validity predicate and `STATE_UNAVAILABLE`, with equivalent LocalConfig/process/registry plumbing; R8-039 restores exact `game_root_select` `{canceled,path,valid}` outcomes, normalization, valid-only persistence, `STATE_UNAVAILABLE` and `INVALID_GAME_ROOT`, with equivalent WinForms dialog/LocalConfig plumbing; R8-040 restores the native `proxy_status` full/reduced projection, `resourceMissing`/`targetMissing`/`needsRepair`/`installed` state precedence, secure/plain mode classification, runtime ownership projection, repair predicate and stable profile/runtime error codes; R8-041 restores exact `game_recovery_status`/`bridge://game-recovery` 11-field serialization, scalar `startedAt`/numeric `noticeId`, native idle defaults, stable notice identity/visibility, terminal `succeeded`/`failed` status and strict runtime/state errors while leaving process-control policy unchanged; R8-042 restores the native `profile_instance_status` public result and connection-state projection; R8-043 recovers the exact seven-field `get_status` top-level schema, route-backed `offline`/`named-pipe` ownership, scalar pending/activity semantics and state error, but keeps `get_status` classified as a rebuild deviation because the native runtime-config migration/normalization result is not yet fully recovered; R8-058 closes exact main-window `app://close-requested` `{instanceCount}` plus global `app_exit_confirm` shutdown ordering, `bridge_exit`, game-close timeout, original-proxy restoration, `APP_EXIT_FAILED` and JSON-null success, while runtime remains fenced because the original proxy install/backup/restore mutation primitive is still absent. R8-062 closes exact `profile_delete` admission/guards, registry + selected-profile transaction, post-commit data cleanup, runtime reconciliation and refreshed profile-list result, but keeps destructive execution fenced on owner-excluded authorization-state admission. R8-063 closes exact `profile_enable_set` capacity-set semantics, valid capacity domain, primary/selection invariants, `enabled`/`locked_reason='license_capacity'` row mutation, selected-profile repair and refreshed list result, but keeps execution fenced because capacity is owner-excluded authorization/license state. R8-064 closes exact `profile_create` quota rejection, 16-byte unpadded Base64URL ID, next-order `账号 N` naming, enabled/unlocked/non-primary insert defaults, 13-field created-profile result and separate frontend selection, while execution remains fenced on entitlement-derived capacity plus incomplete post-create runtime cleanup semantics. R8-065 freshly live-proves the retained manual launch/connect/close and startup auto-launch/connect/close lifecycle against current-v21; these paths are LIVE-WORKING but remain equivalent reimplementation rather than closed parity. R8-075 re-audits the production host↔proxy transport: framing/hello/ack/authentication/command-result/correlation/listener limits/reconnect cleanup are source-backed and the read-only RPC route is live-proven; remaining exact protocol gaps are wire-heartbeat activity ownership, disconnect/write-to-pending failure mapping, and original secure/plain xLua script dispatch. Proxy mutation/launcher lifecycle still need recovery |
| Automation | UI EXACT_BYTES / PARTIAL backend | R8-026 restores Resource Automation config/status/event; R8-027 restores generic `automation_status`; R8-033 restores exact host-local red-packet/treasure claim-delay validation plus mirrored scheduler/chat persistence; R8-045 closes the exact `dispatch_assist_state` top-level schema, unavailable-state error and persisted-job query/order but fences success because the live alliance-dispatch provider/task projection remains unrecovered; R8-048 restores exact read-only `trade_station_catalog`; R8-051 closes `automation_inspect` exact auth-unavailable/disconnect/provider/5-second boundaries and identifies its `allianceGarrison` result rewrite, while implementation stays fenced on owner-excluded role/request state plus incomplete rewrite semantics; R8-060 closes `construction_rewards_claim` profile injection, authorization/profile/runtime/route admission, `claimConstructionRewards` `{}` at 5 seconds, shared Lua-call errors and raw provider JSON return, while the live claim remains fenced on owner-excluded authorization state. R8-068 closes generic `automation_configure` / `automation_start` / `automation_stop`: exact handlers, payloads, shared admission chain, native Configure validator, `configureAutomationTask` / `startAutomationTask` / `stopAutomationTask`, 5-second deadlines and generic result conversion are recovered, but runtime stays fenced because native requests include owner-excluded authorization-derived `premium` / `admin` material. R8-069 closes `chat_automation_configure` / `chat_automation_run_pending` through retained kinds, config validation, 5-second provider calls and generic results, while fencing execution on mandatory owner-excluded authorization-state admission. R8-071 closes Dispatch Assist Schedule/Cancel/Retry through exact handlers, live refresh/arm/cancel providers, persisted job mutations, event emission and validation errors; execution remains fenced on unrecovered live task projection plus mandatory authorization admission. R8-073 closes Resource Automation Run and `trade_station_configure`: exact task mappings/state/busy gates/10-second resource call and exact Trade Station fields/validation/5-second provider/config ownership are recovered, while execution remains fenced on authorization-derived request state and/or unrecovered shared config ownership. Other live actions remain incomplete/provider-gated |
| Map Data | MIXED / not parity-complete; core Manual scan LIVE-WORKING | Large amount recovered; R8-036 restores native `server_jump_history_get/set/import`; R8-044 restores exact `game_asset_image` public behavior plus native-style cache. R8-054 revalidates `lastwar_localize` requested→English→key fallback, 200-key cap and locale-cache error vocabulary, but classifies the current locale service as partial/equivalent because native authorization-state admission is owner-excluded and the rebuild's strict `INVALID_PAYLOAD` parser differs from native permissive outer defaults. R8-065 freshly live-proves same-session all-eight Normal and Fast scans on current-v21 at 2500/2500, 0 failed, 0 unread, with persisted results surviving database reopen after a bounded 9-row AOI compatibility correction. R8-066 additionally live-proves the normal production desktop/WebView path through lifecycle → rendered Start Reading → production Fast Resource scan → native `map_search` → correlated six-cell Resource row, exiting 0 with clean process shutdown. R8-074 closes the retained `map_dispatch_share_alliance` and `map_treasure_claim` host contracts while keeping their live actions fenced on excluded authorization/protected executor boundaries. R8-076 moves original acquisition from UNKNOWN to PARTIAL EXACT_CONTRACT by source-linking `map.records` run-scoped `{type,payload}` batches through the eight-kind/selected-type gate, shared normalized builder and transactional `scan_records`, and separating passive `map.native.capture` updates into published `map_records`. R8-077 proves the host only folds `pendingAcks` into reported `nativePendingRecords`; it does not parse `acks[]` or use the pending aggregate as a terminal admission predicate. R8-078 proves `map.scan.diagnostic` is log-only, while `map.scan.error` performs ordinary failed-run cleanup with native Stop and no staged-row publication; host terminal failure after `map.scan.complete` instead preserves staging into `map_records` and does not resend Stop. Exact ack item consumption/linkage therefore remains below the host. Protected traversal/order/coordinates, `XluaBridgeMapScanTick` scheduling/pacing, queue→batch/ack drain and remaining serializer details are still open; the current scanner remains live-working equivalent plumbing |
| Squads / AFK | UI EXACT_BYTES / PARTIAL backend | R8-023 restores equipment config persistence; R8-024 restores Monster AFK config validation/persistence/status reload; R8-025 restores Alliance Garrison config validation, `tasks.allianceGarrison` persistence and status reload; R8-046 restores exact `squad_list`; R8-047 restores exact `monster_catalog_options` profile/runtime/disconnect/timeout behavior and raw `getMonsterCatalogOptions` JSON forwarding through equivalent authenticated transport. R8-073 closes `equipment_preset_apply` through preset admission, `getSquads`, `applyHeroEquipment`, progress event and provider-result boundaries while fencing execution on authorization state plus incomplete multi-hero planner/aggregation semantics. Live AFK/garrison execution and remaining squad/config commands remain incomplete/protected |
| City Layout | UI EXACT_BYTES / PARTIAL backend | R8-018 restores per-profile `profile_state` draft persistence and `city_layout_draft_get/save/clear`; R8-050 closes the exact snapshot host boundary but fences implementation on its owner-excluded authorization-derived request; R8-052 closes exact `city_layout_apply_status` authorization-unavailable/disconnect/`getCityLayoutApplyStatus`/5-second/generic-result behavior while fencing implementation on native auth-state admission. R8-070 closes `city_layout_validate`, `city_layout_apply_start`, and `city_layout_apply_cancel`: exact handlers, payloads, fresh-snapshot/in-city gate, provider methods, 10s/10s/5s deadlines and generic results are recovered, but execution remains fenced because all three require mandatory owner-excluded authorization-state admission. Planner/executor remain protected |
| Hotkeys | UI EXACT_BYTES / PARTIAL backend | R8-019 restores exact ten-field config/defaults, `hotkey_config_get/save`, native `INVALID_REQUEST` / `STATE_UNAVAILABLE` vocabulary and per-profile runtime config persistence; keyboard/game-action execution remains protected/unimplemented |
| Mini-games | UNKNOWN / PARTIAL EXACT_CONTRACT | Original UI exists. R8-059 closes `monopoly_cell_open`: shared-wrapper selected-profile injection, authorization-state precedence, native profile/runtime/disconnect errors, one `openMonopolyCell` `{}` call at 5 seconds, shared Lua-call failure mapping and raw provider JSON return. Runtime action remains fenced because native authorization-state admission is owner-excluded |
| Settings | PARTIAL backend / original UI contract recovered | R8-020 restores visual-metrics persistence; R8-021 restores updater idle status; R8-022 restores feedback result/progress schemas and blank-`exportId` failure; R8-028 restores native `profile_settings_save` revisioned `settings` persistence for the current local profile; R8-035 restores exact `set_window_theme`; R8-037 restores `append_log`; R8-053 closes exact `profile_settings_get` payload/read/`{profileId,revision,value}` projection but fences the public getter on owner-excluded authorization-state admission; R8-056 closes exact `watermark_lookup` trace-code normalization, authorization/role gate, `/api/watermark/lookup` service path, generic HTTP errors and raw JSON return, while runtime remains fenced on owner-excluded authorization-role/service state. R8-057 closes native `update_status` as a read-only nine-field snapshot over seven exact phases, including 60-second manual-check cooldown, manifest metadata, download progress/directory, opening=100%, error publication and `bridge://update-status`; the rebuild is exact only at idle because updater actions/state ownership remain fenced. Full feedback archive, updater check/download/open execution, full log rotation/segment retention, and remaining multi-profile settings/registry behavior remain incomplete |
| Advanced | EXACT_CONTRACT visibility / backend intentionally fenced | Original normal build hard-hides the page. R8-049 records the VIP18 base read contracts but leaves them unimplemented because `vip18_base_list` carries its dynamic cache identity through an excluded authorization-state-dependent path and `vip18_base_config_get` depends the unrecovered shared config-state normalizer. R8-061 closes `vip18_base_config_save` retained-field validation, exact invalid skin/auto-apply errors, `vip18_profiles`/`config.json` persistence ownership, config-state unavailable behavior and three-field success projection. R8-072 closes `vip18_base_apply` / `vip18_base_restore`: exact 10-second provider calls, Apply `selectedSkinId` patch, Restore `selectedSkinId=null` + `autoApplyOnStart=false`, non-atomic provider→config ordering and raw provider JSON success; runtime remains fenced on owner-excluded authorization state plus the unrecovered shared config migration/normalizer |

## Map Data parity

| Feature | Parity state | Required work |
|---|---|---|
| Original scan command envelope | EXACT_CONTRACT | Preserve |
| Original selected-type allowlist | EXACT_CONTRACT | R8-012 restores exactly eight public Manual kinds; preserve |
| Manual `map_scan_start` mode/default/error contract | EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION | R8-012 restores Manual UI/persistence, Normal=8/Fast=20, null/non-string default-to-Normal and exact invalid-string error/precedence; R8-013 corrects `retryCount:2` to frontend fallback only |
| Normal/Fast concurrency 8/20 | EXACT_CONTRACT | Preserve; protected pacing/retry differences remain separate |
| City | EQUIVALENT_REIMPLEMENTATION | Recover original acquisition algorithm |
| Resource | EQUIVALENT_REIMPLEMENTATION | Recover original acquisition algorithm |
| Monster | EQUIVALENT_REIMPLEMENTATION | Recover original acquisition algorithm |
| Truck | POSSIBLE DEVIATION | Prove direct Train-list mapping against original |
| Railway | POSSIBLE DEVIATION | Prove direct Train-list mapping against original |
| Dispatch | DEVIATION/UNKNOWN | Remove wide-FOV/Quick-Find assumptions; recover original |
| Ghost | EQUIVALENT_REIMPLEMENTATION | Recover original |
| Treasure | PARTIAL EXACT_CONTRACT + FENCED live claim / DEVIATION status route | R8-055 closes native `map_treasure_claim_status` authorization/provider/5-second/cache-update/raw-provider-return behavior and audits the current rebuild as non-exact. R8-074 closes `map_treasure_claim` authorization-first admission, 5-second `getCurrentServerId` preflight, server/scope/default validation, exact candidate-query ownership, 5-second `claimTreasures` envelope, ten immediate result counters and frontend polling contract; live execution stays fenced on owner-excluded authorization state plus protected scope/lucky/scout internals |
| City Excel export | EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION | R8-007 restored API/UI/dialog/paging/workbook/result contract |
| `map_scan_clear` | EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION | R8-008 restored positive current-live-server admission, server-scoped deletion, player-mark preservation and Manual-only Clear |
| `server_jump` public result | EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION | R8-009 restored destination `serverId` alongside `previousServerId` and `changed`; protected travel internals remain separate |
| `map_summary` | EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION | R8-010 restored exact three-field envelope, eight original kinds and shared-state active/published count source selection |
| `map_data_options` | EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION | R8-011 restored recovered top-level order, eight original count kinds, Resource/Monster name families and exact active-run vs server-scoped published selection; rebuild-only `zombie_boss`/`monsterLevels` removed |
| Manual Scan public contract | EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION | R8-012 restores exactly eight kinds and the recovered Normal/Fast request/default/error/UI contract; Auto Scan and protected acquisition remain separate |
| Current-client Manual Normal/Fast execution | LIVE-WORKING / EQUIVALENT_REIMPLEMENTATION | R8-065 fresh real-game proof completed both modes in one owned current-v21 session at 2500/2500 with 0 failed and 0 unread; Fast counts survived database reopen. R8-066 further proves the actual production desktop/WebView Resource path at Fast 2500/2500, 0 failed, 0 unread, with 515 Search rows and a correlated rendered row. Original acquisition remains unrecovered |
| `map_scan_status` / `map_scan_stop` shared state | EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION | R8-013 restores direct shared-state status, lifecycle-dependent native fields, read-only world refresh, publishing→idle completion, idempotent Stop and exact Stop/Clear reset separation; uncommon protected Stop error envelopes remain partial |
| Scheduled Plunder control plane/UI | EXACT_BYTES-derived frontend + EXACT_CONTRACT/EQUIVALENT persistence | R8-016 restores five list/schedule/cancel commands, three tables/indexes, two events and original tab/API/locales; protected robbery execution and exact Truck job-ID entropy remain unclaimed |
| Search/filter/sort/paging | PARTIAL EXACT_CONTRACT | R8-014 restores exact eight-kind admission, original filter ownership/query builder and generic keyword predicate. R8-017 gates unrecovered Monster distance, City shield and Railway quality instead of executing R7 guesses; complete native multi-sort assembly remains partial |
| Mark/Jump/Follow | PARTIAL | Tie every behavior to reference |
| `game_asset_image` | EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION cache/transport | R8-044 restores native source parsing/trim/XOR, exact request/disconnect/PNG errors, single `getAssetImage` call with 15,000 ms timeout, exact one-field `dataUrl` result, disk-cache-before-connect ordering, `asset-cache`/`.png`/SHA-256-lowerhex shape, 60-second maintenance and 256 MiB boundary. Exact native SHA preimage and original transport/file-I/O ordering remain unclaimed |
| Auto Scan frontend scheduler/control plane | EXACT_BYTES-derived + EQUIVALENT public-command wiring | R8-015 restores immutable 0.3.1 config/sanitizer, scheduler state/effect and Auto card byte-for-byte; protected acquisition/travel internals remain separate |

## Original runtime architecture

| Component | Parity state |
|---|---|
| Rust/Tauri host structure | PARTIAL EXACT_CONTRACT |
| profile launcher | PARTIAL EXACT_CONTRACT |
| multi-hook | PARTIAL EXACT_CONTRACT |
| secure/plain xLua proxies | PARTIAL EXACT_CONTRACT |
| bridge pipe framing | PARTIAL EXACT_CONTRACT |
| `bridge-scripts.dat` plaintext | PARTIAL EXACT_CONTRACT / evidence-limited | Substantial package/crypto structure is historically recovered; remaining LWKE1 field-map/AAD work is parked until genuinely new permitted evidence and must not expand into Account/Login/Authentication research |
| protected script handlers | UNKNOWN / retained non-account scope only |

## Completion interpretation

The earlier R7 feature ledger used PASS/LIVE-PROVEN for a reconstructed product scope. Those claims remain valid observations but do not establish one-to-one parity.

For detailed row-by-row status, use `docs/lwbridge-parity-matrix.md`.
