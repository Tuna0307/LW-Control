# LWBridge Overview and Map Data feature ledger

## Current implementation checkpoint - LWB-R7-087, 2026-09-20

Treasure's original **frontend/status claim orchestration is now RECOVERED + OFFLINE-TESTED, while the protected executor remains BLOCKED**. Hash-locked original frontend assets prove that status polling starts only for `queued>0`, runs every 1,000 ms for at most 1,800 attempts, overlays states by UUID, and treats only an existing `batch.state == "running"` as nonterminal. The UI also pins `lwbridge.mapLuckyTreasurePriority` default-on persistence, the complete owner-visible action-phase state/block-reason vocabulary, and the exact single-row Claim disable gate including trimmed UUID, ordinary-complete versus Supplies 1/3/4, active player states, depleted/expired and other-alliance blocking. Production Claim stays absent: protected scope filtering/order, lucky-slot scheduling, scout-slot selection/reservation and exact terminal batch enumeration remain `UNKNOWN/BLOCKED`; SB-79 was preserved and no state-changing action ran. Evidence: [R7-087](../evidence/lwbridge-implementation/2026-09-20-r7-treasure-claim-frontend-status.json).

## Current implementation checkpoint - LWB-R7-086, 2026-09-20

Treasure claim host/direct-current-v19 authority is now **RECOVERED + OFFLINE-TESTED, with the public action still BLOCKED**. The verified original accepts `boxes|season|single`, requires a nonempty `targetUuid` for `single`, defaults `prioritizeLuckySlots=true`, loads exact persisted claim candidates through the completed-ordinary-or-Supplies-1/3/4 query with UUID/expiry guards, and calls protected `claimTreasures` with a 5 s timeout; `getTreasureClaimStatus` is a separate 5 s status call, so queue admission is not claim success. Current-v19 ordinary direct claiming is `detect.event.claim.treasure(uuid:PutLong,targetServer:PutInt)` with `IsHaveGetReward(localUid)` as the UI duplicate gate, `errorCode` as rejection, optional reward on success, `OnGetDigTimesMsg` state mutation, and a UUID-correlated push signal. Supplies uses `CheckBtnState -> LaunchScout(SCOUT_SUPPLIES) -> StartMarch`. The rebuild now reproduces only the recovered request validator, candidate query, direct transport/result classification and transport bounds; protected scope/lucky/scout batch orchestration remains `UNKNOWN/BLOCKED`, production does not route `map_treasure_claim`, and no state-changing action was run. Evidence: [R7-086](../evidence/lwbridge-implementation/2026-09-20-r7-treasure-claim-offline-contract.json).

## Prior implementation checkpoint - LWB-R7-085, 2026-09-20

Dispatch Alliance Share is now **RECOVERED + PAYLOAD/VALIDATION OFFLINE-TESTED, LIVE DELIVERY BLOCKED**. Original 0.3.1 whole-batch validation, exact errors, per-row `shareDispatchTaskToAlliance` timeout and aggregate result fields are pinned. Current-v19 is hash-locked from point-share encoding through Alliance channel selection to `ChatHeroDispatchShare` / `hero.dispatch.share.chat`, including PutLong UUID, PutInt target server, core payload fields and Dispatch label key `456288`. An internal contract builds the source-backed payload and fails closed on unsupported transport widths; the production public command remains absent and no message was sent. Evidence: [R7-085](../evidence/lwbridge-implementation/2026-09-20-r7-dispatch-alliance-share-offline.json).

## Prior implementation checkpoint - LWB-R7-084, 2026-09-20

Dispatch **public schedule/cancel plus durable execution are now IMPLEMENTED + OFFLINE-TESTED end-to-end**. The public schedule command preserves the recovered whole-batch validation, sequential guarded persistence, accumulated scheduled-row return and one post-success change event. Original schedule/cancel server identity is positive signed-64-bit with no 99999 upper bound; Dispatch local persistence/cancel therefore uses `long`, while the current-v19 executor remains restricted to the proven 1..99999 transport domain and fails wider persisted targets before any execution attempt. Live Dispatch plunder acceptance remains NOT_RUN and authorization-gated. Evidence: [R7-084](../evidence/lwbridge-implementation/2026-09-20-r7-dispatch-public-schedule.json).

## Prior implementation checkpoint - LWB-R7-083, 2026-09-20

Dispatch's **durable single-owner worker is IMPLEMENTED + OFFLINE-TESTED**. It wires the recovered active-job store to the current-v19 executor, preserves the 10-second arm lead, due-only offline deferral, expiry and daily-limit terminalization, and increments attempts exactly once through an active-only running transition. The worker deliberately permits cross-server target IDs for the v19 `DispatchSteal(...,targetServer)` executor instead of inheriting Truck's same-server restriction. Safety policy is stricter than the original on restart: stale `running` work becomes terminal `failed / DISPATCH_PLUNDER_CLIENT_RESTARTED`, because prior send state cannot be proven; a guarded running transition also prevents cancel-race resurrection. Public schedule remains disabled pending its final wrapper/event integration. Evidence: [R7-083](../evidence/lwbridge-implementation/2026-09-20-r7-dispatch-durable-worker.json).

## Prior implementation checkpoint - LWB-R7-082, 2026-09-20

Dispatch's **original post-arm/result semantics and internal current-v19 executor are RECOVERED + OFFLINE-TESTED**. Original 0.3.1 result handling is pinned to `map.dispatch-plunder-result` with `(serverId,taskUuid)` identity, `success/errorCode`, optional `serverDayStartAt`, `max(now,executeAt)+30000 ms` result deadline, connection-sensitive arm-failure status, daily-limit stop-all and change event. Current v19 maps `DispatchSteal` to `hero.dispatch.steal` and `Net.Msgs.DispatchTask.DispatchStealMessage`; the internal bridge hooks only that exact handler, preserves 64-bit task identity without Lua `tonumber`, waits for authoritative `executeAt`, rechecks daily/cross-server state, sends once, and makes post-send timeout non-retryable. Public schedule remains blocked pending durable-worker wiring and live-boot/action authorization. Evidence: [R7-082](../evidence/lwbridge-implementation/2026-09-20-r7-dispatch-result-executor.json).

## Prior implementation checkpoint - LWB-R7-081, 2026-09-20

Dispatch's **current-v19 steal prerequisites and original worker arming/persistence primitives are RECOVERED and OFFLINE-TESTED**. The official client sends `DispatchSteal(uuid,targetServer)` only after the recovered completion/protection/already-stolen/daily/per-task/cross-server gates. The current scanner derives `plunderAt` from `protect_times`, `stolenCount` from `stealList.Count`, and `maxStealCount` from `steal_maxtimes`, while keeping `taskExpireTime` unknown rather than equating it with `expiredTime`. Original worker recovery pins a 10-second arm lead, running-before-arm, `armMapPlunder`, 5-second arm timeout, and 30-second response-horizon constant; exact expiry/arm/status/restart/offline/daily-limit SQL is now implemented/tested. Public schedule remains blocked pending arm-result/runtime semantic closure and current-v19 bridge correlation. Evidence: [R7-081](../evidence/lwbridge-implementation/2026-09-20-r7-dispatch-v19-arm-prerequisites.json).

## Prior implementation checkpoint - LWB-R7-080, 2026-09-20

Dispatch plunder schedule/cancel **public validation and persistence contracts are RECOVERED, with the store kernel IMPLEMENTED/OFFLINE-TESTED**. Original 0.3.1 recovery pins the 1..200-row schedule wrapper, decimal task UUID/timing/expiry/steal predicates, exact `INVALID_REQUEST` messages, guarded active-only upsert, `MAP_DATA_ERROR / scheduled plunder job is missing`, cancel target validation, `NOT_FOUND / scheduled plunder job not found`, and `bridge://dispatch-plunder-changed`. Disposable-DB tests prove fresh schedule defaults, active reschedule preservation, running/terminal replacement rejection, cancellable states and reopen persistence. Public Dispatch cancel is now enabled/offline-tested with recovered event/error behavior; public schedule remains disabled pending a recovered current-v19 executor and durable worker. Evidence: [R7-080](../evidence/lwbridge-implementation/2026-09-20-r7-dispatch-schedule-persistence.json).

## Current implementation checkpoint - LWB-R7-079, 2026-09-20

Scheduled Plunder’s **read/list/status persistence surface is CLOSED and current-code audited**. Public `map_plunder_jobs_list` is read-only and returns the recovered `{dispatchJobs,truckJobs}` envelope from persisted Dispatch jobs plus Truck active/history rows with active-first due-time/update-time ordering and scheduler metadata overlays; deterministic disposable-DB checks cover combined listing and reopen stability. The action boundary is now explicit: Truck schedule/cancel + durable worker are implemented/offline-tested through R7-046, but live robbery remains NOT_RUN; R7-080 subsequently enables public Dispatch cancel while schedule remains blocked; alliance share remains blocked pending offline implementation and explicit messaging authorization. Evidence: [R7-079](../evidence/lwbridge-implementation/2026-09-20-r7-scheduled-plunder-read-audit.json).

## Current implementation checkpoint - LWB-R7-078, 2026-09-20

Public `map_summary` is **LIVE-PROVEN on current-v19** across active, completed and reopened saved-context states. During one automatic Fast Monster run the exact recovered three-field envelope `{serverId,counts,scanState}` exposed the owned run as `scanning`; the staging count was truthfully zero before the one-batch source checkpointed. The scan then completed 2,500/2,500 with zero failed/unread blocks and 5,108 Monster rows, and summary counts matched raw persisted counts exactly. After DB reopen, the unique saved-profile server reproduced the same counts while `scanState` remained explicitly non-live (`phase=unavailable`, `serverIdSource=saved_profile_index`). Evidence: [R7-078](../evidence/lwbridge-implementation/2026-09-20-r7-map-summary-live.json).

## Current implementation checkpoint - LWB-R7-077, 2026-09-20

Completed/persisted public `map_data_options` is **LIVE-PROVEN on current-v19**. One automatic Fast all-eight scan completed 2,500/2,500 with zero failed/unread blocks and produced 6,341 rows across 6 positive kinds. The verifier derives the public aggregate families independently from raw persisted rows and requires exact equality for counts, City alliances/no-alliance, Resource/Monster/Zombie Boss name options, Dispatch/Monster levels, Treasure type descriptors, Truck/Railway retained reward items with arrival cutoff, and completed scan-progress identity. The same aggregate metrics reproduce after DB reopen through a new backend instance. The current Fast all-eight capture returns one large logical-block batch, so positive active-staging live observation is not claimed; the recovered active-run selector remains deterministic/static authority. Evidence: [R7-077](../evidence/lwbridge-implementation/2026-09-20-r7-map-data-options-live.json).

## Current implementation checkpoint - LWB-R7-076, 2026-09-20

Monster result filtering is **LIVE-PROVEN on current-v19**. A Fast full-world scan completed 2,500/2,500 and published/reopened 5,105 Monster rows. The verifier derives expected live UUID sets independently from persisted raw records and requires exact equality before and after reopen across 8 scenarios: selected `monsterNameKey`, current owner-policy inclusive `maxLevel` using five-level UI buckets, name+max composition, identity/literal keyword escaping, and the R7-013 bounded `monsterNameKeys` localized-name expansion. Max level 20 matched 2,916 rows and the resolved-name-key path matched 3,101. The localized-name expansion is still explicitly rebuild policy rather than recovered 0.3.1 frontend behavior. No positive Zombie/Boss protection-detail or shield deadline existed in this population. Evidence: [R7-076](../evidence/lwbridge-implementation/2026-09-20-r7-monster-filter-fidelity.json).

## Current implementation checkpoint - LWB-R7-075, 2026-09-20

City public result filtering is **LIVE-PROVEN on current-v19**. A Fast scan completed 2,500/2,500 with zero final failed/unread blocks and published/reopened 1,546 City rows. The live verifier reads the persisted raw City records, derives expected record-key sets independently, and requires exact public-query equality before and after reopen. Seven scenarios pass: alliance equality, no-alliance, `markedOnly`, positive identity keyword, and literal `%`, `_`, and `\\` keyword inputs. To make `markedOnly` a positive proof without changing game or persistent user state, the harness seeds one mark only into its disposable proof database against an authentic live owner and deletes that database in cleanup. One bounded `overview_session_unavailable` acquisition retry recovered normally before publication. Evidence: [R7-075](../evidence/lwbridge-implementation/2026-09-20-r7-city-filter-fidelity.json).

## Current implementation checkpoint - LWB-R7-074, 2026-09-20

Resource result/truth filtering is **LIVE-PROVEN on current-v19**. The Resource live verifier independently derives expected identity sets from authoritative persisted row fields, executes the public query, requires exact equality, and repeats after database reopen. A Fast scan completed 2,500/2,500 with zero failed/unread blocks and published/reopened 1,788 Resource rows. Eight scenarios matched exactly before and after reopen: sampled `resourceNameKey`, `resourceIdleOnly`, `resourceFullOnly`, `excludeBlackTile`, `minLevel`, `maxLevel`, exact level, and the shipped combined idle+full+non-black default. The population contained 1,783 idle rows, 1,665 full rows, 1,788 known non-black rows, and 420 exact level-3 rows. No live black-tile Resource existed, so positive exclusion remains observational rather than fabricated. Evidence: [R7-074](../evidence/lwbridge-implementation/2026-09-20-r7-resource-filter-fidelity.json).

## Current implementation checkpoint - LWB-R7-073, 2026-09-20

Dispatch/Secret Task public result filtering is **LIVE-PROVEN on current-v19**. The reusable Dispatch/Ghost live verifier computes expected row-identity sets independently from persisted authoritative fields, executes the recovered public query at one frozen wall clock, requires exact set equality, and repeats after database reopen. A Fast Dispatch scan completed 2,500/2,500, published/reopened 69 rows, retained the existing 9/9 sort proof, and passed 12/12 filter scenarios before and after reopen: all five quality selectors, `specialOnly`, pending/completed `completionStatus`, `minLevel`, `maxLevel`, exact-level composition, and `plunderableOnly`. Positive branches included SR/SSR/UR, special, both completion states, level filters and plunderability; N/R were exact zero-set checks. A follow-up Railway scan on prior-positive server 2204 completed 2,500/2,500 but exposed zero current Railway rows, so R7-071 remains the dated positive Railway authority and no stale-row Railway filter claim is added. No state-changing gameplay action or message was invoked. Evidence: [R7-073](../evidence/lwbridge-implementation/2026-09-20-r7-dispatch-filter-fidelity.json).

## Current implementation checkpoint - LWB-R7-072, 2026-09-20

Supplies positive-row acceptance remains **POPULATION-PENDING, with the current reachable server group exhaustively checked**. The existing read-only Treasure-state live harness now supports a bounded `LWBRIDGE_SUPPLIES_TARGET_SERVER` jump plus strict `LWBRIDGE_REQUIRE_SUPPLIES=1` acceptance gate. A server-2204 Fast Treasure scan completed 2,500/2,500 with zero failed/unread blocks and published 3 ordinary Treasure rows but 0 authentic point-type-27 `WorldSuppliesPoint` rows; a second strict server-2213 run likewise completed 2,500/2,500, published 2 ordinary Treasure and 0 Supplies, and returned to 2212. Both positive gates failed as designed before any Supplies detail claim could be made. Current-v19 static recovery now additionally pins `SeasonSuppliesShareDataManager` and its `GetActivityInfo`, `IsSeasonActivityOpen`, `GetServerCurrentSeasonConfig`, `lw_supplies_refresh`, and `supplies_para` anchors. A temporary aggregate-only diagnostic used those exact read-only semantics to inspect every currently reachable server 2149-2211; all exposed no active Supplies activity object. Server 2149 was reachable while 2148/2147/2146 and earlier sampled lower servers failed the game's own travel precheck. The diagnostic lane was removed before checkpointing; production semantics are unchanged. Evidence: [R7-072](../evidence/lwbridge-implementation/2026-09-20-r7-supplies-population-gate.json).

## Current implementation checkpoint - LWB-R7-071, 2026-09-20

Railway positive-row acceptance is **LIVE-PROVEN read-only on current-v19**. After the repaired proof harness resolved initial server identity through `CurrentClientMapBlockSource.GetCurrentContextAsync`, an authoritative Fast scan of server 2213 completed 2,500/2,500 with zero Railway rows, disproving the earlier cache-contaminated Train-list hint. A bounded read-only Train-list diagnostic then exposed server 2204 as the Railway row's own source server; that temporary discovery lane was removed and is not acceptance authority. The ordinary Railway-only scanner on server 2204 completed 2,500/2,500 with zero failed/unread blocks, published/reopened 3 authentic Railway rows, exercised 12 independently computed sort scenarios before reopen and the same 12 after reopen, including `itemCount` with nonempty goods, and proved meaningful relative ordering across all 3 live rows. Public `map_march_follow({serverId,marchUuid})` succeeded on an exact published row, and the owned session returned from 2204 to the original 2212 before cleanup. All 3 rows exposed `maxLootCount` and auditable `currentGoods` (46 reward entries total); optional raw `trainData` was absent in this population and remains unclaimed. Evidence: [R7-071](../evidence/lwbridge-implementation/2026-09-20-r7-railway-positive-follow.json).

## Current implementation checkpoint - LWB-R7-070, 2026-09-20

Auto Scan core scheduler is **CLOSED / CURRENT-BUNDLE REGRESSION-PROTECTED**. R7-038/039 remain the live authority for repeated Run Now, ordered two-target execution, disable-during-cycle skip, return-to-original/status truth, full desktop/game restart persistence, unattended scheduled execution and next-deadline advancement. R7-070 verifies the post-R7-067 shipped bundle still persists per-profile enable, 20–1440 minute interval, up to 20 valid target servers, selected types, return preference and `nextRunAt`; enabling makes the run immediately eligible, disabling clears the deadline. The scheduler remains in the top-level app, checks the live config before each target, calls shared `map_scan_start({selectedTypes,resume:false})` without `scanMode`, waits for terminal scan status, returns to the original server in `finally`, advances/persists the next deadline, and polls every 5 seconds. Browser regression, Release build and all six deterministic groups pass. Dedicated live navigation-away/back and a naturally occurring native travel error remain evidence gaps, not missing core scheduler functionality. Evidence: [R7-070](../evidence/lwbridge-implementation/2026-09-20-r7-auto-scheduler-closure-audit.json).

## Current implementation checkpoint - LWB-R7-069, 2026-09-20

Treasure read-only state refresh/status is **IMPLEMENTED/OFFLINE-TESTED and LIVE-PROVEN on current-v19**. Production now handles `map_treasure_state_refresh`, `_refresh_all`, and `map_treasure_claim_status` on the existing owned game session with current-server, active-scan and shared game-operation gates. The current-v19 Lua package is hash-pinned by `tools/inspect_current_treasure_state_sources.py`; ordinary Treasure uses managed `TreasurePointInfo` completion/reward/membership truth, while Supplies uses `WorldGetSuppliesPointDetail`, `WorldSuppliesPointData.CheckBtnState/HasPlayer`, and `WorldChargeData` participation/reward/percentage. Ambiguous labels stay `unknown`; `claimPriority` is deliberately not reconstructed and therefore continues to use R7-068's recovered SQL default 1 when absent.

Fast live proof on server 2212 completed 2,500/2,500 with zero failures, published 8 Treasure rows, returned and cached 8/8 states, overlaid state onto 8/8 first-page rows, and resolved all eight player states to `unclaimed` using viewer-relative membership captured while each remote Treasure object was resident. No other players' membership identities are persisted. No claim, scout, march, attack, collection, message, or consumable action occurred, and cleanup left no game/launcher/LWBridge process. `map_treasure_claim` remains blocked. Evidence: [R7-069](../evidence/lwbridge-implementation/2026-09-20-r7-treasure-state-refresh.json).

## Current implementation checkpoint - LWB-R7-068, 2026-09-20

Treasure visibility, viewer identity, lucky ordering and persistent claim-state overlay are **RECOVERED and IMPLEMENTED/OFFLINE-TESTED**. Hash-locked original SQL proves that `includeForeignRadarTreasures=false` keeps all non-radar rows but restricts radar `treasureType=1` to the viewer alliance, using explicit `viewerAllianceId` when supplied and row-embedded identity otherwise; true omits that predicate. `luckyFirst=true` prepends cached `state_json.claimPriority ASC` with default 1, keyed by explicit or row-embedded `viewerUid`. The original page path joins `treasure_claim_states` by server/player/treasure, projects `state_json`, and uses the recovered expiry-cleanup/upsert transaction. Production now reproduces these semantics and overlays cached state over the indexed row while retaining authoritative indexed `serverId`; wrong-kind Treasure fields remain fail-closed. Current-v19 metadata independently confirms authoritative `TreasurePointInfo` membership/completion/reward methods. At this R7-068 checkpoint the read-only refresh/status mapping remained open; R7-069 subsequently closes that live read-only path while all state-changing Treasure claim behavior remains blocked. Release build and all six deterministic groups pass. Evidence: [R7-068](../evidence/lwbridge-implementation/2026-09-20-r7-treasure-query-state-cache.json).

## Current implementation checkpoint — LWB-R7-067, 2026-09-20

Automatic scan-strategy ownership is **IMPLEMENTED/OFFLINE-TESTED and LIVE-PROVEN on current-v19**. Manual/Auto Normal/Fast selectors, persisted preferences, and frontend `scanMode` start fields are removed. The backend now plans only after live context: standard 1000x1000 normal-world scans choose Fast/concurrency 20, dedicated Monster/Zombie Boss chooses `current_fast_monster_lod2_v1`, proven nonstandard single City/Resource chooses ordinary LOD0/concurrency 8, and other unsupported nonstandard combinations fail closed. Legacy caller `scanMode` is ignored for compatibility; effective mode/concurrency remain run diagnostics and `scanStrategy` is explicit in status. Candidate browser regression, Release build and all six deterministic groups pass. A live no-mode Zombie Boss Start on server 2212 selected `fast / current_fast_monster_lod2_v1 / 20` and completed 2,500/2,500 with zero failures in 5.118 s; zero current rows were accepted because this is strategy/lifecycle proof, not population proof. Evidence: [R7-067](../evidence/lwbridge-implementation/2026-09-20-r7-automatic-scan-strategy.json).

## Current implementation checkpoint — LWB-R7-066, 2026-09-20

City Excel export is **REMOVED BY OWNER OVERRIDE**. The shipped Map Data bundle no longer exposes the Export Excel button/handler or imports the export API; all nine shipped locale bundles drop the three export strings. Production `LWBridgeBackend` no longer routes `map_city_export`; `LWBridgeWindow` no longer owns an Excel `SaveFileDialog`; the OOXML writer file, export-only full-City SQLite snapshot/row-limit/projection path and export-only workbook tests are deleted. A negative backend regression requires the retired command to return `COMMAND_NOT_IMPLEMENTED`, and `tools/check_city_export_removed.cjs` proves the candidate Map Data page loads without JS errors while containing/invoking no export path. The full deterministic suite passes. R6-017/018 and R7-057..060 remain valid **historical original-contract provenance**, not current product functionality. Evidence: [R7-066](../evidence/lwbridge-implementation/2026-09-20-r7-city-export-removal.json).

## Current implementation checkpoint - LWB-R7-065, 2026-09-20

Truck/reward game artwork is **LIVE-PROVEN on current-v19** through the public `game_asset_image({assetPath})` service path. Original 0.3.1 strings recover `game_asset_image`, `getAssetImage`, `assetPath`, `spriteName`, PNG/base64/data-URL handling and `INVALID_ASSET`. Current-v19 metadata/source proves the game-owned loader is `UnityUIExtension.LoadSpriteAuto`; xLua exposes it on the target instance, so production uses `SpriteRenderer:LoadSpriteAuto`. The returned Sprite is atlas-cropped from `textureRect` with GPU `Graphics.Blit`, encoded with `ImageConversion.EncodeToPNG`, strictly validated by the host and retained in a bounded exact-source memory cache. Four representative authoritative Truck paths passed live, including packed atlas content, and focused pixel checks rejected the earlier transparent-camera render attempt and prove the final PNGs contain real artwork. `spriteName` resolution remains fail-closed and does not block Truck `currentGoods`, whose recovered frontend passes `assetPath=e.iconPath`. Evidence: [R7-065](../evidence/lwbridge-implementation/2026-09-20-r7-truck-game-asset-image.json).

## Current implementation checkpoint — LWB-R7-064, 2026-09-19

Cross-server Fast Secret Task stability on server 2207 is **LIVE-PROVEN under repeated same-owned-session acquisition**. Fourteen full investigation scans completed 2,500/2,500 and the historical all-three-attempt non-rectangular sample did not recur, so no guessed AOI reset/re-anchor or slow fallback was added. A separately reproduced early `state=failed` probe omitted success-only target/view fields; production previously masked its native cause by validating those fields first. Failed correlated Fast results now validate identity/freshness and surface the native error before proven-only request/geometry/response/footprint checks. Final exact-code acceptance completed three consecutive Fast Dispatch scans with 67/80/126 rows; the first decoded `overview_session_unavailable`, recovered on attempt 2, and the other two were clean. Exact 10,000-AOI coverage and fail-closed publication are unchanged. Evidence: [R7-064](../evidence/lwbridge-implementation/2026-09-19-r7-server-2207-fast-stability.json).

## Historical checkpoint — LWB-R7-060, 2026-09-19 (superseded by owner removal R7-066)

City Excel export was **RECOVERED and PUBLIC-ENABLED/OFFLINE-TESTED at this historical checkpoint** before the owner later removed it from the rebuilt product. Original pagination is page 1..1000 at 200 rows/page with full filtered-row accumulation and exact overflow `MAP_EXPORT_FAILED / city export exceeded the row limit`, so the effective maximum is 200,000 rows rather than the frontend's first 200. A-L mapping and coercion are now exact: A/B/C/H/I numeric; D/E/F/G/K inline strings; J/L style-3 datetimes; UID/UUID remain lossless text; J uses `protectEndTime` and consults `shieldEndTime` only when the primary key is absent; datetime units switch at `1e11`. The desktop host owns the recovered save dialog and exact cancel/success result envelopes. Deterministic workbook reopen and the full suite pass; no interactive user save was performed. Evidence: [R7-060](../evidence/lwbridge-implementation/2026-09-19-r7-city-export-row-contract.json).

## Current implementation checkpoint — LWB-R7-059, 2026-09-19

The original City-export Windows save-dialog contract is **RECOVERED static**. LWBridge uses rfd 0.16.0, leaves the starting directory and custom title unset, sets the R7-058 filename plus filter `Excel workbook` / `xlsx`, and calls synchronous `save_file()`. The Windows Common Item Dialog therefore chooses its persisted/system location and retains the default overwrite prompt. rfd maps user cancellation and any dialog build/show/get-result error to `None`; the original host serializes that branch exactly as `{canceled:true,path:"",rowCount:0}`. Remaining export blockers are original pagination/scope, direct A-C/J typing/coercion and original large-ID workbook behavior. Evidence: [R7-059](../evidence/lwbridge-implementation/2026-09-19-r7-city-export-dialog.json).

## Current implementation checkpoint — LWB-R7-058, 2026-09-19

The original City export default filename is **RECOVERED static** as `map-cities-{serverId}-{YYYY}{MM}{DD}-{HH}{mm}{ss}.xlsx`. Hash-locked Rust format bytecode proves the six timestamp fields are zero-padded widths `4/2/2/2/2/2`; bounded picker/clock evidence ties the first argument to positive `serverId` and the timestamp to UTC `GetSystemTimePreciseAsFileTime` converted into `time 0.3.54`. The original Feedback exporter independently uses the same timestamp suffix. SB-99 records one later rejected narrow register inspection and is not used as evidence. Public `map_city_export` remains fail-closed because original pagination/scope, direct A-C/J coercion, default save directory, complete picker behavior and original large-ID workbook typing remain unresolved. Evidence: [R7-058](../evidence/lwbridge-implementation/2026-09-19-r7-city-export-filename.json).

## Current implementation checkpoint — LWB-R7-057, 2026-09-19

City Excel export infrastructure is **IMPLEMENTED/OFFLINE-TESTED internally, not public-enabled**. The rebuild can snapshot all filtered/sorted City rows in one SQLite transaction and write/reopen the recovered six-part OOXML structure with exact large-ID text, formula-safe strings, mark labels and style-3 dates; a 235-row case proves the export path is not accidentally capped by the frontend's pageSize 200. Applying full-filter scope and unresolved per-column styles/types is explicitly IMPLEMENTATION POLICY, not a claim about the original host. Static continuation identifies `rfd 0.16.0`, CodeView `lwbridge.pdb` identity and the exact four-XF style table; original pagination/picker/typing/default-directory semantics remain blocked, while R7-058 subsequently closes the filename/timestamp itself. `map_city_export` stays fail-closed. Evidence: [R7-057](../evidence/lwbridge-implementation/2026-09-19-r7-city-export-infrastructure.json).

## Current implementation checkpoint — LWB-R7-056, 2026-09-19

Durable Map Scan run identity is **IMPLEMENTED/OFFLINE-TESTED**. Profile scope is the profile-specific Map Data DB; run rows additionally persist launch session, server/world, geometry, home tile, ordered selected types, mode, concurrency and retry bound. All run mutations revalidate that identity transactionally, legacy schemas migrate additively, and stale/foreign identity is rejected before mutation. Public resume stays unavailable because no original `resumeAvailable=true` producer is recovered. Evidence: [R7-056](../evidence/lwbridge-implementation/2026-09-19-r7-map-scan-run-identity.json).

## Current implementation checkpoint — LWB-R7-055, 2026-09-19

Map Scan progress/completion for the production **direct-snapshot architecture** is now closed. Real scheduler counters and acquisition progress remain capped below 100 until durable completion; exact logical coverage and zero failures are mandatory; stopped/failed/foreign/duplicate capture paths cannot publish; final publication owns the exact running row transactionally. R7-055 explicitly publishes `resumeAvailable=false` and makes bounded WorldMarch enumeration fail closed on overflow/mismatch instead of silently truncating at 50,000 entries. Original queue-only pending/ack/drop metrics are intentionally not fabricated in this architecture. Resume serialization remains unsupported. Evidence: [R7-055](../evidence/lwbridge-implementation/2026-09-19-r7-map-scan-completion-integrity.json).

## Current implementation checkpoint — LWB-R7-054, 2026-09-19

Map Scan Clear is **production-owned and OFFLINE-TESTED against the hash-locked original contract**. `map_scan_clear` now shares Manual Scan ownership with Start/Stop/Status, enforces the recovered active-scan and current-`live`-server gates before deletion, serializes Clear against a competing Start, atomically removes only the selected current server's scan/index scope, preserves player marks and unrelated servers, and publishes an idle/zero-progress state. Fresh Clear may resolve the authoritative lifecycle server without initiating a scan; saved/replay-only context fails closed. Destructive acceptance used only in-memory stores. Evidence: [R7-054](../evidence/lwbridge-implementation/2026-09-19-r7-map-scan-clear-ownership.json).

## Current implementation checkpoint — LWB-R7-053, 2026-09-19

Dispatch/Ghost result sorting is **RECOVERED and production-enabled; Dispatch is LIVE-PROVEN read-only on current v19, while Ghost positive-row live ordering is deferred until 2026-09-24**. Both share the four public keys `level`, `quality`, `completionTime`, and `updatedAt`. Special missions use effective quality 100, completionTime 0 becomes NULL, frontend multi-sort order is retained, NULL is last for ASC/DESC, and final ties use `record_key ASC`. Final Fast Dispatch acceptance scanned 2,500/2,500 in 145.413 s, published/reopened 386 rows, and passed 9 independently computed sort scenarios both before and after DB reopen. The population contained 20 special rows and 380 nonzero completion-time values. Ghost shares the same production path and deterministic coverage; only positive-row live acceptance remains time-window pending. Evidence: [R7-053](../evidence/lwbridge-implementation/2026-09-19-r7-dispatch-ghost-sort-fidelity.json).

## Current implementation checkpoint — LWB-R7-052, 2026-09-19

City result sorting is **LIVE-PROVEN read-only on current v19**. The complete public City sort surface is now recovered and production-enabled: `level`, `health`, `shield`, and `updatedAt`, including the original dual-unit active-shield clock expression, ordered multi-sort, nulls-last in either direction, and final record-key tie-breaking. Final Fast acceptance scanned 2,500/2,500 in 132.903 s, published/reopened 1,319 City rows, and passed 9 independently computed sort scenarios both before and after DB reopen. All 1,319 rows carried health and 140 active shield values / 137 distinct deadlines exercised shield ordering. Dispatch/Ghost were still gated/fail-closed at this checkpoint and are subsequently recovered/production-enabled by `LWB-R7-053`. Evidence: [R7-052](../evidence/lwbridge-implementation/2026-09-19-r7-city-sort-fidelity.json).

## Current implementation checkpoint — LWB-R7-051, 2026-09-19

Resource result sorting is **LIVE-PROVEN read-only on current v19**. The complete public Resource sort surface is now recovered and production-enabled: `level` and `updatedAt`, ordered multi-sort, nulls-last in either direction, plain indexed level, and final record-key tie-breaking. Final Fast acceptance scanned 2,500/2,500 in 126.029 s, published/reopened 1,870 Resource rows, and passed 5 independently computed sort scenarios both before and after DB reopen. City remained gated at this checkpoint and is subsequently closed by `LWB-R7-052`; Dispatch/Ghost are subsequently recovered/production-enabled by `LWB-R7-053`. Evidence: [R7-051](../evidence/lwbridge-implementation/2026-09-19-r7-resource-sort-fidelity.json).

## Current implementation checkpoint — LWB-R7-050, 2026-09-19

At this historical checkpoint, Railway result sorting was **IMPLEMENTED/OFFLINE-TESTED and positive-row live ordering was population-pending**; `LWB-R7-071` subsequently closes the live population, ordering, itemCount and Follow gaps. The public Railway sort surface now accepts the five recovered keys (`quality`, `power`, `itemCount`, `protectTime`, `updatedAt`) with ordered multi-sort, nulls-last in either direction, plain Railway quality, itemCount/itemKey coupling, protectTime zero-to-NULL handling and final record-key tie-breaking. Hash-locked original recovery plus deterministic multi-row acceptance are green. The current-v19 Fast live attempt completed 2,500/2,500 but exposed zero Railway rows, so the positive-population gate intentionally withheld live acceptance. Resource remained gated at this checkpoint and is subsequently closed by `LWB-R7-051`; City is subsequently closed by `LWB-R7-052`, and Dispatch/Ghost are subsequently recovered/production-enabled by `LWB-R7-053`. Evidence: [R7-050](../evidence/lwbridge-implementation/2026-09-19-r7-railway-sort-fidelity.json).

## Current implementation checkpoint — LWB-R7-049, 2026-09-19

Truck result sorting is **LIVE-PROVEN read-only on current v19**. The public Truck sort surface now includes the recovered six keys (`quality`, `power`, `itemCount`, `remainingLootCount`, `arriveTime`, `updatedAt`) with ordered multi-sort, nulls-last in either direction, special-UR quality=100, itemCount/itemKey coupling, and final record-key tie-breaking. Final Fast acceptance scanned 2,500/2,500, published/reopened 469 Trucks, and passed 15 independent sort scenarios both before and after DB reopen. At this checkpoint all non-Truck alternate sort families were gated; Railway is subsequently recovered/implemented by `LWB-R7-050`. Evidence: [R7-049](../evidence/lwbridge-implementation/2026-09-19-r7-truck-sort-fidelity.json).

## Current owner priority - shared Manual Scan engine first, 2026-09-13

**PM17 correction delivery is complete.** `LWB-PM17-001/002` remain IMPLEMENTED/OFFLINE-TESTED at their documented revision. Player City `LWB-PC-001/002/003` remains credited for two fresh city-only acquisitions, correct profile/server persistence, normal `map_search`/summary, newer second result, fresh-process reopen/search and owner-visible saved-row rendering. The initial Map Data page may auto-query persisted City data before manual Search; that is not a fresh scan.

**Active architectural priority:** continue all Map Data work on the one shared Manual Scan engine. M01-M05 are the common acquisition/lifecycle core: readiness and map geometry/traversal, run/block scheduling, backend-selected strategy, native capture/acks/removals, progress/failure/resume state, durable staging/publication, Stop and Clear. `LWB-R7-067` closes user-owned Normal/Fast: `selectedTypes` is the acquisition content input and the planner owns the effective strategy/concurrency. M06-M10 consume persisted/indexed output per kind; result filters are downstream query controls.

**Auto Scan multi-target/timer/disable acceptance CLOSED (`LWB-R7-039`):** one real scheduler cycle processed two configured targets in order and completed both shared Fast Zombie Boss scans at 2,500/2,500 with zero failed/unread before returning to the original server. Disabling Auto during the first target allowed the in-flight scan to finish and skipped the remaining target. A full desktop/game restart with persisted future `nextRunAt` triggered automatically 741 ms after deadline with no Run Now/UI interaction and advanced the next schedule. A frontend-only injected travel failure proves catch/finally/no-scan behavior but is not a native travel-error proof. Explicit navigation-away survival remains open.

**Actual Auto Scan parent scheduler + cross-server return status CLOSED (`LWB-R7-038`):** the normal desktop React parent scheduler is LIVE-PROVEN with dedicated Zombie Boss. The parent sanitizer now preserves `zombie_boss`; a fresh idle Map Scan status initializes its unknown server from the exact owned Overview heartbeat, and every successful `server_jump` publishes its authoritative target immediately. A brand-new game session knew the original server before any scan/jump. Two consecutive real UI Run-now cycles each jumped to a different target, reused the shared Fast Zombie Boss scanner for 2,500/2,500 blocks with zero failed/unread, returned to the original server, and left shared status on the original server before the next cycle. The user's previous Auto config was restored and the owned game stopped cleanly. One multi-target-list cycle plus disable/stop/failure acceptance remain open; unattended naturally elapsed scheduling after full app restart is not separately live-proven.

**Server travel + first Auto Scan backend cycle CLOSED (`LWB-R7-037`):** production now handles `server_jump({serverId})` through the same owned Overview/current-client session used by Map Data. Current-v19 source recovery distinguishes ordinary `CrossServerUtil.OnCrossServer/OnBackSelfServer` from season-only `GotoServerZone`; arrival is accepted only when authoritative `LuaEntry.Player:GetCurServerId()` equals the requested target. Live proof completed same-server no-op and **original -> different target -> original**. A second live run executed `server_jump(current) -> fast zombie_boss map_scan_start` in one owned session and completed 2,500/2,500 with zero failed/unread in 19.363 s, publishing 56 Zombie Bosses. This proved Auto Scan could reuse the shared Manual worker once travel succeeded; the actual parent scheduler/return-status portion is subsequently closed by `LWB-R7-038`.

**Shared Manual selection/mode acceptance CLOSED (`LWB-R7-036`):** mixed subsets of the recovered original eight, all eight together, and same-owned-session repeated all-eight Normal/Fast are LIVE-PROVEN on current v19. Both all-eight mode runs completed 2,500/2,500 with zero failed/unread blocks; Fast used recovered concurrency 20 and reopened with exact per-kind counts. Deterministic coverage drives every normalizer through one full 10,000-AOI run and proves all selected kind scopes publish transactionally. Dedicated Zombie Boss remains a separate ninth standalone selection. Manual multi-select + Normal/Fast controls are restored with Zombie Boss mutually exclusive. Auto Scan is now the acquisition priority and must invoke this same engine.

**Treasure category acceptance CLOSED (`LWB-R7-035`):** current-v19 ordinary Treasure-only Normal Start completed 2,500/2,500 with zero failed/unread blocks in 123.150 s, published one authentic native type-21 `TreasurePointInfo` row and reopened exactly one. The live row preserved the recovered ordinary Treasure type/source identity and source-backed rewarded/digging/reward-max/remaining-box counts. Production also admits only the separately recovered type-27 `WorldSuppliesPoint` + `TableName.LWIceSupplies` shape for Supplies, with mutually exclusive positive Treasure/Supplies type dimensions; zero Supplies rows existed in this live population, so positive Supplies live ingestion remains unobserved. Exact v19 restoration/compatibility and cleanup passed.

**Resource authoritative full-state/filter acceptance CLOSED (`LWB-R7-034`):** current-v19 ordinary Resource-only Normal Start completed 2,500/2,500 with zero failed/unread in 131.208 s, published/reopened 838 rows, and populated source-backed type/level/name/max/occupancy/black-tile classification on all rows. Every one of the 782 final idle rows had authoritative `WorldGetDetail` remaining/full detail: 696 full, 86 partial, zero invalid pairs. The default idle+full+non-black query returned 696. Monster-style LOD2 is not a valid Resource shortcut (live proof returned zero Resource rows), so complete Resource discovery remains LOD0 while detail requests overlap asynchronously. The Lua probe local-budget regression (201 locals) is fixed at 192 with a `<200` deterministic guard.

**Current-client v19 compatibility prerequisite CLOSED (`LWB-OVR-018`):** the untouched official launcher advanced the authoritative LWLF package to content 19 but left LocalLow `version.txt` at 18, then on a second untouched launch explicitly reported local/remote Lua version 19 current. Policy v2 retains exact package metadata and recovered core/critical anchors as the acceptance boundary, requires a positive numeric marker, tolerates only a lagging marker, and re-inspects identity before acceptance. Manual and auto-start production cycles both reached Connected and closed/restored cleanly; post-restore v19 compatibility is green.

**Ghost Ops implementation checkpoint (`LWB-R7-033`, LIVE-PENDING):** current-v19 recovery pins type 29 `GHOSTRECON_POINT`, native `GhostreconPointInfo`, `GetGhostreconPointInfoByIndex`, and `TableName.LwGhostreconTask`. Production keeps Ghost distinct from Dispatch and preserves source-backed config/timing/owner/list metadata. Release build and all six deterministic groups pass. A strict real Ghost-only Normal run reached the 2,500/2,500 positive-row gate with zero failed/unread and exact LOD0/10x100 restoration, but the current population contained zero Ghost rows, so acceptance remains open. Treasure is subsequently LIVE-PROVEN under `LWB-R7-035`; continue mixed/all-eight acceptance and rerun Ghost unchanged when a positive population exists.

**Dispatch/Secret Task category acceptance CLOSED (`LWB-R7-032`):** current-v19 ordinary Dispatch-only Normal Start completed 2,500/2,500 with zero failed/unread blocks in 105.782 s, published 74 authentic Secret Task rows and reopened exactly 74. All rows preserved point type 17, `HeroDispatchMissionPointInfo`, config/level/quality/special flag and source identity; mission timing/owner/list/name-key fields were present on all 74 and alliance ID on 70. Exact v19 restoration/compatibility and cleanup passed. Ghost Ops is subsequently implemented/live-pending under `LWB-R7-033`; Treasure is the active task.

**Railway/Train category acceptance CLOSED (`LWB-R7-031`):** current-v19 ordinary Railway-only Normal Start completed 2,500/2,500 with zero failed/unread blocks in 112.327 s, published one authentic Railway row and reopened exactly one. The row preserved train type/quality/power/config/raw data, arrival/robbery timing, march identity and protection time. `maxLootCount` and nonempty `currentGoods` were absent in this live population and remain optional/source-backed rather than synthesized. Exact v19 restoration/compatibility and cleanup passed. Dispatch/Secret Task is next.

Normal concurrency `8` and Fast concurrency `20` remain RECOVERED fields on the same production service. `LWB-R7-004/005/006` prove context, arbitrary Resource/City blocks and City footprint reuse; `LWB-R7-007/008` prove complete Player City acquisition; `LWB-R7-009` adds native acquisition progress; `LWB-R7-010` proves complete Monster acquisition; and `LWB-R7-012` now LIVE-PROVES the current-v17 5x10/50-AOI footprint, 200-request exact-coverage full-world planner, Monster full runs at 43.620 s Normal / 70.746 s Fast under different live populations, and a dedicated one-Stop-to-idle path. Production status events and `map_summary` now follow the active Manual run. Exact live City/Monster counts and exact wall times are not fixed invariants. `LWB-R7-015` additionally LIVE-PROVES complete Resource ordinary Start/publication/reopen on the same current-v17 planner, `LWB-R7-016` LIVE-PROVES Truck, and `LWB-R7-031` now LIVE-PROVES Railway/Train ordinary Start/publication/reopen on current v19. Ghost Ops is implemented but positive-row live-pending under `LWB-R7-033`; Treasure is LIVE-PROVEN under `LWB-R7-035`; mixed/all-eight + repeated Normal/Fast are LIVE-PROVEN under `LWB-R7-036`. Rerun the unchanged Ghost acceptance when a positive population exists. Auto Scan (M11) is next and must reuse this manual engine rather than duplicate acquisition logic.

**Monster/Zombie Boss fast-only restore recovery CLOSED (`LWB-R7-030`):** owner-reported repeated scans could crawl or stop after a coarse restore timeout. Failure-first evidence found restored camera/`svLod=0` with AOI geometry still stuck at 1000/1. Production removes the slow LOD0 fallback for Monster/Zombie Boss and makes one bounded native `WorldPointManager.UpdateLWAoi_Normal(true)` recovery attempt before a truthful failure. Same-owned-session repeats completed 2,500/2,500 in 38.414 s / 30.715 s with exact 10x100 restoration. A final clean production run naturally exercised recovery and completed in 41.053 s with exact 10x100/LOD0 restoration. Build/suite/current-v18 compatibility/cleanup all pass.

**Monster transient same-session resume CLOSED (`LWB-R7-027`):** a failure-first 7-second forced readiness pause reproduced the remaining lifecycle defect as 94 `game connection unavailable` failures after 2,406 completed blocks. The source now keeps an exact full-world run/session anchor before the LOD2 attempt and distinguishes instantaneous readiness from immutable owned-session identity. Repeating the same pause completed 2,500/2,500 with zero failures in 18.294 s, published/reopened 6,912 Monsters with Distance on every row, and completed 12/12 required Zombie Boss detail replies with 12 active deadlines. Different session identity still fails closed; exact v18 restoration/compatibility and cleanup passed.

**Monster 60-second target CLOSED (`LWB-R7-026`):** current-v18 Monster-only acquisition now uses one native coarse LOD2 whole-world snapshot, then restores exact LOD0/camera state before normalization/publication. Two consecutive ordinary Normal runs complete 2,500/2,500 with zero failures in 11.418 s / 10.903 s, publish/reopen 7,271 / 7,194 dynamic rows, preserve Distance on every row, and keep automatic first-scan Remaining (10/10 and 9/9 required detail replies; 10 and 9 active future deadlines). Invalid coarse acquisition still fell back to the conservative exact-coverage LOD0 scanner at this historical checkpoint; `LWB-R7-030` supersedes that policy with fast-only failure and bounded native AOI recovery. Transient same-session readiness resume is subsequently LIVE-PROVEN by `LWB-R7-027`.

**Monster first-scan gate CLOSED (`LWB-R7-025`):** current-v18 one-scan/no-click/no-Jump live proof completed 2,500/2,500 with zero failures in 108.144 s, published/reopened 4,735 Monsters, found 21 eligible Zombie Bosses, sent 2 serialized detail requests, received 2/2 authoritative replies and persisted 2 active future shield deadlines. The run also proves v18 coverage is 3x10 AOIs per ordinary footprint, so production now uses a conservative 340-request exact-10,000-AOI planner. Railway/Train is the next category acceptance.

**Monster backend positive timer proof (`LWB-R7-023`):** a fresh current-v18 ordinary scan completed all 2,500 blocks with zero failures in 73.206 s, published 13,462 Monster rows, and found 97 exact Invasion Zombie Bosses. Forty authoritative protected replies now persist positive protection deadlines and six were future at publication. The historical 57-retry observation remains valid for R7-023, but R7-024 supersedes that overlapping retry architecture because the game message has one shared request UUID. Frontend countdown generation/browser checks pass; owner-visible positive Remaining remains the final gate before Railway.

**Launch/bootstrap correction (`LWB-R7-022`):** the first R7-021 owner launch black-screened because `M.Pump()` still referenced the removed `pump_monster_protection_batch_retry` function. `Player.log` proved the exact nil-global error. The stale call is removed, a regression guard pins it, and manual plus auto-start current-v18 lifecycle proofs both reached Connected and closed cleanly. Original v18 is restored with no recovery journal.

**Monster current gate (`LWB-R7-021`):** owner scan completion is confirmed and the owner screenshot proves live active Zombie Boss timers. One received protected reply still had deadline `0`; source analysis ties this to the boss unloading before `MonsterProtectionManager` can expose `showEndTime`. Production now preserves the exact source deadline formula while loaded and retries only unanswered UUIDs at final collection. Positive Remaining owner retest is pending; Railway remains gated.

**Historical Monster gate (`LWB-R7-018/019/020`):** R7-018/019 make protection enrichment nonblocking and recover/batch the exact current-v18 click request. R7-020 fixes the ordinary UI consumer contract exposed by the discarded run: UUID deduplication across overlapping AOIs permits a later batch to have a ready carried-over response with no new request in that batch, so `ready <= newRequest` was invalid. The validator now requires only `newRequest <= target` and `ready <= target`, with deterministic carry-over coverage. R7-021/023 supersede the old pending-scan statement: full ordinary Monster scans now complete cleanly; only the owner-visible positive Remaining observation remains gated.

**S03 complete Refresh Status and S06 cross-server travel remain PENDING/DEFERRED.** S02 remains unfinished/unassigned. Existing operation-specific restrictions, including SB-97, remain unchanged. Do not claim all Map Data complete from Player City or from a partial shared-engine milestone.

### Retained earlier resource/PM15 checkpoint — not the active assignment


> Review 15 at `154ce35` accepted the owner no-context result and returned PM15-01/02. Web implemented the bounded collector repairs at `1e9af6e`; `LWB-PM15-001` is READY_FOR_PM_REVIEW with seven isolated regressions. No owner repeat or live test was performed. Fresh acquisition remains blocked. No Daybreak task. [Current review](reviews/2026-09-11-review-15-owner-evidence.md) and [Web prompt](team-workflow.md).


> Current ownership, 2026-09-11: Web implements and automatically captures technical evidence; the owner supplies guided permitted UI observations/screenshots only; PM audits. No separate Sol role. See [workflow](team-workflow.md) and [packet](live-test-handoff.md). `LWB-PM15-001` now isolates each collector launch/session and rejects failed observation state; the accepted `LWB-PM13-009` no-context owner result remains historical and is not rerun. SB-97 and fresh-resource acceptance remain open. Historical observations below retain their original scope.


Checkpoint: 2026-09-11. Status vocabulary follows `task.md`: **RECOVERED** is
original static behavior, **IMPLEMENTED/OFFLINE-TESTED** is rebuild behavior
proved without a live state change, **LIVE-PROVEN** requires current-client
before/after evidence, and **UNKNOWN/BLOCKED** remains open.

## Project-manager review 14 — implementation `7ca6d5c` (2026-09-11)

[Current audit](lwbridge-project-status.md) credits PM12-A/C bounded restoration/correlation and PM12-007 saved real idle evidence. **`LWB-PM13-003` closes the reopened PM12-B bounded-route lifecycle races and `LWB-PM13-004` closes the false-positive proof predicate offline. PM12-D and the resource function remain partial pending PM13-04.** No full acceptance case is newly signed off.

| Slice | Evidence and current status |
|---|---|
| Bounded resource acquire/restore/import | Saved two-read proof and final probe source hashes/correlations rechecked by PM; one real row remains in selected profile DB. LIVE-PROVEN historical bounded result; no new PM acquisition. |
| Ordinary reopened Resource Search | `LWB-PM13-001` implements unambiguous selected-profile saved context; `LWB-PM13-001B` NORMAL-WINDOW SAVED-ROW PROVEN: the exact review-13 package-local DB (one server-2212 resource row) was copied byte-identically into the direct desktop default root for a bounded test, shown through Resource + explicit Search, closed normally, shown again after restart, then the temporary direct-root files were removed. Historical live-row provenance is preserved; this is not a new acquisition or bridge-readiness claim. |
| Default all-category Start / monster acquisition | `LWB-PM13-002` IMPLEMENTED/OFFLINE-TESTED; `LWB-PM13-005` NORMAL-WINDOW VERIFIED for the non-acquiring failure path: all eight types remained selected, normal Start was enabled, and the rebuilt window showed the specific Resource-only guidance without starting game/launcher/helper. Monster acquisition remains queued next. |
| Lifecycle | `LWB-PM13-003` IMPLEMENTED/OFFLINE-TESTED: validation is separated from persistence, `reading -> committing` decides completion versus cancellation atomically, and cleanup ownership is reserved before helper start. Deterministic pre-commit Stop/Close, post-decision Stop and helper-registration Close races pass; PM12-B bounded-route closure is restored. |
| Normal-window fresh proof | `LWB-PM13-004` IMPLEMENTED/OFFLINE-TESTED hardens exact result/query/render freshness. `LWB-PM13-005` NORMAL-WINDOW PARTIAL verifies repaired unsupported-Start and missing-saved-context behavior. `LWB-PM13-006` corrects the recovered coordinate-button shape and proves the historical saved reopen. `LWB-PM13-007` preserves the Computer Use/restriction diagnosis. `LWB-PM13-008` added the reviewed passive owner package. The real owner attempt on the direct profile had no published server and displayed `MAP_SAVED_CONTEXT_UNAVAILABLE`; `LWB-PM13-009` preserves that owner-observed branch. Review 15 found saved-row reopen could reuse session-one aggregate proof and failed process observation could look clean. `LWB-PM15-001` IMPLEMENTED/OFFLINE-TESTED fixes both: per-launch session/PID-owned logs are required for each Search/result/render, and failed app/UI/process/runtime/store evidence is INCOMPLETE. Seven isolated counterexamples/positive cases pass. No owner repeat is required. PM13-04 remains ACTIVE/BLOCKED for the fresh two-read/Search/render operation; no owner/script reroute is allowed. |
| Resource occupancy/name | Known idle observed; live Gathering and type-to-name-key mapping remain open. Chat-only enum lead is not mapping proof. |

[Review 13 evidence](../evidence/lwbridge-implementation/pm-review-13/README.md) and [review 12/follow-ups](reviews/2026-09-10-review-12-and-followups.md) preserve detailed findings. The historical 25–30% planning estimate is not a newly measured percentage. **0/47 full cases signed off** remains unchanged. User-repaired PM8 recovery remains closed; updater/exports/original-pipe expansion deferred. ESC-005 NOT_ASSIGNED; no Daybreak task.

Review 14 reran build and focused offline checks; it did not produce a new live acquisition. Use the [real-game readiness/test checklist](user-test-checklist.md). Computer Use availability is not live acceptance.

Follow the [single-function handoff](implementation-handoff.md). Broader matrices below retain their stated static/offline/full-feature scope; a bounded exception is not completion of the whole row.

## Milestone A environment evidence

| Item | Current evidence |
|---|---|
| Reference executable | `lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff` |
| Branch / starting HEAD | `research/offline-controller` / `cb762a1ad2bb6a856f0834673217ecf8da01e204` |
| Installed root | `%LOCALAPPDATA%\FunFly\Last War-Survival Game` |
| Official launcher | SHA-256 `B6E29176F64C11A6F203D414FB50EE1E02293BE318EFDA9C33CF221C83D763BC` |
| Current `Game\LastWar.exe` | SHA-256 `DF5ABCF8618D48500BEFA9F587B509ED4F58373FF34932BB87CE217F0CF267D5`; file/product version `2019.4.40.16762411` |
| Current original xLua | SHA-256 `21EB704AFDB7E528F4B90FA1B90BF414C221B06BA990D625AAAAED31B292740F` |
| Baseline process state | No matching `LastWar` or `LastWarLauncher` process during the self-check |
| Official runtime evidence | `docs/official-runtime-architecture.md` + `evidence/official-runtime/2026-09-10-pm8-004-official-runtime.json`; read-only inspectors reproduce version/hash/PE/container/hot-update/launcher/startup evidence |

## Shared / Overview matrix

| ID | Original contract / trigger | Rebuild status | Proof / remaining gate |
|---|---|---|---|
| S01 Game status | `get_status(profileId)`, `proxy_status(profileId)`, `bridge://status` | IMPLEMENTED / TECHNICAL LIVE-PROVEN OVL route | Process existence remains separate from bridge readiness. Two fresh current-client cycles produced exact owned profile/session/challenge/PID game-side readiness for the requested message; stale/foreign evidence remains rejected. Owner-visible normal Overview confirmation passed in `LWB-OVL-003`. |
| S02 Pending-task counter | `get_status.pending` | UNKNOWN/BLOCKED | Exact original semantics still need recovery; do not treat an arbitrary queue length as authoritative. |
| S03 Refresh Status | status + proxy + recovery calls; recovered frontend `call_lua("getStatus", ...)` | PENDING/DEFERRED BY OWNER; PARTIAL | Local status/heartbeat routes work. The precise runtime call is absent from production command dispatch and returns COMMAND_NOT_IMPLEMENTED; an existing Overview bridge does not fill this gap. Owner defers completion on 2026-09-13; not a prerequisite to begin Map Data. |
| S04 Theme | original local UI state + `set_window_theme` | IMPLEMENTED/OFFLINE-TESTED | Existing fixture captures remain clean after native adapter integration. |
| S05 Language | nine recovered bundles + `lastwar_localize` | RECOVERED + IMPLEMENTED/OFFLINE-TESTED for runtime localization | `LWB-R7-011` restores the original 200-key request cap and requested-language -> English -> key fallback semantics, verifies all nine embedded gzip/JSON locale assets against the reference manifest, and wires production `lastwar_localize` to the verified offline cache. Real-cache backend proof passes for English and `zh-CN`; synthetic Map Data browser proof shows Monster names re-localize after UI language change with no frontend errors. This is not a current-client live localization proof and does not claim every non-Map-Data consumer has been exercised. |
| S06 Cross-server | `server_jump({serverId})`, history commands | IMPLEMENTED / TECHNICAL LIVE-PROVEN | `LWB-R7-037` restores production `server_jump` on the owned current-client session, and `LWB-R7-038` proves the real Auto Scan parent scheduler can jump away and return while shared status follows the authoritative live server. Recovered validation is integer server IDs 1–99999; recovered busy and timeout public errors are preserved. Current-v19 travel uses `GoToServerPreCheck` plus ordinary `CrossServerUtil.OnCrossServer` / `OnBackSelfServer`, with arrival accepted only after authoritative `GetCurServerId()` matches the target. History persistence remains the existing capped-five local profile contract. |
| S07 Navigation | recovered React `Activity` views/listeners | IMPLEMENTED/OFFLINE-TESTED foundation | All nine deterministic desktop captures pass after adapter integration. `LWB-R7-038` confirms Auto Scan scheduler ownership is the top-level running React parent; explicit navigation-away survival remains a separate UI acceptance case. |
| O01 Game root | `game_root_status()`, `game_root_select()` | IMPLEMENTED/OFFLINE-TESTED / PM17 RETURN | Review 17 accepted the stopped selection/status slice. `LWB-PM17-001/002` now fail closed on pending/unknown shared journals and release abandoned launch identity only after helper/process/journal ownership clears; failed A -> B -> Launch without app restart is covered. PM audit and corrected-build live regression remain separate. |
| O02 Launch Game | `profile_instance_start({profileId,closeUnmanaged:true})` | RECOVERED original API/partial bootstrap + independent rebuild TECHNICAL LIVE-PROVEN | Original proof/ticket/hello.ack gaps remain UNKNOWN/BLOCKED and are not fabricated. `LWB-OVL-002/003` preserve the earlier live/owner proof. `LWB-OVR-015` additionally live-proves the production lifecycle on current Lua content v17 and replaces the obsolete package-hash pin with a fail-closed critical-anchor compatibility policy. `LWB-OVR-016` keeps that read-only compatibility preflight on every start but reuses an exact root + validated-package settle marker so unchanged starts do not repeat the official launcher/game warm-up. `LWB-OVR-017` corrects the marker edge exposed by Lua v18: if a cached candidate launch hits a fresh official Lua CRC mismatch, exact recovery is followed by one forced untouched official settle/update, compatibility revalidation and one helper retry; current v18 passes the same critical-anchor policy. |
| O03 Close Game | `profile_instance_stop({profileId,instanceId})` | RECOVERED API + TECHNICAL LIVE-PROVEN independent policy; PM16-02 CORRECTED/OFFLINE-TESTED | `LWB-OVL-002` retains exact profile/session/PID/path ownership and successful live Close/restoration. `LWB-PM16-001` adds PID/path/creation-time incarnation checks at repair/close/recovery boundaries; replacement or unknown identity fails closed. `LWB-PM16-003` records corrected build/helper identity and CI. |
| O04 Launch at app startup | original default on unless stored false; `profile_instances_reconcile({autoLaunchAll:true})` | OWNER-ACCEPTED historically + current-v17 TECHNICAL LIVE-PROVEN | `LWB-OVL-004/OVR-011` preserve the original owner-accepted startup behavior. `LWB-OVR-015` independently live-proves current-v17 startup reconcile through the same lifecycle. `LWB-OVR-016` additionally reuses exact settlement evidence for unchanged starts, eliminating the repeated warm-up launch/close while retaining update safety. `LWB-OVR-017` adds bounded self-healing for a newly available Lua delta that is first encountered on the cached candidate path. |
| O05 Auto reconnect | `set_automation(name:"autoForceUpdateReload",enabled)` / `auto_force_update_reload` + persisted `game_desired_running` gate | RECOVERED + IMPLEMENTED; OWNER-ACCEPTED under `LWB-OVR-011` | `LWB-OVR-005/006/007/008/009/010` cover the two-gate/manual-stop contract, two-missing-process trigger, exact-PID 30s hang / 60s bridge-offline / 180s observed-health classifiers, confirmed event waiting/in-place verification/60s escalation, updater metadata-activity suppression and 15-minute stall recovery, journal restoration, recovered retry tables, 15s verification and real recovery status/event publication. A bounded normal-app process-exit recovery/intentional-Close run is live-proven, and owner-visible startup/reconnect/intentional-Close verification passed under `LWB-OVR-011`; event/update adapters retain their documented offline-tested scope. |
| O06 Recovery/repair UI | `game_recovery_status`, `bridge://game-recovery`, `profile_instances_update_and_restart` | HISTORICAL LIVE PATH; PM17 JOURNAL GUARD OFFLINE-TESTED | Review 17 accepts process-incarnation correction under LWB-PM16-001 and evidence reconciliation under LWB-PM16-003. `LWB-PM17-001` adds fail-closed shared-journal root-retarget classification. Corrected code still has no new live run; no zero-open or all-recovery coverage claim. |

## Map Data matrix

| ID | Original contract / trigger | Status | Next proof required |
|---|---|---|---|
| M01 Start/stop/mode/type selection | `map_scan_start(payload)`, `map_scan_stop()` | COMMON PRODUCTION SERVICE WIRED / CURRENT-V17 SINGLE-STOP LIVE-PROVEN | `LWB-R7-012` proves one ordinary live Stop after a real block returns terminal `idle` in one request; production now publishes scan-status events and periodic summary state from the active Manual run. `LWB-R7-004` proves ordinary production Start now routes through `ManualMapScanCommandService`/`MapScanEngine`, obtains current-v16 live context, completes one real Normal block and returns idle after bounded Stop. `LWB-R7-003` and `LWB-PC-001` remain bounded source foundations. `LWB-R6-052` recovers the original positive map-dimension gate, exact `ceil(tileWidth/20) * ceil(tileHeight/20)` block-grid cardinality and initial counters. `LWB-R6-055` additionally recovers Stop cleanup to `isReading=false`, `phase=idle`, `inflightBlocks=0`, `resumeAvailable=false`, with conditional `stopMapScan` dispatch under the original active bridge/session predicate. `LWB-R6-056` recovers original Start ownership (`GAME_CONNECTION_UNAVAILABLE`; duplicate `SCAN_RUNNING`) and explicit resume gating: existing-state resume requires requested `resume:true` plus current `resumeAvailable=true`; every recovered state-construction/reset site still writes false, so production resume remains unproven. Player City, Monster, Resource and Truck complete common-engine runs are now proven; Railway/Train is the next category acceptance while scheduler/tick/retry/ack/drain recovery continues. Mixed/all-eight support and original resume semantics remain UNKNOWN/BLOCKED. |
| M02 Native capture | proxy/native map capture path | CURRENT-V17 FULL-WORLD CITY/MONSTER/RESOURCE/TRUCK LIVE-PROVEN | `LWB-R7-012` adds current-v17 live measurement of the full-world coverage footprint as stable 5x10/50 AOIs at both edges and repeated interior targets, and production uses that source-backed footprint for a 200-request exact-union City/Monster planner. `LWB-R7-015` LIVE-PROVES Resource-only full-world ordinary Start through the same 200-request exact-union planner, including 2,500/2,500 blocks, zero failures and exact publish/reopen equality. `LWB-R7-016` now LIVE-PROVES Truck-only full-world ordinary Start on the same planner, including 2,500/2,500 blocks, zero failures/unread blocks, exact publish/reopen equality, fail-closed train-type classification, and `arriveTs`/`robTimes` normalization. `LWB-R7-047` upgrades current-v19 Truck fidelity/performance: XLua enum strings such as `Truck: 1` are normalized, authoritative Lua TrainData is resolved through `LWTrainDataManager`, `GetCurRewardData()` plus exact `maxLootPerTrain` are captured without full Truck TrainData JSON, and one cleaned Fast run completed 2,500/2,500 in 125.451 s with 244/244 published/reopened Trucks and exact client restoration. `LWB-R7-004` proves AOI-backed arbitrary-target Resource block acquisition through the current production source. `LWB-R7-005` proves arbitrary-target Player City blocks on that same source, including proven zero-city current-view AOI cells that do not invoke the legacy home-city fallback. `LWB-R7-006` adds LIVE-PROVEN Player City current-view footprint reuse: `_curViewIndex` is copied with `HashSet<int>.CopyTo(int[])`, validated against its runtime count/geometry, and one fresh response covered all four planned LOD0 cells for both a populated block and an empty edge block. Resource stays on the prior per-cell path. `LWB-PC-001` retains the earlier bounded City source foundation. `LWB-R7-014` keeps the exact 10,000-AOI gate while restoring the zero-hold camera transform in the same callback after each native request; complete live Monster runs prove acquisition survives that anti-flicker path. Direct helper transport remains IMPLEMENTATION POLICY, not original-pipe parity. |
| M03 Typed persistent index | map index service | RECOVERED SCHEMA / IMPLEMENTED-OFFLINE-TESTED FOUNDATION + BOUNDED REAL-ROW ADAPTER | Original SQLite tables/indexes and stored identity `(kind,server_id,record_key)` are recovered and reproduced. `LWB-R6-011` also recovers schema-version metadata/future-schema/legacy-import strings, but versioned migration stays gated on the supported version constant, migration threshold and timestamp unit. `LWB-R6-019` adds test-only transactional one-kind/server replacement from one staging run and proves run/kind/server isolation; `LWB-R6-020` proves an injected failure between delete and staged copy rolls the transaction back to the previous published rows. `LWB-R6-028` adds test-only restart persistence over the recovered `scan_blocks(run_id,block_index)` identity and proves reopen plus clear-cascade behavior. `LWB-R6-029` records that the tracked build-1078 official-runtime readable snapshot contains no direct record-key/scan-bridge or selected map-row vocabulary, and identifies current `Assembly-CSharp.rdl`/active `LWScripts.data` as the next bounded sources. `LWB-R6-034` closes the RDL decoder setup gap by restoring a proven read-only RG/RGMD metadata/CIL inspector into this repository; `LWB-R6-035` extends it with type-reference searches that recover managed ownership links such as `GameEntry.Data -> CustomDataManager.Player`. `LWB-R6-047` pins the original native map/staging upsert helper and shared 14-field input layout; `LWB-R6-048` pins current build-1078 point-index/UUID lookup dimensions; `LWB-R6-049` closes the original normalized `record_key` rule and its write to `+0x78`: ordinary indexed records use signed-decimal point identity, while truck/railway, records carrying `marchUuid`, or records without a point index use nonempty `uuid` else `marchUuid`; `LWB-R6-050` pins current `PointInfo.pointIndex/mainIndex/uuid` and `ResPointInfo.gatherMarchUuid` ownership plus the exact xLua backing tokens and constructor stores without naming unresolved source calls; `LWB-R6-051` pins original raw-to-normalized `level`, `quality`, `power`, `distance`/`distanceFromHome`, and `shieldEndTime`/`protectEndTime` sources and fallback order. `LWB-R7-001` adds an isolated IMPLEMENTATION POLICY adapter that imports one hash-attributed live capture row into `MapDataStore` and displays it via `map_search`; `LWB-R7-002` guarantees that replay owns a separate in-memory store and cannot silently contaminate a normal production index. Unsupported `resourceNameKey` and gather occupancy stay unknown. Direct production capture/serializer ownership, remaining typed normalization/removal, completion eligibility and retry semantics remain UNKNOWN/BLOCKED. |
| M04 Progress/failure/stop/resume | `map_scan_status`, `bridge://map-scan-status` | CURRENT-V17 LIVE STATUS/STOP INTEGRATION + PARTIAL RECOVERED DERIVATION | `LWB-R7-012` wires real `ManualMapScanCommandService` changes to `bridge://map-scan-status`, makes `map_summary` expose the active run rather than stale saved state, and makes one Stop wait through `cancelling` to terminal `idle`; dedicated live Stop and deterministic UI-state regressions pass. `LWB-R6-053` recovers shared display derivation: unread is `max(total-completed-failed,0)` and non-completed progress is one-decimal/capped at 98%, with positive-total exact `completed` permitting 100%. `LWB-R6-054` recovers event counter normalization and two-decimal completed-blocks/second scan rate. `LWB-R6-055` adds native pending aggregation across points/marches/removals/acks, separately tracked dropped records with positive dropped values entering failure before publishing, exact direct-completion coverage/zero-failed-block gates, and recovered Stop reset values. `nativePendingRecords > 0` alone is not yet proven to reject publication. `LWB-R6-056` additionally pins the public Start/resume branch but finds no recovered producer that sets `resumeAvailable=true`; retry/backoff remains open. `LWB-R6-058` recovers the atomic completion ownership edge: the exact run is conditionally transitioned from `running` to `completed` inside publication, zero affected rows reject with `INVALID_SCAN / map scan is not running`, and the exact run is reread after commit with `INVALID_SCAN / map scan disappeared` if absent. Offline helpers reproduce only these recovered rules; R7-003 still keeps unmeasured full-scan metrics null. ACTIVE priority: recover the real block/request scheduler, retries, exact ack/drain treatment, cancel/resume and publication sequencing; do not synthesize events merely to drive these formulas. |
| M05 Clear | `map_scan_clear({serverId})` | RECOVERED OWNERSHIP + IMPLEMENTED/OFFLINE-TESTED SLICE | Backend/store transaction deletes selected-server scan runs and records, preserving marks and other servers. `LWB-R6-057` recovers the original pre-clear ownership gate: active scans reject with `SCAN_RUNNING` / `stop the map scan first`; otherwise requested `serverId` must be positive, equal the current scan-state server, and exact `serverIdSource=live`, or Clear rejects with `SERVER_UNAVAILABLE` / `current server id unavailable`. The validator is offline-only until production owns an authoritative live scan-state server source. Generation/late-response rejection, visible progress/query reset and failure/restart cases remain open. |
| M06 Result tabs/actions | `map_data_options`, `map_search`, `map_summary` plus conditional commands | PARTIAL LIVE-PROVEN SEARCH/RENDER + MONSTER LOCALIZATION OFFLINE-TESTED | [Map Scan findings R6-003-045](lwbridge-map-scan.md) preserve recovered query/aggregate contracts. R7-003 proves fresh Resource Search/Summary, while `LWB-R7-015` proves complete Resource publication plus exact persisted reopen through normal `map_search` counting. `LWB-R7-016` separately proves complete Truck publication/reopen and live source-backed normalization for `arriveTs`/`robTimes`. `LWB-R7-047` closes current-v19 Truck goods/max-loot scan fidelity: the final cleaned read-only Fast run reconstructed `currentGoods` on all 244 published Trucks (1,782 normalized reward items), `maxLootCount` on all 244, preserved 19 special-UR rows, and persisted zero full Truck `trainDataJson` rows. `LWB-R7-048` then LIVE-PROVES Truck ordinary-UR, reindeer-only, plunderable-only and retained-item option/filter behavior on current v19: all 435 final rows carried frontend-compatible `remainingLootCount`, 18 reward options were produced from game-owned display metadata, selective item `reward:7:600002` matched exactly 283/435 rows across reopen, and the final Fast scan remained 2,500/2,500 in 131.059 s with zero Truck `trainDataJson`. Protection/expiry presentation remains source-limited where current-v19 does not expose a positive authoritative value. `LWB-PC-001` proves fresh Player City `map_search`/summary, `LWB-PC-002` proves a new process reopens the same saved city through normal `map_search`, and `LWB-PC-003` proves the reviewed normal Map Data City row plus exact-session Search/render correlation and clean close. `LWB-R7-011` additionally restores the production `lastwar_localize` path used by Monster rows and offline-proves English/`zh-CN` name re-localization. Other result tabs/actions remain open. `LWB-R7-013` supplies the first Monster name/options/countdown slice. `LWB-R7-014` supersedes its presentation policy: Remaining uses only positive `zMBossInfo.shieldEndTime`, current-v17 `TileDistance` is live-proven on all 12,731 rows of that full-world run, and absent shield deadlines remain `-`. `LWB-R7-017` then rechecks original 0.3.1 (no Monster Remaining column), recovers current-v17 typed `ZMBossInfo`, strengthens same-field collection, and live-proves base-relative Distance on all 12,017 rows of a fresh 2,500/2,500 run. `LWB-R7-018/019` make later Monster-Invasion detail optional to base acquisition and automatically reproduce the exact current-v18 click request for loaded eligible bosses. The final 82.962 s run captured 50/81 authoritative details; no active positive deadline was present, so visible positive Remaining remains an observational gate. |
| M07 Search/filter/sort/page/live refresh | `map_search({kind,query})` | PARTIAL IMPLEMENTED/OFFLINE-TESTED | `LWB-R6-003` implements server/kind scope, recovered `LIMIT/OFFSET`, `updatedAt asc|desc` plus `record_key ASC`, `{rows,total}`, city mark projection and `markedOnly`. `LWB-R6-004` recovers the frontend stale-search guard and eight-tab envelope distinctions. `LWB-R6-005/006/007/009/010/013/014` recover and offline-test the supported keyword, city, name-key, retained-item, truck-reindeer, special, treasure-pair, dispatch-level, quality, completion-status and frontend plunderability predicates. `R6-014` also restores the original always-on truck/railway active-arrival cutoff from one sampled precise Unix-ms wall clock. `LWB-R7-048` LIVE-PROVES the current-v19 Truck ordinary-UR exclusion of special-UR, reindeer-only, plunderable-only and retained-item filters against independently derived expected UUID sets; it also proves reward-option generation and DB-reopen stability. The recovered non-`updatedAt` Truck sort keys remain fail-closed because the complete native multi-sort/null/tie-break composition is still unrecovered. `LWB-R6-008` keeps count/page on one SQLite read snapshot as an explicit rebuild policy and proves it against a concurrent WAL writer. `LWB-R6-026` recovers the frontend ordered multi-sort toggle and exact public per-kind sort columns, pins eleven scalar native parser comparisons, four direct expression references, and the adjacent sort/null-order string inventory; it also corrects item-count SQL text to raw `0x00C8A926` after a descriptor byte at `0x00C8A925`. `LWB-R6-027` recovers physical direction selection as `asc -> ASC`, ordinary `desc -> DESC`, with an exact `distance desc -> ASC` override. The distance value mechanism and complete native multi-sort/null/tie-break composition plus remainingLootCount/shield/distance/railway-quality mappings remain open, so treasure visibility/lucky semantics and every non-`updatedAt` production sort stay fail-closed with IMPLEMENTATION POLICY `MAP_QUERY_UNRECOVERED`. `LWB-R7-013` adds the localized-name-key keyword path and prevents raw JSON property-name false matches. `LWB-R7-014` changes the owner-requested Monster level control to inclusive maximum (`level <= maxLevel`), wires visible Level/Distance/Updated sorts through persisted SQL with `record_key ASC` tie-break, and preserves current rows when a query fails; unsupported min/range forms stay fail-closed. `LWB-R7-017` changes only the selector presentation to five-level maximum buckets (`5,10,15,...`); backend semantics stay `level <= maxLevel`. |
| M08 Mark/unmark | `map_player_mark_set`, `bridge://player-mark-changed` | IMPLEMENTED/OFFLINE-TESTED | Original mark key `(server_id,owner_uid)`, upsert/delete, clear-survival and frontend refresh event are recovered and reproduced. Live player-tracker relocation/state transitions remain unproven. |
| M09 Export | retired; no production command | **REMOVED BY OWNER OVERRIDE (`LWB-R7-066`)** | R7-057..060 remain historical proof of the original/recovered City-export contract, but the rebuild now ships no Export Excel control, `map_city_export` route, save dialog, writer, export-only store path or workbook tests. Negative backend + browser regressions prevent accidental reachability. |
| M10 Jump/follow | `map_coordinate_jump`, `map_march_follow` | COORDINATE JUMP + TRUCK/RAILWAY FOLLOW LIVE-PROVEN | `LWB-R7-061` live-proves public `map_coordinate_jump({serverId,x,y})` on current-v19 through the owned normal-world `GotoWorldPos` completion callback and a proven return to the player tile. Shipped UI wiring routes all non-moving result kinds to that command; Truck/Railway use moving Follow. `LWB-R7-040` live-proves Truck Follow through `GoToUtil.JumpToMarchByUuid(marchUuid,serverId,0)` and its authoritative `GetMarchPos` fallback. `LWB-R7-071` closes the Railway population gap: a server-2204 full scan published 3 authentic Railway rows and public `map_march_follow` succeeded on an exact published `marchUuid`. |
| M11 Auto Scan | recovered frontend orchestration + scan/server-jump calls | PARTIAL LIVE-PROVEN BACKEND CYCLE | `LWB-R7-037` proves the same owned session can execute `server_jump(current) -> map_scan_start(fast,zombie_boss)` through the production command service and shared Manual engine: 2,500/2,500, zero failed/unread, 56 Zombie Bosses, 19.363 s scan wall time. Direct multi-server travel is independently live-proven. Remaining gate: drive the actual page-owned scheduler/timer, prove multi-server sequencing + configured return-to-origin, and decide durable single-owner scheduling across navigation/restart. |
| M12 Treasure | treasure refresh/claim/status commands | READ-ONLY REFRESH/STATUS LIVE-PROVEN; CLAIM HOST/DIRECT-v19 + FRONTEND/STATUS CONTRACTS OFFLINE-TESTED; PUBLIC CLAIM BLOCKED | R7-068/069 close query/cache plus live read-only state refresh/status. R7-086 pins original `boxes|season|single`, candidate SQL, hidden 5 s claim/status boundaries and current-v19 direct ordinary `detect.event.claim.treasure` request/result signals, while Supplies is scout/march-driven. R7-087 pins the original 1 s/1,800 status loop, UUID state overlay, running-only nonterminal batch rule, lucky preference and exact row button gate. Protected `claimTreasures` scope filtering/order, lucky-slot scheduling, scout-slot reservation and exact terminal-state enumeration remain unrecovered, so `map_treasure_claim` is not routed and no live claim is claimed. |
| M13 Dispatch plunder schedule | `map_dispatch_plunder_schedule`, `map_dispatch_plunder_cancel` | PUBLIC SCHEDULE/CANCEL + VALIDATION + PERSISTENCE + EXECUTOR + DURABLE WORKER IMPLEMENTED/OFFLINE-TESTED; LIVE ACCEPTANCE OPEN | `LWB-R7-080` pins public validation/cancel and active persistence; R7-081 recovers current-v19 eligibility and original worker timing; R7-082 closes post-arm result/executor; R7-083 wires the durable worker; R7-084 enables the public schedule wrapper with whole-batch validation, sequential guarded writes, accumulated scheduled-row return, one post-success change event, positive-i64 local server identity and fail-closed current-v19 execution outside 1..99999. No live Dispatch plunder has been run. |
| M14 Truck/train actions | truck schedule/cancel and recovered row actions | FOLLOW LIVE-PROVEN; PUBLIC SCHEDULE/CANCEL + RESULT + ARCHIVE/RESCHEDULE + DURABLE WORKER OFFLINE-TESTED; INTERNAL QUICK-ROB EXECUTOR/REWARDS OFFLINE-TESTED; LIVE ROBBERY ACCEPTANCE PENDING | `LWB-R7-040` live-proves rendered Truck Follow. `LWB-R7-041` implements cancel/list persistence. `LWB-R7-042` implements the one-shot current-v19 quick-rob executor. `LWB-R7-043` adds authoritative reward/result persistence. `LWB-R7-044` reproduces archive/reschedule. `LWB-R7-045` adds the single-owner durable worker with recovered 10-second arm lead, exactly-one running attempt increment, offline deferral, transactional status+jobId race protection, terminal no-retry handling for sent/ambiguous outcomes, conservative stale-running restart failure, and frontend change events. `LWB-R7-046` exposes the recovered public `map_truck_plunder_schedule({rows})` contract with exact INVALID_REQUEST validation, 1..200 batching, unit/null success and full-batch prevalidation. Worker execution remains same-live-server only and never jumps implicitly. No live robbery acceptance is claimed. |
| M15 Scheduled Plunder tab | `map_plunder_jobs_list` | READ/LIST/STATUS CLOSED; TRUCK + DISPATCH PUBLIC SCHEDULE/CANCEL AND DURABLE WORKERS OFFLINE-TESTED; LIVE ACTION ACCEPTANCE OPEN | `LWB-R7-041`/`079` close the combined persisted read surface; R7-043..046 close Truck result/archive/worker/public schedule offline. Dispatch R7-080..084 closes validation, persistence, result semantics, current-v19 executor, durable worker and public schedule/cancel wrapper. Live Truck robbery and Dispatch plunder acceptance remain deliberately NOT_RUN because they consume real gameplay actions. |
| M16 Alliance share | `map_dispatch_share_alliance` | ORIGINAL + CURRENT-v19 CONTRACT RECOVERED; PAYLOAD/VALIDATION OFFLINE-TESTED; PUBLIC SEND BLOCKED | R7-085 pins original 1..200 validation, decimal UUID + positive integer-like `serverId/x/y/cfgId`, per-row 5 s `shareDispatchTaskToAlliance`, aggregate `shared/failed/sharedUuids/failedUuids`, and current-v19 `Text_PointShare -> TO_ALLIANCE -> hero.dispatch.share.chat` with `x/y/sid/dispatch=1/cfgId/uuid` + optional `uname/abbr`. Internal payload planning is implemented; production send remains unavailable and live delivery requires explicit messaging authorization. |

## Native adapter implementation ledger

| Capability | Original source | Rebuild implementation | Validation |
|---|---|---|---|
| Command names/payload wrappers | `evidence/lwbridge-0.3.1/frontend/assets/api-ClPPi2JT.js` | `src/LWBridge.Desktop/LWBridgeBackend.cs` | Recovered wrapper strings inspected; unsupported production commands reject explicitly. |
| WebView command/event transport | original Tauri invoke/listen wrapper + rebuild host policy | `LWBridgeWindow.cs` + `WebUi/preview-host.js` + request/subscription registries | Active-profile injection/event envelopes restored. Isolated native WebView proves duplicate active-ID rejection, reload rotation/cancel/subscription reset, stale-session and external-navigation rejection, structured errors, responsive slow storage and no late publication after real window close. Real lifecycle-worker state mutation remains gated on R5/R7. |
| Fixture/read-only isolation | rebuild requirement | `preview-host.js`, `LocalConfigStore`, bootstrap/probe modes | Fixture commands cannot reach live handlers; capture/read-only modes remain isolated. The native host probe uses a unique temp config plus in-memory map DB, reports startup auto-launch suppressed, touches no user config and performs no live-game command. `LWB-PM6-001` also bounds cleanup of the owned probe child and CI exercises a deterministic termination-command-failure path without touching unrelated processes. Requested live without native transport still fails visibly. |
| Stable local profile | original profile routing + rebuild persistence policy | versioned config plus recovered U/W routing | Specified PM2 config/identity regressions pass. Single-save rollback, visible errors, rapid both-fail/mixed-success reconciliation and post-failure recovery pass in provider and real native-host tests. |
| Game-root validation | `game_root_status`, `game_root_select` | `GameInstallationService.cs` + native picker | Installed/missing-root diagnostics and recovered cancel/invalid picker behavior pass in the isolated real WebView host. Both installed `LastWar.exe` and `xlua.dll` report AMD64 `0x8664/PE32+`; `LWB-R5-002` separately proves the exact export ABI fingerprint selector and current secure classification. |
| Overview lifecycle safety | `profile_instance_start/status/stop` | `OverviewLifecycleService.cs`, `tools/run_overview_bridge.py`, `tools/current_overview_bridge.lua` | Bare/isolated backend still fails closed. The normal production window supplies the independent lifecycle; tests reject stale/foreign/duplicate ownership. Two technical live cycles prove same-session readiness, active-session deferred restoration, exact owned normal Close and byte-exact cleanup. Owner-visible normal-window verification passed in `LWB-OVL-003`; this selected milestone is owner-accepted. |
| Bounded fresh resource acquisition | recovered Map Data `map_scan_start` wrapper + current-client `WorldPointManager` source | `LiveResourceProbeCommandService.cs`, `LiveResourceProofRunner.cs`, `tools/run_live_resource_probe.py`, `tools/current_live_resource_probe.lua` | `LWB-R7-003` performs two distinct rebuild-generated requests, waits for a fresh view-response flag transition, imports one resource row into the normal profile store, returns it through `map_search`/summary, and verifies exact script-package restoration. Direct transport is IMPLEMENTATION POLICY; original pipe/session remains blocked. |
| Bounded fresh Player City acquisition | recovered `city` Map Data selection + current build-1078 `PointInfo/BuildPointInfo` and `WorldPointManager` | same production service/helper/importer plus `--live-city-proof` / `--city-reopen-proof` evidence modes and read-only owner collector | `LWB-PC-001` performs two distinct city-only live reads, persists/searches the real row and restores exact client bytes; `LWB-PC-002` proves fresh-process same-profile saved Search; `LWB-PC-003` proves owner-visible normal-page rendering and a later correlated Search on the reviewed build. Player identity is redacted from committed evidence. |

## Current verification commands

```powershell
python tools/build_lwbridge_frontend.py --check
python tools/inspect_official_runtime.py --output evidence/official-runtime/2026-09-08-official-runtime.json
dotnet build src/LWBridge.Desktop/LWBridge.Desktop.csproj --configuration Release
dotnet run --project tests/LWBridge.Desktop.Checks/LWBridge.Desktop.Checks.csproj --configuration Release
node tools/check_lwbridge_live_transport.cjs
node tools/check_lwbridge_transport_boundary.cjs
./tools/capture_lwbridge_ui.ps1
```

Review 9 reran build/backend and frontend/preference/transport checks; refreshed read-only runtime/health comparison. No new binary-analysis/game action/host screenshot matrix. See [review evidence](../evidence/lwbridge-implementation/2026-09-10-pm-review-9.json).

## Current-client v16 update-safety checkpoint — 2026-09-14

| Capability | Status | Evidence / impact |
|---|---|---|
| Overview launcher update safety | LIVE-PROVEN implementation and full lifecycle | `LWB-V16-001/002/003` in [`lastwar-v16-update-safe-lifecycle.md`](lastwar-v16-update-safe-lifecycle.md) proves the modified-package update failure, exact supported v16 identity/unchanged compatibility anchors, and the complete safe sequence: recover pending state → untouched official launcher settle → verify v16 → temporary candidate → genuine same-session READY → exact normal close → exact v16 restore. Durable live evidence is under `evidence/official-runtime/2026-09-15-v16-live-lifecycle/`. |
