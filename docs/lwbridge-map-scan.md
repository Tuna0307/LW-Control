# LWBridge Map Scan recovery

Map Scan is the first feature to recover one-for-one.

## Current finding summary — review 9, 2026-09-10

Review 9 accepts R6-047–051: the original record identity formula and scalar source/fallback rules are recovered. `LWB-R7-001` subsequently proves one bounded current-game resource acquisition and rebuilt display path; production capture/serializer ownership, supported bridge/session connection and scan lifecycle remain unresolved. PM8-0 is closed by user-operated recovery; PM9-A remains deferred while the production first-result path continues. Earlier per-finding limits retain their dates and do not supersede later recovery.

PM7-A/R6-038 integration is complete as a reusable service; R6-039–046 remain static research. Read [the current audit](lwbridge-project-status.md) and [PM8-0 official update incident](lastwar-update-incident.md) before current-client/live work. ESC-005 tracks remaining loader/handler gaps; no specialist assigned. Later confirmed findings supersede earlier interpretations, including R6-043 correcting R6-042.

Use [PM7-A–D](implementation-handoff.md) for current work and [ESC decisions](daybreak-escalations.md) for ownership. R6-038 completes PM7-A's bounded source-aware option/count integration behind the existing public gate; PM7-B is the next production-blocking state/handler seam. ESC-003 still tracks only the remaining serializer with PM status NOT_ASSIGNED. Earlier finding limits below describe their own checkpoints and do not supersede later recovery.

This document preserves dated findings, including superseded interpretations. Use the latest applicable source-backed result below rather than copying an older "remaining unknowns" paragraph as current status.

| Findings | Current interpretation |
|---|---|
| R6-003–005 | Default persistence/query/UI consumers plus alliance/name/item filter foundation. |
| R6-006 → R6-010 | Special-only dispatch/ghost; reindeer-only truck. The R6-006 JSON's railway entry is historical. PM5-01 corrects the old inspector metadata and labels its frontend-gate provenance. |
| R6-007 | Exact literal keyword escaping/substrings; implemented offline. |
| R6-008 | Consistent count/page snapshot; implementation policy and meaningful WAL test, not original transaction recovery. |
| R6-009 | Treasure/supplies and dispatch levels implemented offline; option SQL families partially recovered. Full treasure queries and options remain gated. |
| R6-011 | Schema metadata/migration markers recovered; supported version/transition/timestamp inputs unresolved. |
| R6-012 → R6-013 | Ordinary quality equals 1/2/3/4, UR >=5. The special-UR exclusion applies only to ordinary truck UR. |
| R6-014 | Precise Unix-ms wall clock, reward-option arrival cutoff, recovered frontend completion-status and plunderability predicates implemented offline. |
| R6-015 | Historical persisted-source option kernel for the recovered R6-009 SQL families. R6-038 supersedes its persisted-only service boundary by applying the recovered R6-024/030/031 source/run semantics to one source-aware aggregate path. |
| R6-016 | Frontend count/no-alliance/summary consumers are hash-locked: eight exact kind count keys, same-server count propagation/reset, no-alliance selection retained only while `noAllianceCount > 0`, and stale/profile summary guards. Backend aggregation/server selection remain open. |
| R6-017 | City export frontend contract is hash-locked: city-only/positive-server eligibility, shared query page 1/pageSize 200, exact 12-column localized option envelope, `{canceled,rowCount,path}` result handling and busy/scan button gate. Native writer/pagination/row mapping remain blocked. |
| R6-018 | Native city-export structure is partially recovered from bounded raw PE evidence: dialog/default/error vocabulary, embedded A-L OOXML package/worksheet/styles, frozen header, exact column widths, autofilter/margins and partial row-field adjacency. Full scope, A-C mapping, protect/shield fallback and per-column types remain blocked. |
| R6-019 | Test-only staging/publication infrastructure now reproduces the recovered one-kind/server `map_records` delete + matching `scan_records` copy in one transaction and proves run/kind/server isolation. Completion eligibility and native record-key derivation remain blocked. |
| R6-020 | The test-only publication slice now proves transactional rollback under an injected failure between delete and staged copy: the previous published kind/server survives intact and no replacement row leaks through. Production failure/retry semantics remain blocked. |
| R6-028 | Test-only scan-block checkpoints now persist by the recovered `(run_id,block_index)` identity, replace the same block deterministically, survive a file-backed reopen and cascade away when the owning run is cleared. Scheduling/ack/retry/status-transition semantics remain UNKNOWN/BLOCKED. |
| R6-029 | The tracked build-1078 official-runtime snapshot/architecture contains no direct `record_key`/scan-bridge vocabulary and none of the selected known LWBridge map-row field names. Current `Assembly-CSharp.rdl` and active `LWScripts.data` are now the bounded next current-contract sources; per-kind identity remains UNKNOWN/BLOCKED. |
| R6-021 | The persisted-only options test kernel now returns all eight recovered frontend count keys plus a server-scoped no-alliance count in the same snapshot as the existing option families. Public source/run selection and native count assembly remain blocked. |
| R6-022 → R6-023 | Prefix/xref negative evidence is preserved. R6-023 subsequently resolves the suffix as positive decimal treasureType/suppliesType and adds exact keys to the persisted-only helper. Public options source/full assembly is still unresolved. |
| R6-024 | The verified native `map_index.rs` string cluster pins the two exact public-option source/scope candidates: published `map_records` with `server_id=?1`, or staging `scan_records` with `server_id=?1 AND run_id=?2`. Frontend `map_data_options` supplies only `serverId`, and the native kind-count helper is likewise source/scope-parameterized. At this checkpoint the selector/run identity was still UNKNOWN/BLOCKED; R6-030 later resolves that subproblem. Exact no-alliance production/full response assembly remain open. |
| R6-025 | Native `map_summary` is now traced through its successful result path: shared scan state is obtained first, its `serverId` becomes both the count-helper server and response server, and the response assembles exactly `serverId`, `counts`, `scanState`. The same count helper can select published or staging rows from an internal optional run scope; at this checkpoint the scope producer was UNKNOWN/BLOCKED and is later resolved by R6-030. Production summary still fails closed because unavailable/error integration remains incomplete. |
| R6-030 | The previously unresolved helper `0x140344F1D` is recovered: staging/run scope exists only when the requested server is positive, scan state is `isReading=true`, scan-state `serverId` matches the request, and `scanRunId` is a nonempty string. Otherwise no run scope is returned and the R6-024 persisted source is selected. An internal selector helper is offline-tested; public options/summary remain fail-closed because no-alliance/full response/scan-progress and summary error integration are still incomplete. |
| R6-031 | Bounded option-assembly recovery closes native `noAllianceCount`: grouped alliance rows whose decoded name length is zero (SQL NULL or empty text) contribute their `COUNT(*)` to the no-alliance accumulator and are omitted from `alliances[]`. The native top-level order is `serverId`, `counts`, `alliances`, `names`, `dispatchLevels`, `noAllianceCount`, `rewardItems`, `treasureTypes`, `scanProgress`. `scanProgress` selects newest non-discarded run by server when no active scope exists and exact active `scanRunId` when scope exists. Exact scan-progress JSON serialization/empty/error representation remains open. |
| R6-032 | The persisted `scanProgress` path is narrowed to generic tagged-value conversion plus row-mapper callback `0x140294188`; broader live scan-state metadata is not substituted for the persisted contract. Exact fields/null/error behavior remains blocked after SB-33. |
| R6-033 | `map_summary` shared-state/count failures propagate through the original command error path and never fabricate an empty success. Exact shared-producer error mapping and production state integration remain open. |
| R6-034 | Current build-1078 `Assembly-CSharp.rdl` is now reproducibly decoded with a repository-local read-only RG/RGMD inspector. It exposes xLua-visible `DCPlayer.GetCurServerId()` and `SceneManager.IsInWorld()`; the authoritative DCPlayer access path/bridge producer remains blocked, so summary stays fail-closed. |
| R6-035 | Current build-1078 managed ownership is now traced as static `GameEntry.Data -> CustomDataManager.Player -> DCPlayer.GetCurServerId()`, with generated xLua wrappers for every hop. This closes the managed owner/access-path gap; bridge producer/readiness/error semantics remain blocked. |
| R6-036 | The verified LWBridge shared state producer calls `getWorldMapState` then `getCurrentServerId` through one dispatcher with the recovered 5,000 ms timeout and applies concrete normalization rules for `isInWorld`, `homeServerId`, `seasonServerIds` and `truckMatchServerIds`. Exact named-error mapping and the bridge-command-handler link to the current-client managed chain remain blocked. |
| R6-037 | `src\commands\map.rs` has a second `getCurrentServerId` reference that sends the bridge request and validates its decoded `serverId`; `<= 0` constructs exact `SERVER_UNAVAILABLE` / `current server id unavailable`. The encrypted bridge-script handler and separate map-state-unavailable branch remain blocked. |
| R6-038 | PM7-A integrates the recovered published/staged option/count source contract into `MapDataStore.SelectOptionSource` + `ReadOptionAggregatesAt`. Every recovered option family, counts and scan-progress row selection now use one server/run context and one SQLite read snapshot; conflicting published/staged/two-server/two-run fixtures and a concurrent WAL writer pass. Public `map_data_options` remains `MAP_INDEX_UNAVAILABLE` because exact `scanProgress` JSON serialization and authoritative scan-state/provider prerequisites remain unresolved. |
| R6-039 | PM7-B recovers the package-key/runtime-material boundary: the host auth parser extracts `packageKeyEnvelope`/expiry alongside the authorization ticket, a separate host function constructs `authorization.challenge`, `authorization.ticket`, `package-key.envelope` and `build.manifest` runtime paths, and both embedded proxies carry the matching key-envelope/package-decrypt vocabulary. `bridge-scripts.dat` is still protected LWBP v2 and contains no `LWKE1` marker. Direct parsed-value-to-file dataflow and plaintext command handlers remain blocked. |
| R6-040 | PM7-B closes R6-039's direct persistence gap. The successful `packageKeyEnvelope` branch loads the `package-key.envelope` runtime path from service offsets `+0x1A0/+0x1A8`, formats the parsed response value with descriptor `C0 01 0A 00` as the response string plus one LF byte, and calls shared persistence helper `0x14023D8FC`. Its write chain reaches `ntdll!NtWriteFile` and finalizes through `MoveFileExW(..., MOVEFILE_REPLACE_EXISTING)`; invalid/expired input loads the same path, reaches `DeleteFileW`, and returns `KEY_ENVELOPE_INVALID`. Proxy read/decrypt and protected handlers remain blocked. |
| R6-041 | PM7-B narrows the proxy decrypt prerequisite boundary without guessing the protected package. The host `src\services\auth.rs` marker cluster ties `MachineGuid`, device-key cleanup/error states, `packageKeyEnvelope`, `LWKE1` and the four runtime-material filenames together; the host imports persistent CNG-key and DPAPI primitives. Both verified proxies carry matching `LWKE1`/device-key/key-envelope/package-decrypt vocabulary, URL-safe Base64 alphabet and the runtime paths, and import CNG secret-agreement/derive-key, BCrypt decrypt/hash/signature and file/environment primitives. Import/marker presence does not prove the exact loader call graph, algorithms or handler plaintext, which remain blocked. |
| R6-042 | PM7-B pins exact proxy-local `DEVICE_KEY_PROVIDER`, `DEVICE_KEY_EXPORT`, `DEVICE_KEY_FORMAT` and `DEVICE_KEY_MISSING` markers at raw offsets `0x730E0/0x730F8/0x73110/0x73128`. R6-043 supersedes R6-042's earlier interpretation of the first three as configuration names: bounded secure-proxy control flow proves they are diagnostic labels on provider-open, export and format-validation failure branches. |
| R6-043 | PM7-B recovers the secure proxy's concrete device-key and crypto prerequisites. It hardcodes `Microsoft Software Key Storage Provider` and persisted key `{2D337A4D-7E6C-49EF-9486-54F0A00D8A41}`, exports `ECCPUBLICBLOB`, requires `ECK1`/32-byte coordinates/72-byte blob, emits and accepts the 65-byte `0x04 || X || Y` public form, derives exactly 32 bytes with CNG KDF `TRUNCATE`, and decrypts with AES-GCM using a 32-byte key, 12-byte nonce and 16-byte tag. Exact package-key file caller, envelope field layout, protected handlers and readiness/error propagation remain blocked. |
| R6-044 | PM7-B closes the shared producer's server-source fallback slice: exact `isInWorld=false` can fill a nonpositive state server from positive `homeServerId` with `serverIdSource=live`; the positive-current-server path also writes `live`, but exact `isReading=true` with a different positive state server raises `current server changed during map scan`. With no current server, an active or already-positive state is preserved; otherwise the producer writes `serverId=0`, `serverIdSource=none`, `tileWidth=0` and `tileHeight=0`. Protected handlers, dispatcher errors and the separate `map scan state is unavailable` emitter remain blocked. |
| R6-045 | PM7-B identifies a package-side consumer of the R6-043 AES primitive without assigning the still-blocked envelope reader. Secure-proxy unwind function `RVA 0x3D260-0x3DCF5` directly calls AES-GCM helper `RVA 0x3CB60` at `0x3DA4A` and directly references `LWBP2|`, `package integrity invalid`, and `package build mismatch`. This recovers a package validation/decrypt boundary; the package file caller, `package-key.envelope` open/read/parser and agreement caller, callsite key/nonce/tag ownership, plain-proxy parity and protected handlers remain blocked. |
| R6-046 | PM7-B recovers the secure-proxy package-key path/read boundary. Function `RVA 0x1B300-0x1B4BF` appends exact UTF-16 `\bridge-runtime\package-key.envelope`; enclosing loader `RVA 0x1C0A0-0x1D46F` passes the returned path to bounded reader `RVA 0x1BD80-0x1C09F` at `0x1C45B` with exact `0x1000` maximum. The reader rejects nonpositive/oversized input and strips trailing CR/LF. A second path build at `0x1C8B8` is passed as `RCX` to `0x3F8E0` at `0x1C8E2`, then `AL` is tested. The `0x3F8E0` body/field grammar remains blocked and SB-79 is not replayed. |
| R6-047 | PM7-C recovers the native map-record upsert boundary at `RVA 0x27A1E3-0x27AA73`. An exact 12-byte `scan_records` table argument selects staging; staging prepends `run_id` and otherwise binds the same 14 record fields as published `map_records`. The shared input record carries `record_key` at `+0x78`. This identifies the precise upstream producer slot to trace, but per-kind key derivation and production ingestion remain UNKNOWN/BLOCKED. |
| R6-048 | PM7-C recovers the current build-1078 resource/world-point identity surface. `Protobuf.WorldPointInfo` carries `Id=1`, `ResourceInfo=6`, `Uuid=100`, `ServerId=102`, `SrcServerId=103`, `WorldId=104`; `WorldPointManager::GetWorldTileInfo(int32 pointIndex)` and `GetPointInfoByUuid(int64 uuid)` are separate typed lookup paths, and `GetResourcePointInfoByIndex(int32 pointIndex)` reaches `ResPointInfo`. This narrows current-client candidates but does not prove which value populates R6-047 `record_key` at `+0x78`. |
| R6-049 | PM7-C closes the original normalized `record_key` derivation in builder `RVA 0x2785BA-0x2793BE`. The first present integer among `pointIndex`, `mainIndex`, `pointId`, `index` becomes signed-decimal identity for ordinary indexed rows. Exact `truck`/`railway`, any record with nonempty `marchUuid`, or a record without a point index uses nonempty `uuid` else `marchUuid`. Coordinate fallback separately decrements a copy of the point value. The selected String is written to normalized record `+0x78/+0x88`, which R6-047 persists. Current-client capture-to-builder mapping, remaining typed normalization/removal, scheduling and live bridge readiness remain UNKNOWN/BLOCKED. |
| R6-050 | PM7-C pins the current build-1078 managed/xLua identity fields upstream of capture. Exact `PointInfo` fields are `pointIndex:int32`, `mainIndex:int32`, `uuid:int64`; generated `PointInfoWrap` getters read distinct backing tokens, and `PointInfo.get_isMainPoint()` directly compares the recovered `mainIndex` and `pointIndex` tokens. `PointInfo(WorldPointInfo)` stores all three, with one raw source call feeding both point fields and a separate raw source call feeding `uuid`. `ResPointInfo` defines `gatherMarchUuid:int64` plus `GetResType()`/`GetResLevel()`. Modified source-call tokens remain unnamed, so native capture ownership and R6-049 builder mapping are still UNKNOWN/BLOCKED. |
| R6-051 | PM7-C extends the original normalized-record builder contract for five indexed scalar columns. Exact raw record fields are `level`, `quality`, `power`, `distance` with fallback `distanceFromHome`, and `shieldEndTime` with fallback `protectEndTime`; they populate the R6-047 normalized slots at `+0x10`, `+0x20`, `+0x30`, `+0x40` and `+0x50` respectively. This closes original-side field/fallback provenance only. Current-client native serializer ownership and the live ingestion path remain UNKNOWN/BLOCKED. |
| R7-001 | A build-1078/Lua-v14 instrumented current-client run captured 15,293 world records including 8,000 resources. Selected point `1006` from `WorldPointManager._pointInfos` is server `2212`, coordinates `5,1`, level `3`. Isolated `--first-live-result` imports that exact source record through `MapDataStore` and the recovered Map Data query/UI displays one Resource row. `resourceNameKey` and gather occupancy stay unknown. This is LIVE-PROVEN acquisition plus an IMPLEMENTATION POLICY replay adapter; production bridge/session and `map_scan_start` remain blocked. |

Current remaining query gaps include viewer/foreign-radar/lucky behavior, alternate sort expressions/order, exact public `scanProgress` JSON serialization/empty/error behavior, and the shared-state producer's remaining readiness/error mapping/production integration. `LWB-R6-014` closes the recovered frontend completion/plunderability clocks and reward-option cutoff source/unit; `LWB-R6-015` established the original persisted-only aggregate kernel; `LWB-R6-016` closes the frontend count propagation/reset/fallback rules; `LWB-R6-024` narrows the original source abstraction to the exact published and staging source/scope pairs; `LWB-R6-025` recovers the summary envelope and scan-state-server/count data flow; `LWB-R6-030` closes the shared optional run-scope selector and identifies the active run as the scan state's exact nonempty `scanRunId`; `LWB-R6-031` closes native no-alliance production, top-level option insertion order and persisted-versus-active scan-progress record selection; `LWB-R6-032` identifies the remaining serializer edge; `LWB-R6-033` closes summary failure propagation; `LWB-R6-034` establishes concrete current-client current-server/in-world callables; `LWB-R6-035` closes the managed `DCPlayer` owner/access chain; `LWB-R6-036` closes the native shared-producer request order plus several state-normalization rules; `LWB-R6-037` closes the explicit current-server invalid/unavailable error branch; `LWB-R6-038` integrates the recovered option/count source context and snapshot semantics into one reusable service behind the unchanged public gate; `LWB-R6-039` establishes that the protected bridge package consumes a separate runtime key-envelope material boundary rather than embedding `LWKE1` in `bridge-scripts.dat`; `LWB-R6-040` closes the host parsed-envelope persistence/cleanup path; `LWB-R6-041` narrows the remaining decrypt prerequisite seam to the host device-key boundary plus matching proxy CNG/BCrypt/file-loader surface; `LWB-R6-042` preserves the exact proxy diagnostic labels; `LWB-R6-043` recovers the secure proxy's concrete provider/key identity, public-key format, CNG derivation and AES-GCM input contract; `LWB-R6-044` recovers the shared producer's live/none server fallback and active-read mismatch branches; `LWB-R6-045` source-attributes one package validation/decrypt consumer of the recovered AES helper; `LWB-R6-046` closes exact package-key path construction and bounded text loading while identifying opaque path consumer `RVA 0x3F8E0`; `LWB-R6-049` closes original normalized `record_key` derivation; and `LWB-R6-050` pins the current `PointInfo`/`ResPointInfo` managed field surface and exact xLua backing tokens without assigning unresolved source calls. Exact envelope field grammar/agreement ownership, package-file caller and AES callsite key/nonce/tag ownership, protected handlers, dispatcher failure identities, the separate `map scan state is unavailable` emitter and live readiness integration remain open. Export, current-client native capture/serializer ownership, remaining normalization/removal and live ingestion/scan remain open. Historical restrictions and escalation reasons are in [the ESC register](daybreak-escalations.md). The regular AI owns further investigation until an exhausted-method request is reviewed; these topics are not automatic Daybreak assignments.

`LWB-R6-017` additionally proves the visible city export call uses the shared current city query builder with `page=1` and `pageSize=200`, followed by options containing these twelve headers in order: localized Server, literal `X`, literal `Y`, localized Player, literal `UID`, literal `UUID`, localized Alliance, localized Level, literal `HP`, localized Shield Ends, localized Marked and localized Updated At. `sheetName` is localized City, and `yesLabel`/`noLabel` are localized Yes/No. The frontend consumes `canceled`, `rowCount` and `path`; cancellation suppresses the success message. The 200-row query size is only a recovered frontend request field.

`LWB-R6-018` closes a separate native-format slice. The verified binary embeds `sheetName/Cities`, `yesLabel/Yes`, `noLabel/No`, filename prefix `map-cities-`, `.xlsx`/`xlsx`, dialog label `Excel workbook`, `{canceled,path,rowCount}` vocabulary, `MAP_EXPORT_FAILED`, and the specific `city export server is unavailable` / `city export headers are invalid` errors. Its embedded worksheet fixes A-L widths to `11,11,11,22,24,24,18,12,12,21,10,21`, freezes row 1 with `A2` as the lower pane, sets header row height 20, applies an `A1:L...` autofilter, and encodes the recovered margins. The styles include four cell XFs, custom datetime format `yyyy-mm-dd hh:mm:ss`, and a bold white header with solid `004F81BD` fill. Raw row metadata places `ownerName/ownerUid/uuid/allianceName/level/health/protectEndTime/marked/updatedAt` beside D-L column markers and leaves `shieldEndTime` adjacent, but A-C semantics, the exact protect/shield fallback, per-column value typing, internal pagination/full filtered scope, filename timestamp/default directory and lossless large-ID behavior remain **UNKNOWN/BLOCKED**. Production export therefore remains unavailable.

## RECOVERED control path

Static xrefs tie `enterWorldMap` and `startMapScan` to `src\\services\\map_scan.rs`.

Observed behavior:

1. Reject missing game connection.
2. Reject when another map scan is already running.
3. Read world state (`isInWorld`, `homeServerId`, `seasonServerIds`).
4. Call `enterWorldMap` when needed and wait for world-map readiness.
5. Validate `scanMode` as `normal` or `fast`.
6. Validate selected scan types and map dimensions.
7. Start the scan with `startMapScan`.
8. Track direct block progress while native point/march hooks feed capture queues.
9. Commit/publish map records through the map-index layer.
10. Call `stopMapScan` and close the native capture session on stop/failure/completion cleanup.

Recovered progress/status names include:

`scanRunId`, `totalBlocks`, `completedBlocks`, `readBlocks`, `failedBlocks`, `unreadBlocks`, `inflightBlocks`, `scanRate`, `status`, `lastError`, `resumeAvailable`.

These fields strongly indicate the original design can distinguish successful reads from failed/unread blocks and has a resumable scan state. The rebuild should preserve that distinction instead of treating “loop finished” as success.

## RECOVERED scan modes

The mode branch is now resolved far enough to name the scheduler value exactly:

- `normal` uses `concurrency = 8`.
- `fast` uses `concurrency = 20`.

Static evidence in the Map Scan state machine stores `8` or `20` in the same state field, then later loads that field while serializing the `concurrency` property. The other mode-dependent constants previously seen beside them, `6` and `4`, are the byte lengths of the strings `normal` and `fast`; they are stored with the mode string pointer and later serialized as `scanMode`. They are not a second timing/concurrency setting.

Any additional mode-specific pacing, timeout or retry differences remain UNKNOWN until separately traced.

## RECOVERED selectedTypes contract

The exact accepted values are an eight-entry static allowlist:

1. `city`
2. `resource`
3. `monster`
4. `truck`
5. `railway`
6. `dispatch`
7. `ghost`
8. `treasure`

The allowlist is referenced by the filtering routine at RVA `0x20D26F`; the verified table is at RVA `0xC81908`. A second eight-entry table used by the fallback path at RVA `0x825D98` contains the same values in the same order.

Recovered filtering behavior:

- If `selectedTypes` is absent or is not an array, LWBridge defaults to all eight values above.
- Array entries must be strings and must exactly match one of the eight allowed values.
- Unknown and non-string entries are discarded.
- Duplicate accepted values are removed while preserving the first accepted occurrence.
- If filtering leaves zero valid types, the scan fails with `INVALID_SCAN_TYPES` / `no valid map scan types selected`.

The exact downstream record schema produced for each type is still being traced; the accepted request values themselves are now RECOVERED.

## RECOVERED startMapScan request

The host constructs the `startMapScan` bridge request with these exact fields:

- `scanRunId`
- `serverId`
- `worldId`
- `scanMode`
- `concurrency`
- `selectedTypes`
- `tileWidth`
- `tileHeight`

The request is assembled in the Map Scan state machine around RVA `0xFA983-0xFAD33`. The method name `startMapScan` is installed at RVA `0xFADC0`, with a recovered bridge-call timeout of `5,000 ms` at RVA `0xFADD9`.

LWBridge does not continue merely because the call returns. It reads the response field `accepted` and rejects a false/missing acceptance with `map scan was not accepted`. The rebuild should preserve this acknowledgement gate.

## RECOVERED native capture envelope and field vocabulary

Both verified xLua proxies contain the same native world-capture implementation markers. The native layer resolves `GameAssembly.dll` / IL2CPP metadata and hooks the recovered world manager mutation methods before feeding Map Scan capture queues.

Recovered IL2CPP/runtime class markers include `Assembly-CSharp`, `WorldPointManager`, `WorldTileInfo`, `PointInfo`, `ResPointInfo`, `WorldMarchDataManager`, `WorldMarch`, `WorldTroopManager`, `SFSObject`, and `SFSArray`. The proxy also resolves world-specific getters such as `GetResType`, `GetResLevel`, `GetWorldTreasureType`, `GetMarchCurPosIndex`, `GetMaxHP`, and `IsMonsterOrOrdinaryBoss`.

The native capture result/envelope exposes these exact field names:

`scanRunId`, `ready`, `points`, `marches`, `pointRemovals`, `marchRemovals`, `acks`, `error`, `dropped`, `pendingAcks`, `pendingMarchRemovals`, `pendingPointRemovals`, `pendingMarches`, `pendingPoints`.

Recovered record-field vocabulary includes:

`ownerUid`, `uuid`, `serverId`, `srcServerId`, `worldId`, `itemId`, `id`, `level`, `quality`, `state`, `curHp`, `curMaxHp`, `allianceId`, `playerName`, `alAbbr`, `name`, `power`, `specialType`, `protectEndTime`, `shieldEndTime`, `completionTime`, `cfgId`, `expiredTime`, `taskExpireTime`, `actEndTime`, `gatherMarchUuid`, `gatherUid`, `gatherAllianceId`, `lastHpTime`, `unavailableTime`, `recoverSpeed`, `fireSpeed`, `heroList`, `memberList`, `ownerServer`, `eventId`, `allianceAbbr`, `rewardUserList`, `diggingUserList`, `complete`, `ownerName`, `startTime`, `expireTime`, `createTime`, `fromPoint`, `multiple`, `killerId`, `_uuid`, `targetPos`, `startPos`, `homePos`, `ownerCurServerId`, `allianceUid`, `allianceName`, `endTime`, `status`, `_curHp`, `monsterId`, `monsterType`, `monsterSpecialType`, `monsterRallyNum`, `train`, `config`, `carriageNum`, `pointIds`, `isMainPoint`, `isSpecial`, `resType`, `resLevel`, `stolenCount`, `heroCount`, `memberCount`, `treasureType`, `rewardedCount`, `diggingCount`, `runtimeClass`, `configId`, `marchType`, `maxHp`, `isMonster`, `requiresRally`, `normalType`.

This proves the field vocabulary used by the native capture serializer. Exact per-record-kind grouping, optionality, numeric/string types, and map-index normalization remain UNKNOWN until the serializer branches are traced.

The proxy contains explicit states for `native world capture is initializing`, `native world capture hooks ready`, required-hooks-ready with optional hooks still pending, hooks unavailable, and `native world capture queue overflow run=`. Queue overflow therefore has an explicit detectable native failure condition and must not be silently ignored by the rebuild.

## RECOVERED native capture

The proxy exposes:

- `__XLUA_BRIDGE_NATIVE_WORLD_CAPTURE_RUN`
- `__XluaBridgeNativeWorldCapture`
- `XluaBridgeMapScanTick`
- `XluaBridgeNativeUpdate`
- `XluaBridgePoll`

Recovered manager/method markers cover:

- `WorldPointManager.AddPointInfo` / remove/fold-up paths
- `WorldTileInfo.RemovePointInfo`
- `WorldMarchDataManager.AddMarch`, `UpdateMarch`, `AddOrUpdateMarch`, `TryRemoveMarch`
- `WorldTroopManager.UpdateTroop`

LWBridge also tracks `pendingPoints`, `pendingMarches`, point/march removals, pending acknowledgements, and a `dropped` native-capture condition.

## Evidence boundary

LWBridge's own direct-block/native-capture behavior must be recovered from the verified LWBridge artifact and validated independently against the current installed client. Do not fill unresolved semantics from unrelated implementations.

## RECOVERED Map Data query/presentation contract

The verified 0.3.1 frontend now also establishes the offline query shape used after records reach the map index. `map_search` is called as `{kind, query}`. The result table uses a fixed recovered page size of **50** and starts each kind with `updatedAt desc`. The query builder can emit these fields:

`serverId`, `keyword`, `resourceNameKey`, `monsterNameKey`, `treasureType`, `suppliesType`, `alliance`, `withoutAlliance`, `markedOnly`, `page`, `pageSize`, `sorts`, `quality`, `specialOnly`, `reindeerOnly`, `itemKey`, `completionStatus`, `plunderableOnly`, `includeForeignRadarTreasures`, `luckyFirst`, `viewerUid`, `viewerAllianceId`, `minLevel`, `maxLevel`.

The UI expects `map_search` to return `{rows,total}` and `map_data_options({serverId})` to return at least `serverId`, `alliances`, `names`, `dispatchLevels`, `counts`, `rewardItems`, `treasureTypes`, `noAllianceCount`, and `scanProgress`. `LWB-R6-003` below now implements the recovered default persisted-search slice offline: server/kind scope, `page`/`pageSize` pagination, `updatedAt asc|desc` plus `record_key ASC` stable ordering, `{rows,total}`, and city mark projection/`markedOnly`. `LWB-R6-005/006/007` extend this with recovered literal-substring keyword, city alliance/no-alliance, resource/monster name-key, truck/railway retained-item, truck-only reindeer, and dispatch/ghost special-only predicates. `LWB-R6-008` adds an explicitly labelled **IMPLEMENTATION POLICY** that keeps the count and page reads inside one SQLite read snapshot, so `{rows,total}` cannot describe two different database generations when another connection publishes during a search. Other value-bearing filters and every non-`updatedAt` sort remain `UNKNOWN/BLOCKED`; the rebuild rejects those requests with the **IMPLEMENTATION POLICY** error `MAP_QUERY_UNRECOVERED` instead of approximating them. Omitted/null/empty-string filter fields are treated as absent, while explicit booleans such as treasure flags remain gated even when `false`. `map_data_options` and `map_city_export` remain blocked.

Recovered visible row vocabulary is narrower than the native capture vocabulary and gives useful normalization targets without yet proving backend identity rules:

- city: `ownerUid`, `ownerName`, `allianceName`, `level`, `health`, `protectEndTime`/`shieldEndTime`, `marked`, coordinates, `updatedAt`;
- resource: `resourceNameKey`, `level`, gathering state, coordinates, `updatedAt`;
- monster: `monsterNameKey`, `level`, `distanceFromHome`, coordinates, `updatedAt`;
- truck: `uuid`, `ownerName`/`allianceName`, `quality`, `isSpecialURQuality`, `power`, `currentGoods`, `robTimes`, `maxLootCount`, `protectTime`, `arriveTs`, `updatedAt`;
- railway: `allianceName`/`allianceAbbr`, `quality`, `power`, `currentGoods`, `protectTime`, coordinates, `updatedAt`;
- dispatch/ghost: `ownerName`/`ownerUid`, `level`, `quality`, `isSpecial`, task status, `rewards`, `completionTime`, coordinates, `updatedAt`;
- treasure: `uuid`, `treasureType`, `suppliesType`, `treasureNameKey`, `remainingBoxes`, claim/world-state fields, coordinates, `updatedAt`.

For rendering only, the frontend derives a row key from `uuid`, else `marchUuid`, else `recordKey`, else a composite of `pointIndex`, `ownerUid` and `updatedAt`, then prefixes kind and server. This **does not prove the authoritative persistent identity/update/removal key**. R6 storage must remain blocked on that distinction rather than promoting a React rendering fallback into a database key.

### RECOVERED persistent map-index schema and identities

Static strings from the verified `lwbridge-0.3.1.exe` now expose the original SQLite map-index schema directly. The database filename is `map-data.db`; connection setup includes `journal_mode=WAL`, `synchronous=NORMAL`, `foreign_keys=ON` and `busy_timeout=5000`. The original tables are `metadata`, `map_records`, `scan_runs`, `scan_blocks`, `scan_records`, `player_marks`, `app_settings`, `treasure_claim_states`, `dispatch_plunder_jobs`, `truck_plunder_jobs`, `truck_plunder_history` and `dispatch_assist_jobs`, with the recovered indexes reproduced in `MapDataStore.cs`.

The authoritative **stored** record identity is now proven as `PRIMARY KEY (kind, server_id, record_key)`. Scan staging uses `PRIMARY KEY (run_id, kind, server_id, record_key)`. This resolves the database identity question but does **not** yet recover how each native city/resource/monster/truck/railway/dispatch/ghost/treasure payload is converted into its `record_key`; production scan ingestion therefore remains fail-closed on that derivation.

Player marks use `PRIMARY KEY (server_id, owner_uid)`. The original lookup/delete/upsert SQL all use those two fields, and map search joins city rows to marks using the same `server_id` plus `CAST(json_extract(data_json,'$.ownerUid') AS TEXT)`. The frontend only calls `map_player_mark_set` when a row has `ownerUid`, and listens to `bridge://player-mark-changed` only as a refresh trigger. The rebuild now reproduces this key, mark/unmark persistence and refresh event offline.

The original server-clear transaction contains `DELETE FROM scan_runs WHERE server_id=?1` followed by `DELETE FROM map_records WHERE server_id=?1`. `player_marks` is not deleted, so marks survive a Map Data clear/rescan. The rebuild reproduces and tests that scope. Direct-scan completion separately replaces one kind/server from staging: it deletes that kind/server from `map_records`, then copies the matching `scan_records` for the completed run. Strings `INCOMPLETE_SCAN`, `direct map scan contains failed batches` and `direct map scan is incomplete` show publication is gated on scan completeness.

The original map-search SQL also exposes several query rules: keyword search checks `name`, `alliance_name`, `uuid` and `data_json` with case-insensitive `LIKE`; alliance and no-alliance use the indexed `alliance_name`; city marked state is joined from `player_marks`; pagination uses `LIMIT/OFFSET`; and stable ordering includes `record_key ASC` as a tie-breaker. Additional recovered sort expressions include distance, power, completion time, remaining loot, protection/arrival time, special-quality normalization, health and current-goods item counts. These fragments are evidence for the next R6 query implementation; filters whose exact SQL is not yet recovered remain gated instead of being approximated.

### LWB-R6-003 — frontend query contract and default persisted search (2026-09-08)

**RECOVERED source identity.** The immutable recovered frontend assets are `evidence/lwbridge-0.3.1/frontend/assets/MapDataPanel-C1HVeNHr.js` SHA-256 `fc39de8154c54ee0937b6f9f9377d671e0dbad533ed2d9eda29a99d35e380f5e` and `evidence/lwbridge-0.3.1/frontend/assets/api-ClPPi2JT.js` SHA-256 `062ebd2362885af05e07bf4f0de00ed833293a83fefa6b54c26975fa1db9df47`. The deterministic inspector `tools/inspect_lwbridge_map_query_frontend.py` verifies those hashes, locates the API wrappers and query-builder character range, checks all recovered query field names and sort keys, and pins the `Oe=50` default result page size plus `$n(1,200)` city-export query page size. Reproduce with `python tools\inspect_lwbridge_map_query_frontend.py evidence\lwbridge-0.3.1\frontend --json`; durable output is `evidence/lwbridge-implementation/2026-09-08-r6-map-query-frontend.json`.

**RECOVERED result.** The API wrappers are `map_search({kind,query})`, `map_data_options({serverId})`, and `map_city_export({query,...options})`. The verified query builder emits the field vocabulary listed above. Visible sort keys are `updatedAt`, `level`, `health`, `shield`, `distance`, `quality`, `power`, `remainingLootCount`, `arriveTime`, `protectTime`, `completionTime`, and conditional `itemCount`. Search consumes `rows` and `total`; city export requests the same city query builder with page `1` and page size `200`.

**IMPLEMENTED/OFFLINE-TESTED.** `MapDataStore.SearchIndexed` now serves the supported persisted subset instead of treating every valid search as unavailable. It uses the previously recovered `(kind,server_id,record_key)` map identity, recovered `LIMIT/OFFSET` pagination, `updated_at` ordering with recovered `record_key ASC` tie-breaking, and the recovered city `player_marks` join. City rows receive the visible `marked` boolean and `markedOnly=true` filters through that join. Deterministic tests cover multi-page results, equal-timestamp tie-breaking, large string owner UIDs, marks, and backend `{rows,total}` serialization.

**Validation and limits.** This is static frontend recovery plus offline SQLite/backend verification; it is not **LIVE-PROVEN** and does not prove scan ingestion correctness. The exact SQL predicate construction for nonempty keyword/alliance/resource/monster/treasure/quality/special/reindeer/item/completion/plunder/viewer/level filters and exact SQL expressions for non-`updatedAt` sorts are still **UNKNOWN/BLOCKED**. `MAP_QUERY_UNRECOVERED` and `MAP_INDEX_CORRUPT` are **IMPLEMENTATION POLICY** rebuild errors, not recovered LWBridge error codes. `map_data_options`, native Excel file creation, and per-kind native `record_key` derivation remain open.

### LWB-R6-004 — frontend option/summary consumers and stale-result guards (2026-09-08)

**RECOVERED source identity.** `tools/inspect_lwbridge_map_frontend_consumers.py` verifies the immutable `MapDataPanel-C1HVeNHr.js` and `api-ClPPi2JT.js` hashes above plus `index-sfL2sT3K.js` SHA-256 `4d18cbc036a417b1a13a2c9905f1abc5dda1427531ad1ab4f7b03f4b269704b3`. Durable output is `evidence/lwbridge-implementation/2026-09-08-r6-map-frontend-consumers.json`. Exact character locators include the `map_data_options` wrapper at API offset `21176`, `map_summary` wrapper at `21233`, options consumer at MapDataPanel offset `34234`, summary consumer at index offset `315014`, and map-search generation guard at MapDataPanel offset `37718`.

**RECOVERED result.** `map_data_options({serverId})` is consumed with top-level fields `serverId`, `alliances`, `names`, `dispatchLevels`, `counts`, `rewardItems`, `treasureTypes`, `noAllianceCount`, and `scanProgress`. The UI further consumes alliance `{name,count}` entries, resource/monster name `{key,count}` entries, numeric dispatch levels, truck/railway reward-item `{key,name,iconPath}` entries, and treasure-type entries containing `key`, `count`, `treasureType`, `suppliesType`, and `treasureNameKey`. The visible scan-progress consumer references `serverId`, `id`, `status`, `createdAt`, `updatedAt`, and `error` when present.

`map_summary` is consumed as `{serverId,counts,scanState}`. The parent stores the returned summary, routes `scanState` through the shared Map Scan state path, and only replaces summary counts when the server identity matches. This pins the response envelope but does not prove how the native backend selects the active summary server.

The per-tab query builder now has explicit conditional evidence. City only emits `withoutAlliance` and `markedOnly` when true; truck/railway/dispatch only emit `plunderableOnly` as true when enabled; dispatch omits `minLevel`/`maxLevel` when its level selector is empty. Treasure is materially different: `includeForeignRadarTreasures` and `luckyFirst` are emitted as booleans on treasure queries even when false. Therefore explicit false cannot be collapsed into an omitted filter while the corresponding SQL semantics remain unrecovered.

The frontend also has three independent stale-result guards. `map_search` increments `E.current` for each request and applies rows/total/error/loading effects only while its captured generation still matches. `map_data_options` uses `Ie.current` to ignore stale option responses. `map_summary` uses `Xe.current` and additionally refuses replacement after the selected profile changes.

**IMPLEMENTED/OFFLINE-TESTED.** `tests/LWBridge.Desktop.Checks/Program.cs` now feeds representative serialized query envelopes for all eight result tabs through `MapDataQueryContract.NormalizeSearch`. The matrix pins the current fail-closed unsupported-field set and separately verifies that numeric zero remains distinct from omission. Together with the existing explicit-false treasure check and empty-string/default checks, this protects the recovered false/zero/empty/omitted distinctions without pretending the missing SQL is known. The deterministic suite passes with `mapContract=true`.

**Validation and limits.** Reproduce with `python tools\inspect_lwbridge_map_frontend_consumers.py evidence\lwbridge-0.3.1\frontend --json` and `dotnet run --project tests\LWBridge.Desktop.Checks\LWBridge.Desktop.Checks.csproj`. This is **RECOVERED static** plus **IMPLEMENTED/OFFLINE-TESTED** regression coverage, not **LIVE-PROVEN**. Complete `map_data_options` aggregation SQL/order/deduplication, `map_summary` backend server selection, remaining filter/sort SQL, and native city-export writing remain **UNKNOWN/BLOCKED**. Production `map_data_options` therefore remains fail-closed rather than synthesizing plausible option lists from stored rows.

### LWB-R6-005 — backend filter predicates, first verified subset (2026-09-08)

**RECOVERED source identity.** The verified reference is `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. `tools/inspect_lwbridge_map_query_backend.py` verifies that hash and locates the following raw PE file offsets: alliance `0x00C89B0C`, no-alliance `0x00C89B1D`, resource name key `0x00C89D59`, monster name key `0x00C89C68`, item-membership prefix `0x00C89EAF`, and item-key comparison `0x00C89F04`. Durable output is `evidence/lwbridge-implementation/2026-09-08-r6-map-query-backend.json`.

**RECOVERED result.** The original backend contains exact predicates `alliance_name = ?`, `(alliance_name IS NULL OR alliance_name = '')`, `CAST(json_extract(data_json,'$.resourceNameKey') AS TEXT) = ?`, and `CAST(json_extract(data_json,'$.monsterNameKey') AS TEXT) = ?`. The retained-item filter is an `EXISTS` over `json_each(...,'$.currentGoods')` whose item comparison is `CAST(json_extract(good.value,'$.key') AS TEXT) = ?`. Combined with the already recovered frontend emission rules, this supports city alliance/no-alliance, resource/monster name selection, and truck/railway retained-item filtering without guessing field meanings.

**IMPLEMENTED/OFFLINE-TESTED.** `MapDataQueryContract` now accepts only the recovered frontend-emitted forms for these filters, preserving mismatched kinds and unrecovered forms as `MAP_QUERY_UNRECOVERED`. `MapDataStore.SearchIndexed` applies the recovered predicates to both count and page SQL. Deterministic tests cover alliance, no-alliance, resource/monster name-key equality and truck `currentGoods` membership while retaining the existing fail-closed cases.

**Validation and limits.** Reproduce static evidence with `python tools\inspect_lwbridge_map_query_backend.py ..\LW\lwbridge-0.3.1.exe --json` and the implementation with `dotnet run --project tests\LWBridge.Desktop.Checks\LWBridge.Desktop.Checks.csproj -c Release`. This is **RECOVERED static** plus **IMPLEMENTED/OFFLINE-TESTED**, not **LIVE-PROVEN**. Keyword escaping and LIKE parameter construction, quality/special/reindeer handling, completion-status time source/units, per-kind plunderability, treasure visibility/lucky ordering, alternate sort mapping, `map_data_options`, `map_summary`, and export remained **UNKNOWN/BLOCKED** at this finding; `LWB-R6-006` below closes only the parameter-free special/reindeer boolean predicates.

### LWB-R6-006 — special/reindeer boolean predicates (2026-09-08)

**RECOVERED source identity.** The verified reference is `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. `tools/inspect_lwbridge_map_query_boolean_filters.py` verifies that hash, requires each field and predicate marker to occur exactly once, and requires each field name to be immediately followed by its SQL predicate. Raw PE file offsets are `specialOnly` `0x00C89F38`, its predicate `0x00C89F43`, `reindeerOnly` `0x00C8A0DD`, and its predicate `0x00C8A0E9`. Durable output is `evidence/lwbridge-implementation/2026-09-08-r6-map-query-boolean-filters.json`.

**RECOVERED result.** The exact predicates are `CAST(json_extract(data_json,'$.isSpecial') AS INTEGER) = 1` and `CAST(json_extract(data_json,'$.isSpecialURQuality') AS INTEGER) = 1`. At this checkpoint the frontend-kind correlation admitted `specialOnly=true` for dispatch/ghost and `reindeerOnly=true` for truck/railway. `LWB-R6-010` below supersedes only the railway kind allowance after a direct audit of the visible selector proved the `reindeer` option is truck-only. Explicit false and other mismatched-kind uses are not promoted into supported semantics.

**IMPLEMENTED/OFFLINE-TESTED.** `MapDataQueryContract` accepts only those frontend-emitted true/kind combinations. `MapDataStore.SearchIndexed` applies the exact JSON predicates to both count and page queries. Deterministic synthetic-row tests prove inclusion/exclusion and preserve mismatched kinds as `MAP_QUERY_UNRECOVERED`.

**Reproduction, validation and limits.** Run `python tools\inspect_lwbridge_map_query_boolean_filters.py ..\LW\lwbridge-0.3.1.exe --json` and `dotnet run --project tests\LWBridge.Desktop.Checks\LWBridge.Desktop.Checks.csproj --configuration Release`. This is **RECOVERED static** plus **IMPLEMENTED/OFFLINE-TESTED**, not native-ingestion or live-game proof. Ordinary quality matching, keyword transformation, completion clocks, plunderability, treasure rules and alternate sorts remained **UNKNOWN/BLOCKED** at this finding; `LWB-R6-007` below closes keyword construction only.

### LWB-R6-007 — keyword escaping and parameter construction (2026-09-08)

**RECOVERED source identity and tools.** The verified reference remains `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. `tools/inspect_lwbridge_map_keyword.py` uses `pefile 2024.8.26` and `Capstone 5.0.6`, installed from their pinned PyPI packages in the recorded user-site locations. Reproduce setup with `python -m pip install --user capstone==5.0.6 pefile==2024.8.26`, then run `python tools\inspect_lwbridge_map_keyword.py ..\LW\lwbridge-0.3.1.exe --json`. Durable output is `evidence/lwbridge-implementation/2026-09-08-r6-map-keyword.json`.

**Exact locators and result.** The map-search function spans preferred-image VA `0x140271864`–`0x140276D7B`. At raw PE file offset `0x00C89A56` / xref VA `0x140272AD8`, the original uses the 176-byte predicate `(name LIKE ? ESCAPE '\' COLLATE NOCASE OR alliance_name LIKE ? ESCAPE '\' COLLATE NOCASE OR uuid LIKE ? ESCAPE '\' COLLATE NOCASE OR data_json LIKE ? ESCAPE '\' COLLATE NOCASE)`. The caller then invokes the same replacement routine three times in this exact order: backslash to `\\` (`0x00C89B06`, xref `0x140272B30`, character immediate `0x140272B4A`), percent to `\%` (`0x00C89B08`, xref `0x140272B5D`, immediate `0x140272B74`), and underscore to `\_` (`0x00C89B0A`, xref `0x140272B87`, immediate `0x140272B9E`). The bounded replacement helper at `0x1402948C1` copies each two-byte replacement.

The format descriptor at file offset `0x00C83ECF`, referenced at VA `0x140272BDD`, places one literal percent on each side of the fully escaped value. The loop at `0x140272BF9`–`0x140272C60` initializes a count of four and clones the resulting string once for each SQL placeholder. Therefore a nonempty keyword is a case-insensitive **literal substring**, not a caller-controlled wildcard, across `name`, `alliance_name`, `uuid` and raw `data_json`.

**IMPLEMENTED/OFFLINE-TESTED.** `MapDataQueryContract` now admits nonempty keyword strings for all recovered map kinds. `MapDataStore.SearchIndexed` performs the recovered replacements and percent wrapping, binds four identical values, and applies the exact four-column `NOCASE` predicate to count and page queries. Tests exercise each source column, case-insensitive matching, and a `%_\` input against a wildcard-shaped control row to prove that all three metacharacters remain literal.

**Validation and limits.** This is bounded **RECOVERED static** analysis plus **IMPLEMENTED/OFFLINE-TESTED** SQLite behavior. It is not live ingestion or scan proof. Ordinary quality, completion clocks and per-kind plunderability remained **UNKNOWN/BLOCKED** at this finding; `LWB-R6-013/014` later close those recovered frontend slices. Treasure visibility/lucky order, options source/full assembly, summary selection and alternate sorts remain open.

### LWB-R6-008 — consistent count/page SQLite snapshot (2026-09-08)

**IMPLEMENTATION POLICY source and rationale.** Project-manager review 4 identified that `MapDataStore.SearchIndexed` issued its recovered count and page SQL as separate reads protected only by the store instance's process-local lock. A second SQLite connection could therefore publish rows between the count and page queries. The original LWBridge transaction strategy for `map_search` has not been recovered, so this checkpoint does **not** claim an original transaction mode. The rebuild policy is that one `map_search` response must describe one database generation. The implementation uses Microsoft.Data.Sqlite `10.0.11` `BeginTransaction(deferred: true)`; the inspected package assembly SHA-256 is `4abd9c2a61e580eb853e93ca8953a3cef2c05714ae28d2d1859d4dbc5e5700bc`. The verified LWBridge reference remains SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`, but no new original behavior is inferred from it for this policy.

**IMPLEMENTED/OFFLINE-TESTED.** `MapDataStore.SearchIndexedCore` now starts one deferred SQLite transaction before the count query and assigns the same transaction to both count and page commands. A deterministic file-backed regression opens a second `MapDataStore` connection in WAL mode, commits a third city row after the first connection has observed a count of two, and then verifies that the in-flight search still returns `total=2` with two pre-existing rows. After the read transaction closes, the same database reports all three rows, proving that the writer committed without changing the search snapshot.

**Reproduction and limits.** Run `dotnet run --project tests\LWBridge.Desktop.Checks\LWBridge.Desktop.Checks.csproj --configuration Release`; the deterministic result reports `mapContract=true`. Durable checkpoint metadata is `evidence/lwbridge-implementation/2026-09-08-r6-map-search-snapshot.json`. This is **IMPLEMENTATION POLICY** plus **IMPLEMENTED/OFFLINE-TESTED** behavior, not **RECOVERED** original transaction semantics and not **LIVE-PROVEN** scan ingestion. Advanced filters/sorts, `map_data_options`, authoritative summary state, export and per-kind native keys remain separately gated.

### LWB-R6-009 — option SQL family, treasure selector and dispatch-level filters (2026-09-08)

**RECOVERED source identity and locators.** The verified reference remains `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`; the correlated recovered frontend `MapDataPanel-C1HVeNHr.js` remains SHA-256 `fc39de8154c54ee0937b6f9f9377d671e0dbad533ed2d9eda29a99d35e380f5e`. Bounded raw-PE printable-string inspection records the option SQL family at `0x00C83048-0x00C83A72`, the latest server scan-summary query at `0x00C849FF-0x00C84AC5`, level predicates around `0x00C89BE4-0x00C89C1A`, treasure predicates at `0x00C89D3E-0x00C89F37`, and adjacent derived-sort strings at `0x00C8A918-0x00C8ABEB`. The durable focused excerpt and metadata are `evidence/lwbridge-implementation/2026-09-08-r6-map-options-advanced-filters.txt` and `.json`.

**RECOVERED result.** The option producer exposes city alliance counts ordered `COLLATE NOCASE`, resource/monster name-key counts grouped by kind/name, distinct dispatch levels `>=1` ordered ascending, and treasure option grouping that deliberately normalizes exactly one dimension: `suppliesType>0` produces `supplies_type` with `treasure_type=0`, while ordinary treasure rows produce `treasure_type` with `supplies_type=0`. The reward-item option query groups truck/railway `currentGoods` `{key,name,iconPath}` and excludes expired arrivals through `arriveTs > ?`; its cutoff producer/unit was unrecovered at R6-009 and is closed later by `LWB-R6-014`. `scanProgress` reads the newest non-`discarded` `scan_runs` row for the selected server using `ORDER BY updated_at DESC LIMIT 1`.

The persisted-search builder separately exposes exact treasure predicates `CAST(json_extract(data_json,'$.suppliesType') AS INTEGER) = ?`, `CAST(json_extract(data_json,'$.treasureType') AS INTEGER) = ?`, and the ordinary-treasure guard `COALESCE(CAST(json_extract(data_json,'$.suppliesType') AS INTEGER),0) = 0`. Dispatch level filtering uses `level >= ?` and `level <= ?`; the recovered frontend emits the same selected dispatch level into `minLevel` and `maxLevel`.

**IMPLEMENTED/OFFLINE-TESTED.** `MapDataQueryContract` now accepts only the recovered treasure-selection shape: both numeric dimensions are present, exactly one is positive and the other is zero. `MapDataStore.SearchIndexed` applies the corresponding supplies predicate or ordinary treasure predicate plus the recovered zero guard. Dispatch positive `minLevel`/`maxLevel` values are also admitted for the recovered dispatch kind and applied to both count and page SQL. Deterministic rows prove ordinary treasure versus supplies isolation, reject partial/both-positive treasure selections, and prove an exact dispatch-level result.

**Validation and limits.** `dotnet run --project tests\LWBridge.Desktop.Checks\LWBridge.Desktop.Checks.csproj --configuration Release` reports `mapContract=true`. A convenience executable verifier attempted after the successful bounded byte inspection was rejected by the active environment's automatic safety review; it was not retried or routed through another executor. The saved excerpt records only observations already obtained before that rejection. At R6-009, `map_data_options` remained fail-closed on the reward cutoff clock and the completion/plunderability mappings were still open; `LWB-R6-013/014` later close quality plus the recovered time-dependent public search forms and reward cutoff. Foreign-radar/lucky treasure behavior, authoritative `map_summary` server selection, public options source/run-context/full assembly and remaining non-`updatedAt` sort mapping remain **UNKNOWN/BLOCKED**. This finding is static plus offline SQLite proof, not native-ingestion or live-game proof.

### LWB-R6-010 — quality selector kind gates and railway reindeer correction (2026-09-08)

**RECOVERED source identity and locators.** The immutable recovered `MapDataPanel-C1HVeNHr.js` remains SHA-256 `fc39de8154c54ee0937b6f9f9377d671e0dbad533ed2d9eda29a99d35e380f5e`. Character offset `5977` defines quality-capable kinds as `truck`, `railway`, `dispatch` and `ghost`; offset `9155` maps numeric display quality `1=N`, `2=R`, `3=SR`, `4=SSR`, and every value `>=5` to `UR`. The query builder at offsets `37243`/`37279` omits ordinary `quality` when the selected value is `special` or `reindeer`, emitting `specialOnly`/`reindeerOnly` instead. The visible selector around `52301` offers ordinary `n/r/sr/ssr/ur`, `special` only for dispatch/ghost, and at offset `52723` the `reindeer` option is explicitly guarded by `F===truck`. Durable evidence is `evidence/lwbridge-implementation/2026-09-08-r6-map-quality-selector-kind-gates.txt` and `.json`.

**IMPLEMENTED/OFFLINE-TESTED correction.** `MapDataQueryContract` now accepts `reindeerOnly=true` only for truck. Railway `reindeerOnly=true` returns to `MAP_QUERY_UNRECOVERED`, and a deterministic regression pins that fail-closed behavior. The already recovered binary predicate `CAST(json_extract(data_json,'$.isSpecialURQuality') AS INTEGER) = 1` remains unchanged.

**Validation and limits.** This corrects an overbroad frontend-kind interpretation from `LWB-R6-006`; it does not alter the recovered predicate itself. Ordinary quality filtering remains **UNKNOWN/BLOCKED** because the frontend display mapping proves `UR` covers all numeric qualities `>=5`, while the exact original backend filter/range binding is still unrecovered. The rebuild therefore must not guess `ur` as either equality to `5` or `>=5`. This is recovered frontend evidence plus offline contract verification, not a live-game query proof.

### LWB-R6-011 — schema metadata, future-schema guard and legacy-import markers (2026-09-08)

**RECOVERED source identity and locators.** The verified `..\LW\lwbridge-0.3.1.exe` remains SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. Bounded raw-PE printable-string inspection recovers `SELECT value FROM metadata WHERE key = 'schema_version'` at `0x00C82CED`, the schema-version metadata upsert beginning at `0x00C82E28`, future-schema message fragments at `0x00C82EDE`/`0x00C82EF4`, and `MAP_SCHEMA_TOO_NEW` at `0x00C82F0F`. The adjacent migration SQL at `0x00C82D50` cancels dispatch-assist rows in `scheduled`, `waiting_connection`, `running` or `retry_wait` with `last_error='legacy assist schedule replaced'`. `legacy_import_completed` appears at `0x00C84846`, and the metadata write with literal value `true` begins at `0x00C848A0`. Durable evidence is `evidence/lwbridge-implementation/2026-09-08-r6-map-schema-metadata.txt` and `.json`.

**RECOVERED result.** The original database has an explicit schema-version read/write path and a distinct future-schema failure branch. It also records completion of the legacy import through metadata and contains a specific dispatch-assist migration update. These are recovered original contracts; they do not by themselves reveal which numeric schema version is supported, which prior version triggers the migration, or the unit/source of the bound `updated_at` values.

**UNKNOWN/BLOCKED implementation boundary.** `MapDataStore` is intentionally unchanged by this finding. Writing a guessed schema number, choosing a guessed migration threshold, or inventing the metadata timestamp clock/unit would violate the recovery rule. A direct read-only query of the existing user-profile `map-data.db` was rejected by the current execution environment's automatic safety review and was not retried through another executor; no value from that database is promoted into this finding. Recover the version constant/migration selection and timestamp producer from permitted evidence before implementing the future-schema gate and versioned migration tests.

### LWB-R6-012 — ordinary-quality excludes special-UR rows (2026-09-08)

**RECOVERED source identity and locators.** The verified `..\LW\lwbridge-0.3.1.exe` remains SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. A bounded raw-PE read at `0x00C89E20` shows the `quality` field marker at `0x00C89E59`, immediately followed by `COALESCE(CAST(json_extract(data_json,'$.isSpecialURQuality') AS INTEGER),0) = 0` at `0x00C89E60`; the next `itemKey` marker starts at `0x00C89EAF`. Durable evidence is `evidence/lwbridge-implementation/2026-09-08-r6-map-quality-special-ur-exclusion.txt` and `.json`.

**RECOVERED result at this checkpoint.** The byte adjacency showed the ordinary `quality` branch beside an `isSpecialURQuality = 0` predicate. `LWB-R6-013` below follows the executable control flow and supersedes the broad interpretation here: that exclusion is specifically gated by kind `truck` plus ordinary selector `ur`.

**Historical limit.** At `LWB-R6-012` the numeric ordinary-quality predicate and parameter binding were still unrecovered, so production `quality` remained fail-closed. `LWB-R6-013` now closes that specific gap from instruction/data-flow evidence. This finding remains bounded static recovery, not live-game query proof.

### LWB-R6-013 — exact ordinary-quality mapping and truck UR exclusion (2026-09-08)

**RECOVERED source identity and locators.** The verified `..\LW\lwbridge-0.3.1.exe` remains SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. `tools/inspect_lwbridge_map_quality.py` verifies the hash, the map-search function at preferred-image VA `0x140271864`-`0x140276D7B`, the `quality` raw marker `0x00C89E59` / xref `0x140273E02`, the shared equality helper `0x14027A129`-`0x14027A1E3`, literal UR SQL assembly at `0x140273FCD`/`0x140273FDA`, and the truck+UR guard xref `0x140274093`. Durable evidence is `evidence/lwbridge-implementation/2026-09-08-r6-map-quality-filter.txt` and `.json`.

**RECOVERED result.** The frontend-emitted ordinary selector strings map exactly as follows: `n -> quality = ?` with parameter `1`; `r -> quality = ?` with `2`; `sr -> quality = ?` with `3`; `ssr -> quality = ?` with `4`; `ur -> quality >= 5` with no quality parameter. The shared helper constructs `quality = ?` and binds the integer passed from the selector branch. This resolves the prior UR uncertainty as `>=5`, not equality to `5`.

For ordinary truck `ur` only, the original additionally appends `COALESCE(CAST(json_extract(data_json,'$.isSpecialURQuality') AS INTEGER),0) = 0`, excluding the special-UR/reindeer rows selected separately by `reindeerOnly`. The kind and selector checks are explicit at `0x140274033`-`0x140274078`; railway/dispatch/ghost UR do not receive this additional predicate from that branch. This refines `LWB-R6-012` without deleting its earlier bounded adjacency evidence.

**IMPLEMENTED/OFFLINE-TESTED.** `MapDataQueryContract` now accepts exactly `n/r/sr/ssr/ur` for the four recovered quality-capable kinds (`truck`, `railway`, `dispatch`, `ghost`). `MapDataStore.SearchIndexed` applies equality with parameters `1/2/3/4`, literal `quality >= 5` for UR, and the additional special-quality-zero guard only for truck UR. Deterministic truck rows cover the equality selectors, a quality-5 ordinary UR row, a non-special quality-7 UR row, a quality-6 `isSpecialURQuality=true` row excluded from ordinary truck UR, and a quality-8 row on another server. Page-size-one UR searches prove `{rows,total}` stays consistent at total two for server 7 and excludes the server-8 row. Persisted railway/dispatch/ghost UR rows carry `isSpecialURQuality=true` and remain selected, proving the exclusion is truck-only; railway additionally combines UR with the recovered retained-item predicate. Numeric quality, unknown strings and mismatched kinds stay fail-closed.

**Validation and limits.** Reproduce with `python tools\inspect_lwbridge_map_quality.py ..\LW\lwbridge-0.3.1.exe --json`, `dotnet build src\LWBridge.Desktop\LWBridge.Desktop.csproj --configuration Release`, and `dotnet run --project tests\LWBridge.Desktop.Checks\LWBridge.Desktop.Checks.csproj --configuration Release`. The build succeeds with zero warnings/errors and the deterministic suite reports `ok=true`, `mapContract=true`. Implementation metadata is `evidence/lwbridge-implementation/2026-09-08-r6-map-quality-implementation.json`. The original backend also contains a numeric quality input path that binds equality, but the recovered frontend emits the five string forms above; the rebuild therefore does not broaden its public recovered-UI contract. At R6-013 the completion/plunder clock slice was still open; `LWB-R6-014` below closes the recovered frontend forms. Treasure visibility/lucky behavior and non-`updatedAt` sorts remain separate DB-05 gaps. Static and offline verification are not **LIVE-PROVEN** query/ingestion behavior.

### LWB-R6-014 — precise wall clock, completion status and plunderability (2026-09-09)

**RECOVERED source identity and locators.** The verified reference remains `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. `tools/inspect_lwbridge_map_time_filters.py` verifies `map_search` at preferred-image VA `0x140271864-0x140276D7B`, the shared clock helper `0x14023F1C0-0x14023F244`, and import-table slot `0x1407B94B0 = kernel32.dll!GetSystemTimePreciseAsFileTime`. The helper subtracts FILETIME epoch `116444736000000000` 100-ns ticks and returns `max(FILETIME-epoch,0) // 10000`, i.e. Unix milliseconds. `map_search` samples it once at `0x140272353`; reward-option aggregation samples the same helper at `0x14025A1B5`. Durable evidence is `evidence/lwbridge-implementation/2026-09-09-r6-map-time-filters.json`.

**RECOVERED result.** Truck and railway always append `(arriveTs IS NULL OR arriveTs > now)` before optional plunderability. Their frontend-emitted `plunderableOnly=true` additionally requires non-null `arriveTs` and positive remaining loot, using the recovered fallback `MAX(maxLootCount-robTimes,0)`. The native dispatch/ghost/treasure plunder branch requires positive `completionTime`, positive `plunderAt` falling back to `completionTime`, nonexpired `taskExpireTime`, and remaining steal capacity; the recovered frontend emits `plunderableOnly` only for truck/railway/dispatch, so the rebuild does not expose the wider native branch.

Dispatch/ghost `completionStatus="pending"` means `completionTime IS NULL OR <=0 OR > now`; `"completed"` means `completionTime >0 AND <= now`. Both bind the same sampled Unix-ms clock. R6-014 also closes R6-009's reward-item cutoff gap: reward options use this same clock for their recovered `arriveTs > ?` predicate.

**IMPLEMENTATION boundary.** `MapDataQueryContract` now accepts only frontend-emitted `completionStatus` values (`pending`/`completed`) for dispatch/ghost and `plunderableOnly=true` for truck/railway/dispatch. `MapDataStore.SearchIndexed` applies the exact recovered SQL shapes and one captured time value to count and page queries; truck/railway active-arrival filtering also applies when `plunderableOnly` is absent. `RecoveredWallClock` mirrors the recovered Windows clock source and conversion. Unsupported false/mismatched/unknown forms remain fail-closed through `MAP_QUERY_UNRECOVERED`.

**IMPLEMENTED/OFFLINE-TESTED validation and limits.** Reproduce static evidence with `python tools\inspect_lwbridge_map_time_filters.py ..\LW\lwbridge-0.3.1.exe --json` and frontend kind gates with `python tools\inspect_lwbridge_map_frontend_consumers.py evidence\lwbridge-0.3.1\frontend --json`. `python tools\build_lwbridge_frontend.py --check`, the Release desktop build, `dotnet run --project tests\LWBridge.Desktop.Checks\LWBridge.Desktop.Checks.csproj --configuration Release -- --verify-real-config-unchanged`, and the preference-provider/live-transport/transport-boundary Node checks all pass. The deterministic SQLite cases pin strict `arriveTs > now`, direct/fallback remaining loot, pending/completed equality boundaries, dispatch expiry `> now`, steal capacity and fail-closed unsupported forms. This is **RECOVERED static** plus **IMPLEMENTED/OFFLINE-TESTED**, not **LIVE-PROVEN** ingestion or gameplay behavior. Treasure foreign-radar/viewer/lucky behavior, alternate sorts, public options source/run-context/full response assembly and authoritative summary-server selection remain **UNKNOWN/BLOCKED**.

### LWB-R6-018 — native city-export workbook structure (2026-09-09)

**RECOVERED source identity and locators.** The verified reference is `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. Successful bounded raw-byte reads locate the export option/default cluster at `0x00821EE8-0x0082201F`, the embedded worksheet/package/style region at `0x00C9801B-0x00C990AD`, and the row metadata sequence at `0x00C98375-0x00C983E0`. Durable metadata and the focused excerpt are `evidence/lwbridge-implementation/2026-09-09-r6-map-export-native-structure.json` and `.txt`.

**RECOVERED result.** Native strings establish default sheet name `Cities`, yes/no defaults `Yes`/`No`, default filename prefix `map-cities-`, `.xlsx` extension and `xlsx` filter, dialog label `Excel workbook`, result vocabulary `canceled/path/rowCount`, error code `MAP_EXPORT_FAILED`, and the explicit server-unavailable/invalid-headers errors. The embedded OOXML templates fix the A-L worksheet layout described above, package `[Content_Types].xml`, `_rels/.rels`, `xl/workbook.xml`, `xl/_rels/workbook.xml.rels`, `xl/styles.xml` and `xl/worksheets/sheet1.xml`, and include writer-stage labels `create city export`, `write city export`, `read city export metadata`, `flush city export`. Cell templates exist for ordinary `<v>` values, style-3 datetime values and inline strings.

The raw row metadata sequence is `serverId A B C; D ownerName; E ownerUid; F uuid; G allianceName; level H; health I; protectEndTime J; K marked; updatedAt L; shieldEndTime`. This is sufficient to preserve the original field/column adjacency as evidence, but it is not promoted into a complete runtime mapping: exact A-C semantics, the J-column protect/shield selection and each column's string/numeric/datetime coercion still require control-flow or generated-workbook proof.

**Validation and limits.** No writer code is enabled by this finding and no original workbook was generated. The exact filename timestamp pattern/default directory, picker cancellation branch, internal pagination/full-filter scope, exact row typing and large-identifier reopen behavior remain **UNKNOWN/BLOCKED**. A fresh PE-template xref/disassembly attempt and a later bounded frontend snippet extraction were automatically rejected by the active environment; they are recorded as SB-08/SB-09 and were not retried or rerouted. This checkpoint uses only the successful raw reads obtained before those restrictions and remains **RECOVERED static**, not **LIVE-PROVEN**.

### LWB-R6-019 — staged kind/server publication transaction (2026-09-09)

**RECOVERED source boundary.** The previously recovered map-index schema proves `scan_records` identity `(run_id,kind,server_id,record_key)`, and the verified reference's direct-scan completion path replaces one published kind/server by deleting that scope from `map_records` and copying the matching staged rows. This checkpoint reuses that durable evidence; it does not claim a newly recovered completion state machine.

**IMPLEMENTATION POLICY / OFFLINE-TESTED.** `MapDataStore` now has internal test-only staging/publication helpers. `StageRecordForPublishTest` accepts only an explicit already-derived `recordKey`, preserving the native identity gap. `ReplacePublishedKindFromStagingForTest` performs the recovered delete plus `INSERT ... SELECT` for one `run_id/kind/server_id` inside one SQLite transaction. It deliberately does not decide whether the run is complete or eligible, so unresolved failed-batch/completeness rules cannot be bypassed into production.

Deterministic file-backed tests begin with two published city rows, stage two replacements plus a third row under another run, and verify the selected run replaces exactly the target city/server while the other staged run, another kind on the same server and the same kind on another server remain untouched. The Release build passes with zero warnings/errors; the deterministic backend suite reports `ok=true`, `mapPersistence=true` and `mapContract=true`. Durable metadata is `evidence/lwbridge-implementation/2026-09-09-r6-map-staged-publish.json`.

**Limits.** This is not a production scan worker and is not **LIVE-PROVEN**. Native per-kind `record_key` derivation, exact completion/failed-batch eligibility, block scheduling/ack/retry behavior and session/generation publication ownership remain **UNKNOWN/BLOCKED**. No public command calls the test-only helpers.

### LWB-R6-020 — staged publication rollback reliability (2026-09-09)

**IMPLEMENTATION POLICY.** This checkpoint adds no new original behavior claim. It strengthens the `LWB-R6-019` test-only transaction with the rebuild reliability rule that an exception after the recovered kind/server delete but before the staged-row copy must roll back the entire publication attempt. `ReplacePublishedKindFromStagingForTest` therefore exposes an internal deterministic callback only for failure injection; production scan eligibility, failure classification and retry behavior remain unrecovered.

**OFFLINE-TESTED result.** A deterministic case seeds two published city rows for one server plus one staged replacement, injects an exception after the delete statement and before `INSERT ... SELECT`, then verifies both original rows are still present and the staged row is absent. The Release build completes with zero warnings/errors and the full deterministic backend suite returns `ok=true`, `mapPersistence=true` and `mapContract=true`. Durable metadata is `evidence/lwbridge-implementation/2026-09-09-r6-map-publish-rollback.json`.

**Limits.** This establishes atomic rebuild behavior only. It does not recover the native completion gate, retry policy, error code, session/generation ownership or per-kind `record_key` derivation, and no public command invokes the test-only path.

### LWB-R6-028 — restart-safe scan block checkpoint infrastructure (2026-09-09)

**RECOVERED boundary reused.** The verified LWBridge reference remains `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. The already recovered persistent map-index schema defines `scan_blocks` with columns `run_id`, `block_index`, `payload_json`, `status`, `attempts`, `error`, `updated_at`, primary key `(run_id,block_index)`, and a foreign key to `scan_runs(id) ON DELETE CASCADE`. This checkpoint reuses that schema finding; it does not claim recovery of the original worker state machine.

**IMPLEMENTATION POLICY / OFFLINE-TESTED.** `MapDataStore.UpsertScanBlockCheckpointForTest` and `ReadScanBlockCheckpointsForTest` now exercise restart/checkpoint persistence over exactly that recovered identity. The helper is internal/test-only and deliberately does not interpret status values, decide retry eligibility, schedule blocks, acknowledge native capture or publish staged rows. Deterministic coverage proves a second write to the same run/block replaces the prior checkpoint rather than creating a duplicate, the latest payload/status/attempt/error values survive a file-backed database reopen, and `ClearServer` removes the owning `scan_run` so SQLite's recovered cascade removes its block checkpoints.

**Validation and integration value.** `git diff --check` passes. The Release desktop build succeeds with zero warnings/errors, and `LWBridge.Desktop.Checks` returns `ok=true` with `profileRouting`, `persistence`, `requestLifetime`, `mapPersistence` and `mapContract` all true. This gives a durable storage primitive needed by eventual interrupted-scan resume/reconciliation without weakening the production scan gate. It enables no live/public Map Data operation.

**Limits and current research state.** Original block status vocabulary/transitions, scheduling/concurrency ownership, acknowledgement timing, retry counts/eligibility, inflight treatment after bridge loss, completion/publication eligibility and per-kind native `record_key` derivation remain **UNKNOWN/BLOCKED**. The independent runtime-target check during this checkpoint found no running LastWar/LWBridge process, so no supported active-scan observation was available. SB-25 through SB-27 in the escalation register record three automatic-review rejections from unrelated combined research reads; none was retried or rerouted and none contributed a technical finding. Durable metadata is `evidence/lwbridge-implementation/2026-09-09-r6-scan-block-checkpoint-infrastructure.json`.

### LWB-R6-029 — current-runtime readable search for native record identity (2026-09-09)

**Current source identity.** The maintained official snapshot is `evidence/official-runtime/2026-09-08-official-runtime.json`, build `1.0.361 / 1078`. A direct current-file check locates `Assembly-CSharp.rdl` at `%LOCALAPPDATA%\FunFly\Last War-Survival Game\Game\LastWar_Data\Assemblies\Assembly-CSharp.rdl`, size 15,640,576, SHA-256 `871efe06819fbac438413eb96b7df8193d0be56094f3a44d5ff141e6219adcbd`, matching the snapshot and its recovered `RG` wrapper identity. The active LocalLow `lwScripts\LWScripts.data` is size 42,030,332, SHA-256 `bc10cda45528ef82adb1439b0cac5b08265c7a11770430e7484a27a7031c485c`, also matching the snapshot; the existing launcher log labels that active script format `Lenc` and version 12.

**Attempt/result.** A tracked-text search for `record_key`, `scanRunId`, `scan_records` and `XluaBridgeMapScan` produced matches only in this LWBridge recovery document, with none in the official-runtime snapshot or architecture. A second search of the official-runtime snapshot/architecture for selected known row vocabulary (`ownerUid`, `ownerName`, `monsterNameKey`, `resourceNameKey`, `treasureType`, `suppliesType`, `marchUuid`, `pointIndex`, `protectEndTime`, `shieldEndTime`) returned zero matches. This is useful negative evidence: the maintained official snapshot is an artifact/version inventory, not decoded map-payload evidence, so it cannot establish per-kind `record_key` derivation by itself.

**Limits and next method.** No record-key formula or normalization rule is recovered here, and production ingestion remains fail-closed. The next bounded current-contract sources are the identified build-1078 `Assembly-CSharp.rdl` and active `LWScripts.data`, using an existing repository-supported decoder/loader if a permitted path is available, or a future supported live capture target. SB-28 records an automatic-review rejection of one combined repository tooling/reference search; it was not retried or rerouted and contributes no decoder finding. Do not substitute frontend render keys for the missing native identity. Durable metadata is `evidence/lwbridge-implementation/2026-09-09-r6-record-key-current-runtime-search.json`.

### LWB-R6-021 — persisted-only counts and no-alliance option slice (2026-09-09)

**Recovered boundary.** `LWB-R6-016` proves the exact frontend count keys `city`, `resource`, `monster`, `truck`, `railway`, `dispatch`, `ghost` and `treasure`; `LWB-R6-005` proves the city no-alliance predicate `(alliance_name IS NULL OR alliance_name = '')`. Neither finding proves the original public `map_data_options` source selector or its exact count/noAllianceCount producer SQL.

**IMPLEMENTATION POLICY / OFFLINE-TESTED.** The existing `ReadPersistedOptionAggregatesAtForTest` helper now counts persisted `map_records` for all eight recovered keys and computes a persisted-source no-alliance value with the recovered null/empty predicate. Missing kinds are explicitly zero. These reads use the same deferred SQLite snapshot as alliances, names, dispatch levels, treasure types, reward items and scan progress, so the offline aggregate object is internally coherent while another connection could publish.

Deterministic tests verify server 120 counts `4/3/1/2/1/4/0/4` in the recovered key order, `noAllianceCount=2` for one empty and one NULL alliance, and complete server isolation for server 121. Release build and the deterministic backend suite pass with zero warnings/errors and `ok=true`. Durable metadata is `evidence/lwbridge-implementation/2026-09-09-r6-map-persisted-counts.json`.

**Limits.** This remains test-only persisted-source infrastructure. It does not authorize public `map_data_options`, infer active-run/staging selection, prove native response assembly, or establish authoritative `map_summary` server selection.

### LWB-R6-022 — treasure option key prefixes and xref exhaustion (2026-09-09)

**RECOVERED source identity and locators.** The verified `..\LW\lwbridge-0.3.1.exe` remains SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. The Map Data option-result cluster at raw offsets `0x00C83AE5-0x00C83B61` contains `key`, `iconPath`, `treasure:`, `treasureNameKey`, `count` and `supplies:`. Exact prefix offsets are `treasure:` at `0x00C83B21` and `supplies:` at `0x00C83B59`. A second `treasure:` at `0x00C883D1` is adjacent to season-supply treasure-claim error/query text and is excluded from this option-key interpretation.

**Attempted methods and result.** `tools/inspect_lwbridge_map_treasure_option_keys.py` hash-locks the reference, verifies the cluster offsets, inventories all prefix occurrences, scans executable sections with Capstone 5.0.6 for direct RIP-relative references, and searches the PE bytes with pefile 2024.8.26 for absolute-pointer metadata slots before checking code references to any such slots. Neither option-cluster prefix has a direct code xref or an absolute-pointer metadata slot. This is a useful negative result because those ordinary xref routes need not be repeated.

**Historical limit, superseded by `LWB-R6-023`.** Prefix adjacency alone did not prove whether `treasureTypes[].key` was `treasure:<decimal treasureType>`, `supplies:<decimal suppliesType>` or another formatting/encoding. The direct-string/pointer routes above remain useful negative evidence, but the suffix-format gap is closed by bounded data-flow recovery below. Durable historical evidence remains `evidence/lwbridge-implementation/2026-09-09-r6-map-treasure-option-key-prefixes.json`.

### LWB-R6-023 — exact treasure option key formatting (2026-09-09)

**RECOVERED source identity and locators.** The source remains the verified `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. Rather than repeat the denied whole-executable descriptor-xref route, the enhanced `tools/inspect_lwbridge_map_treasure_option_keys.py` uses pefile 2024.8.26 and Capstone 5.0.6 to disassemble only the already identified option-assembly function at preferred-image VA `0x140257AA4-0x14025B436`. Both prefixes are length-prefixed descriptors: raw `0x00C83B20` is byte `9` followed by `treasure:` and raw `0x00C83B58` is byte `9` followed by `supplies:`. The function references those descriptors directly at `0x140259E4B` and `0x140259C11` respectively. The treasure branch's adjacent 12-byte field literal at VA `0x140838A85` is verified as `treasureType`.

**RECOVERED data flow and result.** The recovered R6-009 SQL returns normalized `supplies_type` first and `treasure_type` second, with exactly one positive for every emitted option. The native row path loads those first two numeric values from `[rsp+0x50]` and `[rsp+0x58]`, copies them to `[rsp+0x200]` and `[rsp+0x250]`, then tests the first value at `0x140259B9D`. A positive first value enters the supplies branch, which formats `[rsp+0x200]` after the `supplies:` descriptor and emits the `suppliesType` field. Otherwise the treasure branch formats `[rsp+0x250]` after the `treasure:` descriptor and emits the verified `treasureType` field. Both paths use formatter `0x1400397B0`; its numeric helper `0x1400394C0-0x14003968E` performs signed base-10 integer conversion using decimal divisions/digit-table output. Because the SQL emission guard requires the selected dimension to be positive, the resulting option keys are exactly `supplies:<positive decimal suppliesType>` and `treasure:<positive decimal treasureType>`.

**IMPLEMENTED/OFFLINE-TESTED.** The existing persisted-only `ReadPersistedOptionAggregatesAtForTest` helper now carries the recovered `key` alongside each treasure option. It uses invariant decimal rendering and the recovered positive-dimension branch; deterministic coverage uses multi-digit `treasureType=12` and `suppliesType=4`, expecting `treasure:12` and `supplies:4`. `git diff --check` and the hash-locked inspector pass; the Release build completes with zero warnings/errors and the deterministic backend suite returns `ok=true`, `mapPersistence=true` and `mapContract=true`. This extends the R6-015 test kernel only. Public `map_data_options` remains fail-closed because original persisted-vs-active-run source selection, exact native count/no-alliance/full response assembly and other source-dependent semantics are still unrecovered. Detailed native evidence is `evidence/lwbridge-implementation/2026-09-09-r6-map-treasure-option-descriptors.json`; compact implementation/validation metadata is `evidence/lwbridge-implementation/2026-09-09-r6-map-treasure-option-key-implementation.json`.

### LWB-R6-025 — native map summary data flow and fail-closed boundary (2026-09-09)

**RECOVERED source identity and locators.** The reference remains `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. The hash-locked `tools/inspect_lwbridge_map_scan.py` identifies the `map_summary` future at preferred-image VA `0x140151509-0x140152388`. It calls the shared map-scan state producer `0x1400F7F47` at `0x140151A26`, looks up the eight-byte `serverId` field at `0x140151AFD`, and calls the native map-count helper `0x140271056` at `0x140151BC0`. The successful response path allocates the keys `serverId` at `0x140151C59`, `counts` at `0x140151CD6`, and `scanState` at `0x140151D74`.

**RECOVERED data flow.** The successful shared scan-state result is copied into an owned local value and later serialized under `scanState`. The `serverId` lookup result is held in `rsi`; immediately before the count call it is passed in `r8`, and the count helper prologue moves that value into its server parameter. The response assembly later serializes the same `rsi` value under `serverId`. The count helper independently proves the R6-024 source abstraction at control-flow level: if either optional run pointer or length is absent it chooses `map_records` with `server_id=?1`; when both are present it chooses `scan_records` with `server_id=?1 AND run_id=?2`. `map_summary` receives that pointer/length from an internal value produced just after `scanState.serverId` extraction. Therefore the original successful envelope is exactly `{serverId, counts, scanState}`, and the server used for counts is the server exposed by the shared scan state. The exact internal rule that supplies or omits the run scope is still **UNKNOWN/BLOCKED**.

**IMPLEMENTATION POLICY / OFFLINE-TESTED boundary.** The production rebuild previously returned a synthetic successful object with `serverId=0`, empty counts, synthetic scan state, and rebuild-only `available`/`unavailableReason` fields. Because the original run-scope selector and production scan-state integration are still unrecovered, the command now rejects with the existing `MAP_INDEX_UNAVAILABLE` gate. The isolated preview fixture retains explicit synthetic data for visual testing. The Release build succeeds with zero warnings/errors and the deterministic backend suite returns `ok=true`, including `mapPersistence=true` and `mapContract=true`. This checkpoint enables no live/public Map Data operation.

**Limits and restrictions.** SB-17 records the automatic-review rejection of fresh bounded dumps for the deeper field/scope helpers; SB-18 records a later rejected range-read of already generated disassembly scratch text; SB-19 records a separate rejected combined validation/cleanup command that was replaced by narrower permitted checks. None was retried or rerouted. The successful bounded function dumps above are sufficient for the stated envelope/server/count flow, but not for the semantic condition inside helper `0x140344F1D`, its run identity source, complete unavailable/error behavior, or live current-client proof. Durable details are in `evidence/lwbridge-implementation/2026-09-09-r6-map-summary-native-flow.json`.

### LWB-R6-030 — native options/summary run-scope selector (2026-09-09)

**RECOVERED source identity and provenance.** The reference remains `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`; the hash was reverified before this continuation. A previously successful bounded static-analysis session on that same reference had already traced helper `0x140344F1D-0x140345030` and the `map_data_options` adapter around `0x14033FEEA`. This checkpoint promotes that returned trace into durable repository evidence rather than rerunning the denied SB-17/SB-18 helper-disassembly paths. Exact preferred-image VA locators and bounded reproduction steps are in `evidence/lwbridge-implementation/2026-09-09-r6-map-option-source-selector.json`.

**RECOVERED decision and run identity.** The selector first requires a positive requested server, then reads `isReading` at `0x140344F46` and accepts only true, reads scan-state `serverId` at `0x140344F75` and requires equality with the requested server, then reads `scanRunId` at `0x140344F99`, requires the native string type and a length greater than zero, and only then reaches the nonempty-run-scope path at `0x140344FE9`. Any failed condition reaches the no-scope return at `0x140344FCE`. The caller at `0x14033FEEA` obtains this optional scope before calling the option routine at `0x14033FF69`. Combined with R6-024, the original decision is now closed: a matching active scan uses `scan_records WHERE server_id=?1 AND run_id=?2` with the exact scan-state `scanRunId`; every other case uses `map_records WHERE server_id=?1`. R6-025 had already identified `0x140344F1D` as the unknown optional-scope producer for summary counts, so this also closes that helper's condition and run identity there.

**IMPLEMENTED/OFFLINE-TESTED boundary.** `MapDataStore.SelectOptionSourceForTest` mirrors only this recovered decision. Deterministic cases cover `isReading=false`, server mismatch, null/empty `scanRunId`, matching nonempty `scanRunId`, and the native raw-length behavior where a whitespace-only string is still nonempty because no trimming is evidenced. The production `map_data_options` and `map_summary` handlers remain unchanged and continue to reject with `MAP_INDEX_UNAVAILABLE`.

**Limits and next method.** Exact native `noAllianceCount` production, complete option-family response assembly/order/deduplication, exact public `scanProgress` serialization/empty states and complete summary unavailable/error behavior remain **UNKNOWN/BLOCKED**. SB-29 through SB-31 record three automatic-review rejections from combined/session-read commands in this continuation; none was retried or rerouted and none contributes technical evidence. ESC-003 is narrowed back to `NEEDS_INFORMATION` for those remaining response-assembly questions while regular AI continues saved/native serializer-edge recovery. This is a research/test-only checkpoint and enables no live/public Map Data operation.

### LWB-R6-031 — native option response/no-alliance/scan-progress selection (2026-09-09)

**RECOVERED source identity.** The reference remains `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. This checkpoint used only bounded reads of the already identified option function `0x140257AA4-0x14025B436`, its small alliance row/text decoders, and the two scan-run helpers. It did not replay any historical denied whole-executable xref operation. Durable locators and reproduction commands are in `evidence/lwbridge-implementation/2026-09-09-r6-map-option-response-assembly.json`.

**RECOVERED `noAllianceCount`.** R6-009's native alliance SQL returns `alliance_name,COUNT(*)` grouped by name. Row decoder `0x140287B25-0x140287C59` reads column 0 as nullable text and column 1 as the count. Its nullable-text path canonicalizes SQL NULL to the same zero-length representation tested by the caller. The option loop initializes a zero accumulator at `0x140258728`; after row decode it loads the name length and grouped count, and `0x14025894A-0x14025894D` branches on zero length. Nonempty names build `{name,count}` and enter `alliances[]`; zero-length names reach `0x140258AE4`, which adds the grouped count to the accumulator. `0x14025ACBC` later loads that accumulator and inserts it under `noAllianceCount`. Therefore NULL and empty alliance groups are both omitted from `alliances[]` and summed into `noAllianceCount`; no separate no-alliance SQL query is used by the original path.

**RECOVERED top-level assembly order.** A bounded RIP-xref pass inside the same option function maps the contiguous result keys to insertion sites in this exact sequence: `serverId` (`0x14025A9C0`), `counts` (`0x14025AA1D`), `alliances` (`0x14025AB2B`), `names` (`0x14025ABA7`), `dispatchLevels` (`0x14025AC23`), `noAllianceCount` (`0x14025AC9F`), `rewardItems` (`0x14025AD09`), `treasureTypes` (`0x14025AD85`), `scanProgress` (`0x14025AE01`). The `counts` insertion immediately follows the already recovered source/scope count-helper call at `0x14025AA62`.

**RECOVERED scan-progress record selection.** The option prologue tests whether the optional run pointer and length are present. On the no-run/persisted branch, `0x14025AE32` calls helper `0x14025EAEC`; that helper directly references the R6-009 SQL selecting the newest non-`discarded` `scan_runs` row for `server_id=?1` and the diagnostic `read latest scan summary`. On the active-run branch, `0x14025B06E-0x14025B079` calls helper `0x140276D7B` with the exact run pointer/length preserved from R6-030; that helper directly references `SELECT id,server_id,selected_types,status,total_blocks,completed_blocks,failed_blocks,created_at,updated_at,error FROM scan_runs WHERE id=?1`. Thus public options use newest non-discarded server progress when persisted and the exact active `scanRunId` when staging.

**IMPLEMENTED/OFFLINE-TESTED boundary.** The existing persisted-only option kernel now mirrors the recovered alliance/no-alliance behavior: NULL is normalized to an empty name, zero-length alliance groups are summed into `noAllianceCount`, only nonempty names enter `alliances[]`, and the former separate implementation-policy no-alliance `COUNT(*)` query is removed. Deterministic tests verify the two no-alliance groups contribute `2` while only `Alpha` remains in the alliance list. Production `map_data_options` is still unchanged and fail-closed.

**Remaining gate.** The common post-query conversion after `0x14025B07E` still needs field-level recovery to prove the exact `scanProgress` JSON value, including its complete fields and precise missing/null/error representation. That is now the principal ESC-003 options-assembly gap. Complete summary unavailable/error behavior and live native ingestion remain separate. This checkpoint enables no public UI operation.

### LWB-R6-032 — scan-progress serializer edge and restricted row mapper (2026-09-09)

**RECOVERED source identity and post-query edge.** The reference remains `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. Bounded reads of the already identified option tail show that the active-run result returns at `0x14025B07E` and, on the ordinary value path, reaches generic tagged-value converter `0x1402AD389` through the call at `0x14025B155`. A bounded read of `0x1402AD389-0x1402AD510` confirms that helper dispatches by a generic value tag and handles multiple value classes; it is not the scan-run-specific field mapper. Therefore the exact public field mapping occurs earlier in the query result path and must not be inferred from the ten SQL columns alone.

**RECOVERED persisted row-mapper locator.** In latest-scan helper `0x14025EAEC-0x14025ED53`, the query setup stores closure/vtable address `0x140C81A30` immediately before issuing the exact R6-031 ten-column `scan_runs` query. The immutable vtable data at raw `0x00C80E30` contains qwords `0`, `8`, `8`, `0x140294188`, identifying `0x140294188` as the persisted query's row-mapper callback. This narrows the unresolved serializer to one concrete callback. The nearby raw field literals `totalBlocks` (`0x00C84B48`), `completedBlocks` (`0x00C84B70`), `failedBlocks` (`0x00C84B98`) and `updatedAt` (`0x00C84BD8`) are recorded only as adjacency evidence; without callback control flow they are not promoted to proven `scanProgress` membership/order.

**RECOVERED metadata separation.** Printable metadata around raw `0x008261A8-0x0082672D` and `0x00C99D1F-0x00C99FC8` belongs to the broader `src\services\map_scan.rs` live scan-state/event contract and includes fields such as `scanRunId`, read/unread/inflight counts, scan rate, native pending/dropped records and capture readiness. The public options frontend only consumes `scanProgress.serverId/id/status/createdAt/updatedAt/error`. The broader live-state vocabulary is therefore not used as a substitute for the still-unrecovered persisted `scanProgress` serializer.

**Restrictions and remaining gate.** SB-32 through SB-35 record automatic-review rejections for a helper-bound lookup, direct bounded disassembly of `0x140294188`, and two raw-region inspection attempts. None was repeated or rerouted and none contributes field-mapping evidence. Saved evidence, bounded option/helper reads, immutable string metadata and the frontend consumer were all checked; no game/launcher process was running, so an active-scan observation target was unavailable. Exact JSON keys/order, `selected_types` conversion, optional progress-count fields, absent-row/null behavior and query/conversion error propagation remain **UNKNOWN/BLOCKED**. `map_data_options` remains fail-closed. Durable evidence: `evidence/lwbridge-implementation/2026-09-09-r6-scan-progress-serializer-edge.json`.

### LWB-R6-033 — native `map_summary` failure propagation (2026-09-09)

**RECOVERED source identity and locators.** The reference remains `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. The hash-locked bounded inspector reproduces `map_summary` at preferred-image VA `0x140151509-0x140152388` and the shared scan-state producer at `0x1400F7F47-0x1400F9333`. After the producer call at `0x140151A26`, the ready-result discriminant at `0x140151AC8-0x140151ACE` sends the non-success path to `0x140151B55-0x140151BA5`. That branch preserves the returned error payload and joins the command's common completion path at `0x140151ED4`.

**RECOVERED propagation behavior.** A shared scan-state failure bypasses the `serverId` lookup at `0x140151B0D`, optional run-scope producer at `0x140151B26`, count helper at `0x140151BC0` and every successful response-key insertion. The count helper has the same command-level rule: after its result at `0x140151BC5`, the non-success branch reaches `0x140151E5A`, preserves the count error and joins the same completion path without assembling `{serverId,counts,scanState}`. Therefore the original successful envelope is emitted only after both prerequisites succeed; internal scan-state/count failure is propagated rather than replaced with an empty or zero-filled summary.

**Boundary and implementation impact.** This closes the orchestration-level unavailable/error question but does not assign exact original error codes to shared-producer conditions. The verified binary contains `SERVER_UNAVAILABLE` / `current server id unavailable` and `map scan state is unavailable` vocabulary, but the bounded producer body does not directly attribute those strings to individual branches, so no condition mapping is guessed from proximity. The rebuild already rejects public `map_summary` with `MAP_INDEX_UNAVAILABLE`; that fail-closed gate remains because the original shared scan-state/current-server producer is not integrated. No production code changes and no public capability are introduced. No LastWar/LWBridge process was running, so this is **RECOVERED static**, not **LIVE-PROVEN**. Durable evidence: `evidence/lwbridge-implementation/2026-09-09-r6-map-summary-error-propagation.json`. SB-36 through SB-40 record resumed-session automatic-review rejections that contributed no technical evidence.

### LWB-R6-034 — current-client current-server and in-world contracts (2026-09-09)

**RECOVERED current-client source identity and tooling.** The current official managed artifact remains build `1.0.361 / 1078` `Assembly-CSharp.rdl`, size `15,640,576`, SHA-256 `871efe06819fbac438413eb96b7df8193d0be56094f3a44d5ff141e6219adcbd`. Its RGMD root is at file offset `0x9a88a8` with `5,828` TypeDefs and `54,474` MethodDefs. The historical read-only RG/RGMD parser from commit `0d23432` was recovered into this repository as `tools/inspect_lastwar_rdl_metadata.py`; it parses preserved managed metadata/CIL without loading or rewriting the game. This closes R6-029's missing-decoder setup gap for this RDL source.

**RECOVERED current-server contract.** Current build method row `3426`, RVA `0x4b3d8`, is `int32 DCPlayer::GetCurServerId()` and xLua wrapper row `22900`, RVA `0x2102ac`, exposes it as `DCPlayerWrap::_m_GetCurServerId(native int L)`. Its CIL tests field token `0x679EE982`: when the signed value is nonnegative, it returns that field; otherwise it returns field token `0x679EE9A2`. Independent method row `3424`, `DCPlayer::GetCrossServerId()` at RVA `0x4b3ab`, returns exactly token `0x679EE982`, proving the first branch is the cross-server value. The fallback token remains deliberately unnamed because the exact `GetSelfServerId` follow-up output was unavailable after SB-45; no field name is inferred.

**RECOVERED in-world contract and LWBridge cross-check.** Current build method row `9116`, RVA `0xe1456`, is static `bool SceneManager::IsInWorld()` and xLua wrapper row `27311`, RVA `0x29a4f4`, exposes it as `SceneManagerWrap::_m_IsInWorld_xlua_st_(native int L)`. Its body compares one static scene-state field with integer `2` and returns the equality result; the modified field token is intentionally left unnamed. R6-025/R6-033 independently show the verified LWBridge shared state path carries `getCurrentServerId`/`isInWorld` markers before `map_summary` consumes `scanState.serverId`. This current-client evidence proves matching managed callable contracts exist, but it does **not** prove LWBridge invokes these exact methods directly or recover the `DCPlayer` instance/access path, bridge transport, scan-state lifecycle, or original condition-to-error mapping.

**Boundary, restrictions and next method.** Production `map_scan_status`/`map_summary` stay fail-closed; the rebuild's current scan status is an explicit unavailable stub and substituting it or an unproven managed access path would invent behavior. SB-41 through SB-45 record automatic-review rejections from a combined binary/string scan, an inaccessible targeted xref session result, a broad decoder search, a broad `DCPlayer` type result and the `GetSelfServerId` result. None is accepted as technical evidence or rerouted. The permitted RDL method/member path produced the accepted finding, so no Daybreak request is warranted. The current RDL hash, Python compile, three exact method reproductions, evidence JSON, `git diff --check`, recovered frontend hash check, Release build and deterministic backend suite all pass; the installed diagnostic reports no running game/launcher, so this remains static rather than live proof. Next regular-AI work is to use this decoder to identify the authoritative `DCPlayer` access/state owner and a bridge-facing producer, while separately continuing source-attributed LWBridge error mapping. Durable evidence: `evidence/lwbridge-implementation/2026-09-09-r6-current-server-world-contract.json`.

### LWB-R6-035 — current-client `DCPlayer` owner/access chain (2026-09-09)

**RECOVERED managed ownership.** The source remains the exact build `1.0.361 / 1078` `Assembly-CSharp.rdl`, SHA-256 `871efe06819fbac438413eb96b7df8193d0be56094f3a44d5ff141e6219adcbd`. The R6-034 read-only inspector now adds `--references-to-type`, which reproduces field row `2447` as `CustomDataManager GameEntry::<Data>k__BackingField` and field row `3534` as `DCPlayer CustomDataManager::<Player>k__BackingField`. `GameEntry::get_Data()` is method row `3736`, RVA `0x502ee`, and its body is exactly `ldsfld token(0x679E83C2); ret`, proving `GameEntry.Data` is static. `CustomDataManager::get_Player()` is method row `5419`, RVA `0xa6fb2`, and reads instance field token `0x679E3B22`, proving the data manager owns the `DCPlayer` reference. Combined with R6-034 method row `3426`, the current managed access sequence is `GameEntry.Data -> CustomDataManager.Player -> DCPlayer.GetCurServerId()`.

**RECOVERED xLua exposure.** Generated wrappers expose every hop: `GameEntryWrap::_g_get_Data` is row `24694` / RVA `0x24b4d0`; `CustomDataManagerWrap::_g_get_Player` is row `22825` / RVA `0x20dea4`; and `DCPlayerWrap::_m_GetCurServerId` is row `22900` / RVA `0x2102ac`. This establishes a current-client xLua-visible property/method sequence without guessing the exact LWBridge script spelling. `GameEntry::Init()` also executes `newobj token(0xA79D3F62)` at IL `0x005e`; `CustomDataManagerWrap::__CreateInstance` independently uses the same constructor token. The protected call immediately after the `GameEntry.Init` construction remains unresolved, so this finding does not claim exact assignment timing or readiness from that sequence alone.

**Boundary and implementation impact.** This closes R6-034's managed state-owner/access-path subproblem, but it does not establish how LWBridge obtains or invokes that sequence, when `Data`/`Player` are guaranteed non-null in the bridge lifecycle, or which original error is returned when those prerequisites are unavailable. No production host/backend code changes and no public command are enabled. `map_summary` and `map_scan_status` remain fail-closed until the bridge-facing producer/readiness/error contract is source-attributed or live-proven. No Daybreak request is warranted because the permitted current-RDL route produced positive evidence. Durable evidence: `evidence/lwbridge-implementation/2026-09-09-r6-current-server-access-chain.json`.

**Validation after interrupted-session resume.** `python -m py_compile tools\inspect_lastwar_rdl_metadata.py`, evidence-JSON parsing/assertions, `python tools\build_lwbridge_frontend.py --check`, the Release desktop build and the deterministic backend suite with `--verify-real-config-unchanged` all pass. The installed diagnostic reports a valid x64 client and no running game/launcher. SB-46 and SB-47 record two rejected combined read/validation commands; neither supplied technical evidence and neither was rerouted. The exact installed-RDL reproduction commands were therefore not replayed during the resumed validation; the R6-035 source facts remain the already captured current-build static evidence, not live proof.

### LWB-R6-036 — shared scan-state producer bridge request/normalization slice (2026-09-09)

**RECOVERED bridge request order.** The source remains verified `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. The already identified shared producer is preferred-image VA `0x1400F7F47-0x1400F9333`. Its first bridge request installs command `getWorldMapState` at `0x1400F7FBE`, length `16`, a `5,000 ms` timeout at `0x1400F7FD7`, then calls common dispatcher `0x1400E4FAD` at `0x1400F7FFF`. Its next bridge request installs `getCurrentServerId` at `0x1400F80B9`, length `18`, the same `5,000 ms` timeout at `0x1400F80D2`, and calls the same dispatcher at `0x1400F80F7`. This closes the native producer's request order without asserting how either command is implemented inside the current client.

**RECOVERED normalization slice.** The `getCurrentServerId` result is queried for `serverId` at `0x1400F81BE-0x1400F81D3`. The saved world-map-state value is queried for `isInWorld` at `0x1400F8225-0x1400F8232`; missing or non-boolean values branch to `0x1400F8325-0x1400F8333`, which normalizes the emitted value to false. `homeServerId` is queried at `0x1400F835F-0x1400F8374` and inserted only when positive. `seasonServerIds` and `truckMatchServerIds` are decoded from array values, accept only numeric IDs `1..99999`, deduplicate them, and sort when multiple values remain. The same producer contains `serverIdSource` with `live` and `none` values, an `isReading` lookup, and the literal `current server changed during map scan`; exact complete source-selection and named public error mapping remain deliberately unassigned.

**Current-client boundary and restrictions.** R6-035 already proves `GameEntry.Data -> CustomDataManager.Player -> DCPlayer.GetCurServerId()` and xLua exposure, but exact current-RDL member-name searches for `WorldMapState` and `CurrentServerId` return no matching managed member names. That negative name search does not prove absence of an equivalent implementation, so this checkpoint does not equate the LWBridge command `getCurrentServerId` with the R6-035 chain. SB-48 records a rejected filtered producer-dump request; it was not replayed or rerouted and supplies no evidence. Production `map_scan_status`/`map_summary` remain fail-closed because dispatcher failure/timeout mapping, `SERVER_UNAVAILABLE`/`map scan state is unavailable` conditions, complete `serverIdSource` selection and live correlation are still UNKNOWN/BLOCKED. Durable evidence: `evidence/lwbridge-implementation/2026-09-09-r6-shared-scan-state-producer.json`.

### LWB-R6-037 — current-server unavailable mapping (2026-09-09)

**RECOVERED command-wrapper branch.** In the same verified LWBridge executable, exact target `getCurrentServerId` has two code references: the R6-036 shared producer at `0x1400F80B9` and a `src\commands\map.rs` function at `0x1400DC995-0x1400DCBF4`. The latter installs the 18-byte command at `0x1400DC9E8` and invokes the same bridge dispatcher `0x1400E4FAD` at `0x1400DCA22`. On the result branch selected by word `0x8000000000000000`, it asks helper `0x14023195E` for `serverId` at `0x1400DCABA-0x1400DCAC7`, then tests the signed result at `0x1400DCACC-0x1400DCACF`. A positive value is retained. A zero or negative value enters `0x1400DCB29`, loads exact code `SERVER_UNAVAILABLE` (length 18) and message `current server id unavailable` (length 29), and constructs that error through `0x1404170BA` at `0x1400DCB51`. Other dispatcher/result variants bypass this custom branch, so their transport error identities remain unassigned rather than being renamed to `SERVER_UNAVAILABLE`.

**Remaining bridge boundary.** `getWorldMapState` still has only the shared-producer code reference. A non-xref raw check locates `map scan state is unavailable` at file offset `0xC9A2D8` inside the map-scan status/error vocabulary, but its exact code reference/trigger remains UNKNOWN/BLOCKED. The embedded `bridge-scripts.dat` remains the verified encrypted/high-entropy LWBP v2 package; loader strings expose `package decrypt failed`, key-envelope failures and `__XluaBridge*` runtime globals, but the reference executable has no raw `DCPlayer`, `GetCurServerId`, `SceneManager`, `IsInWorld`, `GameEntry` or `CustomDataManager` method-name match. R6-035 therefore remains a separate current-client fact until the encrypted bridge handler or live authoritative correlation proves the link. SB-50/51 record two rejected exploratory reads that were not replayed/rerouted. Production summary/status remain fail-closed. Durable evidence: `evidence/lwbridge-implementation/2026-09-09-r6-current-server-error-mapping.json`.

### LWB-R6-039 — bridge package-key/runtime-material boundary (2026-09-09)

**RECOVERED source identity and host response fields.** The source remains verified `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`, preferred image base `0x140000000`. Host function `0x14023C7EC-0x14023CB51` extracts `authorizationTicket` at `0x14023C802` (length `0x13`) and `authorizationTicketExpiresAt` at `0x14023C81F` (length `0x1C`) through helper `0x14023F10C`; the same function extracts `packageKeyEnvelope` at `0x14023C95E` (length `0x12`) and `packageKeyEnvelopeExpiresAt` at `0x14023C97B` (length `0x1B`) through that same helper. It has two direct `KEY_ENVELOPE_INVALID` branches at `0x14023CA27` and `0x14023CA70`, each passing the exact 20-byte code to helper `0x1402361FE`.

**RECOVERED runtime-material path family.** Separate host function `0x14023CC15-0x14023D5B8` constructs four sibling filenames through common path helper `0x1405C6750`: `authorization.challenge` at `0x14023D0F2`, `authorization.ticket` at `0x14023D111`, `package-key.envelope` at `0x14023D130`, and `build.manifest` at `0x14023D153`. This establishes an original runtime key-envelope artifact next to the authorization ticket/build manifest. It does **not** yet prove the direct dataflow that writes the parsed `packageKeyEnvelope` value into that file, so the contents/serialization are not inferred.

**RECOVERED proxy/package boundary.** Both embedded proxies, SHA-256 `481c636b8d0fa9bf4b8f88cba77145054b33e3701ffa42737567b211859ac400` (secure) and `c9a88ec449cc6a9929fb2f2800ebd443f9b9dcdac849e3d3e8da94281dea9794` (plain), contain `LWBRIDGE_PROFILE_RUNTIME_ROOT`, `\bridge-runtime\package-key.envelope`, `\bridge-runtime\build.manifest`, `LWKE1`, key-agreement/decrypt failures, package-decrypt/integrity/build failures and `\LastWar-xLua-Bridge\bridge-scripts.dat`. The embedded script package itself is 1,172,723 bytes, SHA-256 `a1e23419c25c76bee16942922a643381e0d38a857902569d927f22ef02c99c8d`, `LWBP` version 2, build ID `9BupJXpEgm34lybhNhbbcQ`, and contains no `LWKE1` marker. Therefore the envelope marker is separate runtime material, not part of the embedded package bytes.

**Validation, limits and impact.** Reproduce with `python tools\inspect_lwbridge_bridge_runtime_material.py ..\LW\lwbridge-0.3.1.exe`; the inspector is hash-locked and read-only. `python -m py_compile tools\inspect_lwbridge_bridge_runtime_material.py` also passes. No LastWar/LWBridge process was running during this checkpoint, so there is no live correlation. The direct parsed-envelope-to-file content flow, exact proxy open/read function, key-agreement/decrypt inputs, protected package plaintext, `getWorldMapState`/`getCurrentServerId` handler implementation, propagated transport errors/readiness/null/source rules and exact `map scan state is unavailable` trigger remain **UNKNOWN/BLOCKED**. Public `map_summary`/status remain fail-closed; no production provider is enabled. Durable evidence: `evidence/lwbridge-implementation/2026-09-09-r6-bridge-runtime-material.json`.

### LWB-R6-040 — parsed package-key envelope persistence and invalid cleanup (2026-09-09)

**RECOVERED direct parser-to-runtime-file data flow.** The source remains verified `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`, preferred image base `0x140000000`. In the R6-039 authorization-response parser, the successful package-key branch loads service values from `+0x1A0/+0x1A8` at `0x14023C9CE/0x14023C9D5`; these are the string data/length members of the previously recovered `package-key.envelope` runtime-path object. `0x14023C9EF` references formatter descriptor `0x140C80070`, whose exact bytes are `C0 01 0A 00`; the shared host formatter `0x1400276B0` is called at `0x14023C9FE`. Under the verified descriptor grammar this emits the parsed `packageKeyEnvelope` response string followed by one LF byte. The path pair is restored into `rcx/rdx` at `0x14023CA0B/0x14023CA0E` and passed with the formatted bytes to shared persistence helper `0x14023D8FC` at `0x14023CA11`. This closes R6-039's direct parsed-value-to-file content gap.

**RECOVERED write/finalize chain.** Persistence function `0x14023D8FC-0x14023DB02` passes its content to `0x140037A58` at `0x14023DA6B`; that dispatcher calls `0x1405BB570` at `0x140037A94`, and the latter reaches imported `ntdll.dll!NtWriteFile` at `0x1405BB5DA`. After writing the temporary material, `0x14023DAF8` calls finalizer wrapper `0x14027D760`; its `0x14027D777` call enters `0x1405B6BC0`, whose imported `kernel32.dll!MoveFileExW` call uses flag `1`, `MOVEFILE_REPLACE_EXISTING`. The original therefore writes the serialized envelope through temporary runtime material and replaces the destination rather than directly truncating the final path in place.

**RECOVERED invalid/expired cleanup.** The invalid/expired branch loads the same runtime path at `0x14023CA49/0x14023CA50` and calls wrapper `0x1405B6540` at `0x14023CA57`. That wrapper reaches primitive `0x1405B8C00` at `0x1405B65BD`; the primitive directly imports `kernel32.dll!DeleteFileW` at `0x1405B8C17`. The branch then constructs the already recovered exact `KEY_ENVELOPE_INVALID` code. Missing, invalid or expired package-key material therefore removes the persisted envelope before returning that error.

**Validation, limits and impact.** Reproduce with `python tools\inspect_lwbridge_runtime_material_persistence.py ..\LW\lwbridge-0.3.1.exe`; `python -m py_compile tools\inspect_lwbridge_runtime_material_persistence.py` passes and the hash-locked inspector writes `evidence/lwbridge-implementation/2026-09-09-r6-runtime-material-persistence.json`. This is **RECOVERED static** host behavior, not **LIVE-PROVEN**. The exact embedded-proxy open/read/parser function, key-envelope agreement/decrypt inputs, protected `bridge-scripts.dat` plaintext, plaintext `getWorldMapState`/`getCurrentServerId` handlers, propagated readiness/transport errors, exact `map scan state is unavailable` trigger and live current-client correlation remain **UNKNOWN/BLOCKED**. Public `map_summary` and status remain fail-closed; R6-040 unblocks only the host persistence seam, not an authoritative production provider.

### LWB-R6-041 — device-key/package-envelope prerequisite boundary (2026-09-09)

**RECOVERED host auth/device-key boundary.** The source remains verified `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. Raw file offsets `0xC7F253-0xC7F853` form one `src\services\auth.rs` marker cluster containing the `HKLM\SOFTWARE\Microsoft\Cryptography/vMachineGuid` fingerprint path at `0xC7F3BA`, `DEVICE_KEY_CLEANUP_PENDING` at `0xC7F364`, `packageKeyEnvelope` at `0xC7F514`, `packageKeyEnvelopeExpiresAt` at `0xC7F526`, `KEY_ENVELOPE_INVALID` at `0xC7F541`, `LWKE1` at `0xC7F580`, `device-key-cleanup.pending` at `0xC7F5FB`, the four recovered runtime filenames at `0xC7F615-0xC7F654`, and `DEVICE_KEY_MISSING` / `DEVICE_KEY_MISMATCH` / `DEVICE_KEY_UNAVAILABLE` / `KEY_ENVELOPE_EXPIRED` at `0xC7F71B-0xC7F756`. The same verified host imports `NCryptOpenStorageProvider`, `NCryptCreatePersistedKey`, `NCryptFinalizeKey`, `NCryptExportKey`, `NCryptDeleteKey`, `NCryptOpenKey`, `CryptProtectData` and `CryptUnprotectData`. This establishes an auth-service device-key prerequisite boundary; import presence alone does not assign one primitive to a specific branch or prove the storage provider/key name.

**RECOVERED matching proxy loader surface.** Both hash-locked embedded proxies contain `LWKE1` at proxy raw `0x72D4C`, URL-safe Base64 alphabet instances, `device key missing` at `0x72DF0`, `key envelope agreement failed` at `0x72E60`, `device key unavailable` / `device key mismatch` at `0x72EB0/0x72EC8`, `key envelope decrypt failed` at `0x72EE0`, `package integrity invalid` / `package build mismatch` at `0x72F08/0x72F28`, and `LWBP2|` at `0x72F40`. They also retain the R6-039 UTF-16 runtime-root, package-key envelope, build-manifest and protected-package paths. Their imports include `NCryptOpenKey`, `NCryptImportKey`, `NCryptSecretAgreement`, `NCryptDeriveKey`, `NCryptExportKey`, `BCryptImportKeyPair`, `BCryptGenerateSymmetricKey`, `BCryptDecrypt`, BCrypt hash/signature primitives, plus `GetEnvironmentVariableW`, `CreateFileW`, `ReadFile` and `GetFileSizeEx`. This narrows the protected loader to a device-key/envelope/CNG-crypto/file-I/O seam but does not prove exact call ordering, parameters, key format, KDF, cipher, nonce/tag layout or handler plaintext.

**Attempts, restrictions, validation and impact.** The existing proxy Map Scan xref inspector confirmed the ASCII `LWKE1`/package-decrypt markers but cannot locate the UTF-16 runtime path. A direct host `LWKE1` code-xref scan found no direct reference, so indirect descriptor/dataflow remains possible and is not guessed. Automatic review rejected the combined startup read, a fresh proxy pointer-table scan, a proxy import-thunk/callsite disassembly and a wider proxy raw-window read; these are recorded as SB-62–65 and were not replayed or rerouted. The reproducible verifier uses Python `3.12.10` and pefile `2024.8.26`; `python tools\inspect_lwbridge_device_key_boundary.py ..\LW\lwbridge-0.3.1.exe` produced durable evidence at `evidence/lwbridge-implementation/2026-09-09-r6-device-key-boundary.json`, and `python -m py_compile tools\inspect_lwbridge_device_key_boundary.py` passes. No LastWar/LWBridge process was running, so this is **RECOVERED static** evidence only. The exact proxy open/read/decrypt call graph, device-key provider/name, envelope grammar/crypto parameters, protected `getWorldMapState`/`getCurrentServerId` handlers, readiness/transport error mapping, exact `map scan state is unavailable` trigger and live correlation remain **UNKNOWN/BLOCKED**. Public summary/status remain fail-closed. The next permitted method is a source-attributed host/device-key producer trace or a future supported live loader correlation; no Daybreak request is created from SB-62–65 alone.

### LWB-R6-042 — proxy device-key diagnostic marker vocabulary (2026-09-09; interpretation superseded by R6-043)

**RECOVERED marker vocabulary.** The source remains verified `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. In both embedded proxies, the exact ASCII literals `DEVICE_KEY_PROVIDER`, `DEVICE_KEY_EXPORT`, `DEVICE_KEY_FORMAT` and `DEVICE_KEY_MISSING` occur once at proxy raw offsets `0x730E0`, `0x730F8`, `0x73110` and `0x73128`. The parent-host raw offsets are `0x9FA454/0x9FA46C/0x9FA484/0x9FA49C` for secure and `0xA8FC54/0xA8FC6C/0xA8FC84/0xA8FC9C` for plain. Exact whole-host searches show the provider/export/format labels occur only inside those two embedded proxy images and have no UTF-16 occurrence. `DEVICE_KEY_MISSING` also occurs in host auth/device-key state text. R6-042 established literal identity/ownership only; R6-043 later recovered their secure-proxy branch semantics.

**Historical interpretation and supersession.** At R6-042 time these first three literals were conservatively treated as possible configuration names because only exact-string ownership had been recovered. R6-043 supersedes that interpretation with bounded secure-proxy control flow: `DEVICE_KEY_PROVIDER` is a provider-open diagnostic, `DEVICE_KEY_EXPORT` an export diagnostic, and `DEVICE_KEY_FORMAT` a public-key-structure diagnostic. R6-042's literal offsets and host/proxy ownership remain valid historical evidence.

**Reproduction, restrictions and impact.** Reproduce the original R6-042 marker finding with `python tools\inspect_lwbridge_proxy_device_key_config.py ..\LW\lwbridge-0.3.1.exe` or its documented `--output` form. Python compilation and JSON validation pass. A bounded host-function disassembly, a broader crypto-string filter and a proxy PE-metadata probe were rejected by automatic review and were not replayed or rerouted; the restriction outcomes are recorded as SB-67–69. No live LastWar/LWBridge target was available. R6-043 supplies the later branch-level interpretation and crypto contract; public summary/status remain fail-closed.

### LWB-R6-043 — secure-proxy device-key identity, public-key format and crypto contract (2026-09-09)

**Scope and source identity.** This finding corrects the interpretation of R6-042 and recovers a bounded static contract from the verified LWBridge reference `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. The analyzed embedded `xlua-proxy-secure.dll` begins at host raw offset `0x987374`, has SHA-256 `481c636b8d0fa9bf4b8f88cba77145054b33e3701ffa42737567b211859ac400`, preferred image base `0x180000000`, and is checked by the hash-locked read-only verifier `tools/inspect_lwbridge_proxy_device_key_crypto.py`. Durable output is `evidence/lwbridge-implementation/2026-09-09-r6-proxy-device-key-crypto.json`.

**RECOVERED exact locators and correction.** The secure-proxy persisted-key helper is RVA `0x432C0-0x43380`. At `0x432E7 -> 0x432F3` it loads UTF-16 literal `Microsoft Software Key Storage Provider` (raw `0x72E10`, RVA `0x73A10`) and calls the local `NCryptOpenStorageProvider` thunk; its failure branch at `0x432FC` loads ASCII diagnostic label `DEVICE_KEY_PROVIDER`. At `0x4332D -> 0x4333C` it loads persisted key name `{2D337A4D-7E6C-49EF-9486-54F0A00D8A41}` (raw `0x73090`, RVA `0x73C90`) and calls `NCryptOpenKey`; the key-open failure branch loads `DEVICE_KEY_MISSING` at `0x43345`. Therefore R6-042's first three `DEVICE_KEY_*` literals are not runtime configuration-variable names: they are diagnostics attached to concrete branches. The previously recovered offsets remain valid; only their interpretation is superseded.

**RECOVERED device public-key export/format.** Helper RVA `0x430C0-0x432BE` calls `NCryptExportKey` twice with UTF-16 blob type `ECCPUBLICBLOB` (raw `0x708A0`, RVA `0x714A0`) to size then fill the buffer. It requires blob magic `0x314B4345` (`ECK1`) at RVA `0x431D8`, `cbKey = 0x20` at `0x431E0`, and total blob size `0x48` bytes at `0x431E6`. On success it allocates `0x41` bytes at `0x431F5`, writes prefix byte `0x04` at `0x43205`, then copies the 64 coordinate bytes from the CNG blob. `DEVICE_KEY_EXPORT` is loaded on export failure at `0x431CF/0x4328E`; `DEVICE_KEY_FORMAT` is loaded on structural mismatch at `0x43230`. The recovered proxy public representation is therefore exactly 65 bytes: `0x04 || X[32] || Y[32]`.

**RECOVERED agreement/KDF.** Helper RVA `0x3DD00-0x3E01E` accepts only the same 65-byte input (`0x41` at `0x3DD3D`) with leading `0x04` (`0x3DD47`), opens the persisted device key through RVA `0x432C0`, reconstructs an `ECK1`/32-byte `ECCPUBLICBLOB` (`0x3DDDD/0x3DDE3`), opens the same Microsoft provider, imports the peer key with `NCryptImportKey`, and calls `NCryptSecretAgreement`. Two `NCryptDeriveKey` calls at `0x3DEA4` and `0x3DEE6` use UTF-16 KDF name `TRUNCATE` (raw `0x72E80`, RVA `0x73A80`) and both require exactly `0x20` bytes (`0x3DEAD`, `0x3DEEF`). This recovers the exact CNG KDF name and 32-byte derived-key size; it does not identify the enclosing envelope field layout or caller ownership.

**RECOVERED AES-GCM primitive contract.** Symmetric-decrypt helper RVA `0x3CB60-0x3CFEA` requires a 32-byte key (`0x3CB93`), 12-byte nonce (`0x3CBA4`) and 16-byte authentication tag (`0x3CBB5`). It opens algorithm `AES` at `0x3CBF9 -> 0x3CC05`, sets property `ChainingMode` to `ChainingModeGCM` at `0x3CC1C/0x3CC23 -> 0x3CC2F`, creates the symmetric key at `0x3CD18`, and calls `BCryptDecrypt` at `0x3CDFB`. These are exact static parameters of this helper. Without its permitted caller linkage, this finding does not claim which protected object supplies each argument.

**Reproduction, validation and limits.** The verifier uses Python `3.12.10`, pefile `2024.8.26` and Capstone `5.0.6`. Reproduce with `python tools\inspect_lwbridge_proxy_device_key_crypto.py ..\LW\lwbridge-0.3.1.exe` or the `--output evidence\lwbridge-implementation\2026-09-09-r6-proxy-device-key-crypto.json` form, followed by `python -m py_compile tools\inspect_lwbridge_proxy_device_key_crypto.py` and JSON validation. The verifier passes against the exact hashes above. This is static recovery from the secure proxy only; instruction-identical behavior in the plain proxy is not claimed. A fixed raw-byte decoded-string read, bounded host provisioning-function disassembly, secure/plain function-range hash comparison and a follow-up caller-xref pass were automatically rejected and were not replayed or rerouted; SB-71–74 preserve those outcomes. Earlier successful host-wide exact-string/import-call inventory showed host copies of the same provider/GUID/ECCPUBLICBLOB vocabulary and host `NCryptCreatePersistedKey`/`NCryptFinalizeKey` callsites, but direct instruction-level provider/GUID-to-create linkage remains **UNKNOWN/BLOCKED** because the bounded host function read was rejected.

**Implementation impact and next boundary.** PM7-B no longer needs to guess the device-key provider, persisted key name, public export format, KDF name/output size or AES-GCM key/nonce/tag sizes for the secure proxy. No production summary/status provider is enabled: the exact `package-key.envelope` open/read caller, envelope grammar/field ownership, caller link into the recovered agreement/decrypt helpers, protected `getWorldMapState`/`getCurrentServerId` handlers and authoritative readiness/error propagation remain **UNKNOWN/BLOCKED**. The next regular-AI method is a permitted file-loader/caller trace from the already recovered `package-key.envelope` path and `CreateFileW`/`ReadFile` functions; host provisioning remains under investigation separately. No Daybreak request is created.

### LWB-R6-044 — shared scan-state server/source fallback and active-read mismatch behavior (2026-09-09)

**Scope and source identity.** The source remains verified `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. The hash-locked read-only verifier `tools/inspect_lwbridge_scan_state_source_selection.py` checks only the already recovered shared producer at preferred-image VA `0x1400F7F47-0x1400F9333`; it does not perform a fresh whole-image xref search or inspect protected bridge-script plaintext. Durable output is `evidence/lwbridge-implementation/2026-09-09-r6-scan-state-source-selection.json`.

**RECOVERED server ownership and `isInWorld` split.** The producer extracts the `getCurrentServerId` response field `serverId` at `0x1400F81BE-0x1400F81D8` and retains that signed value in `r13`. It separately extracts world-state `homeServerId` at `0x1400F835F-0x1400F8379` into `r12`. The `isInWorld` lookup at `0x1400F8225-0x1400F8260` accepts only the boolean-tagged value; `sete` records an explicit-false marker, while missing/non-boolean input writes zero to that marker at `0x1400F8328`. The branch at `0x1400F87EE-0x1400F87F6` therefore distinguishes exact boolean false from the other input cases before choosing its server fallback path.

**RECOVERED exact-false/home-server fallback.** On the exact `isInWorld=false` path, the producer extracts the current state object's `serverId` at `0x1400F87FC-0x1400F880C`. It proceeds only when `homeServerId > 0` (`0x1400F8811-0x1400F8814`) and the existing state `serverId <= 0` (`0x1400F881A-0x1400F881D`). It then writes that positive home server as `serverId` at `0x1400F8851` and inserts `serverIdSource="live"` through the string/value helpers at `0x1400F8855-0x1400F88C4`. A nonpositive home server or already-positive state server bypasses this fallback instead of being overwritten.

**RECOVERED current-server/live branch and reading mismatch.** The alternate branch tests the recovered current server in `r13` at `0x1400F8978`. When positive, it reads the existing state `serverId` and `isReading`. The exact boolean-true `isReading` path at `0x1400F89E6-0x1400F89EB` checks whether the existing state server is positive and differs from the current server (`0x1400F89F1-0x1400F89FD`). That conjunction enters the exact `current server changed during map scan` error construction at `0x1400F8A18-0x1400F8A2F`; otherwise the producer writes the current server as `serverId` at `0x1400F8A76` and `serverIdSource="live"` at `0x1400F8ACE-0x1400F8B3C`. This closes the earlier R6-036 uncertainty around the active-read server-change guard without renaming any dispatcher failure.

**RECOVERED no-current-server/`none` fallback.** When current server is nonpositive, exact boolean true `isReading` reaches the common completion path without replacing the state (`0x1400F8C53-0x1400F8C5C`). If the state `serverId` is already positive, it is likewise preserved (`0x1400F8C62-0x1400F8C7A`). Only the remaining case writes numeric `serverId=0`, `serverIdSource="none"`, `tileWidth=0` and `tileHeight=0` at `0x1400F8C83-0x1400F8D72`. These are original producer fallbacks, not rebuild defaults.

**Validation, restrictions and remaining boundary.** Reproduce with `python tools\inspect_lwbridge_scan_state_source_selection.py ..\LW\lwbridge-0.3.1.exe` or the documented `--output` form; the verifier uses Python `3.12.10`, pefile `2024.8.26` and Capstone `5.0.6`. The existing full-function reproductions `--dump-rva 0x0F7F47` and `--dump-rva 0x151509` corroborate the producer and `map_summary` propagation boundary. `map_summary` still propagates producer failure before its `serverId` lookup and does not directly construct `map scan state is unavailable`; that separate emitter/condition remains **UNKNOWN/BLOCKED**, as do protected handler/current-client linkage and exact dispatcher failure/timeout identities. SB-75 records two rejected bundled resume/document reads that contributed no evidence; SB-76 records a rejected exact-candidate envelope-format string scan, which was not replayed or rerouted and leaves the `package-key.envelope` grammar under investigation. Public summary/status remain fail-closed and no Daybreak request is created.

### LWB-R6-045 — secure-proxy package validation/decrypt to AES-GCM boundary (2026-09-09)

**Scope and source identity.** The source remains verified `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. The analyzed embedded `xlua-proxy-secure.dll` begins at host raw offset `0x987374`, has SHA-256 `481c636b8d0fa9bf4b8f88cba77145054b33e3701ffa42737567b211859ac400`, preferred image base `0x180000000`, and is checked by read-only hash-locked verifier `tools/inspect_lwbridge_proxy_package_crypto_boundary.py`. Durable output is `evidence/lwbridge-implementation/2026-09-09-r6-proxy-package-crypto-boundary.json`.

**RECOVERED package-side AES consumer.** PE exception/unwind metadata contains exact function range `RVA 0x3D260-0x3DCF5`. Inside that bounded function, direct callsite `0x3DA4A` targets the R6-043 AES-GCM helper at `RVA 0x3CB60`. The same function references `package integrity invalid` at `0x3D53F`, `0x3DBAD` and `0x3DC81`; `package build mismatch` at `0x3D65B` and `0x3DC17`; and the exact `LWBP2|` marker at `0x3DA00`. The literal raw/RVA locations are `0x72F08/0x73B08`, `0x72F28/0x73B28`, and `0x72F40/0x73B40` respectively. This source-attributes a secure-proxy package validation/decrypt function as a direct consumer of the recovered symmetric primitive. It does not establish which protected file supplies the function input.

**Limits, restrictions and impact.** R6-045 does not infer the file/path caller of `RVA 0x3D260-0x3DCF5`, whether that caller is `bridge-scripts.dat`, the `package-key.envelope` open/read/parser or parsed field layout, the caller that feeds envelope fields into agreement helper `RVA 0x3DD00`, the exact AES callsite ownership of key/nonce/tag/ciphertext, plain-proxy parity, protected command-handler plaintext, or live current-client behavior. SB-77 records a rejected bundled resume read; SB-78 and SB-79 record rejected broader/single-function protected-loader reference queries; SB-80 records a rejected fixed instruction window around the AES callsite. None was replayed or rerouted, and no technical fact from those denials is accepted. The successful permitted evidence is the earlier unwind-bounded function pass plus the new exact verifier. Reproduce with `python tools\inspect_lwbridge_proxy_package_crypto_boundary.py ..\LW\lwbridge-0.3.1.exe` or its documented `--output` form, followed by Python compilation and JSON validation. Public summary/status remain fail-closed. PM7-B continues from the independent `package-key.envelope` reader/parser + agreement-caller seam, then the package-file caller/AES argument ownership and protected handler/readiness mapping. No Daybreak request is created.

### LWB-R6-046 — secure-proxy package-key path and bounded text-reader boundary (2026-09-09)

**Scope and source identity.** The source remains verified `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. The analyzed embedded `xlua-proxy-secure.dll` has SHA-256 `481c636b8d0fa9bf4b8f88cba77145054b33e3701ffa42737567b211859ac400` and preferred image base `0x180000000`. Read-only hash-locked verifier `tools/inspect_lwbridge_proxy_package_key_reader.py` writes durable output to `evidence/lwbridge-implementation/2026-09-09-r6-proxy-package-key-reader.json`.

**RECOVERED exact path construction.** Exact UTF-16 suffix `\bridge-runtime\package-key.envelope` is 36 code units / 72 bytes at proxy raw `0x70B70`, RVA `0x71770`. Unwind function `RVA 0x1B300-0x1B4BF` calls helper `0x16360` at `0x1B337`, then appends this exact suffix through direct references at `0x1B373` and `0x1B38F`; its `0x24` code-unit and `0x48` byte constants match the verified literal length.

**RECOVERED bounded read.** Enclosing loader unwind function `RVA 0x1C0A0-0x1D46F` calls the package-key path constructor at `0x1C447`, sets `R8D=0x1000` at `0x1C44D`, moves the returned path to `RDX` at `0x1C453`, and calls reader `RVA 0x1BD80-0x1C09F` at `0x1C45B`. The reader copies the caller maximum from `R8` to `RSI` at `0x1BDAC`, computes a signed length, rejects `<= 0` or `> maximum` at `0x1BEAA-0x1BEB6`, allocates the accepted length, fills the destination through internal stream helpers, and returns empty on stream failure. Its success path repeatedly removes trailing `0x0D`/`0x0A` bytes at `0x1BFAA/0x1BFBC` and NUL-terminates the shortened string at `0x1BFD5`. Internal C++ stream helper names are intentionally not inferred.

**RECOVERED opaque consumer boundary; parser still blocked.** The same enclosing loader calls the path constructor again at `0x1C8B8`, moves the returned path directly to `RCX` at `0x1C8DF`, calls unwind function `RVA 0x3F8E0-0x40A6D` at `0x1C8E2`, and tests `AL` at `0x1C8E7`. This proves that `0x3F8E0` is an exact `package-key.envelope` path consumer. It does not establish its internal grammar, parsed fields or agreement-helper ownership. SB-79 previously rejected an exact-target query inside this function; R6-046 does not inspect, replay or reroute that body. SB-81 records a separate rejected bundled repository/memory read from this resume and contributes no technical evidence.

**Validation and impact.** Reproduce with `python tools\inspect_lwbridge_proxy_package_key_reader.py ..\LW\lwbridge-0.3.1.exe` or its documented `--output` form. The verifier uses Python `3.12.10`, pefile `2024.8.26` and Capstone `5.0.6`. R6-046 and its adjacent R6-045 verifier pass, both Python inspectors compile, and all 35 `2026-09-09-r6-*.json` evidence files parse. SB-82/SB-83 record two rejected combined validation commands; those bundles were not replayed and contribute no evidence. R6-046 closes the exact proxy path/read boundary and gives a concrete opaque consumer callsite without claiming envelope field meanings. The peer-public-key field into agreement helper `RVA 0x3DD00`, envelope nonce/tag/ciphertext ownership, package-file caller/AES callsite ownership, plain-proxy parity, protected handlers and live readiness/error mapping remain **UNKNOWN/BLOCKED**. Public summary/status remain fail-closed; no Daybreak request is created.

### LWB-R6-038 — PM7-A source-aware option/count integration (2026-09-09)

**Scope and source identity.** This is an implementation checkpoint over already recovered contracts, not a new native-behavior claim. The reference `..\LW\lwbridge-0.3.1.exe` was reverified at SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. R6-024 provides the only recovered data-source shapes: published `map_records` scoped by server or staging `scan_records` scoped by server plus exact run. R6-030 provides the selector: staging is used only for `isReading=true`, matching requested/scan-state server and raw nonempty `scanRunId`, including whitespace because no trimming is evidenced. R6-031 provides NULL/empty alliance handling and scan-progress row selection. R6-014 supplies the Unix-ms reward cutoff. Exact source locators and reproduction remain in those findings and their evidence JSON files.

**IMPLEMENTED/OFFLINE-TESTED.** `MapDataStore.SelectOptionSource` now produces one validated `MapOptionSourceSelection`, and `ReadOptionAggregatesAt` carries that context through alliance/no-alliance, resource/monster names, dispatch levels, treasure types, truck/railway reward items, all eight counts and scan-progress record selection. Runtime values are bound parameters; SQL structure is chosen only between the two recovered internal source/scope forms. Active scope reads the exact run from `scan_records` and its exact `scan_runs.id`; inactive/mismatched/missing-run scope reads published `map_records` and newest non-discarded server progress. The former persisted-only aggregate helper has been replaced rather than duplicated.

**IMPLEMENTATION POLICY and validation.** One deferred SQLite read transaction covers every aggregate in a call, preventing staged options from being paired with published counts or count/option families from observing different database generations. Deterministic fixtures deliberately conflict published versus staged records, another run on the same server, and another server; active, idle, server-mismatch, null, empty and whitespace `scanRunId` selector cases are retained. A file-backed two-connection WAL test commits a second city after the first aggregate read and proves alliances and counts remain on the original snapshot until the next call. The Release build succeeds with zero warnings/errors and the deterministic backend suite returns `ok=true` with all five groups true. Durable validation metadata is `evidence/lwbridge-implementation/2026-09-09-r6-map-option-source-integration.json`.

**Public gate and limits.** `LWBridgeBackend` is intentionally unchanged: `map_data_options` still returns `MAP_INDEX_UNAVAILABLE`. `MapScanProgressAggregateRow` remains an internal selected-row shape and must not be serialized as the original public JSON. Exact public `scanProgress` keys/types/order, absent/null/query-conversion error behavior, and the authoritative shared scan-state/provider readiness required to select a live context remain UNKNOWN/BLOCKED. PM7-B therefore becomes the next bounded production-blocking task. No Daybreak request is created; ESC-003 remains NOT_ASSIGNED for the serializer. SB-52/53 record two rejected combined read-only commands during this checkpoint; narrower repository/hash reads succeeded and the rejected operations contributed no technical finding.

### LWB-R6-026 — alternate-sort parser and ordered frontend sort state (2026-09-09)

**RECOVERED source identity and reproduction.** The native source remains the verified `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`; the immutable recovered `MapDataPanel-C1HVeNHr.js` is SHA-256 `fc39de8154c54ee0937b6f9f9377d671e0dbad533ed2d9eda29a99d35e380f5e`. `tools/inspect_lwbridge_map_sorts.py` verifies those hashes and only fixed, bounded bytes/strings inside the already identified native `map_search` function at preferred-image VA `0x140271864-0x140276D7B` plus the recovered frontend column/toggle functions. Reproduce with `python tools\inspect_lwbridge_map_sorts.py ..\LW\lwbridge-0.3.1.exe evidence\lwbridge-0.3.1\frontend --json`. Durable output is `evidence/lwbridge-implementation/2026-09-09-r6-map-sort-inventory.json`.

**RECOVERED public frontend contract.** Every kind defaults to one `updatedAt desc` sort. Selecting an absent sortable column prepends `{sortBy:key,sortOrder:'desc'}` while preserving all existing sorts; selecting the current `desc` key changes it to `asc` and moves it first; selecting it again removes it. The visible public columns are city `level/health/shield/updatedAt`; resource `level/updatedAt`; monster `level/distance/updatedAt`; truck `quality/power/itemCount/remainingLootCount/arriveTime/updatedAt`; railway `quality/power/itemCount/protectTime/updatedAt`; dispatch and ghost `level/quality/completionTime/updatedAt`; treasure `updatedAt`. `itemCount` is exposed only while an item key is selected. This proves that alternate public sorting is an **ordered multi-sort** contract, not a single replacement key.

**RECOVERED native subset.** Fixed instruction bytes pin scalar inline parser comparisons for `distance` (`0x140274B68`), `itemCount` (`0x140274BE5`), `updatedAt` (`0x140274C06`), `quality` (`0x140274C2C`), `shield` (`0x140274CDF`), `health` (`0x140274CFA`), `level` (`0x140274D6A`), `power` (`0x140274D82`), `arriveTime` (`0x140274DC2`), `protectTime` (`0x140274E0A`) and `completionTime` (`0x140274E58`). Direct expression references are pinned for `completionTime` at `0x140274ED6`, `arriveTime` at `0x140275039`, `protectTime` at `0x140275081`, and the item-count descriptor at `0x140275191`. The adjacent exact native expression inventory includes `updated_at AS sort_value_0` at raw `0x00C8A533`; `distance` and `power` literals at `0x00C8A918`/`0x00C8A920`; the item-count `currentGoods` SUM whose one-byte `0x56` descriptor is at `0x00C8A925` and SQL text starts `0x00C8A926`; `completionTime`, `remainingLootCount`, `protectTime`, `arriveTs`, truck special-UR quality, dispatch/ghost special quality and `health` expressions at raw `0x00C8A9E0-0x00C8ABB2`; and dynamic `sort_value_`, `DESC`, plus `IS NULL) ASC` page-order fragments at `0x00C8ABED-0x00C8AC48`.

**UNKNOWN/BLOCKED and implementation impact.** A fixed 18-byte SIMD comparison is present at `0x140274B1D` in the parser region, but this checkpoint does not label it `remainingLootCount` because the compared constant's producer has not been independently pinned. The shield prebuilt CASE value, distance computed value source, railway quality branch, complete per-kind native gates, and complete ordered multi-sort/null-order/tie-break assembly are also unresolved. Because the frontend normally leaves `updatedAt` behind a newly selected sort, enabling a single alternate sort in the rebuild would invent native composition semantics. Production therefore remains unchanged and non-`updatedAt` forms continue to fail closed with `MAP_QUERY_UNRECOVERED`.

**Validation and restrictions.** The verifier caught and corrected the initial handoff offset for item-count text (`0x00C8A926`, not the descriptor byte at `0x00C8A925`). The hash-locked verifier and JSON parsing pass, `git diff --check` is clean apart from line-ending warnings, the Release desktop build succeeds with zero warnings/errors, and the deterministic backend suite returns `ok=true` with profile routing, persistence, request lifetime, map persistence and map contract all true. Production source and tests are unchanged by this research checkpoint. No game or launcher process was running, so this is **RECOVERED static** evidence only and is not **LIVE-PROVEN**. SB-20 through SB-23 record rejected broad/combined inspection and bounded pointer-calculation operations; none produced evidence and none was retried or rerouted. Narrow immutable frontend reads, fixed raw-byte checks and direct string verification provided the accepted R6-026 evidence. The unresolved sort questions remain regular-AI work with concrete permitted methods; no Daybreak request is created from these restrictions alone. Validation metadata is `evidence/lwbridge-implementation/2026-09-09-r6-map-sort-validation.json`.

### LWB-R6-027 — native sort direction branch and distance override (2026-09-09)

**RECOVERED source identity and locators.** The source remains the verified `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`, plus the immutable Map Data frontend SHA-256 `fc39de8154c54ee0937b6f9f9377d671e0dbad533ed2d9eda29a99d35e380f5e`. `tools/inspect_lwbridge_map_sort_direction.py` verifies fixed bytes at preferred-image VA `0x14027525C`, `0x14027526A`, `0x140275296` and `0x1402752AF`, the `.rdata` mapping, raw `ASC` at `0x00C8A530`, raw `DESC` at `0x00C8ABFA`, and the frontend desc/asc toggle. Durable output is `evidence/lwbridge-implementation/2026-09-09-r6-map-sort-direction.json`.

**RECOVERED result.** The public `asc` path selects a three-byte `ASC` direction. The public ordinary `desc` path selects four-byte `DESC`. After the descending direction has been selected, an exact inline comparison against `distance` replaces it with the three-byte `ASC` direction. This is a recovered physical SQL-direction rule; this finding does not invent why the original special-cases distance or whether a separate distance value transformation explains the visible ordering.

**UNKNOWN/BLOCKED and implementation impact.** The distance computed expression/value source, the mechanism/rationale behind physical `ASC` for the distance-desc path, and complete ordered multi-sort/null-order/tie-break assembly remain unresolved. R6-027 therefore enables no production sort; non-`updatedAt` forms still fail with `MAP_QUERY_UNRECOVERED`. A subsequent fresh quality-branch raw-range request was rejected by automatic review as SB-24 and was not retried or rerouted. The fixed-byte direction verifier uses only evidence already obtained through permitted reads. This is **RECOVERED static**, not **LIVE-PROVEN**.

### LWB-R6-047 — native map-record upsert boundary and record layout (2026-09-10)

**RECOVERED source identity and selector.** The source is the verified `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`, preferred image base `0x140000000`. A successful hash-locked read-only `tools/inspect_lwbridge_map_scan.py` xref pass finds the sole `INSERT INTO scan_records` string at raw `0xC8B632` / RVA `0xC8C232` and its code reference at `0x14027A542`, inside helper `0x14027A1E3-0x14027AA73`. The same helper references a published `INSERT INTO map_records` statement at `0x14027A242`. At `0x14027A20E-0x14027A22F` it first requires table-name length `12` and compares the bytes against exact ASCII `scan_records`; equality branches to the staging statement at `0x14027A535`, while the other observed path uses the published statement. The successful execution context reaches exact diagnostic label `upsert map record` at `0x14027A993`.

**RECOVERED shared record field boundary.** The helper binds one already-built record into the SQL columns. Published `map_records(kind,server_id,record_key,point_index,uuid,name,alliance_name,level,quality,power,distance,shield_end_time,updated_at,data_json)` maps, in bind order, to record offsets `+0x60`, `+0x110`, `+0x78`, `+0x00`, optional fields rooted at `+0xA8`, `+0xC0`, `+0xD8`, then `+0x10`, `+0x20`, `+0x30`, `+0x40`, `+0x50`, `+0x118`, and `+0x90`. The staging branch prepends its separate `run_id` parameter and then binds those same 14 record fields in the same order. Therefore `record_key` is already present at input-record offset `+0x78` before this helper executes; this helper is the persistence boundary, not the native identity generator.

**Reproduction, validation and limits.** Reproduce the accepted xref pass with `python tools\inspect_lwbridge_map_scan.py ..\LW\lwbridge-0.3.1.exe --target "INSERT INTO scan_records" --target "INSERT INTO map_records" --target "record_key"`. Focused durable output is [`2026-09-10-r6-map-record-upsert-boundary.txt`](../evidence/lwbridge-implementation/2026-09-10-r6-map-record-upsert-boundary.txt) with machine-readable summary [`2026-09-10-r6-map-record-upsert-boundary.json`](../evidence/lwbridge-implementation/2026-09-10-r6-map-record-upsert-boundary.json). A later direct `--dump-rva` follow-up was rejected by the environment's automatic safety review and was not rerouted; the accepted finding uses only the earlier successful saved xref dump. At this checkpoint upstream callers, per-kind construction of offset `+0x78`, typed normalization/removal and scan scheduling/completion were **UNKNOWN/BLOCKED**; R6-049 subsequently recovers the callers and normalized key construction. No production ingestion, live scan, or user-visible result was enabled by R6-047.

**Implementation impact / next method.** DB-03 now has a precise producer target: trace one native kind backward from the call that supplies this helper's record pointer and identify the source writes that populate `+0x78`, then correlate that kind's current-client source record and normalization before enabling any ingestion. Keep the rebuild's `MapDataStore` requirement for an already-derived `recordKey`; do not derive it from the frontend fallback chain.

### LWB-R6-048 — current-client world-point identity surface for resource data (2026-09-10)

**RECOVERED current-client source identity.** The source is the installed build `1.0.361 / 1078` `Assembly-CSharp.rdl`, size `15,640,576`, SHA-256 `871efe06819fbac438413eb96b7df8193d0be56094f3a44d5ff141e6219adcbd`, with RGMD root at file offset `0x9a88a8`, `5,828` TypeDefs and `54,474` MethodDefs. The repository-local read-only `tools/inspect_lastwar_rdl_metadata.py` reproduces the finding without loading or modifying the game.

**RECOVERED typed world-point schema and lookup axes.** `Protobuf.WorldPointInfo` is type row `4309`. Its protobuf constants identify `Id` field `1`, `ResourceInfo` field `6`, `Uuid` field `100`, `ServerId` field `102`, `SrcServerId` field `103` and `WorldId` field `104`; matching getter rows are `45249`, `45259`, `45298`, `45302`, `45304` and `45306`. `ResPointInfo` is type row `1753` and extends `PointInfo`, adding `id:int32`, `state:int32`, `gatherMarchUuid:int64`, `gatherUid:string`, `gatherAllianceId:string` and `specialType:Protobuf.SpecialType`; its world-point constructor is row `13461` and resource getters include `GetResType` row `13464` and `GetResLevel` row `13465`.

Three bounded current-client methods establish separate identity dimensions without assigning LWBridge behavior. `WorldPointManager::GetWorldTileInfo(int32 pointIndex)` is row `13619`, RVA `0x1555a0`; its CIL uses the supplied `pointIndex` as a dictionary lookup key and returns the value or null. `WorldPointManager::GetPointInfoByUuid(int64 uuid)` is row `13627`, RVA `0x1556e4`; its CIL uses the supplied `uuid` as a dictionary lookup key, obtains a tile value, then returns that tile's point object or null. `WorldPointManager::GetResourcePointInfoByIndex(int32 pointIndex)` is row `13638`, RVA `0x1558dc`; it passes `pointIndex` into an internal point lookup, compares one modified field token with literal `7`, and casts the surviving object to `ResPointInfo`. Because this RG container preserves modified CIL tokens that the current parser cannot resolve in these branches, the backing dictionary tokens and that compared field are deliberately left unnamed rather than inferred.

**Validation, restrictions and implementation impact.** Reproduce with the five exact commands recorded in [`2026-09-10-r6-current-world-point-identities.txt`](../evidence/lwbridge-implementation/2026-09-10-r6-current-world-point-identities.txt); the machine-readable summary is [`2026-09-10-r6-current-world-point-identities.json`](../evidence/lwbridge-implementation/2026-09-10-r6-current-world-point-identities.json). A combined `--references-to-type WorldPointManager`/wrapper query and a later broader `WorldPointManager` field-inventory query were rejected by automatic safety review and were not replayed or rerouted. R6-048 proves that the current client has independently typed point-index and UUID lookup inputs around resource/world-point data. At this checkpoint it did **not** prove which value becomes LWBridge's authoritative `record_key`; R6-049 subsequently recovers the original builder formula and shows ordinary indexed records use the original point integer as signed-decimal identity. The remaining current-client question is the direct capture-to-builder mapping and typed normalization, so R6-048 by itself still enables no production ingestion or live scan.

### LWB-R6-049 — original normalized record-key derivation (2026-09-10)

**RECOVERED source identity and locators.** The source remains verified `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`, preferred image base `0x140000000`. The normalized-record builder is runtime function `RVA 0x2785BA-0x2793BE`; its point-name descriptor table is `RVA 0xC8BC70`; record-key selection is `0x278C47-0x278CEF`; the selected String is copied to normalized record `+0x78/+0x88` at `0x279214-0x27922B`. Signed integer formatter `RVA 0x285410-0x285556` calls decimal digit core `0x394C0-0x3968E`.

**RECOVERED result.** The builder searches four integer aliases in order: `pointIndex`, `mainIndex`, `pointId`, `index`. It preserves the original extracted integer for identity; the coordinate fallback separately decrements a copy before tile-width division. Exact `truck` or `railway`, any other record with nonempty `marchUuid`, or any record without a point index selects nonempty `uuid` when available and otherwise `marchUuid`. Every other indexed record formats the original extracted point integer as signed decimal text. The integer formatter emits `-` for negatives and its digit core uses the recovered `00..99` table with `/10000` and `/100` reduction. The resulting String is written to normalized record offset `+0x78` with its length at `+0x88`, closing the original builder-to-R6-047 persistence identity formula.

Direct calls to R6-047's shared upsert helper are exactly `0x25767E` in `0x2574CD-0x2577E2`, `0x25F368` in `0x25F01D-0x25F5C8`, and `0x270863` in `0x26EC06-0x271056`; the middle call selects exact `scan_records`, while the other two use published `map_records`. Both staging and published paths therefore consume the same normalized record layout and recovered identity slot.

**Reproduction / durable evidence.** Run `python tools\inspect_lwbridge_map_record_identity.py ..\LW\lwbridge-0.3.1.exe` or add `--json`. The hash-locked verifier checks the runtime functions, exact identity branches, four-entry Rust string table, decimal formatter and direct upsert caller set. Machine-readable output is [`2026-09-10-r6-map-record-identity.json`](../evidence/lwbridge-implementation/2026-09-10-r6-map-record-identity.json); accepted full function dumps are [`2026-09-10-r6-map-record-identity-builder.txt`](../evidence/lwbridge-implementation/2026-09-10-r6-map-record-identity-builder.txt) and [`2026-09-10-r6-map-record-key-format.txt`](../evidence/lwbridge-implementation/2026-09-10-r6-map-record-key-format.txt).

**Validation / limits / implementation impact.** This is original-reference static recovery, not current-client or live proof. It removes the unknown original `record_key` formula but does not establish how current build-1078 capture payloads populate the builder's point/march/UUID inputs, remaining typed field normalization/removal, scan scheduling/ack/completion, or bridge readiness. Production ingestion remains fail-closed. One separate raw string-cluster follow-up was rejected by automatic review; it was not replayed or rerouted and contributed no finding. The next PM7-C task is current-client/native-capture field mapping into this recovered builder, beginning with the resource/world-point index path already identified by R6-048.

### LWB-R6-050 — current managed/xLua PointInfo identity ownership (2026-09-10)

**RECOVERED current-client source identity.** The source remains installed build `1.0.361 / 1078` `Assembly-CSharp.rdl`, size `15,640,576`, SHA-256 `871efe06819fbac438413eb96b7df8193d0be56094f3a44d5ff141e6219adcbd`, RGMD root `0x9a88a8`, with `5,828` TypeDefs and `54,474` MethodDefs. The verified LWBridge reference remains SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`. `tools/inspect_lastwar_point_identity_surface.py` hash-gates the current RDL and uses only the repository's read-only RG/RGMD metadata/CIL parser. It does not load or modify the game.

**RECOVERED exact fields and xLua backing ownership.** Exact `PointInfo` type row `1748` defines `pointIndex:int32` field row `9975`, `mainIndex:int32` row `9976`, and `uuid:int64` row `9980`. Generated `XLua.CSObjectWrap.PointInfoWrap` type row `3302` exposes `_g_get_pointIndex` row `26703` / RVA `0x288e6c`, `_g_get_mainIndex` row `26704` / RVA `0x288ec8`, and `_g_get_uuid` row `26708` / RVA `0x289038`. Their single field reads identify raw backing tokens `pointIndex=0x679326C2`, `mainIndex=0x67932162`, and `uuid=0x679321E2`. `PointInfo::get_isMainPoint` row `13408` / RVA `0x14f5bd` reads `mainIndex` then `pointIndex` and applies `ceq`, directly proving that comparison without inferring modified token semantics.

`PointInfo::.ctor(Protobuf.WorldPointInfo)` row `13404` / RVA `0x14f454` stores raw call result `0xA781E1A2` into the proven `uuid` token, then invokes the same raw call token `0xA7819802` twice and stores its results into the proven `pointIndex` and `mainIndex` tokens. The modified call tokens are deliberately left unnamed; this finding does **not** assign them to protobuf getters by instruction order. Exact `ResPointInfo` type row `1753` extends `PointInfo`, defines `gatherMarchUuid:int64` at field row `10139`, and retains `GetResType()` row `13464` / RVA `0x1513c4` plus `GetResLevel()` row `13465` / RVA `0x151400`.

**Reproduction / validation / limits.** Run `python tools\inspect_lastwar_point_identity_surface.py $env:LOCALAPPDATA\FunFly\Last War-Survival Game\Game\LastWar_Data\Assemblies\Assembly-CSharp.rdl --output evidence\lwbridge-implementation\2026-09-10-r6-current-point-identity-surface.json`, then `python -m py_compile tools\inspect_lastwar_point_identity_surface.py`. The hash-gated verifier reproduces the exact type/method counts already recorded by R6-048; a transient uncommitted `5,833 / 54,536` observation was not reproducible and is not used as evidence or as a locator. A separate attempt to batch-resolve modified source getter tokens was rejected by automatic review and was not rerouted. R6-050 proves current managed/xLua field ownership only: native capture/serializer ownership, direct current-client dataflow into R6-049, typed normalization/removal, bridge readiness, one real scan and displayed results remain **UNKNOWN/BLOCKED**. Production ingestion stays fail-closed.

### LWB-R6-051 — original normalized scalar field sources and fallbacks (2026-09-10)

**RECOVERED source identity and locators.** The source remains verified `..\LW\lwbridge-0.3.1.exe`, SHA-256 `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`, image base `0x140000000`. R6-051 reuses the already recovered normalized-record builder `RVA 0x2785BA-0x2793BE` and R6-047 shared upsert mapping. The fixed-locator verifier `tools/inspect_lwbridge_map_record_identity.py` now checks the field-source reads at `0x27902D-0x279118` and their final normalized stores at `0x27925C-0x27929E`.

**RECOVERED result.** The builder reads exact raw field `level` and writes its optional pair to normalized slot `+0x10/+0x18`; `quality` to `+0x20/+0x28`; and `power` to `+0x30/+0x38`. For normalized distance at `+0x40/+0x48`, it reads `distance` first and, when that read is absent, reads `distanceFromHome`. For normalized shield-end time at `+0x50/+0x58`, it reads `shieldEndTime` first and otherwise reads `protectEndTime`. R6-047 independently proves these five normalized slots bind to SQLite columns `level`, `quality`, `power`, `distance`, and `shield_end_time`, so the raw-field-to-persisted-column provenance and two fallback orders are now source-attributed.

**Reproduction, restriction and implementation impact.** Run `python tools\inspect_lwbridge_map_record_identity.py ..\LW\lwbridge-0.3.1.exe --json`; durable output is [`2026-09-10-r6-map-normalized-columns.json`](../evidence/lwbridge-implementation/2026-09-10-r6-map-normalized-columns.json). The verifier checks exact RIP-relative strings, helper callsites and destination stores against the hash-gated immutable reference. A separate secure-proxy xref query for `pointIndex`, `mainIndex`, `uuid`, `gatherMarchUuid`, `resType` and `resLevel` was rejected by automatic review and was not replayed or rerouted; it contributes no evidence. R6-051 is **RECOVERED static**, not current-client or live proof. It does not establish which current native capture serializer emits these fields, per-kind resource naming/gathering normalization, removal identity, scan completion, or bridge readiness. Production ingestion remains fail-closed; the next PM7-C priority is still a permitted current capture/serializer linkage into the proven R6-049/R6-051 builder inputs.

### LWB-R7-001 — bounded current-game resource acquisition and visible Map Data row (2026-09-10)

**LIVE-PROVEN source identity and locator.** Fresh read-only runtime evidence identifies official app `1.0.361`, build `1078`. Launcher log lines `40124`, `40131`, `40135` and `40136` show local manifest `1078`, Lua v14 with size `41301710`/CRC `2371889527`, successful pack-index verification and official `Game/LastWar.exe` startup at `2026-09-10 03:01:45.343`. The instrumented v14 capture package has SHA-256 `028e32ca71cf318758ab696ca05628ec30a28b429646e8bd6834dc0287bfffa0` and matches that version/size/CRC. The resulting diagnostics file has SHA-256 `6447d30f373a76fbfe416a894546f8797ea71e20e44d9f06d3bfd61cd367cbce` and `capturedAt=2026-09-09T19:02:27Z`, about 42 seconds after launcher startup. It contains `15,293` accumulated world records, `8,000` `resource_point` records, `10,000` covered blocks and `65` completed batches. The exact selected row is `WorldPointManager._pointInfos` point `1006`, server/srcServer `2212`, `x=5`, `y=1`, `level=3`, `pointType=7`, `resourceType=wood`, `resourceTypeId=2`, `uuid=0`, `worldId=0`, with `gatherTimeStatus=occupied_time_unavailable`.

**IMPLEMENTATION POLICY and exact rebuild path.** `FirstLiveResultImporter.ImportOneResource` in `src/LWBridge.Desktop/FirstLiveResultImporter.cs` accepts only a hashable diagnostics file containing a usable real `resource_point`, maps that category to recovered public kind `resource`, uses the source point ID as decimal `recordKey`/`pointIndex`, preserves the captured timestamp as `updatedAt`, and writes only source-backed indexed fields to `MapDataStore`. `LWBridgeBackend` exposes the recovered `{serverId,counts,scanState}` summary shape only in this bounded first-live mode so the existing frontend can select the imported server; ordinary production summary/options remain unavailable. `LWBridgeWindow` isolates the mode in an in-memory store and dedicated WebView data directory, suppresses auto-launch, selects the Resource tab for capture, and changes only the selected row's status to `—` because the recovered frontend would otherwise infer Idle solely from absent gather identifiers.

**Validation and visible result.** The focused record was programmatically extracted from the hash-identified full live capture into [`2026-09-10-first-live-resource-capture.json`](../evidence/lwbridge-implementation/2026-09-10-first-live-resource-capture.json), preserving the source-capture hash. Release capture exits `0`; [`2026-09-10-first-live-resource-ui.json`](../evidence/lwbridge-implementation/2026-09-10-first-live-resource-ui.json) and [`2026-09-10-first-live-resource.png`](../evidence/lwbridge-implementation/2026-09-10-first-live-resource.png) show server `2212`, Resource count `1`, `1 items`, `5,1`, `Unknown resource`, level `3`, status `—`, and `9/10/2026 3:02:27 AM`. `resourceType=wood` is not promoted to the original `resourceNameKey` because that mapping is unrecovered; `occupied_time_unavailable` is not promoted to Idle/Gathering. The standard production `map_data_options` remains `MAP_INDEX_UNAVAILABLE` in the same capture.

**Limits and next step.** This proves a real current-game source record reached the rebuilt storage/query/display path, but the rebuilt application did not own the game session or acquire the row through production `map_scan_start`; the adapter replays the captured current-game diagnostics. The guarded v14 production-scanner attempt loaded/registered but did not produce a terminal status result, so exact scan serializer/lifecycle completion remains **UNKNOWN/BLOCKED**. Computer Use initialization was rejected by the environment's automatic safety review before any UI action and was not rerouted. The next work is to connect the supported R5 bridge/session to this proven current resource source and production scan lifecycle, then repeat the same source-correlated display with a freshly acquired row.

## Remaining unknowns

- Exact game-side block scheduling/tick implementation behind `XluaBridgeMapScanTick` after the recovered `startMapScan` request boundary.
- Exact block geometry/order and any mode-specific timing/pacing beyond the recovered `normal=8` / `fast=20` concurrency.
- Exact per-kind point/march field grouping, value types, optionality and map-index normalization.
- Current-client/native-capture mapping into the recovered R6-049 `pointIndex`/`mainIndex`/`pointId`/`index`, `marchUuid` and `uuid` builder inputs.
- Retry/unread/failure transition rules.
- Resume cursor/persistence format.
- Native queue capacities and how `dropped` forces scan failure or retry.
- Exact map-index transaction/commit contract and final completion gate.

## Reliability design to implement

Persist progress before acknowledgement. Treat inflight blocks as uncertain after a disconnect and replay them idempotently. Freeze scheduling on bridge loss, save state, perform a clean injection bootstrap, then resume only unresolved work. Completion must require zero unresolved failed/unread blocks, no pending acknowledgements, no dropped native capture, and a stable final commit.
