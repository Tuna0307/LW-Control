# LWBridge 0.3.1 one-to-one parity matrix

**Current through:** `LWB-R8-080`, 2026-09-27
**Reference:** `C:\Users\chimw\OneDrive\Desktop\Github\LW\lwbridge-0.3.1.exe`
**SHA-256:** `2a2de09b35bb6a03f26b5e05f949f3aea6215f294127e605d7d78481f855cdff`

This is now the product-completion matrix for the retained product scope. The older 47-case Home/Map acceptance matrix remains historical implementation evidence, not one-to-one completion authority. Owner direction changed on 2026-09-26: authentication/authorization/entitlement/account-session internals may be recovered when they are dependencies of retained behavior. Standalone Login/Register/account-management product UI remains non-priority unless required by that retained-state pipeline; synthetic credentials, capacity, roles or premium/admin state are not acceptable substitutes.

| Surface / subsystem | Current parity class | Current fact | Required parity work |
|---|---|---|---|
| Reference EXE identity | EXACT_BYTES authority | Hash reverified 2026-09-24 | Keep immutable |
| Extracted React/Vite feature chunks | EXACT_BYTES source, modified presentation boundary | Original chunks/styles/icons/locales were recovered; generator/API boundary and some product changes alter the shipped rebuild | Remove non-reference transformations except compatibility plumbing that is observationally invisible |
| Reference frontend command/event inventory | EXACT_BYTES-derived inventory / routing audit | R8-067 extracts 104 exact API command literals and 15 `app://`/`bridge://` event literals from the recovered reference frontend. Ten standalone account/activation commands were excluded from the retained frontend routing count, leaving 94 retained frontend commands; 61 have a specific production route, 33 do not. R8-068 closes generic Automation Configure/Start/Stop at the owner-excluded authorization-derived request boundary, moving those three from genuinely unclosed to audited/fenced. R8-074 closes the final two retained frontend gaps, `map_dispatch_share_alliance` and `map_treasure_claim`, at exact host/provider/database boundaries. All 33 unrouted retained frontend commands are now audited/fenced and 0 remain genuinely unclosed. At least eight recovered native handlers have no matching frontend literal, so this is not the complete native inventory | Retained frontend routing inventory is closed; continue native-only handler/callsite/service inventory and do not confuse routing classification with parity completion |
| Original stylesheet/icons/9 locale bundles | EXACT_BYTES | Recovered byte-for-byte | Preserve hashes |
| Normal eight post-login navigation entries | EXACT_CONTRACT / near-exact presentation | Overview, Automation, Map Data, Squads/AFK, City Layout, Hotkeys, Mini-games, Settings recovered | Re-audit every nested state/action against reference |
| Auth/account/session dependencies | DEPENDENCY RECOVERY ALLOWED / standalone account product non-priority | Owner direction changed 2026-09-26: authentication, authorization, entitlement and account/session internals may be recovered when required by retained behavior. Synthetic credentials, capacity, roles or premium/admin state are not acceptable substitutes | Recover only what retained features require and revisit historical authorization fences; standalone Login/Register/account-management UI remains non-priority unless the retained-state pipeline requires it |
| Header geometry / non-account controls | PARTIAL / requires retained-scope audit | Earlier rebuild changed header geometry while removing account controls | Restore only retained non-account header behavior; do not reintroduce account/auth controls |
| Advanced page visibility / VIP18 hidden family | EXACT_CONTRACT visibility / FENCED backend | Reference hard-hides Advanced in normal navigation. R8-049 closes VIP18 config/list read boundaries enough to prove `vip18_base_list` carries its dynamic cache identity through an authorization-state-dependent path and `vip18_base_config_get` depends the unrecovered shared config-state normalizer. R8-061 closes `vip18_base_config_save`; R8-072 closes `vip18_base_apply` / `vip18_base_restore`, including exact 10-second providers, Apply/Restore config patches, provider-before-config non-atomic ordering and raw provider JSON success. All live VIP18 mutations remain fenced on unrecovered authorization state plus unrecovered shared config migration/merge/write semantics | Preserve hidden visibility; recover the original authorization dependency and shared config migration from evidence before enabling VIP18; do not synthesize either |

| Home/Overview frontend | EXACT_BYTES-derived | Original component is used | Audit transformed API boundary and all connected states |
| Home lifecycle backend | EQUIVALENT_REIMPLEMENTATION, parity not closed | Current C# lifecycle works and has live evidence; R8-038 restores the exact `game_root_status` public `{root,source,valid,candidates}` projection, `{path,source}` candidate schema, native lightweight validity predicate, source ordering and `STATE_UNAVAILABLE`; R8-039 restores exact `game_root_select` `{canceled,path,valid}` cancel/invalid/valid results, normalization, valid-only persistence and native error vocabulary while preserving stricter rebuild launch admission internally; R8-040 restores native `proxy_status` full/reduced schemas, target/original paths, proxy resource/hash classification, runtime-managed projection and exact repair predicate while keeping resource discovery/ownership plumbing equivalent; R8-041 restores native `game_recovery_status`/`bridge://game-recovery` field/type/default semantics, notice identity, terminal succeeded/failed publication and strict runtime/state errors without altering the recovered process-control policy; R8-042 restores native `profile_instance_status` null-or-12-field output and retained connection-state projection; R8-043 closes native `get_status` top-level field order/types, route ownership, runtime-root/pending/activity semantics and `STATE_UNAVAILABLE`, while explicitly fencing the unrecovered full `config.json` migration/normalization projection and retaining the current rebuild status object as a documented deviation; R8-058 closes main-window `app://close-requested` `{instanceCount}` and the global `app_exit_confirm` shutdown contract through per-runtime `bridge_exit`, game-close timeout, original-proxy restore, `APP_EXIT_FAILED`, and null success, while implementation stays fenced on the missing proxy mutation/restore lifecycle. R8-065 freshly live-proves manual launch/connect/close plus startup auto-launch/connect/close against current-v21, so the retained lifecycle is LIVE-WORKING while parity remains open | Recover `get_status.config`, original proxy install/backup/restore, Rust/launcher lifecycle and remaining host behavior far enough to prove one-to-one semantics |
| Map Data frontend | EXACT_BYTES-derived with deviations | Original component family is used, but prior rebuild removed/added product behavior | Restore reference controls and remove non-reference additions |
| City Excel export | EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION | R8-007 restores original API/UI/locales, native save dialog, 200-row paging/200k limit, exact 12-column six-part XLSX writer, filename and result envelope | Preserve with strict-parity regression checks |
| `map_scan_clear` | EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION | R8-008 restores the positive current-live-server gate, exact errors, server-scoped deletion, player-mark preservation and Manual-only frontend control | Preserve; do not reintroduce `serverId=0`/saved-server Clear |
| `server_jump` public contract | EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION | R8-009 restores `{changed, previousServerId, serverId}` and retains recovered validation/error behavior | Recover only the still-protected travel internals and exact numeric timeout before claiming full implementation parity |
| `map_summary` | EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION | R8-010 restores exact `{serverId, counts, scanState}`, exactly eight original count keys, shared-state server ownership and active-run vs published count selection | R8-013 now owns the shared scan-state contract; do not reintroduce saved-server fallback or `savedServerIds` into this command |
| `map_data_options` | EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION | R8-011 restores exact recovered top-level order, eight count kinds, Resource/Monster name families, server-scoped published fallback and matching active-run staging; removes public `zombie_boss`, `monsterLevels` and all-server fallback | Complete nested `scanProgress` serializer only when stronger evidence exists; preserve current strict-parity regression |
| Manual `map_scan_start` public contract | EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION | R8-012 restores exactly eight selectable kinds, Manual Normal/Fast UI/persistence, Normal=8/Fast=20, original null/non-string default-to-Normal behavior and exact invalid-string error; current-client strategy choice no longer rewrites public mode | Preserve; R8-013 separately corrects status-field membership and Stop lifecycle |
| `map_scan_status` / `map_scan_stop` | EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION | R8-013 restores shared mutable status, read-only world refresh, lifecycle-dependent native fields, publishing→idle completion, idempotent Stop, exact five-field Stop cleanup and distinct Clear reset | Preserve; generic protected Stop error-envelope precedence and narrow publishing races remain partial |
| `map_search` public kind/filter/sort boundary | PARTIAL EXACT_CONTRACT | R8-014 restores exactly eight public kinds, original kind-owned query fields and generic keyword predicate. R8-017 removes speculative Monster-distance, City-shield and Railway-quality SQL and fails closed on those authentic but unrecovered sort branches | Preserve the original frontend vocabulary; recover those expressions and complete native multi-sort assembly only from stronger evidence |
| Scheduled Plunder control plane/UI | EXACT_BYTES-derived frontend + EXACT_CONTRACT/EQUIVALENT persistence | R8-016 restores original result tab, wrappers/locales, five list/schedule/cancel commands, durable jobs/history and two change events; protected robbery workers remain absent | Preserve recovered control plane; recover actual action execution only from permitted reference evidence |
| `game_asset_image` | EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION | R8-044 restores native optional-string/trim/XOR admission, exact errors, cache-before-connect behavior, one 15-second `getAssetImage` request, PNG signature validation, exact `{dataUrl}` output and native-style disk-cache structure/cadence/size boundary | Byte-close the native SHA-256 cache-key preimage and preserve the retained current-client transport as equivalent plumbing until original transport parity is established |
| Secret Task Quick Find product feature | DEVIATION REMOVED | Added by R7-151 from a current-game native finder; R8-002 removes the product surface because it is not established as LWBridge 0.3.1 behavior | Restore only if reference evidence proves it exists |
| Map scan request envelope | EXACT_CONTRACT | `startMapScan` fields, accepted gate, selected types, normal/fast concurrency recovered | Preserve; recover remaining handler internals |
| Original Map scan algorithm | PARTIAL EXACT_CONTRACT / current scanner LIVE-WORKING EQUIVALENT_REIMPLEMENTATION | R8-076 closes the original host ingestion architecture: exact Map event family; `map.records` run-scoped batches with `{type,payload}` entries filtered by the original eight kinds/current selection, normalized and transactionally written to `scan_records`; separate `map.native.capture` events normalize into published `map_records`. Earlier R6 work already closes 20-tile block cardinality, progress/completion, record identity/scalars and publication. R8-077 proves `pendingAcks` is only one input to reported `nativePendingRecords` in the host complete-event path; the host does not parse `acks[]` items or use that aggregate as a terminal admission predicate. R8-078 proves diagnostics are log-only and separates producer `map.scan.error` (failed run + staging discard + native Stop) from post-complete host failure (failed run + staged-row preservation + no redundant Stop). R8-079 proves both verified proxy native-capture serializers emit literal `acks=[]` and `pendingAcks=0`, while the four point/march/removal pending counts are dynamic. R8-080 proves the outer proxy pump uses `GetTickCount64` and a fixed 50 ms minimum elapsed-time gate for `XluaBridgeMapScanTick`, with no Normal/Fast branch at that gate. Current v21 movement/AOI traversal is not claimed original | Recover protected game-side block order/coordinates, per-tick work/request pacing inside `XluaBridgeMapScanTick`, any separate script/tick acknowledgement semantics, native queue→batch timing/retries, plus remaining per-kind serializer semantics; then validate against current Last War without replacing source-backed behavior with R7 assumptions |
| Current-client Manual Normal/Fast execution | LIVE-WORKING / EQUIVALENT_REIMPLEMENTATION | R8-065 fresh same-session current-v21 proof completed Normal and Fast all-eight selection at 2500/2500, 0 failed, 0 unread, with Fast persisted counts unchanged after database reopen; bounded 9-row AOI compatibility was required. R8-066 then proved the normal Release desktop/WebView path through connected lifecycle, rendered Resource selection/Start Reading, production Fast scan, native `map_search`, correlated six-cell Resource row, exit 0 and clean process shutdown | Preserve the current-client compatibility baseline while separately recovering the original acquisition algorithm; do not promote this row to original parity |
| Player City acquisition | EQUIVALENT_REIMPLEMENTATION only | Current/old traversal can find full population, but algorithm is ours | Recover original acquisition path and compare identities/results |
| Resource acquisition | EQUIVALENT_REIMPLEMENTATION only | Current path works but is not original code | Recover original path |
| Monster / Doom Walker acquisition | EQUIVALENT_REIMPLEMENTATION only | Current path works | Recover original path |
| Zombie Boss acquisition | EQUIVALENT_REIMPLEMENTATION only | Dedicated current-client source is ours | Recover original path |
| Truck acquisition | EQUIVALENT_REIMPLEMENTATION / possible deviation | Direct `LWTrainDataManager` route is a current-game optimization | Prove whether original LWBridge used the same source/semantics |
| Railway acquisition | EQUIVALENT_REIMPLEMENTATION / possible deviation | Direct Train-list route is ours | Recover original source/flow |
| Dispatch / Secret Task acquisition + alliance share | PARTIAL EXACT_CONTRACT share / acquisition UNKNOWN | R8-002 removes the wide-FOV acquisition shortcut after completeness failures; acquisition remains unrecovered. R8-074 closes `map_dispatch_share_alliance` authorization-first admission, exact 1..200 row validation, sequential 5-second `shareDispatchTaskToAlliance` calls, `shared=true` success predicate and `{shared,failed,sharedUuids,failedUuids}` aggregate result; live sharing remains fenced | Recover original acquisition; keep alliance sharing fenced until original authorization admission and protected delivery can be represented exactly |
| Ghost Ops acquisition | EQUIVALENT_REIMPLEMENTATION only | Current full-world path is ours | Recover original implementation |
| Treasure/Supplies read paths | PARTIAL EXACT_CONTRACT + current status DEVIATION | R8-055 closes native `map_treasure_claim_status`: authorization admission, one `getTreasureClaimStatus` call at 5 seconds, transactional `treasure_claim_states` refresh, then original provider JSON return. Current rebuild has extra host guards, an 8-second inspection path, fixed DTO, and incomplete provider-field/cache-maintenance parity | Preserve recovered cache/store evidence; add a dedicated native-style status route only when the authorization admission can be represented without reconstruction; claim execution remains separate |
| Treasure claim orchestration | EXACT_HOST_CONTRACT / FENCED protected executor | R8-074 closes authorization-first admission, server/scope/default validation, 5-second `getCurrentServerId` preflight, candidate-query ownership, 5-second `claimTreasures` request and ten immediate result counters; scope/lucky/scout executor internals remain protected | Keep live execution fenced until original authorization admission and protected executor semantics can be reproduced without invention |
| Map SQLite/index/query semantics | PARTIAL EXACT_CONTRACT + reimplementation | Large portions of SQL/query/normalization recovered | Finish unresolved branches and compare exact outputs/errors |
| Last War locale lookup | PARTIAL EXACT_CONTRACT + EQUIVALENT_REIMPLEMENTATION | R8-054 confirms native max 200, `TOO_MANY_LOCALE_KEYS`, requested-locale→English→key fallback and locale-loader error vocabulary. Current verified local cache is equivalent plumbing; public command still bypasses unrecovered native authorization-state admission and uses stricter `INVALID_PAYLOAD` branches than native outer defaults | Preserve normal lookup/cache; do not label exact until auth admission and full parser semantics are resolved |
| Marks / Jump / Follow | PARTIAL EXACT_CONTRACT + reimplementation | R8-009 restores the public `server_jump` success envelope; R8-036 restores `server_jump_history_get/set/import`, exact ID normalization, import-if-missing migration and `map-data.db/app_settings` persistence for the retained profile | Continue tying remaining mark/jump/follow branches and connected-state errors to reference |
| Auto Scan frontend scheduler/control plane | EXACT_BYTES-derived + EQUIVALENT public-command wiring | R8-015 restores original persisted Fast/Normal config, due predicate, connected-state effect, sequential target flow, Run Now/return/deadline semantics and Auto card; removes R7 restart/one-shot/Stop policies | Preserve; protected `server_jump` and map acquisition internals remain separate |
| Original bridge pipe / RPC | EXACT_HOST_CONTRACT + LIVE-WORKING equivalent plumbing | R8-075 reconciles R5/R7/R8 evidence: exact pipe identity, framing, hello/hello.ack, authenticated listener/session, command/call envelope, first cmd_1, result envelope, payload.id correlation, queue/byte limits, principal timeouts, reconnect generation/route cleanup; R7-127 live-proves the production read-only RPC path. Public route-online vs heartbeat-connected semantics are closed by R8-043/R8-042 | Recover only the remaining exact wire heartbeat/activity ownership and disconnect/write-failure -> outstanding-call completion mapping; do not regress the live-working equivalent transport |
| `bridge-scripts.dat` plaintext | PARTIAL EXACT_CONTRACT / evidence-limited | Earlier R8 work recovered substantial package/crypto structure, but the remaining LWKE1 field map/AAD is blocked on genuinely new permitted evidence | Keep this lane parked while evidence-limited; resume only when retained runtime behavior requires it and new permitted evidence appears |
| Secure/plain xLua proxy behavior | PARTIAL EXACT_CONTRACT / current game-side dispatcher EQUIVALENT_REIMPLEMENTATION | Many hashes, ABI, crypto and loader facts recovered. R8-075 makes the boundary explicit: current `LWBridge.GamePipeAdapter.dll` + mailbox + `current_overview_bridge.lua` preserves the recovered host wire for live read-only RPC but is not the original secure/plain xLua `XluaBridgeHandlePipeMessage`/provider dispatcher | Recover remaining package/handler/control semantics and original script/provider dispatch ownership without replacing the live-working wire with guesses |
| Profile launcher / multi-hook | PARTIAL EXACT_CONTRACT + reimplementation / delete+enable+create FENCED | R8-029 restores the normal single-profile `profile_list` controller-registry query/public field shape and stable selected-profile state; R8-030 restores `profile_note_set`; R8-031 restores `profile_reorder`; R8-032 restores the fixed-primary `profile_primary_set` guard and native primary uniqueness invariant; R8-034 restores `profile_select`; R8-062 closes exact `profile_delete`; R8-063 closes `profile_enable_set` capacity reconciliation; R8-064 closes `profile_create` entitlement-capacity rejection, 22-char Base64URL identity, next-order `账号 N` naming, exact row defaults, 13-field created-profile result and separate frontend selection, while execution remains fenced on unrecovered entitlement capacity and incomplete post-create runtime cleanup semantics | Audit remaining launcher/proxy ownership one-for-one; do not fabricate capacity or bypass native admission |
| Automation page functions | UI EXACT_BYTES / PARTIAL backend | R8-026 restores the two Resource Automation tasks/config/status/event; R8-027 restores generic `automation_status`; R8-033 restores `red_packet_delay_configure` and `treasure_delay_configure` with exact limits/messages and mirrored runtime config persistence; R8-045 closes `dispatch_assist_state`; R8-048 restores exact read-only `trade_station_catalog`; R8-051 closes `automation_inspect` exact authorization-unavailable/disconnect/`inspectAutomationTask`/5-second boundaries and proves a special `allianceGarrison` allies rewrite, but leaves implementation fenced on owner-excluded role/request state and incomplete rewrite semantics; R8-060 closes `construction_rewards_claim` selected-profile injection, authorization/profile/runtime/disconnect admission, one `claimConstructionRewards` `{}` call with 5-second deadline and raw provider JSON forwarding, while the live claim stays fenced on excluded authorization-state admission; R8-068/069 close generic and Chat Automation live command boundaries; R8-071 closes Dispatch Assist Schedule/Cancel/Retry live-provider, DB-mutation, event and validation boundaries while keeping execution fenced; R8-073 closes Resource Automation Run and `trade_station_configure` exact task/state/provider/config boundaries while keeping execution fenced | Keep Resource Run, `trade_station_configure`, Construction Rewards claiming, Dispatch Assist state/actions, and other provider-backed actions fenced until their original live dependencies, including authorization state, can be reproduced from evidence without invented provider data |
| Squads / AFK | UI EXACT_BYTES / PARTIAL backend | R8-023 restores equipment config get/save; R8-024 restores Monster AFK save validation/persistence/status reload; R8-025 restores Alliance Garrison save validation, `/tasks/allianceGarrison` persistence, sibling preservation and status reload; R8-046 restores exact `squad_list`; R8-047 restores exact `monster_catalog_options`; R8-073 closes `equipment_preset_apply` through preset admission, live `getSquads`, `applyHeroEquipment`, progress-event and generic-result boundaries while execution remains fenced on authorization state plus incomplete multi-hero planner/aggregation semantics | Recover remaining offline squad/config surfaces; keep live AFK/garrison/equipment actions fenced until evidence-backed providers/protocols exist |
| City Layout | UI EXACT_BYTES / PARTIAL backend | R8-018 restores exact draft table/key/revision semantics and production draft get/save/clear. R8-050 closes the snapshot host boundary but fences implementation on an owner-excluded authorization-derived request. R8-052 closes exact `city_layout_apply_status` auth-unavailable/disconnect/`getCityLayoutApplyStatus`/5-second/generic-result behavior but fences runtime status on native auth-state admission. R8-070 closes exact `city_layout_validate`, `city_layout_apply_start`, and `city_layout_apply_cancel` handlers, payloads, fresh-snapshot/in-city gate, providers, deadlines and generic result handling; all remain fenced because mandatory authorization-state admission is unrecovered | Preserve draft contract; recover the original authorization admission from evidence and keep live City Layout actions fenced until that and the required planner/executor behavior are source-backed |
| Hotkeys | UI EXACT_BYTES / PARTIAL backend | R8-019 restores the original ten-field config object/defaults, `hotkey_config_get/save`, exact config error vocabulary and per-profile runtime persistence | Preserve config parity; recover keyboard hooks and in-game actions only from native/runtime evidence |
| Mini-games | UI exact-derived / PARTIAL EXACT_CONTRACT backend | R8-059 closes `monopoly_cell_open` through wrapper-injected selected profile, historically owner-excluded, now unrecovered authorization-state admission, profile/runtime/disconnect errors, one `openMonopolyCell` `{}` call with 5-second deadline and raw provider JSON forwarding; runtime remains fenced rather than bypassing native admission | Recover remaining connected Mini-game functions; validate live provider actions only when their native admission can be represented without invented auth reconstruction |
| Settings | PARTIAL backend / original UI contract recovered | R8-020 restores visual-metrics config; R8-021 restores updater idle status; R8-022 recovers feedback result/progress schemas and `exportId` validation; R8-028 restores `profile_settings_save`; R8-035 restores native `set_window_theme`; R8-037 restores `append_log`; R8-053 closes `profile_settings_get`; R8-056 closes `watermark_lookup` trace-code normalization, `/api/watermark/lookup` request ownership and raw service JSON return, while runtime remains fenced at the unrecovered authorization-state boundary; R8-057 closes the exact read-only `update_status` nine-field serializer and seven-phase transition machine, but the rebuild only has the exact idle snapshot because real check/download/open state ownership is still fenced | Recover remaining profile registry/settings behavior, privacy/redaction/cache/archive lifecycle, updater check/download/open execution semantics, and exact log rotation/segment retention; keep standalone account-product settings non-priority unless retained behavior requires them |
| Whole-program one-to-one release | NOT READY | R8-065 freshly live-proves the retained Home lifecycle and core Manual Map scan on current-v21; R8-066 further proves the actual production desktop/WebView Resource scan/search/render path. Many retained reference surfaces remain PARTIAL/UNKNOWN/FENCED and original Map acquisition is only PARTIAL EXACT_CONTRACT | Close every required retained DEVIATION/UNKNOWN and live-prove the final parity product |

## Immediate retained-scope sequence

1. Preserve and fingerprint the reference and every recovered embedded asset.
2. Keep the R8-017 fail-closed boundary for Monster distance, City shield and Railway quality until stronger reference evidence recovers their native expressions and remaining multi-sort assembly.
3. Continue protected Map acquisition/travel recovery only when reference evidence supports the exact behavior.
4. Continue retained whole-program parity across Automation, Squads/AFK, City Layout, Hotkeys, Mini-games and Settings.
5. Keep the evidence-limited package-key lane parked until genuinely new permitted evidence exists; only recover account/auth/session dependencies from it when a retained feature actually requires them.
6. Perform reference-vs-rebuild UI/behavior comparisons and current-client live validation.

No feature is considered complete merely because our current implementation works.
