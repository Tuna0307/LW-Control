> Historical snapshot before PM review 13. Follow the current audit and handoff for active work.

# Project-manager checkpoint — review 12

Reviewed 2026-09-10 against `749b8e3bdca0ed92f7d22aae0f1ab0c1d562a4cd`, branch `research/offline-controller`. Two implementation commits follow PM delivery `58d607b`. Worktree clean at entry. [Review 11 with contributor follow-ups](../reviews/2026-09-10-review-11-and-followups.md) is historical.

## Decision for the owner

**Accept R7-003's current-client route for the bounded acquisition portion of PM10-01. Original LWBridge pipe parity is not required for that portion.** The recorded evidence contains two distinct app-generated requests with fresh captures at 05:14:45Z and 05:15:09Z; each returns server 2212, point 32482 at 481,32, level 3. Both results reach the normal store/search path and summary reports one resource. PM checked the saved evidence, source and result correlations; PM did not perform a third live acquisition.

**PM10-01 remains PARTIAL**, because normal Map Data display/interaction is not demonstrated and the audit found restoration/lifecycle defects. The source is no longer just saved replay, but the route is an on-demand current-view read, not an always-connected bridge or a full-world scan. Both accepted historical runs stopped the game to restore its files; the final probe-online flag is false. Do not promote it to working Overview launch/reconnect, a persistent connection, full scan completion or all 47 acceptance cases.

The PM route decision is now made. Regular AI is authorized to continue PM12-A through PM12-D below without asking for the same decision again. No blanket PM pause after each checkpoint. Each completed slice still needs evidence, checks and GitHub delivery; final feature acceptance and specialist assignment remain separate decisions.

## Accepted implementation

- `f75e9bb` / R5-007: source-attributed frame/proxy-hello slice and codec/parser with passing offline checks. Original ack/readiness/request-result grammar remains unresolved. This audit did not rerun original binary analysis.
- `749b8e3` / R7-003: normal-window service wiring and a separate executable proof runner; resource-only read, request/state/route checks, normal-store import/search and policy summary. Two saved helper/result/status/search chains agree. Probe source bytes match the candidate hash in both records. Existing foreign-result test passes but does not test mutable-file races or cross-process ownership.
- Current installed Lua, xLua and Assembly-CSharp hashes still match the recorded baseline. This is a fresh read-only check, not proof of every restoration failure path. Both successful historical runs required stopping the game after a sharing/access error; retain that limitation.

## Required fixes — current ordered queue

| ID / priority | Audited problem and locator | Required result |
|---|---|---|
| **PM12-A / P1 / first** | `tools/run_live_resource_probe.py:410` calls the three-file install before setting `installed=True`. A second/third replacement failure skips rollback. PM reproduced both cases on dummy files: data or data+metadata remained changed, with zero restore calls. The helper also uses global shared files without a process-wide lease, hardcodes installation paths and stops all `LastWar.exe` processes by image name (`:215`). | Make partial installation/restoration recoverable from the first write; preserve and verify exact backups of all three files. Serialize access across app/helper instances and identify the intended installation/session/PID before close/restart. Prefer normal close before any scoped fallback. Demonstrate fault recovery and concurrent-instance rejection on isolated targets before further installed-file live tests. |
| **PM12-B / P1** | `LiveResourceProbeCommandService.cs:92` waits on the gate before checking `isReading`, so duplicate starts queue rather than reject. It handles no Stop; backend `map_scan_stop` returns the old unavailable fallback. After helper start, cancellation is ignored and data can be persisted after cancellation; window close disposes the store while work continues. `WaitForExitAsync` is unbounded, while the UI normally times out after 30 seconds and the helper allows 120. | One coherent operation lifecycle: reject duplicate starts, truthful Stop/cancelling/restoring state, no late publication for cancelled/closed sessions, restoration allowed to finish safely, bounded supervised failure handling and disposal coordinated with cleanup. Validate delayed/hung fake helpers, cancellation and close without running the game. Do not solve the issue by killing a helper mid-restoration or merely increasing every timeout. |
| **PM12-C / P1** | `ImportCorrelatedResult` validates one file read then the importer hashes/reopens the shared result file (`LiveResourceProbeCommandService.cs:147–167`, `FirstLiveResultImporter.cs:51–54`). A replacement between reads can invalidate the before-persistence guarantee. The reuse branch trusts game-image presence plus heartbeat version/age before a fresh fingerprint/session check. | Validate, hash and ingest the same immutable result bytes; isolate outputs per request and verify actual session/profile/server/build/freshness scope as supported by the chosen route. Preserve unknown optional fields instead of zero substitution. Add replacement-between-reads, wrong-session/server and stale reuse regressions; accepted historical results stay valid within their limits. |
| **PM12-D / P2 / user-visible result** | `--live-resource-proof` exits through a backend-only path before creating a window. The committed proof therefore contains no normal-window interaction. Fresh records omit gather state; the original resource renderer infers Idle from absent gather identifiers, while the unknown-status override runs only in replay mode. | Verify normal Start Scan -> response -> summary/query -> displayed resource through the actual UI path, including refresh and a second read. Show unknown occupancy honestly and do not label a one-view read as a full Normal/Fast world scan. Keep `xluaOnline` truthful. Use permitted UI capabilities only; preserve earlier capture restrictions and do independent supported fixes if visual access is unavailable. |

Post-review `LWB-PM12-004` closes the false-Idle implementation defect in PM12-D without claiming live display proof. The recovered frontend's ordinary Idle/Gathering formatter is preserved, while bounded imported resource rows explicitly mark occupancy unknown and render `—`; the replay-only DOM mutation is removed. Deterministic checks plus all 35 browser checks pass. Native computer APIs remain disabled, so normal-window Start Scan, second refresh and screenshot/interaction evidence are still open. [Evidence](../../evidence/lwbridge-implementation/2026-09-10-pm12-resource-occupancy-unknown.json).

Post-review `LWB-PM12-005` is a **LIVE-PROVEN helper/source-boundary failure finding**, not a completed PM12-A/C fix. From clean process state, the rebuilt `--live-resource-proof` path launched the selected official client and produced request `cb854633e81043e4992289fe6ef77da3` at `2026-09-10T07:17:21Z`, with selected profile `local-bd7b2533b1a146bca0aba9efb193a4a2`, generated launch session `28d5dc84326540b7a8e42fceb159042d`, exact game PID `42332`, and source-backed server-2212 resource point `32482` at `481,32`, level `3`. Exact restoration then failed while that owned PID remained open: the helper retained `restoring_after_failure`, the installed data/metadata still matched the temporary candidate, and the desktop received no successful helper return or first imported result. A one-time exact-PID normal close was used only to recover the already-armed backup; `recover_pending(paths())` restored all three original hashes and cleared recovery state. This proves the live identity chain reaches the request-owned source result and proves the remaining production dependency: a permitted scoped normal-close primitive for the exact helper-owned PID before retrying restoration. The previously denied process-control source patch was not repeated or rerouted. [Evidence](../../evidence/lwbridge-implementation/2026-09-10-pm12-live-restoration-blocker.json).

Post-review `LWB-PM12-006` closes that bounded PM12-A/C dependency. Production now implements the independently observed exact-PID `Process.CloseMainWindow()` behavior as **IMPLEMENTATION POLICY**, with immediate selected-path/PID revalidation, a bounded 10-second normal-exit wait and no force termination. The final helper bytes passed isolated close/identity/recovery regressions and Release build. A fresh two-read `--live-resource-proof` then completed with requests `ab30032ceb5a410391fd666888ab7f34` and `17e85a3099504ce7a6001604bc8b37e0`, owned PIDs `41972` and `34012`, and capture times `07:39:22Z` / `07:39:50Z`; each read produced the server-2212 point `32482` at `481,32`, level `3`, normally closed its exact PID, restored all original script hashes and reached desktop immutable tuple validation/import plus normal search. No game/launcher/helper process remained. PM12-A/C are **LIVE-PROVEN for this bounded resource route**; PM12-D normal-window Start Scan/render/refresh evidence remains open because native desktop control is unavailable. [Evidence](../../evidence/lwbridge-implementation/2026-09-10-pm12-scoped-close-restored-import.json).

Post-review `LWB-PM12-007` resolves the bounded route's unsupported Idle ambiguity when current source fields are readable. The original LWBridge resource row uses `gatherMarchUuid`/`gatherUid` presence to choose Gathering versus Idle; current build-1078 `ResPointInfo` owns those fields. Probe version 2 reflectively reads both fields from the typed resource object, marks occupancy known only when both reads succeed, and the importer/frontend use a separate rebuild boolean so incomplete/older captures still show `—`. Two final-byte live reads at `09:37:53Z` and `09:38:19Z` each returned point `32482` from `ResPointInfo` with `gatherOccupancyKnown=true` and `gatherOccupied=false`; immutable import preserved known idle, both exact owned PIDs exited through the accepted normal-close path, and all original script hashes restored. This is **LIVE-PROVEN idle state for the observed point**, not a live Gathering observation. Resource naming and normal-window interaction/screenshot proof remain open. [Evidence](../../evidence/lwbridge-implementation/2026-09-10-pm12-resource-occupancy-source.json).

Post-review `LWB-PM12-008` prepares the remaining PM12-D normal-window proof as **IMPLEMENTED/OFFLINE-TESTED** without weakening its acceptance gate. A diagnostic desktop mode now opens the persistent Map Data page, drives its actual recovered manual controls to resource-only, invokes Start Reading twice, waits for distinct fresh bounded acquisitions, requires the Resource row to render and captures two WebView screenshots plus correlated status/row text. The recovered original type order proves the positional resource selection, and the existing bounded service remains unchanged. Release build, deterministic desktop checks with real-config preservation, recovered frontend integrity, 35 browser checks, preference scenarios and both transport checks pass. The prior live proof launch was rejected by automatic approval review before execution, so no screenshot or normal-window LIVE-PROVEN result is claimed. Static resource-name work also stops at two separately recovered endpoints—raw `resourceName` in the original builder and public/query `resourceNameKey`—with no proven numeric `resourceTypeId` mapping; `Unknown resource` remains truthful. [Evidence](../../evidence/lwbridge-implementation/2026-09-10-pm12-normal-ui-proof-harness.json).

These are actual reviewed defects/gaps, not hypothetical permission requirements. [The dummy-file reproduction](../../evidence/lwbridge-implementation/2026-09-10-pm12-partial-install-repro.py) imports the helper but replaces fingerprint/candidate/process operations; it never reads or modifies installed game data. Its output records the known defect, not a passing restoration test. Keep the history and add real passing regressions with the fix.

## Specialist decision — ESC-005

**NOT_ASSIGNED / deferred original-pipe parity.** The R5-007 packet is more specific and its supplied attempts/alternatives were reviewed. R7-003 removes the ack/request grammar from the immediate bounded acquisition dependency. The urgent work above is regular implementation/verification, not specialist reverse engineering. The proposed metadata method still must exclude SB-87/88/90 denied range/search/pointer operations; calling a pass metadata-only is not blanket clearance for structurally referenced ranges. Preserve the packet and unresolved contracts; do not request the same generic fields again. Reopen only for a concrete feature dependency and clearly bounded permitted method. No Daybreak task is approved or dispatched.

## Verification and delivery limits

Fresh Release build: zero warnings/errors. Deterministic checks with real-config preservation: six groups true, no failures. Frontend generation/hash integrity, five preference scenarios, both transport checks and Python compilation passed. The independent dummy-file fault probe reproduced PM12-A despite those existing green checks. No game was launched, no new candidate package built, no native UI/capture operation or restricted binary/history investigation was repeated. Source/result evidence checks and three installed-file hashes matched. All 47 acceptance cases remain unchanged and unsigned as full cases.

[Review evidence](../../evidence/lwbridge-implementation/2026-09-10-pm-review-12.json) records checks, correlations and known failures. This audit checkpoint intentionally preserves the reported implementation defects; it is not a release or a claim that PM10-01 is complete.
