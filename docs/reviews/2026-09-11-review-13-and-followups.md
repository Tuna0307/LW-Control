> Historical review and contributor follow-ups. Superseded by review 14; historical references to PM clearance do not grant authority to override environment restrictions.

# Project-manager checkpoint — review 13

Reviewed 2026-09-10 against `b16fb9a3a8948b7ec54c7ae701a7305a0a813c16`, branch `research/offline-controller`, clean at entry. This review covers the eight implementation commits since PM delivery `6fc9586`. [Review 12 and contributor follow-ups](../reviews/2026-09-10-review-12-and-followups.md) are historical.

## Result for the owner

**Post-review regular checkpoints (`LWB-PM13-001` / `001B` / `002` / `003` / `004` / `005` / `006` / `007`, 2026-09-10–11):** PM13-01 is now closed. The code/deterministic repair makes `map_summary` seed browsing only from exactly one published server in the selected profile, labelled `saved_profile_index` with phase `unavailable`; zero/multiple-server states remain explicit. The exact review-13 config/database was then rediscovered under the OpenAI Codex package `LocalCache\Local\LWBridgeRebuild` mirror, with profile `local-bd7b2533b1a146bca0aba9efb193a4a2`, DB SHA-256 `29ddb3f162b45d2899d8e89f7241f568b5099f4a8e1aafc36460fcc0ae5c4ff6`, and the expected single server-2212 resource row. For bounded ordinary-window verification, those exact bytes were copied to the direct desktop default root, the Release app showed Resource count 1 and row `481,32 / unknown resource / level 3 / idle` through explicit Search, the app closed normally, and a second normal session reproduced the same row after restart. The temporary direct-root files were then removed and the pre-existing Presentation data preserved. No fixture row or new acquisition was used. `LWB-PM13-002` closes PM13-01b at IMPLEMENTED/OFFLINE-TESTED scope, and `LWB-PM13-003` closes PM13-03 at the same scope: result validation is separated from persistence, `reading -> committing` is the atomic completion/cancel decision, and helper cleanup ownership is reserved before process start. Deterministic Stop/Close race barriers pass. `LWB-PM13-004` closes PM13-02 at IMPLEMENTED/OFFLINE-TESTED scope by binding each acquisition to an explicit Resource Search request/result and exact rendered row, including same-point freshness rejection. `LWB-PM13-005` additionally revalidates the ordinary rebuilt window for unsupported all-category Start and missing saved context, with no game/helper launch and exact test-root cleanup. `LWB-PM13-006` reproduces and fixes a proof-only false negative: the recovered coordinate td contains `481,32\nJump`, so the proof now extracts the coordinate span instead of comparing the whole td. Its ordinary-window saved-reopen verifier is proven against the exact historical server-2212 row with profile/query/render/time correlation and cleanup. The fresh resource-only two-read normal-window acquisition itself remains blocked under SB-97. `LWB-PM13-007` clarifies that the Computer Use skill is installed locally, but this chat session does not expose its required `node_repl` tool bridge, so the PM's supported native sky path is not callable here. PM13-04 therefore remains ACTIVE/BLOCKED. See `evidence/lwbridge-implementation/2026-09-10-pm13-saved-target-rediscovered.json` and `evidence/lwbridge-implementation/2026-09-11-pm13-normal-window-gate.json`.

The work is useful implementation, including two recorded real resource acquisitions, exact file restoration/import, the now-fixed saved-row reopen path, and closed bounded-route cancellation/ownership races. It is **not yet a usable completed resource scan/search function** because PM13-04 remains open. Review 13 originally observed zero Resource Search rows after reopening despite one real server-2212 row; `LWB-PM13-001/001B` now closes that specific defect. Monster Search still has no saved records, and the live adapter explicitly supports resources only.

The user's new instruction is mandatory: finish one user-visible function, diagnose and fix its failures, and verify it before moving to another. **Current function: resource Start Scan -> stored result -> normal Resource Search/display -> fresh refresh and reopen. Next function: monster scan/search.** The monster example establishes the next priority, without abandoning the resource function already in progress. If a real external restriction prevents closure, retain the same active function and record the exact condition; continue only its permitted prerequisite work, not unrelated checkpoint production.

## Accepted progress and limits

- PM12-A: accepted for the bounded resource route. Fresh isolated regressions passed for every partial install/restore stage, recovery ownership, cross-process lease and exact-path/PID normal close. Saved PM12-006/007 evidence documents restored imports. This is not proof of all live failure paths.
- PM12-C: accepted for the bounded immutable-byte/profile/session/PID correlation slice. PM reproduced saved proof/source SHA matching and both request/result/search correlations; the current profile still contains the saved row. No new acquisition was performed in this audit.
- PM12-B: **bounded-route lifecycle closure is restored by `LWB-PM13-003`**. Existing fake-helper cases still pass, and new deterministic barriers cover Stop/Close before commit, Stop after the commit decision, and Close during helper process start/registration. This is offline lifecycle proof, not a new live acquisition.
- PM12-D: remains open for the live normal-window result. `LWB-PM13-004` hardens the proof harness offline; `LWB-PM13-005` confirms current normal-window feedback/missing-context behavior without launching the helper/game; `LWB-PM13-006` corrects the real coordinate-button predicate and proves the follow-on saved-reopen verifier against the exact historical row. The previously denied live acquisition harness was not replayed or rerouted.
- Source-backed idle occupancy is credited for the observed point. Live Gathering, resource-name mapping, full-world coverage, Normal/Fast differences, persistent bridge/Overview lifecycle and other categories remain unproven or unimplemented. The uncommitted ResourceType enum reported in chat is a lead, not accepted mapping evidence.
- Full `task.md` scope remains all 47 cases; no full case is newly signed off by this audit.

## Audited defects and ordered work within the current function

| ID | Finding and evidence | Required result |
|---|---|---|
| **PM13-01 / P1 / first** | Native UI reproduction: reopen normal app -> Map Data -> Resource -> Search shows 0/no saved data; read-only SQLite inspection finds one resource row in the same selected profile. `LiveResourceProbeCommandService.currentServerId` starts null; `LWBridgeBackend.map_summary` requires that ephemeral value and otherwise raises `MAP_INDEX_UNAVAILABLE`. Exact frontend request/error propagation still needs tracing. | Restore a source-attributed **saved-data browsing context**, distinct from current live server/readiness. Show the existing row on reopen, its saved/fresh status and correct scope. Never label a persisted server as live, silently choose across profiles/servers, or rescan just to hide this defect. Add restart, multiple-server/profile and unavailable-context tests, then verify ordinary Resource Search. |
| **PM13-01b / P2 — CLOSED IMPLEMENTED/OFFLINE-TESTED** | `LWB-PM13-002` traces the generic feedback to Start stringifying the structured error before the recovered formatter could use its code; search failures were log-only. The rebuild now preserves `LIVE_RESOURCE_TYPES_UNSUPPORTED`, adds nine-language Resource-only guidance, keeps no-records as an empty state, and separates missing-context/query failures. | Browser regression passes all four states without helper/game launch or category fallback. Connected normal-window revalidation is retained under PM13-04 rather than promoted to live proof. |
| **PM13-02 / P1 — CLOSED IMPLEMENTED/OFFLINE-TESTED** | `LWB-PM13-004` replaces the old nonempty-`<tr>` predicate. The harness waits for Resource to settle, clicks the real `.map-searchbar > button`, records the native `map_search` request ID/payload/result, revalidates the immutable acquisition tuple, and requires exact server/recordKey/pointIndex/x/y/level/updatedAt agreement before matching the five rendered cells. | Deterministic and Edge/Playwright cases reject loading, empty, unrelated stale, same-point older timestamp, stale same-point query result, wrong server and missing request ID; the exact correlated row passes. Screenshot directories are created before capture. No fresh game/helper run was performed; PM13-04 remains the live final gate. |
| **PM13-03 / P1 — CLOSED IMPLEMENTED/OFFLINE-TESTED** | `LWB-PM13-003` separates immutable result validation from SQLite mutation. Under `stateGate`, only the active open `reading` operation can transition to `committing`; cancellation/Close winning first prevents persistence, while a later Stop/Close cannot retroactively cancel the winning commit. `helperStartPending` reserves cleanup ownership before `Process.Start`, then ownership transfers to `activeHelperProcess`. | Deterministic barriers pass for Stop-before-commit, Close-before-commit, Stop-after-commit-decision, and Close after process start but before active registration. Existing timeout/duplicate/active-close and PM12 recovery/session/scoped-close regressions remain green. No live-game race was injected. |
| **PM13-04 / ACTIVE/BLOCKED final gate** | `LWB-PM13-005` verifies repaired unsupported-Start/missing-context feedback in the ordinary rebuilt Map Data window without starting game/helper. `LWB-PM13-006` fixes the proof's real coordinate-button shape and adds a repeatable saved-reopen verifier, proven against the exact historical server-2212 row; temporary direct-desktop test data was removed while Presentation was preserved. `LWB-PM13-007` confirms the bundled Computer Use skill is present locally (version-directory `26.903.71938`) but this chat lacks its required `node_repl` bridge; Remote Desktop Commander remains filesystem/terminal/process only here. SB-97 remains the rejected fresh-live operation, and the supported skill explicitly forbids helper/custom-protocol substitution. | Obtain the permitted fresh resource-only two-read/display/refresh proof with exact source/session/storage/query/render and cleanup evidence, then run the ready saved-reopen verifier. Do not reroute SB-97 through terminal/WebView automation. Required external condition: approved native Computer Use for a permitted new attempt, or explicit platform/PM clearance for a non-rerouted attempt. |

These are parts of the **same resource function**, not four invitations to switch features. PM13-01 is first because it exposes an already-acquired result without a new game mutation. Fix the lifecycle/proof gates before a further live acquisition. Resource naming research may continue only when its missing mapping blocks an explicit acceptance item; unknown naming alone does not justify hiding known coordinates/level/time.

## Native Computer Use audit

PM read the installed Computer Use skill and its guidance/API, imported `@oai/sky` through `mcp__node_repl__js`, and successfully listed, launched, observed and clicked the actual rebuilt app. This is a separate available native capability; the standard AI's browser-only CUA limitation remains true of that particular tool/environment. Future AIs must inspect their actual available tools, not copy a blanket 'native APIs disabled' conclusion.

Performed: default normal app launch, Overview observation, Map Data navigation, unsupported all-category Start, Monster Search, Resource Search. No fixture/replay mode was used for these observations. No game was launched, no game files modified, and no rejected live acquisition/binary operation rerouted. The unsupported Start is a distinct pre-helper rejection test. The normal app was closed after inspection.

Durable [native screenshots and observations, source hashes, correlations and reproduction scripts](../../evidence/lwbridge-implementation/pm-review-13/README.md) distinguish direct UI observation, saved historical live evidence, source-reviewed races and synthetic counterexamples.

## Verification, assignment and delivery

Fresh Release build: 0 warnings/errors. Desktop checks: all six groups true, failures empty, real config unchanged. Three isolated PM12 recovery/session/scoped-close scripts passed. Frontend recovered-hash regeneration, five preference scenarios, both transport checks and all 35 browser checks passed. `tools/check_pm13_resource_proof.cjs` now passes five DOM cases and confirms empty/loading/unrelated-stale/same-point-stale rejection plus an exact fresh row; deterministic C# cases also reject stale query identity/time and missing request identity. Global Playwright was resolved through the existing installed Edge setup; no repository dependency changed.

No Daybreak assignment. PM13 issues are regular-AI implementation/verification. ESC-005 remains NOT_ASSIGNED/deferred original-pipe parity. Any future monster blocker needs a concrete source question and attempted permitted methods before specialist review; no denied operation is transferred to another model.

This is an audit checkpoint preserving known defects, not a product release. Follow [the updated regular handoff and reusable prompts](../implementation-handoff.md). Commit/push this coherent audit and verify the existing GitHub branch; retain separate feature acceptance gates.
