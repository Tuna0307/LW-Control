# R8-065 - fresh live Home and Map current-v21 acceptance

**Date:** 2026-09-26
**Reference:** verified LWBridge 0.3.1 plus the installed current Last War client
**Scope:** apply the owner's live-working acceptance rule to Home and Map, repair the current-v21 conservative AOI compatibility regression found by that gate, and preserve strict-parity distinctions.

## Live-working rule

For project progress, "working" now means freshly verified against the real installed game. Contract recovery, offline tests, UI completion, or a fenced implementation do not count as working.

This checkpoint does not convert current-client compatibility code into original LWBridge parity. It records two separate facts:

- whether Home/Map actually operate against the live game now;
- whether the implementation is original/exact, equivalent, deviating, fenced, or unknown.

## Fresh Home proof

The current Release build ran the dedicated live Home lifecycle proof:

`dotnet run --project tests\LWBridge.Desktop.Checks\LWBridge.Desktop.Checks.csproj -c Release --no-build -- --live-overview-home-proof`

It launched the real game manually, reached `connectionState="connected"`, closed it, then repeated through startup auto-launch and again reached `connected` before clean shutdown.Observed result:

```json
{"diagnosticOk":true,"proof":"home_overview_v17_manual_and_auto_launch","results":[{"mode":"manual","instanceId":"bc08fa6fb27c4ee98061ce2760e44fbc","pid":41176,"connectionState":"connected"},{"mode":"auto-start","instanceId":"fa6811ba50c4423981401089ab9be964","pid":24644,"connectionState":"connected"}]}
```

The proof exited 0. Under the owner's acceptance rule, the retained Home launch/connect/close and startup-auto-launch/connect/close lifecycle is **LIVE-WORKING** on this current client.

## Map live failure found by the new gate

The first fresh all-eight Map proof correctly failed instead of being counted as working. On server 2212, the current-v21 client returned a contiguous 3 x 9 AOI footprint at target `(665,875)`:

- rows 80 through 88;
- columns 65 through 67;
- 27 native current-set AOIs.

The conservative scanner still required exactly ten rows, so Normal all-eight stopped with 218 failed blocks and 2281 unread blocks.

A targeted live probe ten tiles lower at `(665,885)` exposed a contiguous 3 x 10 footprint covering rows 81 through 90. The diagnostic itself rejected that sample because the camera-restoration proof did not settle, but the copied native AOI set established the current-v21 vertical geometry needed for the compatibility correction.## Compatibility correction

The production scanner remains on the conservative complete-coverage path. R8-065 does **not** restore the removed R7-151 wide-FOV shortcut.

The correction:

- accepts only contiguous rectangular 9- or 10-row footprints;
- preserves the existing contiguous 2-5-column bound;
- permits at most the observed one-row downward shift relative to the ten-row logical band;
- when only the band's final AOI row remains missing for the active column, nudges the target exactly one 10-tile AOI row downward;
- unions that bounded footprint into the same coverage set;
- still requires exact 10,000-AOI union before full-world acquisition succeeds.

The live proof harness was also corrected to the recovered R8-013 status lifecycle: successful publication returns to `phase="idle"`; it no longer incorrectly requires a stale terminal `completed` phase.

## Final Map live acceptance

The same all-eight proof was rerun from a clean stopped-game state:

`dotnet run --project tests\LWBridge.Desktop.Checks\LWBridge.Desktop.Checks.csproj -c Release --no-build -- --live-current-client-all-eight-modes`

The proof used current client content version 21, game SHA-256 `905c98c1f89841f90b492556192ba0642f3d209a873cb8c1f7b3c340aca0733d`, and owned session `5ec8c8bc97ab4427bef410dff6e0d2b7`.

Normal completed 2500/2500 blocks with 0 failed and 0 unread. Fast completed 2500/2500 with 0 failed and 0 unread. The published Fast counts were identical after the SQLite database was closed and reopened.Final live result:

```json
{"ok":true,"proof":"same_owned_session_all_eight_normal_then_fast","normal":{"scanMode":"normal","concurrency":8,"totalBlocks":2500,"readBlocks":2500,"failedBlocks":0,"unreadBlocks":0,"scanWallSeconds":118.1438987,"counts":{"city":6732,"resource":934,"monster":2927,"truck":210,"railway":2,"dispatch":159,"ghost":0,"treasure":0}},"fast":{"scanMode":"fast","concurrency":20,"totalBlocks":2500,"readBlocks":2500,"failedBlocks":0,"unreadBlocks":0,"scanWallSeconds":112.943038,"counts":{"city":6732,"resource":1932,"monster":4087,"truck":249,"railway":2,"dispatch":290,"ghost":0,"treasure":7},"reopenedCounts":{"city":6732,"resource":1932,"monster":4087,"truck":249,"railway":2,"dispatch":290,"ghost":0,"treasure":7}}}
```

Exit code was 0.

## Regression gate

After the live proof:

- Desktop Release build: 0 warnings / 0 errors;
- Checks Release build: 0 warnings / 0 errors;
- deterministic suite: `ok=true`, `failures=[]`;
- recovered frontend hash/build check: passed;
- `git diff --check`: passed.

## Classification

- Home retained launch/connect/close lifecycle on current client: **LIVE-WORKING / EQUIVALENT_REIMPLEMENTATION**.
- Manual Map all-eight Normal/Fast scan on current client: **LIVE-WORKING / EQUIVALENT_REIMPLEMENTATION**.
- Original LWBridge Map acquisition algorithm: **still UNKNOWN / not recovered**.
- Whole-program one-to-one parity: **NOT COMPLETE**.

The live-working label must not be generalized to untested Map actions such as Plunder/Treasure claim execution or to other tabs.