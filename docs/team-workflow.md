# Team workflow — shared Map Data scan engine first

**Current owner Zombie Boss countdown follow-up, 2026-09-18:** `LWB-R7-029` fixes the remaining presentation defect: Zombie Boss now participates in the existing one-second Map Data clock. Live proof on saved rows, with no rescan, observed three Remaining values decrease by four seconds over 4.2 seconds. Build/tests and exact v18 cleanup pass; Railway/Train remains next.

**Current owner Zombie Boss checkpoint, 2026-09-18:** `LWB-R7-028` is LIVE-PROVEN. Zombie Boss is now a dedicated persisted/queryable scan kind, ordinary Monster no longer performs protection-detail requests, and the desktop Zombie Boss tab preserves localized keyword + <= Level filters across rescans/app restarts. Final live UI runs completed in 18.510 s and 14.594 s consecutively with visible positive Remaining and no filter reset; exact official v18 restoration/compatibility and cleanup pass. Railway/Train remains the next separate category.

**Current owner Monster follow-up, 2026-09-17:** `LWB-R7-017` is the current user-test candidate. The immutable 0.3.1 Monster table was rechecked and has no original Remaining column/level selector; the rebuild therefore keeps Remaining as explicit usability policy sourced only from current-v17 `WorldMarch.zMBossInfo.shieldEndTime`. Current-v17 metadata confirms typed `ZMBossInfo` fields including `shieldHp`, `shieldMaxHp` and `shieldEndTime`; production does not substitute generic `endTime` or `nextStageTime`. The level selector now shows `5,10,15,...` buckets while retaining `level <= selectedMaximum`. Distance is explicitly from the player's authoritative base/home tile through game-owned `WorldScene.TileDistance(homeTile, monsterTile)`. A fresh ordinary Monster run completed 2,500/2,500 with zero failed/unread, published/reopened 12,017 rows, and populated Distance for all 12,017. That population had zero `ZMBossInfo` / shield-deadline rows, so `-` is the truthful Remaining value until a shielded Zombie Boss exists. Release builds, deterministic suite and all 36 browser checks pass. Owner retest is next before returning to Railway/Train.

**Current owner return, 2026-09-16:** the owner resumed Map Data and reported slow/stale Monster progress plus a two-click Stop symptom. `LWB-R7-012` now LIVE-PROVES the current-v17 5x10 footprint and 200-request full-world planner, two complete Monster timings (43.620 s Normal / 70.746 s Fast under different live populations), and one live Stop returning terminal idle. Production scan-status events and `map_summary` now follow the active Manual run. `LWB-OVR-015` remains the current-v17 Home lifecycle prerequisite and compatibility guard.

**Current owner-correction checkpoint, 2026-09-17:** `LWB-R7-014` supersedes the R7-013 result presentation: Monster Remaining is only the Zombie/Boss `zMBossInfo.shieldEndTime`, level means `<= selected`, Level/Distance sorts remain populated, failed queries preserve the table, and current-v17 game-owned `TileDistance` is LIVE-PROVEN for all 12,731 Monster rows in the latest complete run. Zero-hold full-world acquisition restores camera position/FOV in the same callback after queuing each request and still completes exact coverage. `LWB-OVR-016` also reuses exact validated official-settlement evidence so unchanged starts do not repeat the visible warm-up launch/close. Resource was subsequently completed under `LWB-R7-015`; the current category checkpoint is Truck `LWB-R7-016`.

**Current Truck acceptance checkpoint, 2026-09-17:** `LWB-R7-016` LIVE-PROVES Truck-only ordinary Manual Start on current v17: all 2,500 logical blocks completed with zero failures/unread blocks in 71.321 s Normal, 175 live Truck rows published and exactly 175 reopened. Every row in that population carried authoritative Truck type, quality, power, train config/raw train data plus live-proven `arriveTs` and `robTimes` normalization. Counts are dynamic observations. `maxLootCount`, `protectTime`, `isSpecialURQuality`, and UI-ready `currentGoods` remain unproven and are not synthesized. Railway/Train is now the next category on the same scanner; do not create a second acquisition engine.

Latest owner direction, 2026-09-13: stop treating City, Resource, Monster, Truck, Railway, Dispatch, Ghost and Treasure as eight separate scanners. The active implementation priority is the **shared Manual Scan engine** shown on the Map Data page. Player City remains the first acceptance category for that common engine because its current-client acquisition/storage path is already proven.

Player City `LWB-PC-001/002/003` remains valid historical/live evidence for fresh city acquisition, persistence, reopen/search and owner-visible saved-row rendering. It is a foundation, not proof that the normal Start button performs a complete world scan. Initial Map Data load may show a persisted City row before Search; that is saved-data browsing, not a new scan.

## Active implementation order

1. `LWB-R7-008` LIVE-PROVES complete Player City acquisition through ordinary Manual Start; `LWB-R7-009` adds truthful native AOI-union progress and owner selection/Jump fixes; `LWB-R7-010` now LIVE-PROVES **Monster-only ordinary Manual Start** over all 10,000 LOD0 AOIs / 2,500 logical blocks with zero failed blocks, publishing/reopening 6,934 game-classified monsters/bosses in 71.161 s. Monster uses `WorldScene.MarchDataManager` plus the game-owned `MonsterTemplateManager` and is not Zombie-Invasion-specific. `LWB-R7-011` restores the reference-backed offline `lastwar_localize` path, so Monster readable names and UI-language switching are IMPLEMENTED/OFFLINE-TESTED. `LWB-R7-012` then implements the owner-requested full-world performance/status/Stop pass: current-v17 5x10 footprints support 200 sequential requests instead of 250 with the exact 10,000-cell gate unchanged; one Fast live run completed in about 1m10.7s and one Normal run in 43.6s. `LWB-R7-013` adds the first result-usability slice; `LWB-R7-014` corrects it to shield-only Remaining, inclusive-maximum level filtering, live Distance and working Level/Distance sorts, while same-tick camera restoration preserves the shared acquisition engine. Keep this single production engine. Resource is LIVE-PROVEN under `LWB-R7-015` and Truck is now LIVE-PROVEN under `LWB-R7-016`; Railway/Train is next, then Dispatch/Secret Task, Ghost Ops, Treasure, mixed selections and all eight. Native acknowledgement/removal/drain semantics remain unresolved and must not be fabricated.
2. **Normal** and **Fast** use the same production engine with recovered concurrency fields `8` and `20`. `LWB-R7-012` now live-proves complete full-world Monster runs through both mode selections. Current camera acquisition stays single-owner/sequential because native `XluaBridgeMapScanTick` pacing is still unrecovered; the concurrency field must not be turned into invented parallel navigation, and Fast is therefore not claimed to beat Normal wall time under every live population/server condition.
3. Make the progress/status bar truthful from real scan state: total/completed/read/failed/unread/inflight work, scan rate, progress, capture readiness/pending/dropped state, last error and resume state where recovered. `LWB-R6-053` recovers unread derivation plus one-decimal progress, with every non-`completed` state capped at 98% and positive-total exact `completed` permitting 100%. `LWB-R6-054` recovers scan-event counter validation/bounding and two-decimal completed-blocks-per-second scan rate. `LWB-R6-055` adds pending native-record aggregation, separate dropped-record failure handling, exact direct-completion coverage/zero-failed-block checks and recovered Stop reset values. Do not infer that a positive pending count alone rejects publication. `LWB-R6-056` additionally recovers Start ownership and resume gating: original duplicate Start is `SCAN_RUNNING`, requested resume uses existing state only when current `resumeAvailable=true`, and every recovered state-construction/reset site in 0.3.1 writes that field false. Feed these rules only with real scheduler events/counters; `100%` alone is not completion, and do not advertise resume until a true-producing state path is recovered. `LWB-R6-058` additionally recovers final publication ownership: inside the completion transaction, the exact run must still transition from `running` to `completed` (zero affected rows -> `INVALID_SCAN / map scan is not running`), and the exact run is reread after commit (`INVALID_SCAN / map scan disappeared` if absent). `LWB-R6-059` recovers the immediately preceding terminal admission: positive `failedBlocks` or present nonempty `lastError` populate a terminal failure list routed through failure/Stop cleanup; only an empty list enters `publishing` and invokes direct completion. `LWB-R6-060` recovers scan-event admission before those counters are consumed: processing requires active `isReading`, present nonempty `scanRunId` must match the active run, and present positive `serverId` must match the active server; missing identity remains tolerated rather than synthesized. `LWB-R6-061` recovers Start readiness before run construction: `enterWorldMap` uses 5,000 ms timeout, world readiness waits up to 10,000 ms with 500 ms polls, timeout is `WORLD_MAP_FAILED`, and post-readiness server admission requires positive `serverId` plus exact `serverIdSource=live` or `SERVER_UNAVAILABLE`. `pendingAcks` is not part of the R6-059 list, but acknowledgement consumption/drain semantics remain unresolved.
4. Finish **Clear Map Data** as part of the scan lifecycle: server-scoped clear, active-run conflict/cancel handling, generation protection against late-result resurrection, progress/query reset, and preservation of marks where the recovered contract says they survive. `LWB-R6-057` recovers Clear ownership and current live-server validation; keep its validator offline until the shared scanner has an authoritative live server source.
5. Route the eight **Scan Content** selections through the same engine. Player City, Monster, Resource and Truck are now live-proven through ordinary Manual Start. Prove Railway/Train next, then Dispatch/Secret Task, Ghost Ops and Treasure, then mixed selections and finally all eight together.
6. After acquisition is reliable, finish per-kind normalization/storage plus Search/filter/sort/options/export/navigation. Scan-content selection controls acquisition; result filters operate on already acquired persisted data and must remain separate concepts.
7. Build Auto Scan only on top of the proven Manual Scan engine. Do not create a second scanner; Auto Scan owns scheduling/server sequencing and calls the same scan lifecycle.

## Current production boundary

The common engine/persistence layer is now LIVE-PROVEN for complete ordinary-Start Player City (`LWB-R7-008`) and Monster (`LWB-R7-010`) acquisition/publication, while `LWB-R7-011` separately makes Monster localization/readable names IMPLEMENTED/OFFLINE-TESTED; `LWB-R7-012` LIVE-PROVES the current-v17 200-request performance planner plus terminal single-Stop backend and production status/summary integration. `LWB-R7-013` is superseded for result presentation by `LWB-R7-014`, which adds live Monster Distance, shield-only Remaining, <= level filtering, working Level/Distance sorts and same-tick camera restoration. `LWB-OVR-016` removes repeated unchanged-package warm-up launches. Production accepts exactly one `city`, `resource`, `monster`, or `truck` kind; all four are complete-run LIVE-PROVEN, while mixed selections and the other four kinds still fail closed. Railway/Train is the next category acceptance. `LWB-R7-009` exposes truthful native acquisition progress while publication remains all-or-nothing. Native ack/removal/drain remains unresolved. The older `LiveResourceProbeCommandService` remains historical/support evidence, not ordinary Start ownership.

Existing operation-specific restrictions remain in force, but they must be interpreted narrowly. The owner's priority is the finished working feature, not a particular tool: use any otherwise-permitted software, implementation route, observation method or independent test that advances the shared engine. In particular, do not recreate the exact SB-97 rejected proof operation through another executor, owner clicks, helper-direct execution or repackaged automation. SB-97 is not a blanket ban on Resource work, opening/using the game normally, alternative engineering approaches or separately permitted live tests. Continue all such permitted work toward the shared engine instead of treating the whole feature area as blocked.

Web remains the single implementation/research/technical-verification worker. The owner supplies only simple UI observations/screenshots when needed. Commit/push/verify each coherent checkpoint under `AGENTS.md`; do not request another Player City saved-row retest unless a concrete regression requires it.
